---
title: Distributed MPC For Coordinated Path-Following
title_zh: 用于协同路径跟踪的分布式模型预测控制
authors: "Lusine Poghosyan, Anna Manucharyan, Mikayel Aramyan, Naira Hovakimyan, Tigran Bakaryan"
date: 2026-03-25
pdf: "https://arxiv.org/pdf/2603.24748v1"
tags: ["query:robot-planning"]
score: 9.0
evidence: 用于多机器人协同路径跟随的分布式MPC
tldr: 本文针对协同路径跟踪问题，提出了一种分布式模型预测控制（MPC）算法。该方法利用时间关键型协同框架将空间与时间解耦，并结合归一化拉普拉斯矩阵将MPC动力学分解为独立模式。研究证明了在预测时域为1且网络连通时，系统具备指数稳定性。该方案不仅计算高效、通信开销低，还能灵活处理避障和车辆限制等约束，为离散时间分布式MPC在协同控制领域的收敛性分析提供了重要参考。
selection_source: fresh_fetch
motivation: 旨在解决协同路径跟踪中时空耦合带来的复杂性，并提升系统在处理车辆限制和避障等动态约束时的灵活性。
method: 采用分布式MPC架构，通过归一化拉普拉斯矩阵解耦系统动力学，并建立当前状态与预测状态的递归关系。
result: 理论证明了系统在特定条件下具有指数稳定性，仿真验证了其在通信故障和复杂场景下的敏捷性与收敛性。
conclusion: 该分布式MPC方案实现了可扩展且高效的实时协同控制，有效平衡了控制性能与任务约束的处理能力。
---

## 摘要
本文研究了一种用于协同路径跟踪的分布式模型预测控制（MPC）算法。基于时间关键型协同路径跟踪框架（该框架将空间与时间解耦，并将协同问题简化为一维设置），我们制定了一种用于时间协同的分布式 MPC 方案。利用归一化拉普拉斯矩阵的性质，我们将 MPC 动力学解耦为独立模态，并推导出了连接当前状态与预测状态的递归关系。利用该结构，我们证明了在预测时域 $K=1$ 且通信网络固定连通的情况下，即使存在路径跟踪误差，系统也是指数稳定的。这为该框架下离散时间分布式 MPC 方案的收敛性分析提供了首个结果。所提出的方法能够实现具有低通信开销的可扩展且高效的实时部署。此外，与时间关键型协同路径跟踪框架相比，这种基于优化的结构通过允许纳入特定任务需求（如车辆限制、避障和冲突解决），放宽了对预规划的依赖。仿真结果证明了其在复杂场景下的适用性，突显了在通信故障下的灵活性和指数收敛性。

## Abstract
In this paper, we consider a distributed model predictive control (MPC) algorithm for coordinated path-following. Relying on the time-critical cooperative path-following framework, which decouples space and time and reduces the coordination problem to a one-dimensional setting, we formulate a distributed MPC scheme for time coordination. Leveraging properties of the normalized Laplacian, we decouple the MPC dynamics into independent modes and derive a recursive relation linking current and predicted states. Using this structure, we prove that, for prediction horizon $K=1$ and a fixed connected communication network, the system is exponentially stable even in the presence of path-following errors. This provides a first result on the convergence analysis of discrete-time distributed MPC schemes within this framework.   The proposed approach enables scalable and efficient real-time implementation with low communication overhead. Moreover, in contrast to the time-critical cooperative path-following framework, the optimization-based structure relaxes the reliance on preplanning by allowing the incorporation of mission-specific requirements, such as vehicle limitations, collision avoidance, and conflict resolution. Simulation results demonstrate applicability to complex scenarios, highlighting agility and exponential convergence under communication failures.

---

## 论文详细总结（自动生成）

以下是对论文《Distributed MPC For Coordinated Path-Following》的结构化深入总结：

### 1. 核心问题与整体含义（研究动机和背景）
论文关注的是**多无人机（UAV）系统的协同路径跟踪**问题。在复杂的搜索救援、监视或测绘任务中，多个无人机需要精确同步时间以达成任务目标。
*   **研究动机**：现有的协同方法（如基于比例积分 PI 的控制器）高度依赖预规划，缺乏应对动态约束（如避障、车辆物理极限）的灵活性。
*   **核心挑战**：如何在分布式架构下，既能保证系统在离散时间环境下的稳定性，又能实时处理复杂的非线性约束和通信限制。
*   **整体含义**：本文提出了一种分布式模型预测控制（DMPC）方案，首次在“时间关键型协同路径跟踪”框架下，为离散时间 DMPC 提供了指数稳定性的理论证明。

### 2. 论文提出的方法论
核心思想是将空间路径跟踪与时间协同解耦，将复杂的协同问题简化为一维的“虚拟时间”协调问题。
*   **虚拟时间（Virtual Time）**：引入变量 $\gamma_i$ 表示无人机 $i$ 在名义任务时间线上的进度。通过调节 $\dot{\gamma}_i$（进度速率）来实现同步。
*   **DMPC 建模**：
    *   **状态变量**：定义偏差变量 $\delta_i$（虚拟时间与实际时间的偏差）及其变化率。
    *   **代价函数**：包含三部分：同步项（惩罚与邻居的偏差）、步调保持项（惩罚偏离名义速度）、控制项（惩罚过大的加速度）。
    *   **约束处理**：直接在优化问题中加入速度和加速度的物理限制。
*   **关键技术细节**：
    *   利用**归一化拉普拉斯矩阵（Normalized Laplacian）**的谱性质，将耦合的系统动力学分解为独立的模态。
    *   通过递归关系推导，证明了在预测时域 $K=1$ 时，闭环系统矩阵是一个收缩映射。
    *   引入路径跟踪误差修正项 $\alpha_i$，使协同层能够感知底层控制器的跟踪质量。

### 3. 实验设计
*   **实验场景**：
    1.  **基准测试**：不同数量无人机（5, 10, 15个）在圆形轨迹上的协同。
    2.  **敏感性分析**：对比不同预测时域 $K$（1, 5, 10, 25）对收敛速度和计算开销的影响。
    3.  **走廊导航（挑战场景）**：模拟狭窄通道，要求无人机依次通过以避免碰撞。
*   **对比方法（Benchmark）**：
    *   **离线预设顺序（Offline Ordering）**：基于预先规划的优先级通过走廊。
    *   **自主博弈/优化方案（Autonomous Game-based）**：无人机根据实时状态自主竞争通过顺序。
*   **仿真平台**：使用基于 Python 的多旋翼模拟器 **RotorPy**，硬件平台模拟 Bitcraze Crazyflie 无人机。

### 4. 资源与算力
*   **硬件环境**：GIGABYTE G5 MF5 笔记本电脑，配置 Intel Core i7-13620H 处理器（16核），32 GiB RAM，运行 Ubuntu 22.04。
*   **算力表现**：
    *   在 $K=1$ 时，平均更新频率约为 **100 Hz**（单次计算约 0.006s - 0.008s）。
    *   随着无人机数量增加，计算时间几乎保持不变，证明了算法的分布式扩展性。
    *   文中未提及 GPU 加速，主要依赖 CPU 进行实时优化求解。

### 5. 实验数量与充分性
*   **实验规模**：进行了多组不同规模（5-15架无人机）的仿真，涵盖了理想状态、通信故障和存在干扰的场景。
*   **充分性评价**：实验设计较为充分。通过对比不同 $K$ 值，验证了理论证明中 $K=1$ 的有效性；通过走廊场景的对比，客观展示了博弈论 DMPC 相比传统预规划方法的鲁棒性。
*   **客观性**：使用了高保真物理模拟器，考虑了空气动力学效应，实验结果具有较高的参考价值。

### 6. 主要结论与发现
*   **稳定性证明**：理论证明并仿真验证了在固定连通网络下，预测时域 $K=1$ 的 DMPC 方案具有**指数稳定性**。
*   **实时性与扩展性**：分布式架构使得计算负担不随无人机总数增加而显著增长，适合大规模集群。
*   **灵活性**：基于优化的 DMPC 能够比 PI 控制器更好地处理物理约束。在走廊实验中，自主博弈方案成功避免了碰撞，而预规划方案在干扰下发生了碰撞。
*   **时域影响**：虽然 $K=1$ 保证了稳定性且计算最快，但较长的预测时域（如 $K=25$）能显著缩短达成协同所需的时间。

### 7. 优点
*   **理论突破**：填补了该框架下离散时间分布式 MPC 收敛性分析的空白。
*   **计算高效**：通过模态解耦简化了分析，且 $K=1$ 的设置极大地降低了嵌入式设备的计算压力。
*   **鲁棒性强**：能够处理路径跟踪误差和通信中断，且无需复杂的参数调优。

### 8. 不足与局限
*   **理论限制**：目前的指数稳定性证明仅限于 **$K=1$** 和**固定通信网络**。对于更长时域 $K>1$ 或时变拓扑的理论证明尚在进行中。
*   **实验局限**：所有实验均在仿真环境（RotorPy）中完成，缺乏真实物理环境（如风力干扰、传感器噪声）下的实机飞行验证。
*   **代价函数依赖**：虽然放宽了预规划，但对于复杂任务（如走廊通过），仍需人工设计特定的代价函数项（如指示函数和惩罚项）。

（完）
