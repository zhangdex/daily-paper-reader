---
title: "IndoorR2X: Indoor Robot-to-Everything Coordination with LLM-Driven Planning"
title_zh: IndoorR2X：基于大语言模型（LLM）驱动规划的室内机器人与万物（R2X）协同
authors: "Fan Yang, Soumya Teotia, Shaunak A. Mehta, Prajit KrisshnaKumar, Quanting Xie, Jun Liu, Yueqi Song, Li Wenkai, Atsunori Moteki, Kanji Uchino, Yonatan Bisk"
date: 2026-03-20
pdf: "https://arxiv.org/pdf/2603.20182v1"
tags: ["query:robot-planning"]
score: 9.0
evidence: 基于大语言驱动的R2X感知多机器人任务规划
tldr: 针对多机器人协作中局部观测受限和探索成本高的问题，本文提出IndoorR2X，这是首个结合物联网（IoT）传感器与大语言模型（LLM）驱动规划的室内机器人到万物（R2X）协作基准和仿真框架。该框架通过整合移动机器人与静态IoT设备的观测，构建全局语义状态，减少冗余探索并提升场景理解能力。实验证明，IoT增强的世界模型显著提高了多机器人任务的效率和可靠性，为LLM驱动的异构感知协作提供了新思路。
selection_source: fresh_fetch
motivation: 传统的机器人间通信（R2R）难以在不增加探索开销的情况下解决局部观测问题，而室内既有的IoT传感器能提供持久且广泛的全局上下文。
method: 开发了IndoorR2X仿真框架，利用LLM对机器人和IoT设备的集成感知数据进行高层任务规划，并构建全局语义状态以支持可扩展的场景理解。
result: 实验表明，引入IoT辅助的感知与通信显著提升了多机器人团队在复杂室内环境中的协作效率、可靠性及任务成功率。
conclusion: 将室内IoT传感器与机器人团队结合并利用LLM进行协调，是实现高效、可扩展的室内场景理解和复杂任务规划的有效途径。
---

## 摘要
尽管机器人对机器人（R2R）通信提升了室内场景理解能力，超越了单机器人的局限，但若不增加大量的探索开销或扩大团队规模，仅靠 R2R 无法克服部分可观测性问题。相比之下，许多室内环境已经部署了低成本的物联网（IoT）传感器（如摄像头），它们能提供超越机载感知的、覆盖全建筑的持久上下文信息。因此，我们推出了 IndoorR2X，这是首个针对室内环境下具有机器人与万物（R2X）感知与通信能力的、由大语言模型（LLM）驱动的多机器人任务规划基准和仿真框架。IndoorR2X 整合了移动机器人和静态 IoT 设备的观测结果，以构建全局语义状态，支持可扩展的场景理解，减少冗余探索，并通过基于 LLM 的规划实现高层协同。IndoorR2X 提供了可配置的仿真环境、传感器布局、机器人团队和任务套件，用于系统地评估高层语义协同策略。在多种设置下的广泛实验表明，IoT 增强的世界建模提高了多机器人的效率和可靠性，我们还强调了推进机器人团队与室内 IoT 传感器之间基于 LLM 协作的关键见解和失败模式。

## Abstract
Although robot-to-robot (R2R) communication improves indoor scene understanding beyond what a single robot can achieve, R2R alone cannot overcome partial observability without substantial exploration overhead or scaling team size. In contrast, many indoor environments already include low-cost Internet of Things (IoT) sensors (e.g., cameras) that provide persistent, building-wide context beyond onboard perception. We therefore introduce IndoorR2X, the first benchmark and simulation framework for Large Language Model (LLM)-driven multi-robot task planning with Robot-to-Everything (R2X) perception and communication in indoor environments. IndoorR2X integrates observations from mobile robots and static IoT devices to construct a global semantic state that supports scalable scene understanding, reduces redundant exploration, and enables high-level coordination through LLM-based planning. IndoorR2X provides configurable simulation environments, sensor layouts, robot teams, and task suites to systematically evaluate high-level semantic coordination strategies. Extensive experiments across diverse settings demonstrate that IoT-augmented world modeling improves multi-robot efficiency and reliability, and we highlight key insights and failure modes for advancing LLM-based collaboration between robot teams and indoor IoT sensors.

---

## 论文详细总结（自动生成）

### 论文总结：IndoorR2X - 基于 LLM 驱动规划的室内机器人与万物协同

#### 1. 核心问题与整体含义（研究动机和背景）
在室内多机器人协作中，核心挑战在于**局部可观测性（Partial Observability）**。每个机器人仅能感知其视野范围和已探索区域，这导致了大量的冗余探索、任务分配冲突以及在动态环境下的规划脆弱性。
虽然现有的研究关注机器人间（R2R）的通信，但往往忽略了室内环境中已有的低成本**物联网（IoT）传感器**（如 CCTV 摄像头、智能家电状态）。论文提出了 **IndoorR2X** 框架，旨在利用 LLM 的推理能力，将异构的 IoT 感知数据与机器人机载感知融合，构建全局语义状态，从而实现更高效、低成本的多机器人协同规划。

#### 2. 方法论：核心思想与关键技术
IndoorR2X 的核心是一个**在线感知-规划-执行循环**，主要包含以下关键组件：
*   **协调枢纽（Coordination Hub）与全局语义状态**：枢纽维护一个全局状态 $W_t$，包含机器人集合、对象集合和区域状态。它通过转移函数 $F$ 融合来自移动机器人和静态 IoT 设备（如通过 VLM 处理的 CCTV 文本日志）的异构观测数据。
*   **R2X 数据融合**：利用视觉语言模型（如 Qwen-VL）将 IoT 视频流转化为带时间戳的文本事件日志，并与机器人的机载感知数据同步，确立物体的层级关系（如物体是否在某个容器内）。
*   **LLM 驱动的在线规划**：LLM 充当中心规划器，根据全局语义状态生成一个**有向无环图（DAG）**形式的并行计划。每个节点代表一个动作，边代表依赖关系。
*   **并行动作编排与监控**：系统根据机器人的技能和空间位置动态分配任务，并实时监控执行状态。一旦检测到失败或 IoT 传回关键环境变化，立即触发状态感知的重规划。

#### 3. 实验设计
*   **仿真环境**：基于 AI2-THOR 引擎，使用了 10 个 ArchitecTHOR 房屋和 75 个 RoboTHOR 模块化公寓，共计 **85 个多房间场景**。
*   **任务设置**：包含 850 个复杂的家庭任务（如清理台面、关闭电器、跨房间搬运物体）。
*   **Benchmark 与对比方法**：
    *   **IR (Isolated)**：机器人仅靠局部感知，无通信。
    *   **R2R (Robot-to-Robot)**：机器人间共享地图，无 IoT 支持。
    *   **R2X (Robot-to-Everything)**：本文方法，整合 IoT 传感器。
    *   **基准对比**：与 SOTA 框架如 SMART-LLM (2024) 和 EMOS (2025) 进行对比。
*   **评估指标**：成功率（SR）、平均动作步数、平均路径长度、平均 LLM Token 消耗。

#### 4. 资源与算力
*   **模型使用**：实验使用了 **GPT-4.1** 作为主要规划器，并对比了 **Gemma-3-27b** 和 **Llama-3.1-8b**。感知端使用了 **Qwen-VL** 处理视频流。
*   **算力说明**：论文**未明确说明**具体的 GPU 型号、数量或训练/推理的总时长。由于主要依赖现成的 LLM API 和仿真环境，其算力消耗主要集中在 LLM 推理和 VLM 视频解析上。

#### 5. 实验数量与充分性
*   **实验规模**：在 85 个场景中进行了 850 次任务测试，实验规模较大。
*   **消融实验**：涵盖了通信配置（IR vs R2R vs R2X）、LLM 规模（8B 到 1.8T 参数）、IoT 延迟（0 到 10 步）、团队规模（2 到 6 个机器人）以及 IoT 故障鲁棒性（信息缺失 vs 信息错误）。
*   **客观性与公平性**：通过严格限制机器人的初始知识（强制局部观测），确保了对比的公平性。实验设计全面，涵盖了从效率到鲁棒性的多个维度，结论较为客观。

#### 6. 主要结论与发现
*   **效率提升**：R2X 相比 R2R 在保持高成功率（92%）的同时，显著减少了路径长度（约 11%）和动作步数（约 7%）。
*   **成本降低**：由于 IoT 提供了全局先验，减少了 LLM 探索决策的需求，Token 消耗降低了约 11%。
*   **模型依赖**：协同质量高度依赖 LLM 能力，GPT-4.1 表现优异，而 8B 规模的小模型几乎无法完成复杂协同。
*   **鲁棒性不对称**：系统对 **IoT 信息缺失**（如摄像头没看到物体）非常鲁棒（成功率不变，仅效率下降）；但对 **IoT 语义错误**（如错误报告设备状态）非常敏感，会导致规划失败。

#### 7. 优点
*   **首创性**：首次将 V2X（车联网）理念引入室内多机器人领域，填补了利用环境既有 IoT 设施进行语义协同的空白。
*   **现实性约束**：不同于以往假设“上帝视角”的仿真，IndoorR2X 严格限制了机器人的感知范围，更符合实际应用。
*   **系统性框架**：提出了完整的语义融合与并行任务分配流程，且在真实物理机器人（Stretch）上进行了验证。

#### 8. 不足与局限
*   **模型门槛高**：实验表明该框架高度依赖顶级 LLM（如 GPT-4），在边缘侧部署低参数模型时效果极差。
*   **语义错误风险**：框架缺乏对 IoT 错误信息的验证机制，一旦传感器发生语义误报，系统容易陷入不可恢复的错误。
*   **实时性挑战**：虽然讨论了延迟，但在极高动态环境下，LLM 的推理延迟可能成为瓶颈。
*   **实测规模有限**：虽然有物理部署，但相比仿真环境，真实世界的实验场景和任务复杂度仍有待进一步扩大。

（完）
