---
title: Route-Phasing-Split-Encoded Genetic Algorithm for Multi-Satellite On-Orbit Servicing Mission Planning
title_zh: 针对多卫星在轨服务任务规划的路径-调相-拆分编码遗传算法
authors: "Shridhar Velhal, Avijit Banerjee, George Nikolakopoulos"
date: 2026-03-23
pdf: "https://arxiv.org/pdf/2603.22210v1"
tags: ["query:space-defense"]
score: 10.0
evidence: 多卫星在轨服务任务规划与任务分配
tldr: 本研究针对地球同步轨道（GEO）多服务航天器在轨服务任务规划问题，提出了一种路径-相位-分割编码遗传算法（RPS-GA）。该算法通过创新的三元组染色体结构，同时优化目标序列、轨道相位旋转及多航天器间的任务分配。结合约束感知适应度函数和局部搜索策略，RPS-GA有效解决了路径决策与轨道动力学约束的耦合难题，在满足严格推进剂和时间限制的同时，显著提升了多航天器协同作业的燃油效率。
selection_source: fresh_fetch
motivation: 旨在解决GEO多航天器在轨服务中，路径决策与随时间变化的轨道相位及严格资源约束之间高度耦合的复杂优化挑战。
method: 提出RPS-GA算法，采用三元组染色体统一编码任务顺序、相位决策与路径分割，并结合约束感知惩罚机制与基于后悔值的局部搜索策略。
result: "在典型GEO服务场景下，该算法相比现有LNS-AGA基准方法，将总速度增量（ΔV）降低了24.5%，从1956.36 m/s优化至1476.32 m/s。"
conclusion: 研究证明了通过集成编码与约束感知优化框架，能够有效处理多航天器任务规划中的复杂约束，显著提升任务的燃油效率和收敛稳定性。
---

## 摘要
本文研究地球同步轨道（GEO）中的多服务航天器在轨服务任务规划，其中路径决策与随时间变化的轨道调相以及严格的推进剂和任务时长约束紧密耦合。我们提出了一种路径-调相-拆分遗传算法（RPS-GA），该算法同时优化目标排序、离散调相旋转决策（即调相圈数/等待周期）以及多个服务航天器（SSC）之间的路径划分。RPS三元组染色体在统一结构中对路径顺序、调相旋转和路径拆分进行编码，从而在不破坏可行多服务航天器路径块的情况下实现拆分感知的重组。通过约束感知适应度函数强制执行可行性，该函数根据总 $ΔV$ 对可行解进行排序，同时利用聚合惩罚和不平衡惩罚对违反推进剂和任务时长约束的情况进行惩罚。这种公式化方法抑制了违规情况集中在单个服务航天器（SSC）上的现象。一旦识别出可行的最优解，它将在后续代中作为可行解保留，从而增强收敛稳定性。该框架结合了拆分感知交叉、变异以及基于后悔值的大邻域搜索以进行局部强化。在代表性GEO服务场景下的实验表明，与最先进的LNS-AGA基准相比，RPS-GA生成的可行多服务计划显著提高了燃油效率，将总 $ΔV$ 降低了 $24.5\%$（从 $1956.36 \ m/s$ 降至 $1476.32\ m/s$）。

## Abstract
This article addresses multi-servicer on-orbit servicing mission planning in geosynchronous Earth orbit, where routing decisions are tightly coupled with time-dependent orbital phasing and strict propellant and mission-duration constraints. We propose a Route-Phasing-Split Genetic Algorithm (RPS-GA) that simultaneously optimizes target sequencing, discrete phasing rotation decisions (i.e., the number of phasing revolutions/waiting cycles), and route partitioning across multiple servicing spacecrafts (SSCs). An RPS triplet chromosome encodes route order, phasing rotations, and route splits in a unified structure, enabling split-aware recombination without disrupting feasible multi-servicer route blocks. Feasibility is enforced through a constraint-aware fitness function that ranks feasible solutions based on total $ΔV$, while penalizing propellant and mission duration violations, using aggregate and imbalance penalties. This formulation discourages the concentration of violations on a single servicing spacecraft (SSC). Once a feasible best solution is identified, it is preserved as feasible in subsequent generations, thereby enhancing convergence stability. The framework incorporates split-aware crossover, mutation and a regret-based Large Neighborhood Search for local intensification. Experiments on representative GEO servicing scenarios demonstrate that RPS-GA produces feasible multi-servicer plans with substantially improved fuel efficiency, reducing total $ΔV$ by $24.5\%$, (from $1956.36 \ m/s$ to $ 1476.32\ m/s $) compared with a state-of-the-art LNS-AGA baseline.

---

## 论文详细总结（自动生成）

### 针对多卫星在轨服务任务规划的 RPS-GA 算法详细总结

#### 1. 核心问题与研究动机
*   **核心问题**：如何在满足严格的推进剂限制（$\Delta V$）和任务截止时间的前提下，为多个服务航天器（SSC）规划最优的在轨服务路径。
*   **研究动机**：
    *   **高度耦合性**：地球同步轨道（GEO）的路径规划与时间相关的轨道调相（Phasing）紧密耦合，传统的车辆路径问题（VRP）模型无法直接应用。
    *   **非平稳成本**：轨道转移成本随时间变化，改变访问顺序会直接影响调相窗口和转移可行性。
    *   **现有方法不足**：现有的元启发式算法在处理多航天器分配时，容易出现约束违规在不同航天器间“震荡”的问题，且难以同时优化路径顺序、调相决策和任务分配。

#### 2. 核心方法论
论文提出了 **RPS-GA（Route-Phasing-Split Genetic Algorithm）**，其核心思想是将三个关键决策维度集成在一个统一的编码框架中：
*   **RPS 三元组染色体**：
    *   **Route (R)**：所有待服务任务的全局排列顺序。
    *   **Phasing (n)**：每个任务转移对应的离散调相旋转圈数。
    *   **Split (L)**：定义如何将全局任务序列分割给不同的服务航天器。
*   **约束感知适应度函数**：
    *   不仅惩罚总体的推进剂和时间违规，还引入了**不平衡惩罚（Imbalance Penalty）**，防止违规行为集中在单个航天器上。
    *   设计了**前向不变性（Forward-Invariant）**机制：一旦发现可行解，该解在后续进化中将始终保持可行，避免被低成本但不可行的解替代。
*   **关键算子**：
    *   **拆分感知交叉（RBC）**：以路径块为单位进行交换，保护已有的可行子路径。
    *   **局部强化（LNS）**：嵌入基于后悔值（Regret-based）的大邻域搜索，通过“破坏-修复”机制优化任务插入位置，并进行调相参数的精细打磨（Phasing Polish）。

#### 3. 实验设计
*   **实验场景**：考虑一个真实的 GEO 服务场景，包含 **2 个服务航天器（SSC）** 和 **14 个目标卫星**。
*   **约束条件**：每个 SSC 的 $\Delta V$ 上限为 1000 m/s，总任务周期为 30 天，每个卫星的服务时长固定为 20 小时。
*   **基准方法（Benchmark）**：
    *   **LNS-AGA**：目前最先进的自适应遗传算法（Han et al., 2022）。
    *   **标准 GA**：传统的遗传算法。
*   **对比指标**：总速度增量（Total $\Delta V$）、任务完成时间、约束满足情况。

#### 4. 资源与算力
*   **硬件环境**：1.8 GHz Intel Core i7 CPU，48 GB RAM。
*   **软件环境**：MATLAB 2024b。
*   **算力说明**：论文未提及 GPU 使用情况（此类组合优化问题通常在 CPU 上运行）。算法设置了最小迭代次数 100 次，种群规模 100，LNS 内部迭代次数为 5。

#### 5. 实验数量与充分性
*   **实验规模**：针对给定的 GEO 场景进行了 **100 次独立运行**，以评估算法的统计稳定性。
*   **消融实验**：专门设计了针对“不可行惩罚项”的消融实验，验证了该项在维持进化稳定性和防止解震荡方面的必要性。
*   **充分性评价**：实验设计较为充分，通过 100 次运行的直方图展示了结果符合伽马分布，证明了算法的鲁棒性。对比实验采用了同一场景下的已知最优基准，具有较强的说服力。

#### 6. 主要结论与发现
*   **大幅降低能耗**：RPS-GA 将总 $\Delta V$ 从基准方法的 **1956.36 m/s 降低至 1476.32 m/s**，燃油效率提升了 **24.5%**。
*   **约束完全满足**：所有生成的计划均严格遵守了 1000 m/s 的推进剂限制和 30 天的时间限制。
*   **收敛稳定性**：通过 RPS 编码和特定的惩罚机制，算法能够稳定地收敛到高质量的可行解空间，解决了多航天器任务分配中的可行性震荡问题。

#### 7. 优点与亮点
*   **集成化编码**：RPS 三元组结构巧妙地解决了路径、调相和分配三个维度的耦合问题，比顺序优化更高效。
*   **惩罚机制创新**：引入不平衡惩罚项，有效引导算法寻找全局可行的多航天器协同方案。
*   **局部搜索协同**：将 LNS 作为 GA 的局部增强模块，既保留了全局搜索能力，又提升了对轨道调相机会的利用率。

#### 8. 不足与局限
*   **模型简化**：轨道转移采用的是脉冲式（Impulsive）假设，未考虑连续推力（Continuous Thrust）情况，这在某些电推航天器任务中可能存在偏差。
*   **任务同质化**：假设所有任务的服务时长固定（20小时），未探讨具有不同优先级或变动服务时长的异构任务场景。
*   **计算复杂度**：虽然比精确算法快，但由于引入了 LNS 和复杂的适应度评估，其在大规模卫星星座（如数百颗卫星）场景下的实时性仍有待验证。

（完）
