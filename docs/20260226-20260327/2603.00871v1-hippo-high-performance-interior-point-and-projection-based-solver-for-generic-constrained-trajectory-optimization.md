---
title: "Hippo: High-performance Interior-Point and Projection-based Solver for Generic Constrained Trajectory Optimization"
title_zh: Hippo：用于通用约束轨迹优化的高性能内点法与基于投影的求解器
authors: "Haizhou Zhao, Ludovic Righetti, Majid Khadiv"
date: 2026-03-01
pdf: "https://arxiv.org/pdf/2603.00871v1"
tags: ["query:robot-planning"]
score: 9.0
evidence: 通用约束轨迹优化求解器
tldr: 轨迹优化是现代机器人控制的核心，但现有SQP或DDP方法在处理复杂硬约束时存在效率低、灵活性差及收敛难的问题。本文提出Hippo求解器，通过带自适应障碍更新策略的内点法（IPM）处理不等式约束，并结合投影或IPM处理硬等式约束。实验证明Hippo在足式运动和机械臂操作等复杂任务中，比现有最先进求解器更具鲁棒性和高效性。
selection_source: fresh_fetch
motivation: 现有轨迹优化方法在处理复杂硬约束任务时，普遍存在计算效率低、建模灵活性差和收敛性能不足的局限性。
method: 开发了Hippo求解器，采用自适应障碍更新的内点法处理不等式约束，并利用投影或内点法确保硬等式约束的满足。
result: 在涉及足式行走和物体操纵的数值基准测试中，Hippo展现出比当前主流求解器更高的求解质量和计算效率。
conclusion: Hippo为复杂机器人轨迹优化问题提供了一个鲁棒且高效的通用求解框架，特别适用于对解质量要求极高的应用场景。
---

## 摘要
轨迹优化是现代基于模型的机器人控制和运动规划的核心。现有的轨迹优化器基于序列二次规划 (SQP) 或微分动态规划 (DDP)，通常受限于计算效率低、建模灵活性差，以及在处理需要硬约束的复杂任务时收敛性不佳。在本文中，我们介绍了 Hippo，这是一种能够使用带有自适应障碍更新策略的内点法 (IPM) 处理不等式约束，并通过投影或 IPM 处理硬等式约束的求解器。通过广泛的数值基准测试，我们证明了对于需要高质量解决方案的困难机器人轨迹优化问题（如足式移动和操作），Hippo 是现有最先进求解器的鲁棒且高效的替代方案。

## Abstract
Trajectory optimization is the core of modern model-based robotic control and motion planning. Existing trajectory optimizers, based on sequential quadratic programming (SQP) or differential dynamic programming (DDP), are often limited by their slow computation efficiency, low modeling flexibility, and poor convergence for complex tasks requiring hard constraints. In this paper, we introduce Hippo, a solver that can handle inequality constraints using the interior-point method (IPM) with an adaptive barrier update strategy and hard equality constraints via projection or IPM. Through extensive numerical benchmarks, we show that Hippo is a robust and efficient alternative to existing state-of-the-art solvers for difficult robotic trajectory optimization problems requiring high-quality solutions, such as locomotion and manipulation.

---

## 论文详细总结（自动生成）

以下是对论文《Hippo: High-performance Interior-Point and Projection-based Solver for Generic Constrained Trajectory Optimization》的结构化深入总结：

### 1. 核心问题与研究动机
*   **核心问题**：现代机器人（如足式机器人、协作机械臂）的轨迹优化（TO）面临复杂硬约束、非线性动力学和多接触状态切换。现有的求解器（如基于SQP或DDP的方法）在处理这些问题时，往往存在计算效率低、建模灵活性差、在硬约束下收敛性差等痛点。
*   **研究动机**：
    *   **ALM（增广拉格朗日法）的局限**：对不等式约束的处理依赖于活跃集检测，容易陷入局部最优，且惩罚参数调节困难。
    *   **IPM（内点法）的潜力**：具有多项式收敛复杂度，但如何在保持稀疏性的同时实现高性能的机器人轨迹优化仍具挑战。
    *   **现有工具不足**：如IPOPT虽鲁棒但未利用时间稀疏性导致速度慢；acados等工具在处理复杂非线性约束时灵活性受限。

### 2. 方法论
Hippo 结合了**正则化内点法（IPM）**和**基于投影的序列二次规划（SQP）**，核心思想如下：
*   **基于投影的 Riccati 递归**：通过零空间投影（Null-space projection）处理等式约束（如动力学、接触约束），将大规模KKT系统投影到较小的子空间中求解，降低了计算维度。
*   **正则化原对偶内点法**：
    *   引入松弛变量将不等式约束转化为等式约束。
    *   **自适应障碍策略**：采用 Mehrotra 预测器-校正器（MP-C）方案动态更新障碍参数 $\mu$，提高收敛速度。
    *   **正则化项**：在Hessian矩阵中加入微小正则化参数 $\rho$，防止松弛变量趋于零时出现数值病态。
*   **稀疏性与并行化**：利用最优控制问题（OCP）的时间稀疏结构，在递归阶段仅计算原变量步长，在后处理阶段并行重建对偶变量步长。
*   **全局化策略**：采用固定步长的回溯线搜索（Backtracking Line Search），并结合故障恢复机制（如重置 $\mu$ 和松弛变量）。

### 3. 实验设计
论文通过两个具有代表性的机器人基准测试验证了 Hippo 的性能：
*   **场景 1：UR5 机械臂随机抓取**
    *   **任务**：100个随机生成的末端执行器位姿目标，包含硬终端约束、关节限位和力矩限制。
    *   **对比方法**：fatrop, acados, aligator, mim solver。
*   **场景 2：Unitree Go2 四足机器人 2 步行走**
    *   **任务**：测试小步幅（Simple）和大步幅（Hard）下的对步（Trot）和跳跃（Hopping）步态。
    *   **约束设置**：分为“简化版”（Reduced，无摩擦锥）和“完整版”（Full，包含摩擦锥和位置级接触约束）。
    *   **对比方法**：同上。

### 4. 资源与算力
*   **硬件平台**：AMD Ryzen 9 7945HX 笔记本处理器。
*   **软件实现**：基于 C++ 开发，利用 Eigen 进行线性代数运算，BLASFEO 进行高性能矩阵计算，CasADi 和 Pinocchio 进行自动微分和动力学建模。
*   **并行化**：实验中使用了 **4 个线程** 进行并行计算。
*   **训练时长**：该论文属于数值优化领域，不涉及深度学习训练，主要关注单次优化问题的求解耗时（通常在毫秒级）。

### 5. 实验数量与充分性
*   **实验数量**：
    *   UR5 实验：100 组随机试验。
    *   Go2 实验：2 种步态 $\times$ 2 种难度 $\times$ 2 种约束设置，每种设置 100 组随机试验，总计约 900 次优化任务。
*   **充分性与客观性**：实验覆盖了操纵（Manipulation）和移动（Locomotion）两大核心领域。对比了当前最先进的（SOTA）四种求解器，并对基线方法的参数进行了仔细调优，实验设计较为公平、客观。

### 6. 主要结论与发现
*   **鲁棒性显著占优**：在处理硬约束和秩亏损（Rank-deficient）约束时，Hippo（尤其是使用IPM处理所有约束的模式）的成功率远高于基于 ALM 的求解器（如 aligator 和 mim solver）。
*   **收敛效率高**：得益于自适应 IPM 策略，Hippo 在达到相同精度时所需的 SQP 迭代次数通常更少。
*   **速度竞争力**：虽然单次迭代耗时略高于专门针对特定结构优化的求解器（如 acados），但由于总迭代次数少且支持并行化，其总求解时间（Wall time）在复杂任务中表现最优。
*   **IPM vs ALM**：实验证明在不等式约束较多的机器人任务中，IPM 比 ALM 更稳定，且对初始参数的敏感度更低。

### 7. 优点
*   **极简实现与高性能**：在保持代码结构简洁的同时，实现了与高度优化求解器相当甚至更优的性能。
*   **参数免调优**：相比 ALM 需要针对不同任务精调惩罚参数，Hippo 的自适应障碍策略减少了人工干预。
*   **通用性强**：支持隐式动力学和跨时间步的纯状态约束，适用于多种机器人形态。

### 8. 不足与局限
*   **全局化策略尚简**：目前仅使用简单的线搜索，在极度非线性的可行域边缘可能不如 IPOPT 的“过滤器线搜索（Filter Line Search）”鲁棒。
*   **秩亏损处理**：虽然提供了 IPM 模式处理等式约束，但在极端奇异位姿下，基于投影的方法仍可能面临数值挑战。
*   **应用范围**：目前主要在仿真环境下验证，尚未展示在真实机器人硬件上的实时闭环控制效果（尽管其耗时已达到实时要求）。

（完）
