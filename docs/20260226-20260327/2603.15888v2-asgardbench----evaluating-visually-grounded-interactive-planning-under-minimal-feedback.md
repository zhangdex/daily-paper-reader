---
title: AsgardBench -- Evaluating Visually Grounded Interactive Planning Under Minimal Feedback
title_zh: AsgardBench —— 评估极简反馈下的视觉对齐交互式规划
authors: "Andrea Tupini, Lars Liden, Reuben Tan, Yu Wang, Jianfeng Gao"
date: 2026-03-16
pdf: "https://arxiv.org/pdf/2603.15888v2"
tags: ["query:robot-planning"]
score: 9.0
evidence: 评估具身智能中基于视觉的交互式规划
tldr: AsgardBench是一个专注于评估视觉驱动交互式规划能力的基准测试，旨在测试模型在极简反馈下根据视觉观察调整高层动作序列的能力。该基准包含12类任务，强调在执行过程中的条件分支选择和计划修复，通过隔离底层控制噪声，深入探究视觉语言模型在动态环境中的推理与适应水平。
selection_source: fresh_fetch
motivation: 现有基准常将高层推理与底层导航混淆，且提供过多反馈，无法有效评估模型仅依赖视觉观察进行动态计划调整的能力。
method: 设计了108个受控任务实例，通过改变物体状态和场景配置，要求模型根据图像、动作历史和简单的成功/失败信号进行交互式规划。
result: 实验表明领先的视觉语言模型在失去视觉输入后性能骤降，暴露出其在视觉定位和状态跟踪以支持交互式规划方面的显著弱点。
conclusion: AsgardBench证明了当前模型在利用视觉反馈进行实时计划修正方面仍存在挑战，为具身智能的交互式规划研究提供了精准的评估工具。
---

## 摘要
通过 AsgardBench，我们旨在评估视觉对齐的高层动作序列生成和交互式规划，特别关注执行过程中基于视觉观察的规划调整，而非导航或底层操作。在具身智能（Embodied AI）基准测试领域，AsgardBench 针对的是交互式规划这一能力类别，它比离线高层规划更复杂，因为它要求智能体根据环境反馈修正规划，但又与底层执行有所区别。不同于以往将推理与导航混为一谈，或提供丰富的纠错反馈以替代感知的具身智能基准测试，AsgardBench 将智能体的输入限制为图像、动作历史和轻量级的成功/失败信号，从而在没有底层控制噪声的受控模拟器中隔离出交互式规划。该基准测试包含跨越 12 种任务类型的 108 个任务实例，每个实例通过物体状态、放置位置和场景配置进行系统性变化。这些受控变量创建了条件分支，使得同一条指令根据智能体观察到的情况可能需要不同的动作序列，强调了执行过程中的条件分支和规划修复。我们对领先视觉语言模型的评估表明，在没有视觉输入的情况下，性能会急剧下降，揭示了视觉对齐和状态跟踪方面的弱点，这些弱点最终损害了交互式规划。我们的基准测试聚焦于一个更具体的问题：当情况不如预期时，模型是否真的能利用其所见来调整规划？

## Abstract
With AsgardBench we aim to evaluate visually grounded, high-level action sequence generation and interactive planning, focusing specifically on plan adaptation during execution based on visual observations rather than navigation or low-level manipulation. In the landscape of embodied AI benchmarks, AsgardBench targets the capability category of interactive planning, which is more sophisticated than offline high-level planning as it requires agents to revise plans in response to environmental feedback, yet remains distinct from low-level execution. Unlike prior embodied AI benchmarks that conflate reasoning with navigation or provide rich corrective feedback that substitutes for perception, AsgardBench restricts agent input to images, action history, and lightweight success/failure signals, isolating interactive planning in a controlled simulator without low-level control noise. The benchmark contains 108 task instances spanning 12 task types, each systematically varied through object state, placement, and scene configuration. These controlled variations create conditional branches in which a single instruction can require different action sequences depending on what the agent observes, emphasizing conditional branching and plan repair during execution. Our evaluations of leading vision language models show that performance drops sharply without visual input, revealing weaknesses in visual grounding and state tracking that ultimately undermine interactive planning. Our benchmark zeroes in on a narrower question: can a model actually use what it sees to adapt a plan when things do not go as expected?

---

## 论文详细总结（自动生成）

### AsgardBench：评估极简反馈下的视觉对齐交互式规划 —— 论文深度总结

#### 1. 核心问题与整体含义（研究动机和背景）
具身智能（Embodied AI）的现有基准测试（如 ALFRED, ALFWorld）存在两个主要缺陷：
*   **能力混淆**：将高层推理与底层导航、运动控制混在一起，导致难以区分失败是由于“没想对”还是“没走对”。
*   **反馈冗余**：许多基准提供详细的文本错误信息（如“杯子不在桌上”），这实际上充当了“视觉替代品”，使得模型无需真正理解图像也能通过文本先验完成任务。

**AsgardBench 的核心目标**是隔离出**视觉对齐的交互式规划（Visually Grounded Interactive Planning）**能力。它研究的是：在没有导航干扰、仅有极简反馈（成功/失败信号）的情况下，智能体能否仅凭视觉观察来识别环境状态，并动态修正其高层动作序列。

#### 2. 论文提出的方法论
*   **核心思想**：通过抽象掉底层控制，强制模型依赖视觉感知进行逻辑推理。
*   **关键技术细节**：
    *   **动作抽象**：基于 AI2-THOR 模拟器，引入 `FIND` 动作自动完成导航，`PUT` 动作自动处理放置逻辑，从而消除物理控制噪声。
    *   **条件分支设计**：同一指令（如“洗杯子并收好”）会根据初始状态（杯子是否脏、水槽是否被占用）产生不同的执行路径。
    *   **交互式循环**：智能体每步需输出**完整的剩余计划序列**，但环境仅执行第一条动作。随后，智能体接收更新后的图像和简单的“成功/失败”信号，重新评估并修正计划。
    *   **视觉增强**：为了消除模拟器中物体悬浮的歧义，在智能体持物时在图像上叠加半透明“手部”遮罩。
    *   **记忆辅助（Things to Remember）**：允许模型在每步生成一段文本笔记传给下一步的自己，以弥补视觉模型在状态跟踪上的短板。

#### 3. 实验设计
*   **数据集/场景**：基于 AI2-THOR 的厨房、客厅和浴室场景。
*   **Benchmark 规模**：包含 108 个受控任务实例，涵盖 12 种任务类型（如烹饪、清洁、整理等）。
*   **对比方法**：
    *   **前沿模型**：GPT-4o, Kimi-K2.5, Claude Opus 4.5, Gemini 3 Pro。
    *   **开源/轻量模型**：Qwen3-VL, GLM-4.6V, Mistral-Large-3, Llama-4 Maverick。
*   **消融实验设置**：
    *   **反馈类型**：无反馈 vs. 简单成功/失败（基准）vs. 详细错误描述。
    *   **视觉输入**：双图（前后状态）vs. 单图（当前状态）vs. 纯文本模式。
    *   **辅助机制**：有无手部遮罩、有无记忆笔记。

#### 4. 资源与算力
*   **训练算力**：论文未提及任何模型训练过程，因为该研究属于**评估型工作**，直接调用现有模型的 API 或进行推理。
*   **推理算力**：实验通过 Azure Foundry 和 OpenRouter 接口调用模型。文中未明确列出推理所需的具体 GPU 型号或总时长，但提到为了控制成本，部分昂贵模型（如 GPT-5.2, Claude Opus）未参与所有消融实验。

#### 5. 实验数量与充分性
*   **实验规模**：108 个任务实例虽然数量不算巨大，但每个实例都经过了确定性状态机验证，确保 100% 可解。
*   **充分性**：实验设计非常全面。通过对比“纯文本”和“视觉”模式，有力证明了该基准对视觉定位的依赖性。消融实验覆盖了感知、记忆、反馈三个维度，能够精准定位模型的失败原因。
*   **客观性**：针对 API 模型的不确定性，大多数实验运行了多次并取**中位数**，保证了结果的可靠性。

#### 6. 主要结论与发现
*   **视觉是必需的**：所有高性能模型在视觉模式下的表现均远优于纯文本模式（性能提升一倍以上），证明 AsgardBench 成功避免了“文本先验”捷径。
*   **状态跟踪脆弱**：模型经常忘记物体状态（如是否已清洗）或自身状态（是否正拿着东西）。
*   **反馈依赖性**：当提供“详细反馈”时，纯文本模型的表现甚至能追平视觉模型，说明当前模型更擅长从文本提示中纠错，而非从视觉观察中发现问题。
*   **长程规划困难**：任务步骤越长、分支越多，成功率越低。模型容易陷入重复动作的死循环。

#### 7. 优点
*   **隔离性极佳**：成功将高层规划与底层控制解耦，是目前评估具身智能“大脑”推理能力的精准工具。
*   **逻辑深度高**：通过物体状态触发的条件分支设计，比单纯的指令跟随更接近真实世界的复杂性。
*   **诊断功能强**：消融实验设计精巧，能清晰揭示模型是“看不见”、“记不住”还是“想不通”。

#### 8. 不足与局限
*   **环境多样性有限**：仅限于 AI2-THOR 的模拟环境，视觉真实度和物体种类与现实世界仍有差距。
*   **物理交互简化**：完全忽略了抓取角度、碰撞等物理约束，可能导致评估结果在迁移到真实机器人时过于乐观。
*   **静态场景**：环境在智能体不动作时是静态的，未考虑动态变化或其他智能体的干扰。

（完）
