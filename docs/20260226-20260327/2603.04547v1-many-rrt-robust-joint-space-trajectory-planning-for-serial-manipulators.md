---
title: "Many-RRT*: Robust Joint-Space Trajectory Planning for Serial Manipulators"
title_zh: "Many-RRT*：串联机械臂的鲁棒关节空间轨迹规划"
authors: "Theodore M. Belmont, Benjamin A. Christie, Anton Netchaev"
date: 2026-03-04
pdf: "https://arxiv.org/pdf/2603.04547v1"
tags: ["query:robot-planning"]
score: 9.0
evidence: 串联机械臂的关节空间轨迹规划
tldr: "针对高自由度串联机械臂在关节空间规划时因逆运动学多解导致的规划效率低和易失败问题，本文提出 Many-RRT* 算法。该方法扩展了 RRT*-Connect，通过并行处理多个逆运动学解作为目标点，并同时从起始点和多个目标点生长搜索树。实验证明，该算法在保持运行效率的同时，显著提升了规划成功率并降低了轨迹代价，具有渐近最优性。"
selection_source: fresh_fetch
motivation: 机械臂末端位姿对应多个关节空间解，传统方法若选错目标解会导致规划失败或轨迹质量差。
method: "提出 Many-RRT* 算法，通过生成多个 IK 解并并行构建多个目标树与单一起始树进行连接，实现多目标并行规划。"
result: "在多种环境下，该算法实现了 100% 的规划成功率，且在相同运行时间内轨迹代价比现有方法降低了 44.5%。"
conclusion: "Many-RRT* 有效解决了关节空间多目标规划的鲁棒性问题，是高维机械臂运动规划的高效且渐近最优的解决方案。"
---

## 摘要
高自由度（DoF）串联机械臂的快速发展，使得在高维空间中使用快速的基于采样的运动规划器变得必不可少。虽然像快速探索随机树（RRT）这样基于采样的规划器被广泛使用，但由于正向运动学的不可逆性，在机械臂的关节空间中进行规划面临着重大挑战。单个任务空间的末端执行器位姿可能对应多个构型空间状态，这为规划器带来了一个多臂老虎机问题。在复杂环境中，仅仅选择错误的关节空间目标就可能导致次优轨迹，甚至无法找到可行方案。为了解决这一规划问题，我们提出了 Many-RRT*：它是 RRT*-Connect 的一种扩展，能够并行地向多个目标进行规划。通过生成多个逆运动学（IK）解，并从这些目标构型中与单个起始树同步生长独立的树，Many-RRT* 确保了计算资源不会浪费在次优的 IK 解上。该方法保持了鲁棒的收敛性和渐近最优性。在不同机器人形态和多样化障碍物环境下的实验评估表明，与之前的 RRT 迭代版本相比，Many-RRT* 在不牺牲运行性能的前提下，提供了更高质量的轨迹（在相同运行时间内成本降低了 44.5%），且成功率显著提高（100% 对比次优方案的 1.6%）。

## Abstract
The rapid advancement of high degree-of-freedom (DoF) serial manipulators necessitates the use of swift, sampling-based motion planners for high-dimensional spaces. While sampling-based planners like the Rapidly-Exploring Random Tree (RRT) are widely used, planning in the manipulator's joint space presents significant challenges due to non-invertible forward kinematics. A single task-space end-effector pose can correspond to multiple configuration-space states, creating a multi-arm bandit problem for the planner. In complex environments, simply choosing the wrong joint space goal can result in suboptimal trajectories or even failure to find a viable plan. To address this planning problem, we propose Many-RRT*: an extension of RRT*-Connect that plans to multiple goals in parallel. By generating multiple IK solutions and growing independent trees from these goal configurations simultaneously alongside a single start tree, Many-RRT* ensures that computational effort is not wasted on suboptimal IK solutions. This approach maintains robust convergence and asymptotic optimality. Experimental evaluations across robot morphologies and diverse obstacle environments demonstrate that Many-RRT* provides higher quality trajectories (44.5% lower cost in the same runtime) with a significantly higher success rate (100% vs. the next best of 1.6%) than previous RRT iterations without compromising on runtime performance.

---

## 论文详细总结（自动生成）

### Many-RRT*: 串联机械臂的鲁棒关节空间轨迹规划总结

#### 1. 论文的核心问题与整体含义
在高自由度（DoF）串联机械臂的运动规划中，通常目标是以任务空间（末端位姿）给出的，但规划是在关节空间（构型空间）进行的。由于正向运动学（FK）的非互逆性，一个末端位姿对应多个（甚至无限个）关节配置。
*   **核心问题**：传统的采样规划器（如 RRT*）通常只选择一个逆运动学（IK）解作为目标。如果选中的目标点处于构型空间的“死胡同”或障碍物后方，规划将失败或产生极高代价的轨迹。这本质上是一个“多臂老虎机”问题，即如何从多个潜在目标中选出最优解。
*   **研究动机**：通过并行处理多个 IK 目标解，消除对单一目标选择的依赖，从而提高复杂环境下的规划成功率和轨迹质量。

#### 2. 论文提出的方法论
论文提出了 **Many-RRT*** 算法，其核心思想是将双向 RRT 扩展为“多向”并行搜索：
*   **多目标 IK 采样**：首先利用预计算的关节空间 KD-Tree 进行拒绝采样，生成大量种子点，再通过约束优化求解出 $N$ 个不同的 IK 目标解。
*   **并行树生长**：
    *   为每个 IK 目标解独立建立一棵“目标树”（Goal Tree）。
    *   建立一棵从起始点出发的“起始树”（Start Tree）。
    *   所有树在独立的线程中并行生长。
*   **采样策略与连接**：起始树以一定概率探索空间，以一定概率尝试连接到任意一棵目标树。
*   **渐近最优性**：引入 RRT* 的重连（Rewire）逻辑，确保随着迭代次数增加，路径代价趋于全局最优。
*   **并行化实现**：利用现代多核处理器的并行能力，使得维护多棵树的时间开销与传统单棵树相当。

#### 3. 实验设计
*   **实验场景**：设计了四种难度递增的仿真环境：
    1.  **Table（桌面）**：基本自由空间。
    2.  **Wall（墙壁）**：简单障碍物绕行。
    3.  **Passage（通道）**：构型空间被高度分割，存在狭窄通道。
    4.  **Random（随机）**：布满随机球体障碍物的极高难度环境。
*   **测试平台**：6-DoF 的 UR10e 机械臂和 7-DoF 的 Franka Panda 机械臂。
*   **Benchmark（对比方法）**：RRT* 和 RRT*-Connect。
*   **评估指标**：成功率、首次解迭代次数、中值路径代价、平均运行时间。

#### 4. 资源与算力
*   **硬件环境**：实验在配备 **AMD Ryzen 9 7950X CPU**（16核/32线程）和 128GB RAM 的工作站上完成。
*   **并行计算**：算法充分利用了 CPU 的多线程能力。文中提到 Many-RRT* 使用了 10 个 IK 解，即同时维护 11 棵树（10 目标树 + 1 起始树）。
*   **算力说明**：虽然未直接使用 GPU，但作者指出该方法可以轻易移植到 GPU 或 NPU 上以进一步提升性能。

#### 5. 实验数量与充分性
*   **实验规模**：每个场景、每种算法、每种机械臂均进行了 **500 次独立试验**，总计进行了数千次实验。
*   **充分性**：实验覆盖了不同自由度的机器人和从简单到极难的多种环境。通过 500 次试验的统计（10th/50th/90th 分位数），结果具有很强的统计学意义。
*   **客观性**：对比实验在相同的超时限制（3000ms）和最大迭代次数（3000次）下进行，确保了公平性。

#### 6. 论文的主要结论与发现
*   **成功率大幅提升**：在最难的“随机”环境中，传统 RRT* 和 RRT*-Connect 的成功率仅为 1.4% - 8.8%，而 Many-RRT* 达到了 **96.8% - 100%**。
*   **轨迹质量更优**：在相同运行时间内，Many-RRT* 找到的路径代价比基准算法平均降低了 **44.5%**。
*   **收敛速度快**：在 7-DoF 机器人实验中，Many-RRT* 找到首个可行解所需的迭代次数比基准算法少 6 倍。
*   **运行效率**：得益于并行化，Many-RRT* 在增加搜索树数量的同时，并未显著增加总运行时间。

#### 7. 优点
*   **解决多解歧义**：巧妙地通过并行化解决了 IK 多解带来的规划不确定性。
*   **全局最优潜力**：通过对 IK 预映射流形的采样，使算法从局部渐近最优提升向全局渐近最优迈进。
*   **高鲁棒性**：在狭窄通道和复杂障碍物环境中表现极佳，解决了传统方法易陷入局部解的问题。
*   **易于集成**：算法逻辑清晰，且天然适合现代多核计算架构。

#### 8. 不足与局限
*   **计算开销上限**：虽然并行化缓解了压力，但 `CONN_TREE`（树连接）步骤需要比较起始树与所有目标树的节点，随着节点数增加，这部分的计算复杂度会成为瓶颈。
*   **内存占用**：同时维护多棵 RRT* 树会消耗比单树更多的内存。
*   **IK 种子点依赖**：算法性能仍部分取决于初始 IK 种子点的采样质量，若种子点未能覆盖关键的构型空间区域，仍可能影响最优性。
*   **动态环境未测试**：目前实验主要集中在静态障碍物环境，尚未探讨在动态变化环境中的实时重规划表现。

（完）
