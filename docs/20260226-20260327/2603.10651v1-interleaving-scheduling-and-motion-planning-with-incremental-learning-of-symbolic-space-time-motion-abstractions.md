---
title: Interleaving Scheduling and Motion Planning with Incremental Learning of Symbolic Space-Time Motion Abstractions
title_zh: 通过符号化时空运动抽象的增量学习实现调度与运动规划的交替
authors: "Elisa Tosello, Arthur Bit-Monnot, Davide Lusuardi, Alessandro Valentini, Andrea Micheli"
date: 2026-03-11
pdf: "https://arxiv.org/pdf/2603.10651v1"
tags: ["query:robot-planning"]
score: 9.0
evidence: 交替进行多目标导航的任务调度与运动规划
tldr: 针对多目标导航中的调度与运动规划（SMP）问题，本文提出一种交替执行调度与运动规划的新型框架。该框架通过增量学习循环，利用调度器生成候选方案，并由运动规划器反馈空间冲突和时间调整等符号化信息，从而引导调度器生成满足复杂时空约束的可行方案。实验证明该方法在物流和车间调度基准测试中能有效生成同步运动计划。
selection_source: fresh_fetch
motivation: 在自动化仓库等场景中，如何在资源、时间和运动约束下安全高效地执行预定义任务是多目标导航面临的主要挑战。
method: 提出一种将现成调度器与运动规划器交替运行的框架，通过运动规划器返回的符号化反馈（空间冲突和时间调整）进行增量学习。
result: 在物流和作业车间调度基准测试中，该框架成功在复杂的时空约束下生成了有效的同步运动计划。
conclusion: 通过交替调度与运动规划并引入符号化反馈机制，可以有效解决多机器人协作中关键的同步运动规划难题。
---

## 摘要
任务与运动规划将高层任务排序（做什么）与底层运动规划（如何做）相结合，以生成可行且无碰撞的执行方案。然而，在许多现实领域（如自动化仓库）中，任务是预定义的，挑战随之转向如何在资源、时间和运动约束下，安全且高效地确定是否、何时以及如何执行这些任务。在本文中，我们将此形式化为共享工作空间中多对象导航的调度与运动规划问题。我们提出了一种新颖的解决方案框架，在增量学习循环中交替使用现成的调度器和运动规划器。调度器生成候选计划，而运动规划器检查可行性并返回符号化反馈（即空间冲突和时间调整），以引导调度器寻找运动可行的解决方案。我们在增加了运动任务的物流和作业车间调度基准测试上验证了我们的提议，并使用了最先进的调度器和基于采样的运动规划器。我们的结果表明，该框架在复杂的时空约束下（其中同步运动至关重要）生成有效计划方面具有显著效果。

## Abstract
Task and Motion Planning combines high-level task sequencing (what to do) with low-level motion planning (how to do it) to generate feasible, collision-free execution plans. However, in many real-world domains, such as automated warehouses, tasks are predefined, shifting the challenge to if, when, and how to execute them safely and efficiently under resource, time and motion constraints. In this paper, we formalize this as the Scheduling and Motion Planning problem for multi-object navigation in shared workspaces. We propose a novel solution framework that interleaves off-the-shelf schedulers and motion planners in an incremental learning loop. The scheduler generates candidate plans, while the motion planner checks feasibility and returns symbolic feedback, i.e., spatial conflicts and timing adjustments, to guide the scheduler towards motion-feasible solutions. We validate our proposal on logistics and job-shop scheduling benchmarks augmented with motion tasks, using state-of-the-art schedulers and sampling-based motion planners. Our results show the effectiveness of our framework in generating valid plans under complex temporal and spatial constraints, where synchronized motion is critical.

---

## 论文详细总结（自动生成）

这篇论文介绍了一种针对多机器人协作场景的“调度与运动规划”（Scheduling and Motion Planning, SAMP）问题的创新框架。以下是对该论文的深度结构化总结：

### 1. 核心问题与整体含义
*   **研究动机**：传统的任务与运动规划（TAMP）侧重于解决“做什么”和“怎么做”。但在自动化仓库等现实工业场景中，任务通常是预定义的，核心挑战变成了“何时”以及“如何”在资源、时间和运动约束下安全高效地执行这些任务。
*   **核心问题**：如何在高层符号化调度（处理优先级、资源分配和时间窗）与底层连续空间运动规划（处理动力学可行性、避障和多机同步）之间建立高效的反馈机制，以解决多对象导航中的时空冲突。

### 2. 方法论
该论文提出了一种通过**增量学习符号化时空运动抽象**来交替执行调度与运动规划的框架：
*   **核心思想**：将调度器视为“提议者”，运动规划器视为“验证者”。调度器生成候选计划，运动规划器检查其几何与时间可行性。如果不可行，运动规划器会返回符号化的“精化约束”（Refinements），引导调度器在下一轮迭代中避开冲突。
*   **关键技术细节**：
    *   **并行运动组（Parallel Motion Groups）**：将时间上重叠的运动任务分组，以便集中检测多机碰撞。
    *   **分层架构（Layered Architecture）**：
        *   **第一层（Layer 1）**：单任务几何与时间检查，快速排除单机不可达或超时的情况。
        *   **第二层（Layer 2）**：多机同步检查，使用 ST-RRT* 算法处理复杂的时空冲突。
    *   **符号化精化（Refinements）**：
        *   **几何精化**：识别导致不可达的障碍物（如未开启的门），向调度器反馈必须先执行特定动作（如开门）。
        *   **时间精化**：计算必要的延迟（Delays）和持续时间（Durations），强制调度器调整任务的起始时间或执行时长。
*   **算法流程**：从初始问题出发，调度器生成计划 -> 运动规划器验证 -> 失败则添加约束并重启调度 -> 成功则输出包含轨迹的完整方案。

### 3. 实验设计
*   **场景与数据集**：
    *   **物流场景（Logistics）**：1-3 台机器人在带有狭窄通道和可移动门（障碍物）的仓库中搬运货物。
    *   **作业车间调度（JSP）**：结合了经典的 Taillard 基准测试，要求机器人在不同机器间运送工件，且机器带有需要操作的门。
*   **对比基准（Benchmark）**：
    *   **调度器**：对比了 Aries（基于 CP-SAT）和 OR-Tools（CPSE），涵盖了带状态变量（Fluents）和不带状态变量的配置。
    *   **规划模式**：对比了“最优”与“非最优”模式。
    *   **消融实验**：对比了有无分层架构、有无时间精化反馈的效果。
    *   **基准线**：与完全顺序执行（无并行化）的方案进行对比。

### 4. 资源与算力
*   **硬件环境**：实验运行在 AMD EPYC 7413 处理器上。
*   **参数设置**：
    *   总超时时间：1800 秒。
    *   单次运动规划调用超时（$t_p$）：10 秒（若失败会倍增）。
    *   内存限制：20 GB。
*   **算力说明**：论文未提及 GPU 使用情况，主要依赖 CPU 进行逻辑推理和采样规划。

### 5. 实验数量与充分性
*   **实验规模**：共设计了 60 个典型实例（24 个物流实例 + 36 个 JSP 实例），每个实例运行 3 次以减少采样算法随机性的影响。
*   **充分性评价**：实验涵盖了不同机器人数量（1-3 台）、不同空间复杂度（门开/关）和不同任务密度。通过消融实验验证了分层架构的必要性（开启分层后解决的问题数量从 140 提升至 359），实验设计较为客观、公平，对比了多种主流调度引擎。

### 6. 主要结论与发现
*   **交替机制的必要性**：传统的顺序流水线（先调度再规划，无反馈）在复杂时空约束下几乎无法求解。
*   **性能提升**：该框架生成的并行计划相比顺序执行方案，在完工时间（Makespan）上平均优化了 **41%**。
*   **状态表示的影响**：使用带状态变量（Fluents）的调度器（如 Aries with fluents）表现最佳，因为丰富的状态信息能更好地引导精化过程。
*   **瓶颈所在**：运动规划仍然是计算重灾区，占据了总规划时间的 **74% 到 92%**。

### 7. 优点
*   **通用性强**：框架不依赖特定的调度器或运动规划器，具有良好的模块化特性。
*   **处理连续空间**：不同于离散的 MAPF（多智能体路径规划），该方法直接在连续配置空间中处理动力学约束。
*   **高效反馈**：通过符号化抽象（而非底层数值反馈）连接了高层逻辑与底层几何，显著缩小了搜索空间。

### 8. 不足与局限
*   **维度灾难风险**：虽然使用了分层和分组，但随着机器人数量进一步增加（如超过 10 台），ST-RRT* 的采样效率和调度器的约束组合爆炸仍是潜在威胁。
*   **2D 局限性**：目前实验主要集中在 2D 导航，虽然理论上可扩展至 3D 机械臂操作，但计算复杂度会剧增。
*   **假设限制**：框架假设机器人可以随时执行“停止-等待”动作，这在某些具有动态不稳定性或严格动量要求的场景中可能不成立。

（完）
