---
title: Bilevel Planning with Learned Symbolic Abstractions from Interaction Data
title_zh: 基于交互数据学习符号抽象的双层规划
authors: "Fatih Dogangun, Burcu Kilic, Serdar Bahar, Emre Ugur"
date: 2026-03-09
pdf: "https://arxiv.org/pdf/2603.08599v1"
tags: ["query:robot-planning"]
score: 9.0
evidence: 结合符号抽象与连续动力学的双层规划
tldr: 本研究针对智能体在复杂环境中需兼顾连续动力学与离散表示的需求，提出一种双层神经符号规划框架。该框架通过从交互数据中学习概率符号规则进行高层快速规划，并利用连续效应模型在底层进行方案验证与前向搜索。该方法克服了以往符号抽象方法缺乏验证机制且难以捕捉连续动态的局限，在多物体操作任务中实现了高效且可靠的规划，性能可媲美连续搜索。
selection_source: fresh_fetch
motivation: 现有的符号抽象方法通常依赖确定性领域且缺乏对连续动力学的验证，导致在复杂环境中的规划可靠性不足。
method: 提出一种双层神经符号框架，高层利用学习到的概率符号规则快速生成候选方案，底层通过连续效应模型进行验证和前向搜索。
result: 在多物体操作任务中，该方法能有效识别失败方案，且在保持高效符号推理的同时，达到了与连续前向搜索相当的规划性能。
conclusion: 结合符号抽象与连续动力学验证的双层规划框架，显著提升了智能体在复杂环境中的规划效率与鲁棒性。
---

## 摘要
智能体必须在连续动力学和离散表示上进行推理，以便在复杂环境中生成有效的规划。先前的研究表明，符号抽象可以从通过机器人无监督探索训练的神经效应预测器中涌现。然而，这些方法依赖于确定性符号域，缺乏验证生成的符号规划的机制，且仅在抽象层面运行，往往无法捕捉环境的连续动力学。为了克服这些局限性，我们提出了一个双层神经符号框架，其中学习到的概率符号规则在高层快速生成候选规划，而学习到的连续效应模型在底层验证这些规划，并在必要时执行前向搜索。我们在多物体操控任务上的实验表明，所提出的双层方法优于纯符号方法，能够通过验证可靠地识别失败的规划，并在通过高效符号推理解决大多数问题的同时，实现了在统计上与连续前向搜索相当的规划性能。

## Abstract
Intelligent agents must reason over both continuous dynamics and discrete representations to generate effective plans in complex environments. Previous studies have shown that symbolic abstractions can emerge from neural effect predictors trained with a robot's unsupervised exploration. However, these methods rely on deterministic symbolic domains, lack mechanisms to verify the generated symbolic plans, and operate only at the abstract level, often failing to capture the continuous dynamics of the environment. To overcome these limitations, we propose a bilevel neuro-symbolic framework in which learned probabilistic symbolic rules generate candidate plans rapidly at the high level, and learned continuous effect models verify these plans and perform forward search when necessary at the low level. Our experiments on multi-object manipulation tasks demonstrate that the proposed bilevel method outperforms symbolic-only approaches, reliably identifying failing plans through verification, and achieves planning performance statistically comparable to continuous forward search while resolving most problems via efficient symbolic reasoning.

---

## 论文详细总结（自动生成）

这是一份关于论文《Bilevel Planning with Learned Symbolic Abstractions from Interaction Data》的结构化总结：

### 1. 核心问题与研究动机
*   **核心问题**：如何在连续的机器人操作环境中，既利用符号规划的高效性，又保持连续空间推理的精确性？
*   **研究动机**：
    *   传统的符号抽象方法（如 DeepSym）通常依赖确定性规则，忽略了环境的随机性。
    *   符号抽象本质上是有损的，往往会丢失关键的几何或动力学约束，导致生成的符号规划在实际执行时失败。
    *   缺乏一种验证机制来预判符号规划在连续域中的可行性，且在符号规划失效时缺乏后备方案。

### 2. 方法论
该论文提出了一种**双层神经符号规划框架**，核心组件包括：
*   **双模型方案 (Dual-Model Scheme)**：
    *   **符号模型 (Symbolic Model)**：使用 Gumbel-Sigmoid 瓶颈层，旨在从交互数据中发现离散符号并学习概率算子（PPDDL）。
    *   **动力学模型 (Dynamics Model)**：采用 Sigmoid 激活函数，专注于高精度的连续效应预测，用于规划验证和底层搜索。
*   **第一层：概率符号规划与验证**：
    *   **概率规划**：从学习到的概率算子中采样生成 $N$ 个确定性 PDDL 领域，利用现成的 AI 规划器（Fast-Downward）生成候选规划序列。
    *   **连续验证**：利用“动力学模型”对候选符号规划进行前向模拟。如果预测的最终状态与目标的欧氏距离小于阈值 $\tau_{verify}$，则接受该规划。
*   **第二层：连续前向搜索后备机制**：
    *   如果所有符号规划均未通过验证，则启动基于加权 A* 算法的连续空间搜索。
    *   **动作剪枝策略**：为了处理高维动作空间，仅生成指向目标位置或物体自身的候选动作，以提高搜索效率。

### 3. 实验设计
*   **实验场景**：模拟桌面环境，包含 UR10e 机械臂和不同数量（2-4个）、不同类型（大小）的方块。
*   **任务设置**：生成 1 到 5 步动作长度的目标状态，要求机器人通过拾取和放置（Pick-and-Place）达成目标。
*   **对比基准 (Benchmarks)**：
    1.  **确定性符号规划器**：仅使用最频繁效应的基准方法（类似先前研究 [6]）。
    2.  **概率符号规划器**：本文提出的采样多领域方法。
    3.  **连续前向搜索**：直接在连续空间进行 A* 搜索。
    4.  **双层规划器 (Bilevel)**：本文提出的完整框架。

### 4. 资源与算力
*   **训练数据**：140,000 个交互样本。
*   **训练参数**：使用 Adam 优化器，学习率 0.0001，Batch Size 128，训练 4000 个 Epoch。
*   **算力说明**：文中**未明确提及**具体的 GPU 型号、数量或总训练时长。

### 5. 实验数量与充分性
*   **实验规模**：针对每种物体数量（2, 3, 4）和动作步数（1-5）的组合，分别测试了 100 个独立问题。
*   **验证充分性**：
    *   进行了模型预测误差的定量分析（对比了符号模型与动力学模型的精度）。
    *   对验证机制的分类性能（F1 分数、准确率）进行了消融实验，测试了不同阈值的影响。
    *   使用了配对 t 检验（Paired t-tests）来验证方法间差异的统计显著性。
*   **评价**：实验设计较为全面，涵盖了从单模型性能到整体系统成功率的多个维度，对比实验客观且具有统计学意义。

### 6. 主要结论与发现
*   **双模型必要性**：符号模型在离散推理上更强，而动力学模型在连续预测上更准，两者结合能显著提升规划成功率。
*   **概率规划优势**：考虑算子的概率分布比仅考虑确定性规则能解决更多复杂问题（成功率显著提升，p < 0.0001）。
*   **双层框架效能**：双层规划器的性能在统计上与纯连续前向搜索相当（p = 0.71），但它能通过高效的符号推理解决大部分问题，仅在必要时才调用昂贵的连续搜索。
*   **验证可靠性**：连续验证机制达到了 0.91 的准确率，能有效拦截 87.3% 的错误符号规划。

### 7. 优点与亮点
*   **神经符号结合**：成功将无监督符号学习与经典 AI 规划器集成，并引入了连续域的“守门员”（验证器）。
*   **鲁棒性**：通过概率采样和双层回退机制，增强了系统处理模型不确定性和复杂几何约束的能力。
*   **效率平衡**：既保留了符号规划的快速响应，又具备了连续搜索的保底能力。

### 8. 不足与局限
*   **数据采集**：依赖随机探索收集数据，对于长程规划中罕见但关键的状态转换覆盖不足。
*   **阈值敏感性**：验证阈值 $\tau_{verify}$ 是固定的，可能需要根据物体数量或任务难度进行动态调整。
*   **层间衔接**：当符号规划失败转入连续搜索时，是从头开始搜索，未能利用已有的符号规划信息作为“热启动”引导。
*   **应用限制**：目前仅在简单的几何体（方块）操作中验证，尚未在具有复杂动力学或非刚体交互的环境中测试。

（完）
