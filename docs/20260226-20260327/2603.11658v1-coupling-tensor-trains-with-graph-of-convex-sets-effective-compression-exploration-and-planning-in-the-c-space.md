---
title: "Coupling Tensor Trains with Graph of Convex Sets: Effective Compression, Exploration, and Planning in the C-Space"
title_zh: 将张量列与凸集图耦合：在配置空间中进行有效的压缩、探索与规划
authors: "Gerhard Reinerth, Riddhiman Laha, Marcello Romano"
date: 2026-03-12
pdf: "https://arxiv.org/pdf/2603.11658v1"
tags: ["query:robot-planning"]
score: 9.0
evidence: 用于轨迹生成的创新运动规划框架
tldr: 本研究提出 TANGO 框架，通过结合张量列（Tensor Train）分解与凸集图（GCS）优化，解决了高维配置空间中运动规划的建模难题。TANGO 利用张量压缩技术近似可行空间，并将其嵌入图结构进行几何感知规划，克服了传统方法对预定义凸分解的依赖。实验表明，该方法在实现高效空间压缩的同时，能生成更高质量的轨迹，显著提升了复杂环境下规划的扩展性。
selection_source: fresh_fetch
motivation: 传统的凸集图（GCS）规划方法高度依赖于难以预先获取的高维配置空间精确凸分解。
method: 提出 TANGO 框架，利用张量列分解压缩近似可行配置空间，并将其集成到类 GCS 的图优化结构中。
result: 在平面和真实机器人上的实验验证了该方法具有出色的压缩效率和更优的轨迹生成质量。
conclusion: TANGO 通过耦合张量压缩与结构化图推理，为高维机器人配置空间的高效表示和运动规划开辟了新方向。
---

## 摘要
我们提出了 TANGO（张量与图优化），这是一种新型运动规划框架，它将基于张量的压缩与结构化图优化相结合，以实现高效且可扩展的轨迹生成。虽然像凸集图 (GCS) 这样基于优化的规划器为生成平滑、最优轨迹提供了强大的工具，但它们通常依赖于高维配置空间的预定义凸表征，而这一要求对于通用机器人任务而言往往是难以实现的。TANGO 进一步利用张量列 (Tensor Train) 分解以压缩形式近似可行配置空间，从而实现对任务相关区域的快速发现和估计。随后，这些区域被嵌入到类 GCS 结构中，从而实现既遵循系统约束又考虑环境复杂性的几何感知运动规划。通过将基于张量的压缩与结构化图推理相结合，TANGO 实现了高效的几何感知运动规划，并为未来机器人系统中更具表现力和可扩展性的配置空间表示奠定了基础。在平面和真实机器人上进行的严格仿真研究证实了我们在有效压缩和更高质量轨迹方面的结论。

## Abstract
We present TANGO (Tensor ANd Graph Optimization), a novel motion planning framework that integrates tensor-based compression with structured graph optimization to enable efficient and scalable trajectory generation. While optimization-based planners such as the Graph of Convex Sets (GCS) offer powerful tools for generating smooth, optimal trajectories, they typically rely on a predefined convex characterization of the high-dimensional configuration space-a requirement that is often intractable for general robotic tasks. TANGO builds further by using Tensor Train decomposition to approximate the feasible configuration space in a compressed form, enabling rapid discovery and estimation of task-relevant regions. These regions are then embedded into a GCS-like structure, allowing for geometry-aware motion planning that respects both system constraints and environmental complexity. By coupling tensor-based compression with structured graph reasoning, TANGO enables efficient, geometry-aware motion planning and lays the groundwork for more expressive and scalable representations of configuration space in future robotic systems. Rigorous simulation studies on planar and real robots reinforce our claims of effective compression and higher quality trajectories.

---

## 论文详细总结（自动生成）

以下是对论文《Coupling Tensor Trains with Graph of Convex Sets: Effective Compression, Exploration, and Planning in the C-Space》的结构化深入总结：

### 1. 核心问题与研究背景
*   **核心问题**：在高维机器人配置空间（C-Space）中，如何高效地表征可行区域并生成最优轨迹。
*   **研究背景**：
    *   **GCS（凸集图）规划器**虽然强大，但高度依赖于对配置空间的预定义凸分解，这在复杂任务中通常是不可计算的（P-hard）。
    *   **采样规划器（如 RRT）**在处理窄通道或特定任务约束（如奇异性规避）时效率低下，且生成的路径往往不够平滑。
    *   **维度灾难**：随着机器人自由度（DoF）增加，存储和搜索配置空间的成本呈指数级增长。

### 2. 方法论：TANGO 框架
TANGO（Tensor ANd Graph Optimization）将张量压缩技术与结构化图优化相结合，分为两个主要阶段：

*   **阶段一：张量列（Tensor Train, TT）预处理**
    1.  **指标定义**：定义任务相关的度量函数（如 Yoshikawa 可操作度或黎曼度量），将其转化为概率密度函数（PDF）。
    2.  **TT-Cross 近似**：利用 TT-Cross 算法以极低的秩（Rank）近似高维 PDF，将指数级存储需求转化为线性增长。
    3.  **采样与分类**：从压缩的 TT 表示中快速抽取“好”（可行）和“坏”（障碍/奇异）的配置点。
    4.  **凸区域生长（IRIS）**：以可行点为种子，利用 IRIS 算法在配置空间中膨胀出不含障碍物的最大凸多胞形。

*   **阶段二：基于 GCS 的最短路径规划**
    1.  **图构建**：将发现的凸区域作为节点，若两个区域相交则建立边。
    2.  **优化求解**：在凸集图上求解最短路径问题（SPP），同时优化离散的路径选择和连续的顶点配置，确保轨迹在几何上是最优且平滑的。

### 3. 实验设计
*   **实验场景**：
    1.  **3-DoF 平面机械臂**：用于可视化验证 TT 近似 PDF 的准确性以及凸区域生成的有效性。
    2.  **7-DoF Franka Emika Panda 机器人**：为了控制实验复杂度，锁定了 3 个关节，在 **4-DoF** 配置空间中进行压力测试。
*   **基准对比（Benchmark）**：传统的 **RRT 规划器**。
*   **评估指标**：执行时间、PDF 分数（衡量轨迹的可操作性/质量）、路径点数量、内存占用及计算耗时。

### 4. 资源与算力
*   **硬件说明**：文中提到使用了**多进程（Multiprocessing）**来加速 IRIS 凸区域计算和剪枝过程。
*   **算力细节**：**未明确说明**具体的 GPU 型号、数量或 CPU 核心数。
*   **训练/处理时长**：对于 4-DoF 机械臂，TT-Cross 近似约需 30 次迭代；重塑后的张量处理时间平均约为 5.5 秒。

### 5. 实验数量与充分性
*   **实验规模**：在 Panda 机器人实验中，随机选择了 **100 组** 具有大跨度关节距离的起点-终点对。
*   **充分性评价**：
    *   **客观性**：对比了原始张量与重塑张量的内存/时间开销，证明了 TT 分解的优越性。
    *   **公平性**：所有生成的路径均通过 TOPPRA 进行了后处理平滑，确保了轨迹质量对比的公平性。
    *   **局限性**：虽然展示了 4-DoF 的扩展性，但尚未在完整的 7-DoF 或更高维度（如双臂协作）上进行大规模验证。

### 6. 主要结论与发现
*   **高质量轨迹**：相比 RRT，TANGO 生成的轨迹更平滑，路径点更少（通常仅需 3-4 个关键点），且具有更高的平均可操作性（PDF 分数更高）。
*   **高效压缩**：通过张量重塑（Reshaping），内存占用从 $4.36 \times 10^4$ 个参数降低到 $7.11 \times 10^3$，执行速度提升了约 5 倍。
*   **安全性与约束**：通过 GCS 优化，路径被严格限制在预先计算的“安全走廊”内，有效规避了奇异位姿。

### 7. 优点与亮点
*   **创新耦合**：首次将张量列（TT）的全局探索能力与 GCS 的局部优化能力结合，解决了 GCS 缺乏初始凸分解的问题。
*   **几何感知**：通过黎曼度量引导采样，使规划器天生具备规避奇异点和提升机械臂灵巧性的能力。
*   **压缩效率**：TT 分解极大地缓解了高维 C-Space 的存储压力，使复杂环境的建模变得可行。

### 8. 不足与局限
*   **维度瓶颈**：尽管 TT 具有线性扩展性，但随着维度进一步增加（如 >7 DoF），IRIS 算法生成的凸多胞形几何复杂度会剧增，计算开销可能成为瓶颈。
*   **动态环境限制**：目前的预处理过程（TT 近似和 IRIS 生长）主要针对静态环境，对于快速变化的动态障碍物，实时重构图结构的成本较高。
*   **超参数敏感**：TT 分解的秩、采样数量 $k_c$ 以及 IRIS 迭代次数等参数需要根据具体任务进行经验性调整。

（完）
