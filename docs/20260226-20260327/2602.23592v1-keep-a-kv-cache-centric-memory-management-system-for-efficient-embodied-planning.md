---
title: "KEEP: A KV-Cache-Centric Memory Management System for Efficient Embodied Planning"
title_zh: KEEP：一种以 KV 缓存为中心的高效具身规划记忆管理系统
authors: "Zebin Yang, Tong Xie, Baotong Lu, Shaoshan Liu, Bo Yu, Meng Li"
date: 2026-02-27
pdf: "https://arxiv.org/pdf/2602.23592v1"
tags: ["query:robot-planning"]
score: 9.0
evidence: 具身规划中的KV缓存管理
tldr: 针对具身智能规划中LLM记忆管理导致的预填充延迟高和KV缓存更新频繁等问题，本文提出了KEEP系统。KEEP通过静态-动态内存构建、多跳内存重计算和层平衡内存加载三大创新技术，实现了高效的KV缓存管理。在ALFRED数据集上的实验表明，KEEP在保持高成功率的同时，显著提升了推理速度并降低了首字延迟，为长程具身任务提供了高效的记忆增强方案。
selection_source: fresh_fetch
motivation: 现有的具身规划方法将记忆存储为原始文本，导致提示词过长且KV缓存频繁更新，严重影响了推理效率。
method: 提出了包含混合粒度内存分组、动态跨注意力重构以及层间负载平衡加载的KV缓存中心化管理系统。
result: "在ALFRED数据集上，KEEP相比文本记忆方法实现了2.68倍加速，相比CacheBlend提升了4.13%的成功率并减少了1.90倍的首字延迟。"
conclusion: KEEP通过创新的KV缓存管理机制，有效解决了具身智能长程规划中的效率瓶颈，为高效记忆增强型LLM应用提供了新方案。
---

## 摘要
记忆增强的大语言模型（LLMs）在复杂且长程的具身规划中展现出了卓越的能力。通过追踪过去的经验和环境状态，记忆使 LLMs 能够保持全局视野，从而避免重复探索。然而，现有方法通常将记忆存储为原始文本，导致提示词过长且预填充延迟高。虽然可以存储并重用 KV 缓存，但由于频繁的 KV 缓存更新，其效率优势被大大削弱。在本文中，我们提出了 KEEP，一个以 KV 缓存为中心的高效具身规划记忆管理系统。KEEP 具有 3 项关键创新：(1) 一种静态-动态记忆构建算法，通过混合粒度的记忆组减少 KV 缓存重计算；(2) 一种多跳记忆重计算算法，动态识别不同记忆组之间的重要交叉注意力并迭代地重建记忆交互；(3) 一种层平衡记忆加载，消除了不同层之间不平衡的 KV 缓存加载和交叉注意力计算。大量的实验结果表明，在 ALFRED 数据集上，与基于文本的记忆方法相比，KEEP 在准确率损失可忽略的情况下实现了 2.68 倍的加速。与 KV 重计算方法 CacheBlend (EuroSys'25) 相比，KEEP 的成功率提高了 4.13%，首字延迟（TTFT）降低了 1.90 倍。我们的代码可在 https://github.com/PKU-SEC-Lab/KEEP_Embodied_Memory 获取。

## Abstract
Memory-augmented Large Language Models (LLMs) have demonstrated remarkable capability for complex and long-horizon embodied planning. By keeping track of past experiences and environmental states, memory enables LLMs to maintain a global view, thereby avoiding repetitive exploration. However, existing approaches often store the memory as raw text, leading to excessively long prompts and high prefill latency. While it is possible to store and reuse the KV caches, the efficiency benefits are greatly undermined due to frequent KV cache updates. In this paper, we propose KEEP, a KV-cache-centric memory management system for efficient embodied planning. KEEP features 3 key innovations: (1) a Static-Dynamic Memory Construction algorithm that reduces KV cache recomputation by mixed-granularity memory group; (2) a Multi-hop Memory Re-computation algorithm that dynamically identifies important cross-attention among different memory groups and reconstructs memory interactions iteratively; (3) a Layer-balanced Memory Loading that eliminates unbalanced KV cache loading and cross-attention computation across different layers. Extensive experimental results have demonstrated that KEEP achieves 2.68x speedup with negligible accuracy loss compared with text-based memory methods on ALFRED dataset. Compared with the KV re-computation method CacheBlend (EuroSys'25), KEEP shows 4.13% success rate improvement and 1.90x time-to-first-token (TTFT) reduction. Our code is available on https://github.com/PKU-SEC-Lab/KEEP_Embodied_Memory.

---

## 论文详细总结（自动生成）

这是一份关于论文《KEEP: A KV-Cache-Centric Memory Management System for Efficient Embodied Planning》的结构化总结：

### 1. 论文的核心问题与整体含义
具身智能（Embodied AI）代理在执行长程任务时，需要依赖“记忆”来跟踪环境状态和历史动作。目前的 LLM 规划器通常将记忆以**原始文本**形式存入 Prompt，这导致了极长的上下文和巨大的**预填充（Prefill）延迟**。虽然 KV Cache 重用技术（如 Prefix Caching）可以加速推理，但具身场景下的记忆是**高度动态**的（如物体位置改变），一旦中间某段记忆更新，后续所有缓存都会失效。
**KEEP** 旨在解决这一矛盾：在频繁更新的具身环境下，如何高效构建和管理 KV Cache 记忆系统，以在保证规划准确率的同时，大幅降低首字延迟（TTFT）。

### 2. 方法论：核心思想与关键技术
KEEP 提出了三个核心创新点来优化 KV Cache 的生命周期：

*   **静态-动态记忆构建（Static-Dynamic Memory Construction）：**
    *   根据更新频率对记忆进行分组。
    *   **静态组：** 长期不变的记忆（如任务历史）以组为单位计算 KV Cache，保留内部完整的上下文关联。
    *   **动态组：** 频繁变化的记忆（如物体状态）以细粒度片段（Segment）独立计算，确保单个片段更新不会导致整个缓存失效。
*   **多跳记忆重计算（Multi-hop Memory Re-computation）：**
    *   为了弥补独立计算 KV Cache 导致的跨片段注意力丢失，系统通过“重要性传播”机制动态选择关键记忆进行重计算。
    *   **流程：** 首先识别与 Query 直接相关的记忆，再通过这些记忆找到与之相关的二级记忆（如：Query“开门” -> 记忆“门锁了” -> 记忆“钥匙在桌上”），迭代传播重要性分数，仅对高分片段进行重计算。
*   **层平衡内存加载（Layer-balanced Memory Loading）：**
    *   由于 LLM 前几层重计算比例高（加载少）、后几层重计算比例低（加载多），导致计算与 I/O 不平衡。
    *   **优化：** 采用主动预加载策略，利用前几层计算时的空闲带宽，提前从 CPU 加载后几层确定不被重计算的 KV Cache，消除流水线气泡。

### 3. 实验设计
*   **数据集/场景：** 
    *   **ALFRED：** 室内日常任务指令遵循基准。
    *   **WAH-NL：** 复杂的具身任务规划基准。
*   **模型：** Qwen-2.5-14B 和 Qwen-2.5-32B（INT4 量化）。
*   **对比方法（Baselines）：**
    *   传统方法：LLM-Planner, FLARE, KARMA。
    *   KV 重用/重计算方法：Full Recompute (LoTA), Full Reuse, CacheBlend (EuroSys'25)。

### 4. 资源与算力
*   **硬件：** 每台设备配备 1 张 **NVIDIA A6000 GPU**。
*   **存储：** 使用 CPU RAM 作为 KV Cache 的二级存储，GPU HBM 作为一级存储。
*   **训练/推理：** 该系统是 **Training-free（免训练）** 的，直接应用于现有的预训练 LLM。论文未提及具体的推理总时长，但详细对比了 TTFT（首字延迟）。

### 5. 实验数量与充分性
*   **实验规模：** 涵盖了两个主流具身基准测试，并针对不同参数规模的模型（14B, 32B）进行了验证。
*   **消融实验：** 专门设计了消融实验来验证静态-动态构建、多跳重计算、层平衡加载这三个组件的各自贡献。
*   **扩展性分析：** 测试了在不同数量的检索记忆片段（5 到 40 个）下，系统延迟的增长曲线。
*   **评价：** 实验设计较为充分，对比了最新的 SOTA 方法（如 CacheBlend），并从成功率（SR）和延迟（TTFT）两个维度进行了客观评估。

### 6. 主要结论与发现
*   **性能提升：** 在 ALFRED 数据集上，KEEP 相比纯文本记忆方法实现了 **2.68 倍** 的加速。
*   **准确率保持：** 相比于会损失精度的 Full Reuse 方法，KEEP 在大幅提速的同时，准确率损失几乎可以忽略不计。
*   **对比优势：** 相比于 CacheBlend，KEEP 的成功率提升了 **4.13%**，且 TTFT 降低了 **1.90 倍**。
*   **动态适应性：** 静态-动态分组机制有效解决了具身场景中“牵一发而动全身”的缓存失效问题。

### 7. 优点
*   **针对性强：** 深刻洞察了具身智能中“记忆频繁更新”与“KV Cache 线性失效”的矛盾。
*   **系统级优化：** 不仅有算法层面的重要性传播，还有系统层面的 CPU-GPU 加载流水线优化。
*   **粒度平衡：** 巧妙地在“固定块大小”和“文本片段”之间找到了平衡，既保证了重用率又兼顾了语义完整性。

### 8. 不足与局限
*   **硬件依赖：** 性能高度依赖于 CPU 与 GPU 之间的 PCIe 带宽，在带宽受限的边缘设备上效果可能打折扣。
*   **超参数敏感性：** 静态/动态组转换的阈值 $t$ 以及重计算比例 $r$ 可能需要针对不同环境进行微调。
*   **应用范围：** 目前主要针对结构化程度较高的具身记忆，对于非结构化的长文本对话等场景的普适性有待进一步验证。

（完）
