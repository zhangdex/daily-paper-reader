---
title: Interleaving Scheduling and Motion Planning with Incremental Learning of Symbolic Space-Time Motion Abstractions
title_zh: 通过符号化时空运动抽象的增量学习实现调度与运动规划的交替
authors: "Elisa Tosello, Arthur Bit-Monnot, Davide Lusuardi, Alessandro Valentini, Andrea Micheli"
date: 2026-03-11
pdf: "https://arxiv.org/pdf/2603.10651v2"
tags: ["query:robot-planning"]
score: 10.0
evidence: 多物体导航的任务与运动规划
tldr: 本文针对自动化仓库等场景中的多对象导航问题，提出了“调度与运动规划”（SMP）框架。该方法将现成的调度器与运动规划器交替运行，通过增量学习循环，利用运动规划器提供的空间冲突和时间调整等符号反馈来指导调度器生成可行方案。研究解决了在复杂时空约束下，如何高效生成安全且同步的执行计划，为多机器人协作任务提供了新的解决思路。
selection_source: fresh_fetch
motivation: 在自动化仓库等共享工作空间中，如何在满足资源、时间和运动约束的前提下，安全高效地调度并执行预定义任务是一项重大挑战。
method: 提出一种交替执行调度与运动规划的框架，利用运动规划器的符号化时空反馈进行增量学习，从而引导调度器寻找运动可行的解。
result: 在物流和作业车间调度基准测试中的实验表明，该框架能有效处理复杂的时空约束，并生成需要高度同步的有效运动计划。
conclusion: 通过在调度过程中引入符号化的运动抽象反馈，可以有效解决多对象协作中的运动可行性问题，提升了复杂任务规划的成功率。
---

## 摘要
任务与运动规划将高层任务排序（做什么）与底层运动规划（如何做）相结合，以生成可行且无碰撞的执行方案。然而，在许多现实领域（如自动化仓库）中，任务是预定义的，挑战随之转向如何在资源、时间和运动约束下，安全且高效地确定是否、何时以及如何执行这些任务。在本文中，我们将此形式化为共享工作空间中多对象导航的调度与运动规划问题。我们提出了一种新颖的解决方案框架，在增量学习循环中交替使用现成的调度器和运动规划器。调度器生成候选计划，而运动规划器检查可行性并返回符号化反馈（即空间冲突和时间调整），以引导调度器寻找运动可行的解决方案。我们在增加了运动任务的物流和作业车间调度基准测试上验证了我们的提议，并使用了最先进的调度器和基于采样的运动规划器。我们的结果表明，该框架在生成复杂时空约束下的有效计划方面具有显著效果，尤其是在同步运动至关重要的情况下。

## Abstract
Task and Motion Planning combines high-level task sequencing (what to do) with low-level motion planning (how to do it) to generate feasible, collision-free execution plans. However, in many real-world domains, such as automated warehouses, tasks are predefined, shifting the challenge to if, when, and how to execute them safely and efficiently under resource, time and motion constraints. In this paper, we formalize this as the Scheduling and Motion Planning problem for multi-object navigation in shared workspaces. We propose a novel solution framework that interleaves off-the-shelf schedulers and motion planners in an incremental learning loop. The scheduler generates candidate plans, while the motion planner checks feasibility and returns symbolic feedback, i.e., spatial conflicts and timing adjustments, to guide the scheduler towards motion-feasible solutions. We validate our proposal on logistics and job-shop scheduling benchmarks augmented with motion tasks, using state-of-the-art schedulers and sampling-based motion planners. Our results show the effectiveness of our framework in generating valid plans under complex temporal and spatial constraints, where synchronized motion is critical.

---

## 论文详细总结（自动生成）

### 论文总结：通过符号化时空运动抽象的增量学习实现调度与运动规划的交替

#### 1. 核心问题与整体含义（研究动机和背景）
本文探讨了**调度与运动规划（Scheduling and Motion Planning, SAMP）**问题。传统的任务与运动规划（TAMP）侧重于决定“做什么”以及“如何做”，但在自动化仓库等实际应用中，任务通常是预先定义的。此时，核心挑战转向了**“是否执行”、“何时执行”以及“如何在资源、时间和运动约束下安全高效地执行”**。
*   **研究动机**：在多机器人共享空间中，简单的离散路径规划无法处理连续配置空间中的动力学约束和精确的时间同步。
*   **核心目标**：建立一个能够处理多对象导航、满足复杂时空约束并实现多智能体协同的集成框架。

#### 2. 方法论：核心思想与技术细节
论文提出了一种将现成的调度器与运动规划器交替运行的框架，通过**增量学习符号化运动抽象**来引导搜索。
*   **核心思想**：调度器负责生成不考虑底层运动的候选计划；运动规划器（作为黑盒）评估该计划的运动可行性。如果不可行，规划器会返回符号化的**细化约束（Refinements）**，反馈给调度器进行下一轮迭代。
*   **关键技术细节**：
    *   **并行运动组（Parallel Motion Groups）**：将计划中可能相互干扰的活动分组，以降低运动规划的计算复杂度。
    *   **分层架构（Layering Architecture）**：
        *   **第一层（Layer 1）**：对单个活动进行几何和时间可行性检查（路径搜索）。
        *   **第二层（Layer 2）**：对整个并行组进行多机器人时空同步检查（ST-RRT* 算法）。
    *   **符号化反馈机制**：
        *   **几何细化（Geometric Refinements）**：识别不可达配置（$\Sigma$）和阻碍障碍物（$\Omega$），强制调度器改变物体状态。
        *   **时间细化（Temporal Refinements）**：计算所需的最小持续时间（$d$）和延迟（$\delta$），引导调度器调整活动的时间窗口。
*   **算法流程**：`SOLVE` 算法不断调用调度器获取 $\rho$，通过 `GET_MOTION_OR_REFINE` 获取轨迹 $\tau$ 或生成新约束，直到找到有效解或超时。

#### 3. 实验设计
*   **场景与数据集**：
    *   **物流（Logistics）**：机器人从货架搬运物品到仓库，涉及狭窄走廊、封闭门（需操作）和多机器人对冲场景。
    *   **作业车间调度（JSP）**：在传统 JSP 基础上增加运输任务，机器人需在不同机器间移动工件，且机器被门阻挡。
*   **基准测试（Benchmark）**：基于经典的 Taillard (1993) 调度基准进行扩展，增加了 2D 导航任务。
*   **对比方法**：
    *   **调度器**：Aries（支持/不支持 Fluent）、OR-Tools (CPSE)。
    *   **运动规划器**：RRT（用于路径检查）和 ST-RRT*（用于时空同步）。
    *   **消融实验**：对比了有无分层架构、有无 Fluent 表达、以及纯顺序执行（无并行）的效果。

#### 4. 资源与算力
*   **硬件环境**：实验在配备 **AMD EPYC 7413** 处理器的服务器上运行。
*   **算力细节**：
    *   内存限制为 **20 GB**。
    *   每个问题的总超时时间设置为 **1800 秒**。
    *   运动规划器的单次调用超时（$t_p$）初始为 10 秒，失败后会倍增。
*   **GPU 使用**：文中**未提及**使用 GPU，考虑到使用的是基于采样的运动规划器（OMPL 库）和约束规划调度器，该框架主要依赖 CPU 算力。

#### 5. 实验数量与充分性
*   **实验规模**：
    *   物流场景：24 个实例（涉及 1-3 个机器人，不同门状态和取货策略）。
    *   JSP 场景：36 个实例（涉及 1-3 个机器人，不同机器数量和工件数量）。
    *   每个实例重复运行 **3 次**以应对采样算法的随机性。
*   **充分性评价**：实验设计较为充分。通过对比不同调度引擎和优化目标（是否最小化 Makespan），验证了框架的通用性。消融实验证明了分层架构对提升覆盖率的关键作用。实验涵盖了从简单几何冲突到复杂时空同步的多种情况，结果具有客观性。

#### 6. 主要结论与发现
*   **有效性**：该框架能成功生成需要高度同步的有效计划（如机器人停等避让）。
*   **性能**：使用 Fluent（流体/状态表达）的 Aries 调度器表现最佳，能更好地利用细化约束。
*   **并行优势**：相比纯顺序执行，该框架生成的并行计划使 Makespan 平均减少了 **41%**。
*   **瓶颈**：运动规划占据了总规划时间的 **70%-92%**，是主要的计算开销所在。
*   **反馈价值**：即使在简单的场景中，时间细化也是必不可少的，因为预估的持续时间往往忽略了多智能体交互产生的延迟。

#### 7. 优点
*   **模块化与通用性**：支持现成的调度器和运动规划器，易于扩展到不同的机器人平台和任务领域。
*   **符号化抽象**：通过增量学习运动约束，避免了预先对所有几何约束进行全量建模，提升了在大规模动态环境中的可扩展性。
*   **时空耦合**：能够处理具有类车动力学（Car-like dynamics）约束的连续空间运动，而非简单的离散网格路径。

#### 8. 不足与局限
*   **维度限制**：目前实验主要集中在 **2D 导航**，尚未在复杂的 3D 操纵（如双臂协作抓取）中验证。
*   **采样敏感性**：依赖采样运动规划器可能导致“伪不可解”问题（False Negatives），虽然通过超时倍增机制有所缓解，但仍可能影响效率。
*   **扩展性挑战**：虽然测试了 3 个机器人，但在极高密度（如数十个机器人）的拥挤空间中，ST-RRT* 的计算压力和调度器的约束传播可能会面临严峻挑战。
*   **应用限制**：目前假设环境地图已知且障碍物（如门）是可预测操作的，对完全未知或高度动态的随机障碍物处理能力有限。

（完）
