---
title: "LDHP: Library-Driven Hierarchical Planning for Non-prehensile Dexterous Manipulation"
title_zh: LDHP：用于非抓取灵巧操作的库驱动分层规划
authors: "Tierui He, Chao Zhao"
date: 2026-03-14
pdf: "https://arxiv.org/pdf/2603.13844v1"
tags: ["query:robot-planning"]
score: 9.0
evidence: 灵巧操作的分层规划
tldr: LDHP是一种针对非抓取式灵巧操作的库驱动分层规划框架。它通过将任务分解为顶层的物体运动规划和底层的抓取序列合成，解决了传统方法依赖人工设计或物理不可行的问题。该方法利用MoveObject和AdjustGrasp原语，结合准静态力学验证和二分细化技术，确保了动作的可执行性，在多种复杂任务中表现出极强的泛化能力和鲁棒性。
selection_source: fresh_fetch
motivation: 现有的非抓取式操作规划方法要么依赖繁琐的人工设计，要么生成的动作在物理上不可行，且难以泛化到新任务。
method: 提出一种分层规划器，顶层负责物体姿态路径规划，底层负责合成可行的抓取序列，并利用物理约束和二分法进行可行性验证与细化。
result: 在零移动提升和插槽任务的真实机器人实验中，该方法展示了高度的执行一致性以及对形状和环境变化的鲁棒性。
conclusion: LDHP通过解耦物体运动与抓取实现，提供了一个任务无关且物理可靠的灵巧操作流水线，显著提升了机器人在非结构化环境下的操作能力。
---

## 摘要
非抓取操作对于在非结构化环境中处理薄、大或无法抓取的物体至关重要。先前的规划和基于搜索的方法通常依赖于特定的手动设计，或者由于忽略了关键的夹持器属性而生成物理上无法实现的运动；而基于训练的方法则属于数据密集型，且难以泛化到新颖的分布外任务。我们提出了一种库驱动的分层规划器（LDHP），将可执行性作为首要设计目标：顶层接触状态规划器使用 MoveObject 原语提出物体位姿路径，底层抓取规划器使用 AdjustGrasp 原语合成可行的抓取序列；可行性通过碰撞检测和准静态力学进行验证，接触敏感段则通过有界二分细化进行恢复。这种感知夹持器的分解将物体运动与抓取可实现性解耦，产生了一个与任务无关的流水线，无需重新设计即可在各种操作任务和几何变化中进行迁移，并为可选的学习先验提供了清晰的接口。在零机动性提升和插槽插入方面的真实机器人研究证明了其执行的一致性以及对形状和环境变化的鲁棒性。

## Abstract
Non-prehensile manipulation is essential for handling thin, large, or otherwise ungraspable objects in unstructured settings. Prior planning and search-based methods often rely on ad-hoc manual designs or generate physically unrealizable motions by ignoring critical gripper properties, while training-based approaches are data-intensive and struggle to generalize to novel, out-of-distribution tasks. We propose a library-driven hierarchical planner (LDHP) that makes executability a first-class design goal: a top-tier contact-state planner proposes object-pose paths using MoveObject primitives, and a bottom-tier grasp planner synthesizes feasible grasp sequences with AdjustGrasp primitives; feasibility is certified by collision checks and quasi-static mechanics, and contact-sensitive segments are recovered via a bounded dichotomy refinement. This gripper-aware decomposition decouples object motion from grasp realizability, yields a task-agnostic pipeline that transfers across manipulation tasks and geometric variations without re-design, and exposes clean hooks for optional learned priors. Real-robot studies on zero-mobility lifting and slot insertion demonstrate consistent execution and robustness to shape and environment changes.

---

## 论文详细总结（自动生成）

以下是对论文《LDHP: Library-Driven Hierarchical Planning for Non-prehensile Dexterous Manipulation》的结构化总结：

### 1. 核心问题与整体含义（研究动机和背景）
论文聚焦于**非抓取式灵巧操作（Non-prehensile Manipulation, NPM）**，如推、滑、翻转等。这类操作对于处理薄片、大型或形状复杂的物体至关重要。
*   **研究动机**：现有的规划方法要么依赖于针对特定任务的人工设计（缺乏通用性），要么在搜索过程中忽略了夹持器（Gripper）的物理属性（如形状、开合限制），导致生成的动作在现实中无法执行。而基于学习的方法虽然灵活，但需要海量数据且难以泛化到分布外（OOD）的任务。
*   **核心目标**：提出一种**感知夹持器（Gripper-aware）**的分层规划框架，将物体运动规划与抓取可行性验证解耦，确保生成的长程操作序列在物理上是可执行的。

### 2. 方法论：核心思想与技术细节
LDHP 采用双层规划架构，结合了一个专门设计的**动作原语库（Primitive Library）**：
*   **核心思想**：将任务分解为“物体怎么动”和“夹持器怎么抓”两个层面。
*   **原语库设计**：
    *   **MoveObject (MMO)**：改变物体位姿，包括倾斜（TIP）、推送（PUSH）、双平面滑动（BIPLANAR-SLIDE）和空间移动（AIR-MOVE）。
    *   **AdjustGrasp (MAG)**：在固定位姿下调整抓取，包括开合（OPEN/CLOSE）、旋转（PIVOT）、滑动（SLIDE）、翻转（FLIP）和接近接触（APPROACH-CONTACT）。
*   **分层规划流程**：
    1.  **顶层（物体位姿规划）**：在接触状态图（Contact-state Graph）中搜索，利用 MoveObject 原语生成一条从初始位姿到目标位姿的路径。
    2.  **底层（抓取序列合成）**：针对顶层路径，在分层抓取图中搜索可行的抓取序列。它会检查碰撞、夹持器开合限制以及**准静态力学（Quasi-static Mechanics）**平衡（基于库仑摩擦模型）。
    3.  **二分细化（Dichotomy Refinement）**：如果某个接触敏感段（如 TIP）无法找到可行抓取，规划器会通过二分法细化该动作段，尝试恢复可行性。

### 3. 实验设计
实验在真实机器人平台（UR10 机械臂 + Robotiq 140 夹持器 + FT-300 力矩传感器）上进行：
*   **场景一：零移动提升（Zero-mobility Lifting）**：模拟夹持器失去开合能力（固定间距）时，如何通过环境辅助提升物体。
*   **场景二：插槽插入（Slot Insertion）**：在受限空间内，通过复杂的滑动和翻转动作将物体插入窄槽。
*   **泛化性测试**：测试了不同形状（11 边形、9 边形凹多边形）、环境尺寸变化以及任务的反向执行（如从槽中取出物体）。
*   **消融实验**：逐一移除原语库中的动作，验证各原语对任务完成的必要性。

### 4. 资源与算力
*   **算力说明**：文中**未明确提及**具体的 GPU 型号或训练时长。
*   **原因**：LDHP 属于**基于规划（Planning-based）**的方法，而非深度学习训练方法。其计算开销主要集中在几何碰撞检测、准静态力学验证和 Dijkstra 图搜索上。实验中提到对物体边缘进行了 K=100 的离散化采样。

### 5. 实验数量与充分性
*   **实验规模**：核心任务（提升和插入）各进行了 10 次真实机器人实验，成功率均为 100%。
*   **充分性**：实验设计较为全面，不仅涵盖了基础任务，还通过改变几何形状和环境约束验证了算法的鲁棒性。
*   **客观性**：通过消融实验定量展示了原语库设计的合理性，并诚实地指出了在反向插槽任务中由于控制层级简单（仅基于力矩触发）导致的 2/10 失败案例。

### 6. 主要结论与发现
*   **可执行性优先**：通过将夹持器约束引入规划底层，LDHP 能生成比传统搜索方法更具物理可行性的动作。
*   **无需重新设计**：同一套规划框架和原语库可以适应完全不同的任务和物体形状，无需针对新场景重新建模或训练。
*   **原语必要性**：消融实验证明，TIP、PUSH、FLIP 等原语在处理复杂非抓取任务时是不可或缺的，移除它们会导致规划失败。

### 7. 优点
*   **物理严谨性**：结合了准静态力学分析，确保动作符合摩擦锥和力矩平衡约束。
*   **感知夹持器**：显式考虑了夹持器的几何形状和开合极限，避免了“幻影”动作。
*   **低感知依赖**：仅需低频的力/力矩反馈作为接触事件触发，无需昂贵的多摄像头追踪系统。
*   **模块化**：分层架构允许轻松接入学习到的先验知识（如预测抓取可行性得分）。

### 8. 不足与局限
*   **维度限制**：目前仅限于 **SE(2)** 平面内的准静态操作，尚未扩展到全 3D（SE(3)）动态操作。
*   **离散化风险**：依赖于对位姿和参数空间的离散化采样，在极度狭窄的可行域内可能会漏掉解。
*   **控制层薄弱**：执行层主要采用位置控制加力矩触发，缺乏主动的力顺应控制，在处理极高精度要求的任务时可能失稳。

（完）
