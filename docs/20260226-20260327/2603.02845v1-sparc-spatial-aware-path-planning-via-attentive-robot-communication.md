---
title: "SPARC: Spatial-Aware Path Planning via Attentive Robot Communication"
title_zh: SPARC：通过注意力机器人通信实现的空间感知路径规划
authors: "Sayang Mu, Xiangyu Wu, Bo An"
date: 2026-03-03
pdf: "https://arxiv.org/pdf/2603.02845v1"
tags: ["query:robot-planning"]
score: 9.0
evidence: 基于注意力通信的分散式多机器人路径规划
tldr: 针对去中心化多机器人路径规划（MRPP）中通信效率低的问题，本文提出SPARC框架。其核心是关系增强多头注意力（RMHA）机制，通过将曼哈顿距离显式嵌入注意力权重计算，使机器人能动态优先处理空间相关的邻居信息。结合距离约束掩码和GRU门控消息融合，该方法在MAPPO框架下实现了稳定训练。实验表明，该方法在从8台训练机器人扩展到128台的零样本泛化测试中表现优异，显著提升了高密度环境下的规划成功率。
selection_source: fresh_fetch
motivation: 现有的多机器人通信方法忽略了空间邻近性，导致在拥挤区域注意力分散，难以实现高效的避障协作。
method: 提出RMHA机制，将曼哈顿距离嵌入注意力权重，并结合距离约束掩码与GRU门控融合技术优化通信。
result: "在128台机器人的零样本泛化测试中，该方法在30%障碍物密度下达到75%的成功率，超出基准方法25个百分点以上。"
conclusion: 空间感知通信机制是提升高密度环境下多机器人路径规划性能、协作效率及泛化能力的关键因素。
---

## 摘要
高效的通信对于去中心化多机器人路径规划（MRPP）至关重要，然而现有的学习通信方法往往忽略空间邻近性，平等地对待所有相邻机器人，导致在最需要协调的拥挤区域注意力被稀释。我们提出了关系增强多头注意力（RMHA），这是一种将成对曼哈顿距离显式嵌入注意力权重计算的通信机制，使每个机器人能够动态地优先处理来自空间相关邻居的消息。结合距离约束的注意力掩码和 GRU 门控消息融合，RMHA 与 MAPPO 无缝集成，实现了稳定的端到端训练。在从 8 个训练机器人到 40x40 网格上 128 个测试机器人的零样本泛化实验中，RMHA 在 30% 的障碍物密度下实现了约 75% 的成功率，比最优基准方法高出 25 个百分点以上。消融研究证实，距离关系编码是高密度环境下成功率提升的关键因素。索引词：多机器人路径规划，图注意力机制，多头注意力，通信优化，协作决策。

## Abstract
Efficient communication is critical for decentralized Multi-Robot Path Planning (MRPP), yet existing learned communication methods treat all neighboring robots equally regardless of their spatial proximity, leading to diluted attention in congested regions where coordination matters most. We propose Relation enhanced Multi Head Attention (RMHA), a communication mechanism that explicitly embeds pairwise Manhattan distances into the attention weight computation, enabling each robot to dynamically prioritize messages from spatially relevant neighbors. Combined with a distance-constrained attention mask and GRU gated message fusion, RMHA integrates seamlessly with MAPPO for stable end-to-end training. In zero-shot generalization from 8 training robots to 128 test robots on 40x40 grids, RMHA achieves approximately 75 percent success rate at 30 percent obstacle density outperforming the best baseline by over 25 percentage points. Ablation studies confirm that distance-relation encoding is the key contributor to success rate improvement in high-density environments. Index Terms-Multi-robot path planning, graph attention mechanism, multi-head attention, communication optimization, cooperative decision-making

---

## 论文详细总结（自动生成）

以下是对论文《SPARC: Spatial-Aware Path Planning via Attentive Robot Communication》的结构化总结：

### 1. 核心问题与研究动机
*   **核心问题**：在去中心化的多机器人路径规划（MRPP）中，现有的基于学习的通信机制（如GNN或Transformer）通常仅根据消息内容计算注意力权重，而忽略了机器人之间的**空间邻近性**。
*   **研究动机**：在机器人密集的拥挤区域，这种“空间不可知”的通信会导致注意力被无关的远端机器人稀释，从而降低协作效率，导致死锁或碰撞。论文旨在通过引入空间关系感知，使机器人能动态优先处理距离更近、威胁更大的邻居信息。

### 2. 方法论：RMHA 通信机制
*   **核心思想**：提出**关系增强多头注意力（RMHA）**，将机器人间的相对空间位置（曼哈顿距离）显式嵌入到注意力权重的计算中。
*   **关键技术细节**：
    *   **曼哈顿距离嵌入**：将成对的曼哈顿距离编码为高维向量，并与局部观测特征结合。选择曼哈顿距离是因为它在网格世界中比欧几里得距离更能准确反映最短路径。
    *   **距离约束掩码**：为了降低通信开销并模拟带宽限制，引入了基于距离的注意力掩码，只允许预设半径内的机器人进行通信。
    *   **GRU 门控融合**：使用 GRU 单元对历史消息和当前注意力聚合的信息进行融合，增强时序一致性。
    *   **MAPPO 框架集成**：采用“集中式训练、分布式执行”（CTDE）架构，将 RMHA 模块嵌入到 MAPPO 算法中，通过裁剪的 PPO 目标函数进行端到端优化。

### 3. 实验设计
*   **场景设置**：
    *   **训练环境**：在 10x10 到 40x40 的随机网格图中训练 8 个机器人，障碍物密度最高达 50%。
    *   **测试环境**：评估模型在 40x40 网格上扩展到 128 个机器人的**零样本泛化能力**。
    *   **地图类型**：包括规整的“仓库（Warehouse）”地图和不规则的“城市/游戏（City/Game）”地图。
*   **对比基准（Benchmarks）**：
    *   **学习类方法**：SCRIMP（基于GNN）、DHC、PICO。
    *   **传统算法**：ODrM*（中心化规划器的变体）。
*   **评估指标**：成功率（SR）、最大到达目标数（MR）、碰撞率（CO）。

### 4. 资源与算力
*   **硬件环境**：使用了配备 **双 NVIDIA RTX 8000 GPU** 的 Ubuntu 服务器。
*   **软件环境**：PyTorch 2.1, Python 3.7。
*   **训练细节**：文中未明确给出具体的总训练时长，但提到了最大回合步数为 256 步，且在消融实验中展示了奖励函数的收敛曲线。

### 5. 实验数量与充分性
*   **实验规模**：每种设置下运行了至少 **100 组独立实验**以确保统计显著性。
*   **消融实验**：对比了纯 MAPPO（无通信）、MAPPO+Graph Comm（无距离增强）以及完整的 RMHA，验证了距离编码的必要性。
*   **扩展性测试**：测试了从 16 到 128 个机器人不同规模下的性能，验证了算法的鲁棒性。
*   **充分性评价**：实验设计较为全面，涵盖了不同障碍物密度、不同地图拓扑以及不同机器人规模，对比了当前最先进的（SOTA）方法，实验结果具有较高的说服力。

### 6. 主要结论与发现
*   **性能领先**：在 30% 障碍物密度的极高难度场景下，RMHA 的成功率达到约 **75%**，比表现最好的基准方法（SCRIMP）高出 **25 个百分点以上**。
*   **空间感知的关键性**：消融实验证明，显式嵌入曼哈顿距离是提升高密度环境下协作效率的核心因素。
*   **死锁解决**：可视化分析显示，RMHA 能使机器人在狭窄通道内主动避让，有效解决了传统方法难以处理的对头死锁问题。

### 7. 优点与亮点
*   **创新的特征融合**：将拓扑距离与注意力机制深度耦合，解决了大规模 MRPP 中的注意力稀释问题。
*   **强大的泛化能力**：仅用 8 个机器人训练的模型即可直接应用于 128 个机器人的复杂场景，展现了极佳的零样本迁移能力。
*   **端到端稳定性**：通过距离掩码和 MAPPO 的结合，克服了 Transformer 在大规模强化学习训练中梯度不稳定的难题。

### 8. 不足与局限
*   **环境限制**：目前仅在离散的**二维网格世界**中验证，未涉及连续空间或三维环境。
*   **同质化假设**：实验假设所有机器人具有相同的运动学能力，未考虑异构机器人团队。
*   **距离度量的局限**：曼哈顿距离在有复杂障碍物遮挡时可能无法完全反映真实的“可达距离”，未来可能需要结合环境拓扑的学习度量。
*   **通信延迟**：虽然考虑了一步延迟，但未深入探讨在极端通信丢包或高延迟环境下的表现。

（完）
