---
title: Geometry-Aligned LLM Fine-Tuning for Sequential Narrow-Opening Planning
title_zh: 针对连续窄口规划的几何对齐大语言模型微调
authors: "Al Jaber Mahmud, Xuan Wang"
date: 2026-03-17
pdf: "https://arxiv.org/pdf/2603.16028v1"
tags: ["query:robot-planning"]
score: 9.0
evidence: 用于连续狭窄开口运动规划的大模型微调
tldr: 针对多重连续狭窄开口的刚体运动规划难题，本文提出一种几何对齐的大语言模型（LLM）微调框架。该框架通过双层训练流程，结合失败驱动的LoRA监督微调与基于几何验证的群体相对策略优化（GRPO），使LLM能够生成具备长程几何推理能力的路径点序列。实验证明，该方法在分布内及分布外环境下均取得了最高的规划成功率，有效解决了复杂空间约束下的连续运动规划问题。
selection_source: fresh_fetch
motivation: 传统的运动规划在处理连续狭窄开口时，由于前期动作对后期可达性的高度约束，难以实现有效的长程几何推理。
method: 提出一种结合失败驱动LoRA监督微调与基于几何奖励的GRPO强化学习的双层微调框架，以生成几何可行的路径点序列。
result: 实验结果显示，该方法在模拟环境的分布内和分布外测试中均达到了最高的成功率，并展现出卓越的长程几何协调能力。
conclusion: 通过将几何约束引入LLM微调过程，可以显著提升模型在复杂受限空间下的连续运动规划性能。
---

## 摘要
我们研究了穿过多个连续窄口的刚体运动规划，这需要长程几何推理，因为穿越早期开口所使用的位姿会限制后续开口的可达位姿集合。为此，我们提出了一种几何对齐的大语言模型（LLM）微调框架，该框架生成固定长度、机器可读的路标点序列，这些序列在几何上是可行的，并且在不同开口之间是协调的。我们的方法采用了一种双层训练流水线。首先，我们在人类演示数据上进行故障驱动的 LoRA 有监督微调（SFT），其中结合了结构化的故障反馈，以教导模型识别常见的失败模式并强制执行输出格式。其次，我们使用带有几何验证的群体相对策略优化（GRPO）来细化相同的 LoRA 适配器：每个采样的路标点序列由基于模型的规划器进行稠密化，并根据确定性的几何衍生奖励进行评分，以实现连续运动的可行性。为了验证所提方法的有效性，我们提供了仿真实验的定量和定性结果。我们的方法在分布内和分布外环境中均实现了最高的成功率，并在定性上通过选择有利于进入后续开口的出口位姿，展示了长程几何推理能力。

## Abstract
We study rigid-body motion planning through multiple sequential narrow openings, which requires long-horizon geometric reasoning because the configuration used to traverse an early opening constrains the set of reachable configurations for subsequent ones. To achieve this, we propose a geometry-aligned large language model (LLM) fine-tuning framework that generates fixed-length, machine-readable waypoint sequences that are both geometrically feasible and coordinated across openings. Our approach uses a bi-level training pipeline. First, we perform failure-driven LoRA supervised fine-tuning (SFT) on human demonstrations, which incorporates structured failure feedback to teach the model common failure modes and enforce the output format. Second, we refine the same LoRA adapters using Group Relative Policy Optimization (GRPO) with geometric verification: each sampled waypoint sequence is densified by a model-based planner and scored with a deterministic geometry-derived reward to achieve continuous-motion feasibility. To validate the effectiveness of our proposed method, we provide both quantitative and qualitative results from simulations. Our method achieves the highest success rate in both in-distribution and out-of-distribution environments and qualitatively exhibits long-horizon geometric reasoning by selecting exit poses that facilitate entry into subsequent openings.

---

## 论文详细总结（自动生成）

这篇论文提出了一种结合大语言模型（LLM）推理能力与几何约束验证的微调框架，旨在解决机器人路径规划中极具挑战性的“连续窄口穿越”问题。以下是对该论文的深度总结：

### 1. 核心问题与研究动机
*   **核心问题**：刚体在穿过多重连续狭窄开口（如连续的门或障碍物缝隙）时的运动规划。
*   **研究动机**：
    *   **长程几何约束**：穿越第一个开口时的姿态（Pose）会直接限制后续开口的可达性。如果只考虑局部可行性，往往会导致后续路径死锁。
    *   **现有方法局限**：传统模型驱动规划器（如RRT、PRM）在窄口环境下采样效率极低且对参数敏感；学习类规划器缺乏几何保证，且在分布外（OOD）场景表现差；LLM虽具备推理潜力，但难以直接输出符合物理几何约束的精确路径。

### 2. 方法论：几何对齐的双层微调框架
论文提出了一种**双层（Bi-level）训练流水线**，将LLM生成的路标点（Waypoints）与确定性的几何验证器对齐：
*   **核心思想**：利用LLM进行长程推理，利用几何验证器提供硬约束反馈，通过强化学习将几何规则内化到模型参数中。
*   **关键技术细节**：
    1.  **数据采集**：通过GUI收集人类演示数据，记录在复杂窄口场景下的战略性位姿选择。
    2.  **Level-1：失败驱动的LoRA监督微调（SFT）**：
        *   在模仿人类演示的基础上，引入“失败反馈循环”。
        *   如果模型生成的路径失败，验证器会返回首个失败点的索引及错误类型（如碰撞、越界、步长超限），并将其作为Prompt的一部分重新训练，教导模型识别错误模式。
    3.  **Level-2：基于几何验证的GRPO（群体相对策略优化）**：
        *   **群体采样**：针对同一Prompt采样多组输出。
        *   **几何奖励函数**：将生成的路标点通过A*算法稠密化为连续轨迹，由几何验证器计算得分。奖励函数 $R = 1 / (1 + \alpha C)$，其中 $C$ 是包含边界违规、障碍物碰撞和步长平滑性的综合代价。
        *   **优势计算**：GRPO通过组内相对得分计算优势函数，无需额外的奖励模型（Reward Model），直接利用确定性的几何得分优化LoRA适配器。

### 3. 实验设计
*   **实验场景**：2D工作空间，包含多个由矩形障碍物形成的连续窄口。
*   **数据集**：
    *   **分布内（ID）**：500个与训练集分布一致的环境。
    *   **分布外（OOD）**：1000个环境，具有不同的障碍物布局、开口宽度和间距。
*   **对比基准（Baselines）**：
    1.  **Llama-3.1-8B-Instruct**：原始零样本模型。
    2.  **标准LoRA SFT**：仅进行人类演示的模仿学习。
    3.  **失败驱动LoRA SFT**：加入失败反馈但未进行GRPO。
    4.  **子目标分解PRM**：传统的采样规划器（定性对比）。

### 4. 资源与算力
*   **模型基础**：使用了 **Llama-3.1-8B-Instruct** 作为基础模型。
*   **微调技术**：采用 **LoRA**（低秩自适应）技术，仅更新注意力层和MLP投影层的参数，基座模型冻结。
*   **算力说明**：论文**未明确指出**具体的GPU型号、数量及训练总时长。但考虑到8B参数模型及GRPO的采样需求，通常需要至少1-2张 A100/H100 级别的显卡进行高效微调。

### 5. 实验数量与充分性
*   **实验规模**：共进行了1500组测试（500 ID + 1000 OOD），样本量较大，具有统计学意义。
*   **充分性与公平性**：
    *   **消融实验**：通过对比SFT、失败驱动SFT和GRPO三个阶段，清晰展示了各模块对成功率的贡献。
    *   **客观性**：所有方法均使用相同的确定性几何验证器进行评估，消除了主观评分偏差。
    *   **泛化性测试**：专门设置了1000组OOD实验，验证了模型在未见场景下的推理能力。

### 6. 主要结论与发现
*   **性能飞跃**：所提方法在ID环境下成功率达到 **92.6%**，在OOD环境下达到 **77.3%**，远超所有基准方法。
*   **几何推理能力**：定性分析显示，经过GRPO优化的模型学会了“预判”，即在离开前一个开口时，会自动调整位姿以方便进入下一个开口（Pre-orientation）。
*   **错误减少**：GRPO显著降低了“扫掠碰撞”（Swept-collision）和步长违规，这些是仅靠SFT难以解决的连续运动细节问题。

### 7. 优点与亮点
*   **无需奖励模型**：巧妙利用确定性的几何规则作为GRPO的奖励来源，避免了传统RLHF中人类偏好模型的不确定性。
*   **长程协调**：通过LLM的序列生成能力，实现了跨开口的全局路径协调，解决了传统规划器容易陷入局部最优的问题。
*   **结构化反馈**：将几何失败信息转化为文本反馈，实现了“从错误中学习”的闭环。

### 8. 不足与局限
*   **维度限制**：目前仅在 **2D SE(2)** 空间验证，尚未扩展到更复杂的3D环境（SE(3)）。
*   **依赖外部规划器**：模型生成的路标点仍需依赖传统的A*算法进行稠密化，LLM尚未完全实现端到端的连续轨迹生成。
*   **输入表征**：使用符号化的文本描述几何场景，对于极其复杂的非规则障碍物，文本描述的效率和准确性可能会下降。

（完）
