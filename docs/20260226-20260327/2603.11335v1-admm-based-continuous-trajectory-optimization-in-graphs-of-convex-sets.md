---
title: ADMM-based Continuous Trajectory Optimization in Graphs of Convex Sets
title_zh: 基于 ADMM 的凸集图连续轨迹优化
authors: "Lukas Pries, Jon Arrizabalaga, Zachary Manchester, Markus Ryll"
date: 2026-03-11
pdf: "https://arxiv.org/pdf/2603.11335v1"
tags: ["query:robot-planning"]
score: 9.0
evidence: 非凸环境下的连续轨迹优化
tldr: 本文提出一种基于ADMM的数值求解器，用于解决非凸环境下的连续轨迹优化。该方法通过多项式参数化实现闭式原始更新，并引入时空分配图将松弛更新转化为最短路径搜索。通过联合优化离散空间与连续时间，该方法比传统解耦方法拥有更大的搜索空间，且具备极强的结构鲁棒性，无需复杂的初始化即可在非凸环境中可靠收敛。
selection_source: fresh_fetch
motivation: 针对非凸环境下轨迹优化中离散路径选择与连续时间分配难以高效联合优化的问题。
method: 采用ADMM框架，结合多项式轨迹表示的闭式更新与基于最短路径搜索的时空分配图优化。
result: 该方法能够探索更优的轨迹方案，并消除了非凸优化中对复杂预热启动的依赖。
conclusion: 本研究为复杂环境下的轨迹规划提供了一个高效、鲁棒且搜索空间更广的数值求解方案。
---

## 摘要
本文提出了一种用于在非凸环境中计算连续轨迹的数值求解器。我们的方法依赖于基于两个关键组件构建的交替方向乘子法 (ADMM) 的定制实现：首先，我们将轨迹参数化为多项式，使得原始更新可以作为最小控制代价问题以闭式解形式计算。其次，我们引入了基于混合整数公式的时空分配图概念，并将松弛更新建模为最短路径搜索。这些要素的结合使得该求解器与现有技术相比具有若干显著优势。通过对离散空间域和连续时间域进行联合优化，我们的方法比现有的解耦方法能够访问更大的搜索空间，从而发现更优的轨迹。此外，该求解器的结构鲁棒性确保了从朴素初始化开始的可靠收敛，消除了非凸环境中复杂热启动的瓶颈。

## Abstract
This paper presents a numerical solver for computing continuous trajectories in non-convex environments. Our approach relies on a customized implementation of the Alternating Direction Method of Multipliers (ADMM) built upon two key components: First, we parameterize trajectories as polynomials, allowing the primal update to be computed in closed form as a minimum-control-effort problem. Second, we introduce the concept of a spatio-temporal allocation graph based on a mixed-integer formulation and pose the slack update as a shortest-path search. The combination of these ingredients results in a solver with several distinct advantages over the state of the art. By jointly optimizing over both discrete spatial and continuous temporal domains, our method accesses a larger search space than existing decoupled approaches, enabling the discovery of superior trajectories. Additionally, the solver's structural robustness ensures reliable convergence from naive initializations, removing the bottleneck of complex warm starting in non-convex environments.

---

## 论文详细总结（自动生成）

### 论文总结：基于 ADMM 的凸集图连续轨迹优化 (ACTOR)

#### 1. 论文的核心问题与整体含义
在机器人导航和控制领域，**非凸环境下的轨迹优化**是一个核心难题。传统方法通常将问题解耦为两个阶段：高层离散规划（寻找拓扑路径）和底层连续控制（平滑轨迹）。这种“数值上的便利”导致优化器无法感知全局景观，容易陷入局部最优或无法找到可行解。
本文提出了 **ACTOR** 求解器，旨在通过 **ADMM（交替方向乘子法）** 框架，将离散的空间选择（在哪些凸集中行驶）与连续的时间轨迹（多项式参数化）进行**联合优化**，从而在复杂的非凸空间中寻找更优且满足动力学约束的轨迹。

#### 2. 论文提出的方法论
ACTOR 的核心思想是将非凸约束分解为易于处理的子问题，利用 ADMM 进行迭代求解：
*   **轨迹参数化（Bézier 曲线）：** 使用分段 Bernstein 多项式表示轨迹。利用其“凸包性质”，将轨迹在凸集内的安全约束转化为控制点的线性约束，并将最小控制代价（如 Minimum-snap）转化为二次型。
*   **时空分配图 (Spatio-temporal Allocation Graph)：** 引入混合整数公式，将轨迹段与凸集的分配关系建模为一个**有向无环图 (DAG)**。每一层代表一个轨迹段，节点代表可选的凸集，边代表连续段之间的连通性。
*   **ADMM 三步迭代流程：**
    1.  **原始更新 (Primal Update)：** 求解一个带等式约束的二次规划 (QP)。由于矩阵结构固定，可预先分解，迭代时仅需稀疏矩阵乘法，实现“无因子分解”的高效计算。
    2.  **松弛更新 (Slack Update)：** 包含两个子步骤：首先将控制点投影到所有可能的凸集上；然后在分配图中利用**动态规划 (DP)** 寻找最短路径，确定当前最优的轨迹段-凸集分配方案。
    3.  **对偶更新 (Dual Update)：** 执行梯度上升，强制原始变量与松弛变量之间的一致性。

#### 3. 实验设计
论文通过多个仿真场景验证了 ACTOR 的性能：
*   **对比基准 (Benchmarks)：**
    *   **OBCA:** 基于非线性规划 (NLP) 的碰撞避免方法（使用 IPOPT 求解）。
    *   **SafeC:** 安全走廊优化方法（使用 SNOPT 求解）。
    *   **GCS:** 凸集图运动规划（基于 MIQP 松弛）。
    *   **MIQP:** 使用 Gurobi 求解的全局最优混合整数二次规划。
*   **实验场景：**
    *   **U型障碍物：** 测试算法跳出局部陷阱的能力。
    *   **迷宫/多路径环境：** 测试在多个同伦类中寻找最优路径的能力。
    *   **点云环境：** 验证在朴素空间分解下的鲁棒性。
    *   **四旋翼 Minimum-snap：** 验证高阶动力学约束（速度、加速度限制）下的表现。

#### 4. 资源与算力
*   **实现语言：** 算法使用 **Python/NumPy** 实现（对比方法如 SNOPT/Gurobi 通常使用 C++/优化库）。
*   **算力说明：** 文中未明确提及具体的 GPU 或 CPU 型号，但强调了算法的**线性扩展性**。由于原始更新采用了预分解技术，且松弛更新基于高效的动态规划，其单次迭代计算开销极低。

#### 5. 实验数量与充分性
*   **实验规模：** 论文包含了定性分析（可视化轨迹对比）和定量分析（运行时间随凸集数量的变化曲线）。
*   **充分性：** 实验覆盖了从简单几何体到复杂点云、从低阶运动学到高阶动力学的多种情况。通过与商业求解器（Gurobi）和成熟 NLP 求解器（IPOPT/SNOPT）的对比，证明了该方法在处理大规模非凸问题时的效率优势。实验设计较为客观，特别是展示了算法在“朴素初始化”下的收敛能力。

#### 6. 论文的主要结论与发现
*   **联合优化的优越性：** 相比于先定路径再优化的解耦方法，ACTOR 能探索更大的搜索空间，发现更平滑、代价更低的轨迹。
*   **结构鲁棒性：** 算法对初始值不敏感，能够从穿过障碍物的直线初始化自动收敛到可行的非凸空间路径。
*   **高效扩展性：** 算法复杂度随轨迹段数和凸集数量呈**线性增长**，而传统的 MIQP 或 NLP 方法通常呈指数或组合爆炸式增长。

#### 7. 优点
*   **无需复杂热启动：** 解决了非凸优化中极度依赖良好初始值的痛点。
*   **计算效率高：** 将复杂的混合整数问题转化为 DAG 上的最短路径问题，利用动态规划极大地降低了搜索成本。
*   **数学框架优雅：** 巧妙地结合了 Bézier 曲线的几何特性与 ADMM 的算子分裂思想。

#### 8. 不足与局限
*   **局部最优风险：** 尽管比传统 NLP 鲁棒，但作为基于梯度的 ADMM 变体，在理论上仍只能保证收敛到平稳点，无法像 MIQP 那样保证全局最优。
*   **时间分配固定：** 虽然可以通过增加轨迹段数来获得灵活性，但每段的时间长度在优化过程中通常是预设的，未实现完全的时间-空间联合变分优化。
*   **实现性能：** 目前使用 Python 实现，虽然展示了线性扩展性，但在实时性要求极高的嵌入式平台上，其绝对运行速度仍需 C++ 进一步验证。

（完）
