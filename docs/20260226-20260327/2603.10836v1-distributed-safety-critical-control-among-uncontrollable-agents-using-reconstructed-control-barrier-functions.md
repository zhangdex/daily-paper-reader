---
title: Distributed Safety Critical Control among Uncontrollable Agents using Reconstructed Control Barrier Functions
title_zh: 基于重构控制障碍函数的不可控智能体间分布式安全关键控制
authors: "Yuzhang Peng, Wei Wang, Jiaqi Yan, Mengze Yu"
date: 2026-03-11
pdf: "https://arxiv.org/pdf/2603.10836v1"
tags: ["query:space-defense"]
score: 9.0
evidence: 使用控制屏障函数的分布式安全关键控制
tldr: 针对包含不可控且行为不确定智能体的多智能体系统，本文提出了一种分布式安全关键控制方案。为解决协作任务中控制屏障函数（CBF）约束耦合导致的分布式设计难题，研究引入了重构CBF方法，结合分布式自适应观测器和预设性能自适应参数，确保在不确定动态环境下系统依然满足安全约束，实现了完全分布式的安全控制。
selection_source: fresh_fetch
motivation: 旨在解决多智能体协作中CBF约束耦合以及不可控智能体行为不确定性导致的分布式安全控制挑战。
method: 提出重构CBF方法，通过分布式自适应观测器估计状态，并利用预设性能自适应参数修正约束以确保安全性。
result: 开发了基于重构CBF的安全关键二次规划控制器，理论证明了其在复杂不确定环境下的安全性。
conclusion: 该方案成功实现了多智能体系统的分布式安全保障，有效应对了不可控智能体带来的不确定性影响。
---

## 摘要
本文研究了在存在行为不确定的不可控智能体的情况下，多智能体系统（MAS）的分布式安全关键控制问题。为了确保系统安全性，本文采用了控制障碍函数（CBF）。然而，一个关键挑战在于，当多智能体系统执行协作任务时，CBF 约束是耦合的，这些约束依赖于多个智能体的信息，从而阻碍了全分布式安全控制方案的设计。为了克服这一问题，本文提出了一种新颖的重构 CBF 方法。在该方法中，通过利用从分布式自适应观测器获得的其他智能体的状态估计，对耦合的 CBF 进行重构。此外，设计了一个预设性能自适应参数来修正该重构过程，确保满足重构后的 CBF 约束足以满足原始的耦合约束。基于重构的 CBF，我们设计了一个安全关键二次规划（QP）控制器，并证明了所提出的分布式控制方案即使在包含不可控智能体的不确定动态环境中，也能严格保证多智能体系统的安全性。通过仿真验证了所提方法的有效性。

## Abstract
This paper investigates the distributed safety critical control for multi-agent systems (MASs) in the presence of uncontrollable agents with uncertain behaviors. To ensure system safety, the control barrier function (CBF) is employed in this paper. However, a key challenge is that the CBF constraints are coupled when MASs perform collaborative tasks, which depend on information from multiple agents and impede the design of a fully distributed safe control scheme. To overcome this, a novel reconstructed CBF approach is proposed. In this method, the coupled CBF is reconstructed by leveraging state estimates of other agents obtained from a distributed adaptive observer. Furthermore, a prescribed performance adaptive parameter is designed to modify this reconstruction, ensuring that satisfying the reconstructed CBF constraint is sufficient to meet the original coupled one. Based on the reconstructed CBF, we design a safety-critical quadratic programming (QP) controller and prove that the proposed distributed control scheme rigorously guarantees the safety of the MAS, even in the uncertain dynamic environments involving uncontrollable agents. The effectiveness of the proposed method is illustrated through simulations.

---

## 论文详细总结（自动生成）

这篇论文针对多智能体系统（MAS）在复杂环境下的分布式安全控制问题提出了创新方案。以下是对该论文的深度结构化总结：

### 1. 核心问题与研究动机
*   **核心问题**：如何在存在**不可控智能体**（如行人、人类驾驶车辆）且**安全约束耦合**（多个智能体状态共同决定安全性）的情况下，实现多智能体系统的全分布式安全关键控制。
*   **研究背景**：
    *   传统的控制障碍函数（CBF）方法通常是中心化的，因为安全约束往往依赖于全局状态。
    *   现有的分布式 CBF 方法（如约束分解法）要求所有相关智能体都必须是可控的，且必须遵循分配的局部约束。
    *   在现实中，环境中存在行为不确定的不可控智能体，无法强制它们满足特定的安全约束，这使得传统的分布式安全方案失效。

### 2. 论文提出的方法论
论文提出了一种基于**重构控制障碍函数（Reconstructed CBF, RCBF）**的分布式控制框架，核心步骤如下：
*   **分布式自适应观测器**：每个可控智能体设计一个观测器，利用局部通信和自适应参数（用于应对未知的 Lipschitz 常数和控制增益上界）来估计其他智能体（包括不可控智能体）的状态。
*   **CBF 重构技术**：智能体 $i$ 不直接使用原始耦合 CBF $h(x)$，而是利用估计状态构建重构函数 $\hat{h}_i$。
*   **预设性能自适应修正（PPC）**：引入一个基于预设性能控制理论的自适应参数 $\vartheta_i$ 来修正重构函数。通过设计误差演化规律，确保重构误差始终保持在预设的范围内。
*   **安全性保证逻辑**：理论证明了只要重构后的局部约束 $\hat{h}_i \geq 0$ 得到满足，原始的全局耦合约束 $h(x) \geq 0$ 就必然成立。
*   **分布式 QP 控制器**：每个智能体独立求解一个局部二次规划（QP）问题，计算最优控制输入，从而在保证安全的前提下尽可能完成名义任务（如轨迹跟踪）。

### 3. 实验设计
*   **实验场景**：多机器人导航任务。包含 4 个机器人（3 个可控，1 个不可控），环境中有 3 个圆形障碍物。
*   **任务目标**：
    *   不可控机器人前往目标点，可控机器人前往目标区域。
    *   **约束条件**：包括避障（机器人与障碍物）、防撞（机器人之间）以及耦合的距离保持约束（如机器人 1 和 2 需保持在一定距离内，机器人 3 需跟随不可控的机器人 4）。
*   **对比与基准**：实验主要展示了所提方法在复杂耦合约束下的有效性，通过监测重构 CBF 的值、重构误差以及控制输入曲线来验证安全性。

### 4. 资源与算力
*   **算力说明**：论文中**未明确说明**具体的硬件配置（如 GPU/CPU 型号）或训练时长。
*   **实现性质**：由于该方法基于解析的自适应控制和实时 QP 求解，通常在标准的 CPU 环境下即可运行，不涉及深度学习的大规模训练。

### 5. 实验数量与充分性
*   **实验规模**：论文展示了一组具有代表性的多机器人协作导航仿真实验。
*   **充分性评价**：
    *   实验涵盖了避障、防撞和异构耦合约束等多种情况，验证了理论的正确性。
    *   **局限性**：缺乏与现有其他分布式 CBF 方法（如约束分解法）的定量对比实验，也未进行大规模智能体集群（如数十个智能体）的扩展性测试。

### 6. 主要结论与发现
*   **分布式可行性**：通过状态估计和函数重构，成功将全局耦合约束转化为局部可解的约束。
*   **补偿能力**：可控智能体能够通过调整自身行为，补偿不可控智能体带来的不确定性，从而维持整体系统的安全性。
*   **严格安全性**：数学证明和仿真均表明，该方案能确保安全集（Safe Set）的前向不变性，即系统状态永远不会违反安全约束。

### 7. 优点（亮点）
*   **突破限制**：解决了以往方法要求“所有智能体必须可控”的苛刻前提，更符合自动驾驶、人机协作等实际场景。
*   **理论严密**：结合了自适应观测器、预设性能控制（PPC）和 CBF，提供了一套完整的稳定性与安全性证明。
*   **灵活性**：重构 CBF 方法不依赖于特定的约束分解形式，适用于更广泛的非线性耦合约束。

### 8. 不足与局限
*   **通信依赖**：虽然是分布式的，但仍假设图是连通的，且至少有一个可控智能体能观测到不可控智能体的状态。
*   **先验知识**：假设智能体已知安全约束 $h(x)$ 的函数形式（虽然不知道具体状态值），在某些完全未知的环境中可能受限。
*   **计算负担**：每个智能体需要运行多个观测器（估计所有其他智能体状态）并实时求解 QP，随着智能体数量增加，局部计算压力会增大。
*   **实验维度**：仅限于数值仿真，缺乏实物机器人验证或动态环境下的鲁棒性深度分析。

（完）
