---
title: "From Global to Local: Hierarchical Probabilistic Verification for Reachability Learning"
title_zh: 从全局到局部：可达性学习的分层概率验证
authors: "Ebonye Smith, Sampada Deglurkar, Jingqi Li, Gechen Qu, Claire J. Tomlin"
date: 2026-03-26
pdf: "https://arxiv.org/pdf/2603.24990v1"
tags: ["query:ra-rl-pe"]
score: 9.0
evidence: Hamilton-Jacobi可达性学习与验证
tldr: 针对高维非线性系统在可达性学习中存在的计算困难及安全性风险，本文提出一种分层概率验证框架。该框架结合了离线全局认证与在线局部细化：首先利用场景优化构建粗略的全局安全集，随后通过在线求解凸规划序列在边界处进行局部扩展，以减少保守性。实验表明，该方法在保证概率安全的同时，显著提升了复杂约束下无人机竞速任务的成功率。
selection_source: fresh_fetch
motivation: 传统HJ可达性分析在高维空间面临计算爆炸问题，而现有的学习近似方法往往缺乏可靠的安全保证。
method: 提出一种分层验证框架，利用场景优化提供全局概率证书，并通过在线凸规划序列对安全集边界进行局部细化。
result: 在双无人机竞速仿真实验中，该方法在保持安全性的前提下，通过减少保守性显著提高了任务成功率。
conclusion: 该研究成功结合了全局概率认证与局部在线优化，为高维非线性系统的安全控制提供了一种高效且具有理论保障的方案。
---

## 摘要
Hamilton-Jacobi (HJ) 可达性为非线性系统提供了形式化的安全保证。然而，在高维环境下，其计算变得难以处理，这促使了基于学习的近似方法的发展，但这些方法可能会引入不安全误差或过于乐观的安全集。在这项工作中，我们提出了一种用于可达性学习的分层概率验证框架，该框架连接了离线全局认证和在线局部细化。我们首先利用场景优化构建一个粗略的安全集，提供高效的全局概率保证。然后，我们引入了一个在线局部细化模块，通过求解一系列凸规划，在边界附近扩展已认证的安全集，从而恢复被全局验证排除的区域。这种细化在减少保守性的同时，将计算集中在状态空间的关键区域。我们为全局和局部细化后的集合都提供了概率安全保证。该框架与学习到的可达性策略和基于模型的控制器之间的切换机制相结合，提高了具有安全约束的目标到达任务的成功率，这一点在两架无人机在复杂安全约束下竞速到达目标的仿真实验中得到了证明。

## Abstract
Hamilton-Jacobi (HJ) reachability provides formal safety guarantees for nonlinear systems. However, it becomes computationally intractable in high-dimensional settings, motivating learning-based approximations that may introduce unsafe errors or overly optimistic safe sets. In this work, we propose a hierarchical probabilistic verification framework for reachability learning that bridges offline global certification and online local refinement. We first construct a coarse safe set using scenario optimization, providing an efficient global probabilistic certificate. We then introduce an online local refinement module that expands the certified safe set near its boundary by solving a sequence of convex programs, recovering regions excluded by the global verification. This refinement reduces conservatism while focusing computation on critical regions of the state space. We provide probabilistic safety guarantees for both the global and locally refined sets. Integrated with a switching mechanism between a learned reachability policy and a model-based controller, the proposed framework improves success rates in goal-reaching tasks with safety constraints, as demonstrated in simulation experiments of two drones racing to a goal with complex safety constraints.

---

## 论文详细总结（自动生成）

### 论文总结：从全局到局部：可达性学习的分层概率验证

#### 1. 论文的核心问题与整体含义
论文针对高维非线性系统在执行安全关键任务（如无人机竞速、多机协作）时，传统 **Hamilton-Jacobi (HJ) 可达性分析** 面临的“维度灾难”问题展开研究。虽然基于深度学习的近似方法（如 DeepReach）提升了计算效率，但其结果往往缺乏严谨的安全保证，容易产生过于乐观的估计，将危险区域误判为安全。
**核心动机**：开发一种既能处理高维动态系统，又能提供形式化安全保证的验证框架，通过结合离线全局认证与在线局部细化，平衡计算效率与安全保守性。

#### 2. 论文提出的方法论
提出了一种**分层概率验证框架**，其核心思想是将验证过程分为不同层级，根据系统状态实时切换控制策略：

*   **核心技术细节**：
    *   **全局概率认证（Tier 1）**：在离线阶段，利用**场景优化（Scenario Optimization）**理论，通过对系统动力学进行随机采样，估计轨迹偏差的概率上界。基于此上界构建一个粗略但具有概率保证的全局安全集 $V_{global}$。
    *   **在线局部细化（Tier 2）**：当系统处于全局安全集边缘时，启动在线细化模块。该模块通过在当前状态附近的边界点进行局部采样，并求解一系列凸规划问题，动态扩展（“收复”）被全局验证误判为不安全的区域。
    *   **混合切换机制**：
        *   **层级 0（目标维持）**：进入目标区域后，使用快速 MPPI 控制器保持状态。
        *   **层级 1 & 2（验证模式）**：若状态在已验证集合内，直接执行学习到的可达性策略 $\pi_{lp}$。
        *   **层级 3（安全恢复）**：若状态在验证集外，使用以学习策略为“热启动”的 **MPPI（模型预测路径积分）** 控制器，引导系统返回安全区域。
*   **理论保障**：利用场景优化理论证明了全局和局部集合均满足指定的风险水平 $\epsilon$ 和置信度 $\beta$。

#### 3. 实验设计
*   **实验场景**：**12维（12D）双无人机竞速**。自机（Ego）需在避开对手无人机下洗流（非凸约束）及门框障碍的同时，安全超越对手并率先通过终点。
*   **Benchmark 与对比方法**：
    *   **安全基准**：MPPI + 控制屏障函数 (CBF) 滤波器、MPPI + 安全软约束、PPO + CBF。
    *   **消融实验**：纯 MPPI（无安全考虑）、MPPI + 可达性热启动、纯可达性学习策略、无可达性引导的混合消融模型。

#### 4. 资源与算力
*   论文**未明确说明**具体的硬件配置（如 GPU 型号、显存数量）及具体的训练时长。
*   文中提到全局验证所需的样本量 $N$ 是根据场景优化公式计算的（例如在 $\epsilon=0.1, \beta=0.001$ 时需要 158 个样本），这表明该方法在样本效率上具有优势，适合在线计算。

#### 5. 实验数量与充分性
*   **实验规模**：在有界状态空间内随机采样了 **500 组初始条件**进行蒙特卡洛仿真。
*   **充分性评价**：实验设计较为充分。不仅对比了主流的安全强化学习和控制方法，还通过消融实验验证了“分层切换”和“可达性引导”各自的贡献。12维动力学模型在可达性分析领域属于较高维度，具有较强的说服力。
*   **客观性**：通过成功率（Success Rate）这一硬指标进行量化对比，结果显示该方法（90.2%）显著优于基准方法（14.2% - 68.4%）。

#### 6. 论文的主要结论与发现
*   **分层验证的有效性**：单一的全局验证过于保守，而引入在线局部细化能有效减少保守性，提高任务达成率。
*   **学习与模型的结合**：将学习到的策略作为模型预测控制（MPPI）的热启动，比单纯依赖学习策略或单纯依赖模型控制更鲁棒。
*   **概率保证的实用性**：在无法获得确定性几何保证的高维空间中，基于场景优化的概率保证提供了一种可行的替代方案。

#### 7. 优点
*   **创新性地结合了全局与局部验证**：解决了离线验证太保守、在线验证计算量大的矛盾。
*   **理论支撑扎实**：利用场景优化理论为学习到的神经网络策略提供了可量化的概率安全边界。
*   **处理复杂约束能力强**：在处理非凸、动态避障约束（如无人机下洗流区域）时表现优异。

#### 8. 不足与局限
*   **仿真与现实的差距**：实验完全在仿真环境下完成，未在真实无人机硬件上验证，实际环境中的传感器噪声和动力学失配可能影响验证的准确性。
*   **样本依赖性**：虽然样本量可控，但概率保证的严谨性高度依赖于采样分布的代表性。
*   **计算开销**：虽然局部细化被描述为“快速”，但在极高频率的控制循环中，在线求解多个轨迹和凸规划仍可能面临实时性挑战。
*   **参数敏感性**：风险参数 $\epsilon$ 的选择需要在保守性与样本量之间权衡，论文未深入讨论如何自动优化该参数。

（完）
