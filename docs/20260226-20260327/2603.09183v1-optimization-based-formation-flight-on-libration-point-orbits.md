---
title: Optimization-Based Formation Flight on Libration Point Orbits
title_zh: 基于优化的平动点轨道编队飞行
authors: "Yuri Shimane, Purnanand Elango, Avishai Weiss"
date: 2026-03-10
pdf: "https://arxiv.org/pdf/2603.09183v1"
tags: ["query:space-defense"]
score: 9.0
evidence: 用于航天器编队飞行和路径约束的MPC
tldr: 本研究针对平动点轨道上的航天器编队飞行，提出了一种基于模型预测控制（MPC）的站位保持框架。该方法通过求解多航天器最优控制问题，在满足有限轨道机动机会等操作约束的同时，利用序列凸规划解决非凸优化。通过路径约束紧致化和等周重构技术，确保了控制的递归可行性并防止采样间约束违规。在近直线晕轨道（NRHO）的高保真仿真验证中，该方案成功维持了编队构型，且燃料消耗与现有方法相当。
selection_source: fresh_fetch
motivation: 旨在解决平动点轨道编队飞行中，受限机动机会下的高精度站位保持与复杂路径约束避让问题。
method: 采用模型预测控制结合序列凸规划，并引入约束紧致化与等周重构技术以提升算法的鲁棒性与可行性。
result: 在高保真历表模型下的NRHO编队仿真证明，该方法能严苛执行航天器间距及太阳相位角约束。
conclusion: 该基于优化的控制策略在保证编队安全与任务约束的前提下，实现了与传统方法相当的燃料效率。
---

## 摘要
本文开发了一种模型预测控制 (MPC) 框架，用于沿平动点轨道飞行的航天器编队保持。在每个控制周期，MPC 策略求解一个多飞行器最优控制问题 (MVOCP)，在跟踪参考轨迹的同时，对编队的相对运动施加路径约束。该控制策略利用了一组有限的控制节点，这与每圈仅允许少量机动机会的操作约束相一致。为了提高递归可行性，路径约束在预测时域内被逐步收紧。采用约束的等周重构来防止采样点之间的违规。由此产生的 MVOCP 是一个非凸规划问题，通过序列凸规划进行求解。所提方法在具有真实不确定性的高保真历表模型中进行了评估，针对沿近直线晕轨道 (NRHO) 飞行的编队，并受到航天器间距和相对太阳相位角的路径约束。结果表明，该方法能够维持满足路径约束的航天器编队，且累计推进剂消耗与现有方法相当。

## Abstract
A model predictive control (MPC) framework is developed for station-keeping in spacecraft formation flight along libration point orbits. At each control period, the MPC policy solves a multi-vehicle optimal control problem (MVOCP) that tracks a reference trajectory, while enforcing path constraints on the relative motion of the formation. The control policy makes use of a limited set of control nodes consistent with operational constraints that allow only a small number of maneuver opportunities per revolution. To promote recursive feasibility, path constraints are progressively tightened across the prediction horizon. An isoperimetric reformulation of the constraints is used to prevent inter-sample violations. The resulting MVOCP is a nonconvex program, which is solved via sequential convex programming. The proposed approach is evaluated in a high-fidelity ephemeris model under realistic uncertainties for a formation along the near-rectilinear halo orbit (NRHO), and subject to path constraints on interspacecraft separation and relative Sun phase angle. The results demonstrate maintenance of a spacecraft formation that satisfies the path constraints with cumulative propellant consumption comparable to that of existing methods

---

## 论文详细总结（自动生成）

这是一份关于论文《Optimization-Based Formation Flight on Libration Point Orbits》的深度结构化总结：

### 1. 核心问题与整体含义（研究动机和背景）
该论文关注的是在**平动点轨道（LPO）**，特别是**近直线晕轨道（NRHO）**上实现多航天器编队飞行的站位保持问题。
*   **研究动机**：未来的地月空间任务（如 NASA 的 Gateway 门户站）需要航天器在复杂的非开普勒动力学环境下维持精确的编队构型。
*   **核心挑战**：在高保真历表模型（HFEM）中，轨道不再具有严格的周期性，传统的基于动力学结构（如准周期环面 QPT）的方法难以直接处理复杂的任务约束（如太阳相位角、通信距离限制），且在存在导航和执行误差时难以保证持续的安全性。
*   **整体含义**：论文旨在提出一个统一的优化框架，同时解决绝对站位保持（留在轨道上）和相对站位保持（维持编队），并确保在有限的机动机会下满足严苛的物理约束。

### 2. 方法论：核心思想与关键技术
论文开发了一个基于**模型预测控制（MPC）**的集中式控制框架，核心步骤如下：
*   **多航天器最优控制问题 (MVOCP)**：将编队保持建模为一个最小化推进剂消耗的优化问题。
*   **序列凸规划 (SCP)**：由于动力学和路径约束是非凸的，利用基于增强拉格朗日（Augmented Lagrangian）的 SCP 算法，通过迭代求解凸化后的子问题来逼近原问题的局部最优解。
*   **连续时间约束满足 (CTCS)**：引入**等周重构（Isoperimetric Reformulation）**技术。通过增加辅助状态变量来积分路径约束的违规量，将无限维的连续时间约束转化为有限维的边界约束，从而防止在两个控制节点之间发生碰撞或违规。
*   **递归可行性保障**：采用**逐步约束紧致化（Progressive Constraint Tightening）**策略。在预测时域内随时间推移逐渐收紧约束边界，为未来的不确定性预留裕度，确保 MPC 在滚动优化过程中始终有解。
*   **操作约束**：设定每圈仅两次脉冲机动（分别在真近点角 160° 和 200°），符合实际任务中的操作限制。

### 3. 实验设计
*   **场景设定**：两个航天器在 9:2 共振的 NRHO 轨道上进行编队飞行。
*   **动力学模型**：使用高保真历表模型（HFEM），包含月球非球性引力（4阶）、太阳辐射压力（SRP）以及地球和太阳的第三体引力。
*   **不确定性来源**：考虑了初始注入误差、SRP 建模误差、导航误差（位置/速度）以及控制执行误差（Gates 模型）。
*   **对比实验 (Benchmark)**：
    *   **实验 I & II**：仅考虑相对距离约束，对比“连续时间约束”与“仅节点约束”。
    *   **实验 III & IV**：同时考虑距离和太阳相位角约束，对比“连续时间约束”与“仅节点约束”。
    *   **基准对比**：与现有的基于局部环绕坐标（LTC）的方法进行推进剂消耗对比。

### 4. 资源与算力
*   **软件环境**：使用 Julia 语言开发，利用 `DifferentialEquations.jl` 进行高精度积分，`JuMP` 进行数学建模，`Clarabel` 作为底层凸优化求解器。
*   **算力说明**：文中**未明确提及**具体的硬件型号（如 CPU/GPU）或具体的计算耗时。但提到 SCP 算法中的状态转移矩阵（STM）积分过程可以通过并行化加速。

### 5. 实验数量与充分性
*   **实验规模**：针对四种主要实验配置，每组进行了 **100 次蒙特卡洛（Monte Carlo）模拟**。
*   **时长**：每次模拟持续 10 圈 NRHO 轨道（约 65.5 天）。
*   **充分性评价**：实验设计较为充分。除了核心的功能验证，还进行了**灵敏度分析**（针对约束紧致化参数 $\eta$ 和 $\kappa$），验证了算法在不同不确定性水平下的鲁棒性。实验涵盖了从简单距离约束到复杂光学可见性约束的演进，逻辑严密。

### 6. 主要结论与发现
*   **安全性**：等周重构技术能有效消除采样点间的约束违规，而仅在节点处施加约束的方法（实验 II/IV）频繁出现违规。
*   **燃料效率**：所提方法的年均推进剂消耗（3-$\sigma$ 值）约为每台航天器数百 cm/s，与现有先进方法相当，证明了优化框架在保证安全的同时并未牺牲经济性。
*   **约束代价**：引入复杂的太阳相位角约束仅导致约 30% 的平均成本增加，且 3-$\sigma$ 峰值成本增加极小（约 2.4%），说明该框架能高效处理多重约束。
*   **递归可行性**：适当的约束紧致化能显著提高 MPC 在长周期运行中的成功率（从 50% 提升至 100%）。

### 7. 优点（亮点）
*   **严谨的约束处理**：通过等周重构解决了航天器控制中常见的“采样间违规”痛点。
*   **高度通用性**：该框架不依赖于特定的动力学结构（如 QPT），可以轻松扩展到任意数量的航天器和任何可解析的路径约束（如指向约束、通信链路约束）。
*   **工程实用性**：考虑了真实的控制节奏和多种环境不确定性，结论对实际任务设计有直接参考价值。

### 8. 不足与局限
*   **计算复杂度**：作为集中式 MPC，随着编队中航天器数量 $M$ 的增加，决策变量和约束数量呈平方级增长（$O(M^2)$），可能面临计算瓶颈。
*   **导航假设**：实验假设了绝对导航误差，未深入探讨编队成员间相对导航（如星间链路测量）对性能的进一步提升作用。
*   **局部最优风险**：SCP 算法仅能保证收敛到局部最优解，在极度复杂的约束环境下可能存在初始化敏感问题。
*   **应用限制**：目前仅在 NRHO 轨道验证，对于更不稳定的平动点轨道（如某些 Halo 轨道），可能需要更短的控制节奏或更复杂的终端约束设计。

（完）
