---
title: GIANT - Global Path Integration and Attentive Graph Networks for Multi-Agent Trajectory Planning
title_zh: GIANT - 用于多智能体轨迹规划的全局路径集成与注意力图网络
authors: "Jonas le Fevre Sejersen, Toyotaro Suzumura, Erdal Kayacan"
date: 2026-03-04
pdf: "https://arxiv.org/pdf/2603.04659v1"
tags: ["query:robot-planning"]
score: 9.0
evidence: 多智能体轨迹规划与避障
tldr: 本研究提出GIANT框架，旨在解决多机器人系统在复杂动态环境下的避障与路径规划问题。该方法结合了全局路径规划与局部导航策略，利用注意力图神经网络处理智能体间的动态交互。通过在训练中引入噪声增强鲁棒性，使机器人在遵循全局最优路径的同时能灵活应对环境变化。实验表明，GIANT在成功率、避障效率及复杂场景适应性上均优于现有主流基准模型。
selection_source: fresh_fetch
motivation: 现有的多机器人导航方法在复杂动态环境中难以兼顾全局路径的最优性与局部避障的灵活性。
method: 提出一种集成全局路径信息的局部导航模型，并利用注意力图神经网络捕捉智能体间的交互，同时通过训练噪声提升鲁棒性。
result: 在多种仿真场景中，该模型在成功率、碰撞率和导航效率方面均显著优于NH-ORCA、DRL-NAV等基准模型。
conclusion: GIANT框架为复杂动态环境下的多机器人导航提供了高效且鲁棒的解决方案，在物流等需要高度适应性的领域具有重要应用价值。
---

## 摘要
本文提出了一种多机器人避障的新方法，该方法将全局路径规划与局部导航策略相结合，并利用注意力图神经网络来管理智能体之间的动态交互。我们引入了一种利用预规划全局路径的局部导航模型，使机器人能够在动态调整以适应环境变化的同时，遵循最优路径。通过在训练过程中引入噪声，增强了模型的鲁棒性，使其在复杂的动态环境中表现出卓越的性能。我们在各种结构多样的模拟场景中，将该方法与现有的基准模型（包括 NH-ORCA、DRL-NAV 和 GA3C-CADRL）进行了评估对比。结果表明，我们的模型始终能获得更高的成功率、更低的碰撞率以及更高效的导航，特别是在基准模型难以应对的挑战性场景中。这项工作推动了多机器人导航技术的发展，对于在不同复杂程度的动态环境中（如物流领域，其适应性对于应对不可预见的障碍和不可预测的变化至关重要）实现鲁棒性能具有重要意义。

## Abstract
This paper presents a novel approach to multi-robot collision avoidance that integrates global path planning with local navigation strategies, utilizing attentive graph neural networks to manage dynamic interactions among agents. We introduce a local navigation model that leverages pre-planned global paths, allowing robots to adhere to optimal routes while dynamically adjusting to environmental changes. The models robustness is enhanced through the introduction of noise during training, resulting in superior performance in complex, dynamic environments. Our approach is evaluated against established baselines, including NH-ORCA, DRL-NAV, and GA3C-CADRL, across various structurally diverse simulated scenarios. The results demonstrate that our model achieves consistently higher success rates, lower collision rates, and more efficient navigation, particularly in challenging scenarios where baseline models struggle. This work offers an advancement in multi-robot navigation, with implications for robust performance in complex, dynamic environments with varying degrees of complexity, such as those encountered in logistics, where adaptability is essential for accommodating unforeseen obstacles and unpredictable changes.

---

## 论文详细总结（自动生成）

以下是对论文《GIANT - Global Path Integration and Attentive Graph Networks for Multi-Agent Trajectory Planning》的深度结构化总结：

### 1. 核心问题与研究背景
*   **核心问题**：如何在复杂、动态且多智能体的环境中，实现既能遵循全局最优路径、又能灵活避开动态障碍物的高效导航。
*   **研究背景**：
    *   **局部导航的局限**：纯局部避障方法（如传统的 DRL）容易陷入局部最优（如死胡同），缺乏长程视野。
    *   **全局规划的局限**：全局规划器（如 A*）通常只考虑静态地图，难以实时应对其他移动智能体的动态交互。
    *   **集成挑战**：现有的集成方法往往简单地将全局路径点作为临时目标，导致智能体过度关注局部点而降低整体效率。

### 2. 核心方法论
GIANT 采用了一种**两阶段集成架构**，结合了全局路径引导与基于注意力机制的图神经网络（GNN）：
*   **全局路径集成（Global Path Integration）**：
    *   首先使用 A* 算法在静态地图上生成全局路径。
    *   在局部导航中，引入一个“动态目标点”（Running Target Point），该点随智能体移动沿全局路径前移，并作为观测输入。
*   **注意力图网络（Attentive GNN）**：
    *   利用 LiDAR 数据提取动态聚类（Dynamic Clusters），并将其表示为图结构。
    *   使用注意力机制处理邻近智能体/障碍物的交互，使模型能处理任意数量的邻居。
*   **多模态观测空间**：输入包括静态 LiDAR 特征、时间序列 LiDAR 特征（捕捉运动趋势）、自身状态、全局路径目标点以及邻居智能体的图表示。
*   **强化学习训练**：采用 **PPO（Proximal Policy Optimization）** 算法。奖励函数设计包含了目标达成、碰撞惩罚、社交距离维护以及**沿全局路径的进度奖励**。

### 3. 实验设计
*   **实验场景**：使用 VMAS 仿真框架构建了 6 种训练环境和 4 种测试环境（圆形对冲 Circle、窄门 Doorway、窄走廊 Narrow Corridor、随机障碍物 Random）。
*   **智能体规模**：测试了从 10 到 40 个智能体不等的大规模场景。
*   **基准模型（Benchmarks）**：
    *   **NH-ORCA**：基于启发式的非圆柱体互惠避障算法。
    *   **DRL-NAV**：经典的端到端深度强化学习导航模型。
    *   **GA3C-CADRL**：基于智能体抽象信息的深度强化学习模型。
*   **评估指标**：成功率（Success Rate）、碰撞/卡住率（Collision/Stuck Rate）、额外时间（Extra Time）、平均速度。

### 4. 资源与算力
*   **软件框架**：使用了 PyTorch 的 TorchRL 数据驱动框架和 VMAS 向量化仿真器。
*   **算力说明**：论文**未明确指出**具体的 GPU 型号、数量及训练总时长。但提到训练是在 6 种不同的 VMAS 环境中进行的，且使用了 PPO 算法。

### 5. 实验数量与充分性
*   **实验规模**：在 4 种主要测试场景下，针对不同数量的智能体进行了多组对比实验。
*   **消融实验**：专门针对“全局路径信息”和“GNN 交互模块”进行了消融研究，验证了这两个核心组件对提升成功率和降低碰撞率的必要性。
*   **客观性与公平性**：
    *   为了公平对比，所有基准模型都被赋予了相同的全局路径引导信息（将目标设为路径上的动态点）。
    *   在观测中引入了传感器噪声（位置 ±0.1m，速度 ±0.1m/s，LiDAR ±3.5% 噪声），以模拟真实世界的鲁棒性。

### 6. 主要结论与发现
*   **性能领先**：GIANT 在所有测试场景中均表现出最高的成功率和最低的碰撞率。特别是在“窄门”和“窄走廊”等高密度瓶颈场景中，基准模型普遍失效，而 GIANT 仍能保持 95% 以上的成功率。
*   **全局与局部的平衡**：GIANT 能够有效区分“全局路径”和“最终目标”，在遵循路径的同时，能更灵活地绕过动态障碍物，而不是死板地贴合路径。
*   **鲁棒性**：通过在训练中引入噪声，模型在面对不完美的传感器数据时表现出极强的适应性。

### 7. 论文亮点
*   **创新的观测表示**：将 LiDAR 聚类后的动态物体转化为图结构，并结合注意力机制，解决了多智能体交互中邻居数量多变的问题。
*   **路径遵循与避障的解耦**：通过引入动态目标点和进度奖励，解决了 DRL 导航中常见的“局部最小值”问题。
*   **开源贡献**：提供了开源实现，便于社区进行基准测试和后续研究。

### 8. 不足与局限
*   **同质化假设**：实验主要针对同质（Homogeneous）智能体，未深入探讨不同动力学特性的异质智能体混合场景。
*   **非合作博弈缺失**：虽然考虑了动态避障，但未充分讨论面对“恶意”或“完全不合作”的对抗性智能体时的表现。
*   **算力细节缺失**：未提供训练成本和推理延迟的具体数据，难以评估其在嵌入式硬件上的实时部署潜力。

（完）
