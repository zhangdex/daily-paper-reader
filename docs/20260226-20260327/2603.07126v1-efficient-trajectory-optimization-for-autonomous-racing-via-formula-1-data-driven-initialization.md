---
title: Efficient Trajectory Optimization for Autonomous Racing via Formula-1 Data-Driven Initialization
title_zh: 基于一级方程式数据驱动初始化的自动赛车高效轨迹优化
authors: "Samir Shehadeh, Lukas Kutsch, Nils Dengler, Sicong Pan, Maren Bennewitz"
date: 2026-03-07
pdf: "https://arxiv.org/pdf/2603.07126v1"
tags: ["query:robot-planning"]
score: 9.0
evidence: 基于数据驱动初始化的自动赛车轨迹优化
tldr: 针对自动驾驶赛车轨迹优化对初始值敏感、收敛慢的问题，本文提出一种基于F1赛车真实遥测数据的学习型初始化策略。研究者构建了包含17条赛道的F1轨迹数据集，并训练神经网络根据赛道几何形状预测专家级赛车线偏移量。该预测值作为最小时间最优控制求解器的初始种子，在保持最优性能的同时显著提升了求解效率。
selection_source: fresh_fetch
motivation: 传统的几何启发式初始化方法（如中心线）常导致轨迹优化求解器收敛缓慢或陷入局部最优解。
method: 利用17条F1赛道的真实遥测数据训练神经网络，根据赛道几何特征预测专家级赛车线作为优化求解器的初始种子。
result: 在所有测试赛道上，该学习型初始化策略显著加快了求解器的收敛速度并缩短了运行时间，且不损失单圈性能。
conclusion: 将专家驾驶经验作为先验知识引入优化流程，是提升高速自动驾驶赛车计算效率和鲁棒性的有效途径。
---

## 摘要
轨迹优化是实现快速且高效的自动赛车的核心组件。然而，实际的优化流程对初始化高度敏感，当使用中心线或最小曲率路径等启发式轨迹作为种子时，可能会收敛缓慢或陷入次优局部解。为了解决这一局限性，我们利用专家驾驶行为作为初始化先验，并提出了一种基于真实世界一级方程式（Formula 1）遥测数据的学习型初始化策略。为此，我们首先通过重建噪声 GPS 遥测数据并将其与 17 条赛道的标准化参考线表示对齐，构建了一个多赛道一级方程式轨迹数据集。在此基础上，我们提出了一个神经网络，直接从局部赛道几何形状预测类似专家的赛车线偏移量，而无需显式建模车辆动力学或受力。预测的赛车线随后被用作最小时间最优控制求解器的先验种子。在所有 17 条赛道上的实验表明，与传统的几何基准相比，学习到的初始化加速了求解器的收敛并显著减少了运行时间，同时保持了最终优化的单圈耗时。

## Abstract
Trajectory optimization is a central component of fast and efficient autonomous racing. However practical optimization pipelines remain highly sensitive to initialization and may converge slowly or to suboptimal local solutions when seeded with heuristic trajectories such as the centerline or minimum-curvature paths. To address this limitation, we leverage expert driving behavior as a initialization prior and propose a learning-informed initialization strategy based on real-world Formula 1 telemetry. To this end, we first construct a multi-track Formula~1 trajectory dataset by reconstructing and aligning noisy GPS telemetry to a standardized reference-line representation across 17 tracks. Building on this, we present a neural network that predicts an expert-like raceline offset directly from local track geometry, without explicitly modeling vehicle dynamics or forces. The predicted raceline is then used as an informed seed for a minimum-time optimal control solver. Experiments on all 17 tracks demonstrate that the learned initialization accelerates solver convergence and significantly reduces runtime compared to traditional geometric baselines, while preserving the final optimized lap time.

---

## 论文详细总结（自动生成）

这是一份关于论文《Efficient Trajectory Optimization for Autonomous Racing via Formula-1 Data-Driven Initialization》的结构化总结：

### 1. 核心问题与整体含义
该论文针对**自动驾驶赛车轨迹优化**中的“最小单圈时间问题”（MLTP）展开研究。核心挑战在于：现有的非线性规划（NLP）求解器对**初始值（Initialization）高度敏感**。传统的启发式初始化方法（如赛道中心线或最小曲率路径）往往远离全局最优解，导致求解器收敛缓慢、容易陷入局部最优，甚至求解失败。论文提出利用**一级方程式（F1）真实比赛的专家级遥测数据**来训练神经网络，预测一个接近最优的赛车线作为初始种子，从而显著提升优化效率。

### 2. 方法论
论文提出了一套结合数据驱动预测与物理约束优化的混合流程：
*   **数据集构建**：从 17 条 F1 赛道中提取 GPS 遥测数据（2024-2025赛季），通过全局相似性配准算法将噪声较大的 GPS 坐标对齐到标准化的赛道参考线（Frenet 坐标系）上，提取出专家级的横向偏移量 $d(s)$。
*   **神经网络架构（F1-NN）**：
    *   **输入**：采用滑动窗口机制，输入包含历史窗口（过去轨迹和几何特征）和未来窗口（赛道曲率、边界偏移）。
    *   **核心组件**：使用**扩张时间卷积网络（Dilated TCN）**捕捉长程空间依赖，结合**多头时间注意力机制（Multi-head Temporal Attention）**融合历史与未来特征。
    *   **输出**：预测目标窗口内的赛车线横向偏移量。
*   **优化集成**：将 F1-NN 预测的几何轨迹作为初始值，输入到基于物理模型的最小时间最优控制求解器（使用 IPOPT 求解器）中。求解器在满足车辆动力学约束（如轮胎力、加速度限制）的前提下，对轨迹进行精细化调整。

### 3. 实验设计
*   **数据集/场景**：涵盖了 17 条真实的 F1 赛道（如蒙扎、斯帕、银石等），数据源自 Assetto Corsa 模拟器和 TUM 赛道数据库。
*   **基准方法（Benchmarks）**：
    1.  **CL (Centerline)**：赛道中心线，最简单的基准。
    2.  **MC (Minimum Curvature)**：最小曲率轨迹，传统的几何优化方法。
    3.  **F1-GT**：重建的 F1 专家真实轨迹（作为性能上限参考）。
*   **对比指标**：求解器迭代次数、优化耗时、初始化生成耗时、总运行时间、最终单圈时间、几何误差（RMSE/MAE）。
*   **硬件验证**：在 1:10 的 **RoboRacer** 自动驾驶赛车平台上进行实车测试，验证从全尺寸 F1 数据到缩比模型的跨域迁移能力。

### 4. 资源与算力
*   论文**未明确说明**具体的训练算力细节（如 GPU 型号、数量或具体训练时长）。
*   但在推理效率方面有明确数据：F1-NN 生成初始轨迹仅需 **0.63 秒**，远快于最小曲率（MC）方法的生成时间。

### 5. 实验数量与充分性
*   **实验规模**：在全部 17 条赛道上进行了交叉验证（Leave-three-out 策略），确保模型对未见赛道的泛化能力。
*   **充分性**：实验设计较为全面，不仅对比了数值优化指标（收敛速度），还对比了最终的物理性能（单圈时间）。
*   **客观性**：通过统计显著性检验（p-value）验证了结果的可靠性。引入实车硬件验证进一步增强了论文的说服力，证明了该方法在存在领域偏移（Domain Shift）时的鲁棒性。

### 6. 主要结论与发现
*   **效率提升**：相比中心线初始化，F1-NN 使求解器迭代次数减少了 **17%**，优化时间缩短了约 **26 秒**。
*   **总耗时最优**：虽然最小曲率（MC）也能加速收敛，但其自身生成耗时较长。F1-NN 凭借极低的推理成本，实现了最低的总运行时间（比 MC 快约 50%）。
*   **性能保持**：学习到的初始化不仅加快了速度，且最终优化的单圈时间与使用专家真实轨迹（F1-GT）作为初始值的结果非常接近。
*   **跨域有效**：在 1:10 缩比赛车上的实验显示，该方法比中心线初始化实现了更快的单圈速度和更低的轨迹跟踪误差。

### 7. 优点
*   **创新性地利用专家数据**：将顶尖人类驾驶员的直觉（隐含在遥测数据中）转化为数学优化的先验知识。
*   **计算效率极高**：通过神经网络替代复杂的几何预优化，极大地缩短了端到端的轨迹规划时间。
*   **实用性强**：不依赖复杂的动力学模型进行预测，仅凭几何特征即可生成高质量种子点，易于部署。

### 8. 不足与局限
*   **动力学解耦**：神经网络仅预测几何路径，未考虑车辆动力学（如不同车辆的加速/制动特性），这可能导致在某些极端动力学限制下初始值质量下降。
*   **环境假设单一**：实验假设路面摩擦系数恒定，未讨论雨天或路面磨损等变摩擦力场景下的表现。
*   **样本量限制**：虽然 17 条赛道涵盖了多种特征，但对于深度学习而言，样本量仍属于小规模，可能在极特殊形状的赛道上泛化受限。

（完）
