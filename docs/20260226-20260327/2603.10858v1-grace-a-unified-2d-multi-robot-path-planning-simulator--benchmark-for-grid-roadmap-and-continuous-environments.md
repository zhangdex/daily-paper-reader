---
title: "GRACE: A Unified 2D Multi-Robot Path Planning Simulator & Benchmark for Grid, Roadmap, And Continuous Environments"
title_zh: GRACE：一个面向栅格、路图及连续环境的统一二维多机器人路径规划仿真器与基准测试
authors: "Chuanlong Zang, Anna Mannucci, Isabelle Barz, Philipp Schillinger, Florian Lier, Wolfgang Hönig"
date: 2026-03-11
pdf: "https://arxiv.org/pdf/2603.10858v1"
tags: ["query:robot-planning"]
score: 9.0
evidence: 多机器人路径规划模拟器与基准
tldr: 针对多机器人路径规划中缺乏统一评估平台的问题，本文提出了GRACE，一个集成了网格、路标图和连续环境的2D仿真与基准测试平台。它通过统一的算子和评估协议，实现了不同抽象层级任务的公平对比，揭示了表示精度与计算效率之间的权衡，为多机器人规划研究提供了标准化工具。
selection_source: fresh_fetch
motivation: 现有的多机器人路径规划工具往往在简化假设下运行或缺乏可比性，难以在不同环境表示法之间进行透明且可重复的性能评估。
method: 提出了GRACE平台，通过显式、可重复的算子和通用评估协议，在网格、路标图和连续空间等多个抽象层级上实例化相同的规划任务。
result: 实验量化了表示精度与保真度之间的权衡，发现MRMP在更高保真度下解决问题但速度较慢，而网格和路标图规划器则具有更好的扩展性。
conclusion: GRACE通过整合环境表示、执行和评估，为跨表示研究提供了可比性，推动了多机器人规划研究向实际应用的转化。
---

## 摘要
推进多智能体路径寻找 (MAPF) 和多机器人运动规划 (MRMP) 需要能够对不同建模选择进行透明、可重复比较的平台。现有工具要么在简化假设（如栅格、同质智能体）下具有较好的扩展性，要么提供更高的保真度但缺乏可比的评估手段。我们提出了 GRACE，这是一个统一的二维仿真器兼基准测试，通过显式、可重复的算子和通用的评估协议，在多个抽象层级（栅格、路图、连续）上实例化相同的任务。我们在公开地图和代表性规划器上的实证结果，使得在共享实例集上进行同等规模的比较成为可能。此外，我们量化了预期的表示-保真度权衡（MRMP 以更高的保真度但较低的速度解决实例，而栅格/路图规划器的扩展性更强）。通过整合表示、执行和评估，GRACE 旨在使跨表示研究更具可比性，并为推进多机器人规划研究及其向实践的转化提供手段。

## Abstract
Advancing Multi-Agent Pathfinding (MAPF) and Multi-Robot Motion Planning (MRMP) requires platforms that enable transparent, reproducible comparisons across modeling choices. Existing tools either scale under simplifying assumptions (grids, homogeneous agents) or offer higher fidelity with less comparable instrumentation. We present GRACE, a unified 2D simulator+benchmark that instantiates the same task at multiple abstraction levels (grid, roadmap, continuous) via explicit, reproducible operators and a common evaluation protocol. Our empirical results on public maps and representative planners enable commensurate comparisons on a shared instance set. Furthermore, we quantify the expected representation-fidelity trade-offs (MRMP solves instances at higher fidelity but lower speed, while grid/roadmap planners scale farther). By consolidating representation, execution, and evaluation, GRACE thereby aims to make cross-representation studies more comparable and provides a means to advance multi-robot planning research and its translation to practice.

---

## 论文详细总结（自动生成）

### 论文总结：GRACE 统一二维多机器人路径规划仿真器与基准测试

#### 1. 核心问题与整体含义
该论文针对多智能体路径寻找（MAPF）与多机器人运动规划（MRMP）领域长期存在的**碎片化问题**提出了解决方案。
*   **研究动机**：MAPF 通常基于离散栅格和同质智能体，侧重算法扩展性；而 MRMP 在连续时空中考虑动力学约束，侧重物理保真度。目前缺乏一个统一平台能在相同的任务实例下，公平、透明地比较不同环境表示法（栅格、路图、连续空间）对规划性能的影响。
*   **核心价值**：GRACE 提供了一个统一的 API 和评估协议，允许研究者在不同抽象层级上实例化同一任务，从而量化“表示精度”与“计算效率”之间的权衡。

#### 2. 方法论
GRACE 的核心思想是通过**显式的抽象算子**实现跨表示的一致性：
*   **环境抽象算子 ($\Phi_{env}$)**：将原始连续空间（$X_{free}$）转换为路图（$G_r$）或栅格（$G_g$）。
*   **智能体抽象算子 ($\Phi_{ag}$)**：处理异构足迹（Footprint）。在连续空间保留任意凸多边形；在路图和栅格中，通过计算最小包围圆（$r_{com}$）将智能体抽象为圆盘。
*   **可行性定义**：
    *   **连续空间**：基于扫掠体积（Swept Volume）的碰撞检测。
    *   **路图**：强制执行顶点容量、边交换检查及几何交点处的虚拟顶点约束。
    *   **栅格**：遵循标准的 MAPF 规则（禁止协同占用、对角线穿越等）。
*   **系统架构**：采用 C++20 开发，底层使用 **Box2D** 物理引擎确保连续时间仿真的确定性，并提供交互式 UI（SFML + ImGui）进行可视化编辑。

#### 3. 实验设计
实验分为三个维度，旨在验证平台的通用性、研究价值和实用性：
*   **跨环境模型对比**：在 4 种地图（空地、迷宫、仓库、窗口）上，使用 8 个异构智能体，对比连续空间规划器（db-CBS）、不同密度的路图规划器及栅格规划器。
*   **跨算法对比（栅格）**：在 MovingAI 标准地图上对比了 6 种主流规划器：
    *   经典搜索类：CBSH2-RTC, EECBS, LaCAM。
    *   元启发式：MAPF-LNS2。
    *   学习/混合类：SCRIMP, MAPF-GPT-DDG。
*   **扩展性测试**：在大型仓库地图上使用 LaCAM 算法，测试智能体数量从个位数增加到 2500 个时的系统表现。

#### 4. 资源与算力
*   **硬件环境**：笔记本电脑，配置为 Intel Core i7-13850HX CPU，32GB 内存。
*   **GPU 使用**：配备 NVIDIA RTX 3500 Ada (12GB)，仅用于学习型规划器（如 SCRIMP）的**推理阶段**。
*   **训练时长**：文中未涉及模型训练，所有学习类模型均直接使用官方发布的预训练权重。
*   **仿真性能**：在 2500 个智能体规模下，系统仍能保持实时运行（RTF > 1）。

#### 5. 实验数量与充分性
*   **实验规模**：涵盖了 4 种地图家族、多种智能体规模（最高 2500 个）、3 种环境表示法以及 6 种不同类型的算法。
*   **充分性**：实验设计较为全面，不仅对比了算法性能，还深入探讨了环境表示（如路图密度）对结果的影响。
*   **客观性**：通过固定随机种子、使用统一的 Box2D 物理后端以及标准化的成功准则，确保了不同表示法之间比较的公平性。

#### 6. 主要结论与发现
*   **表示法权衡**：路图（Roadmap）是极佳的折中方案。其路径成本（SoC）仅比连续空间高 8%-18%，但规划速度提升了 2-3 个数量级。
*   **栅格局限性**：栅格规划速度最快，但路径成本最高（平均比连续空间高 23%），且受环境结构（如走廊宽度）影响较大。
*   **算法表现**：LaCAM 在大规模场景下表现出极强的鲁棒性（2500 智能体 100% 成功率）；而学习类方法在特定地图（如随机地图）表现优异，但在复杂地图（如迷宫）中方差较大。

#### 7. 优点
*   **统一性**：首次在单一框架内打通了 MAPF 和 MRMP 的评估，解决了“苹果与橘子”难以对比的问题。
*   **确定性**：基于 Box2D 的仿真确保了实验结果的可重复性（Bitwise identical）。
*   **易用性**：提供了丰富的交互式 UI 和标准化的插件接口，方便集成新算法。
*   **高性能**：C++ 核心保证了极高的仿真效率，支持数千个智能体的实时回放。

#### 8. 不足与局限
*   **维度限制**：目前仅支持 2D 环境，未涵盖 3D 空间（如无人机集群）。
*   **动态性不足**：实验主要针对静态地图和单次任务（Single-shot），未深入探讨动态障碍物或终身规划（Lifelong MAPF）场景。
*   **形状假设**：在路图和栅格抽象中，依然假设智能体为圆盘，对于长条形等极度非对称的机器人，抽象误差可能较大。
*   **动力学简化**：虽然支持连续空间，但目前的抽象算子对复杂非完整约束（Non-holonomic）的支持仍有待加强。

（完）
