---
title: Reconfiguration of Squares Using a Constant Number of Moves Each
title_zh: 每个正方形仅使用常数次移动的正方形重构
authors: "Thijs van der Horst, Maarten Löffler, Tim Ophelders, Tom Peters"
date: 2026-03-05
pdf: "https://arxiv.org/pdf/2603.05203v1"
tags: ["query:robot-planning"]
score: 9.0
evidence: 多机器人运动规划
tldr: 本研究探讨了多机器人运动规划中的正方形重构问题，限制每个机器人仅能移动常数次。研究分析了在不同约束（如是否带标签、正方形尺寸是否统一）下的计算复杂度。结果表明，除无标签且为单位正方形的特殊情况外，该问题在大多数情况下仍为NP困难。
selection_source: fresh_fetch
motivation: 针对多机器人运动规划的复杂性，探索限制每个机器人移动次数这一约束对问题求解难度的影响。
method: 研究了正方形机器人在平面上沿任意曲线滑动至新位置的重构过程，并限制每个正方形的移动次数为常数。
result: 证明了在大多数情况下该问题是NP困难的，但在无标签且正方形为单位尺寸时存在例外。
conclusion: 即使限制了移动次数，正方形重构问题在大多数变体下依然具有极高的计算复杂度。
---

## 摘要
多机器人运动规划是一个难题。我们研究了该问题的受限变体，其中正方形机器人被允许沿任意曲线滑动到新位置，且每个机器人仅限移动常数次。我们证明了该问题在大多数情况下仍然是 NP 难的，除非正方形具有单位尺寸且问题是无标记的（即，每个正方形在目标配置中的位置未指定）。

## Abstract
Multi-robot motion planning is a hard problem. We investigate restricted variants of the problem where square robots are allowed to slide over an arbitrary curve to a new position only a constant number of times each. We show that the problem remains NP-hard in most cases, except when the squares have unit size and when the problem is unlabeled, i.e., the location of each square in the target configuration is left unspecified.

---

## 论文详细总结（自动生成）

这是一份关于论文《Reconfiguration of Squares Using a Constant Number of Moves Each》（每个正方形仅使用常数次移动的正方形重构）的结构化总结：

### 1. 论文的核心问题与整体含义
该论文探讨了**多机器人运动规划（Multi-robot Motion Planning, MRMP）**的一个受限变体。在标准 MRMP 中，判断一组机器人是否能从初始配置移动到目标配置通常是 PSPACE 困难的。
为了降低复杂性，本文引入了一个约束：**限制每个正方形机器人只能移动常数 $x$ 次**（例如每个机器人只准动 1 次或 $k$ 次）。研究的核心在于：这种对移动次数的严格限制是否能使原本困难的重构问题变得易于求解（多项式时间内可解）。

### 2. 论文提出的方法论
论文通过理论证明和算法设计，对不同约束组合下的问题复杂度进行了分类研究。
*   **NP-困难性证明（针对大多数情况）：**
    *   **带标签单次移动（Labeled Monotonic）：** 通过从“平面单调 3-SAT 问题”归约证明，即使是单位正方形，只要每个机器人有指定的目标位置，问题就是 NP 困难的。
    *   **无标签大正方形（Unlabeled, Size > 1）：** 通过从“哈密顿路径问题”归约证明，当正方形尺寸大于 $1 \times 1$ 时，即使不指定目标位置，重构依然困难。
    *   **有界区域单位正方形多次移动：** 同样利用哈密顿路径归约，证明了在多边形边界内移动的复杂性。
*   **多项式时间算法（针对无标签单位正方形）：**
    *   **核心思想：** 将问题转化为**网络流（Network Flow）**问题。
    *   **流程：** 
        1. 将空间离散化为网格图。
        2. 建立流网络：源点连接初始位置，目标位置连接汇点，网格边容量设为机器人总数 $n$。
        3. 计算最大流并消除环路，得到一个有向无环图（DAG）。
        4. 按照 DAG 的拓扑顺序提取移动路径，确保移动过程中不发生碰撞。

### 3. 实验设计
由于这是一篇**理论计算几何论文**，它没有传统意义上的实验数据集或 Benchmark 跑分，而是通过**逻辑归约（Reductions）**和**分类讨论**来验证结论。
*   **对比场景：** 论文构建了一个完整的分类矩阵（见文中 Table 1），对比了以下维度：
    *   **标签：** 带标签（每个机器人有专属终点）vs 无标签（任意机器人到任意终点）。
    *   **边界：** 有界（在多边形内）vs 无界（无限平面）。
    *   **尺寸：** 单位正方形 ($1 \times 1$) vs 大正方形 ($2 \times 2$ 或更大)。
    *   **移动次数：** 1 次（单调重构）vs $k$ 次（常数次）。

### 4. 资源与算力
*   **算力说明：** 文中**未提及**任何 GPU、算力消耗或训练时长。
*   **性质：** 该研究属于纯理论证明和算法复杂度分析，不涉及深度学习或大规模数值仿真，其成果体现为数学证明。

### 5. 实验数量与充分性
*   **覆盖程度：** 论文对 Table 1 中列出的所有 16 种主要场景（4个维度交叉）都给出了明确的复杂度结论（要么给出 NP-hard 证明，要么给出多项式算法，或者指出其为平凡解）。
*   **充分性：** 从理论完备性角度看，实验（证明过程）是非常充分的。它不仅涵盖了单次移动的极端情况，还通过“锁存器（Latch）”小部件将证明推广到了任意常数 $k$ 次移动的情况。

### 6. 论文的主要结论与发现
*   **绝大多数变体仍是 NP 困难的：** 限制移动次数并不能像预想中那样显著降低 MRMP 的复杂度。
*   **唯一的“绿洲”：** 只有在**无标签**且机器人为**单位正方形**的情况下，问题才存在多项式时间解法。
*   **尺寸的影响：** 一旦正方形尺寸大于 $1 \times 1$，即使是无标签的单调重构也会变成 NP 困难。
*   **无界平面的平凡性：** 在无边界限制且允许移动 $\ge 2$ 次时，问题变得平凡（可以先把所有机器人移到无限远处，再按需移回）。

### 7. 优点
*   **分类系统化：** 对重构问题的各个参数维度进行了详尽的分类，填补了该领域在“常数次移动”约束下的理论空白。
*   **证明技巧精妙：** 设计了多种复杂的几何小部件（Gadgets），如变量小部件、子句小部件、哈密顿路径中的顶点和边小部件，逻辑严密。
*   **算法实用性：** 为无标签单位正方形提供的网络流算法不仅证明了可行性，且具有实际的可操作性。

### 8. 不足与局限
*   **形状限制：** 研究仅限于正方形。虽然文中提到结果可能推广到圆盘，但未给出具体证明。
*   **环境约束：** 证明中使用的多边形区域包含“洞”，目前尚不清楚在没有洞的简单多边形中，这些 NP-困难性结论是否依然成立。
*   **非优化性：** 论文主要关注“是否存在解”（判定问题），而非“最优解”（如最小化总移动距离或总时间）。
*   **应用限制：** 限制移动次数为常数在现实机器人任务中可能过于严苛，实际应用中机器人往往需要多次往复移动。

（完）
