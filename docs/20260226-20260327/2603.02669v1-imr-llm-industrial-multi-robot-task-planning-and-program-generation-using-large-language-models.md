---
title: "IMR-LLM: Industrial Multi-Robot Task Planning and Program Generation using Large Language Models"
title_zh: IMR-LLM：基于大语言模型的工业多机器人任务规划与程序生成
authors: "Xiangyu Su, Juzhan Xu, Oliver van Kaick, Kai Xu, Ruizhen Hu"
date: 2026-03-03
pdf: "https://arxiv.org/pdf/2603.02669v1"
tags: ["query:robot-planning"]
score: 9.0
evidence: 使用大语言模型的工业多机器人任务规划
tldr: 针对工业生产中多机器人协作面临的严格时序约束和复杂任务依赖挑战，本文提出 IMR-LLM 框架。该框架结合大语言模型（LLM）的推理能力与确定性求解方法，通过析取图构建高层任务规划，并利用过程树指导底层程序生成。此外，研究者发布了包含三个难度等级的 IMR-Bench 基准测试。实验证明，该方法在多机器人工业任务规划与程序生成方面显著优于现有方法。
selection_source: fresh_fetch
motivation: 工业场景中复杂的任务依赖和严格的时序约束使得现有 LLM 在多机器人协作规划中难以直接应用。
method: 提出 IMR-LLM 框架，利用 LLM 辅助构建析取图进行高层规划，并结合过程树引导 LLM 生成可执行的底层程序。
result: 在涵盖多复杂等级的 IMR-Bench 基准测试中，该方法在所有评估指标上均显著超越了现有技术。
conclusion: IMR-LLM 通过结合 LLM 的推理能力与确定性算法，为工业多机器人系统的任务规划与自动化编程提供了高效且可行的解决方案。
---

## 摘要
在现代工业生产中，多台机器人通常协作完成复杂的制造任务。大语言模型（LLMs）凭借其强大的推理能力，在协调机器人执行简单的家庭和操作任务方面展现出了潜力。然而，在工业场景中，更严格的时序约束和任务内更复杂的依赖关系给 LLMs 带来了新的挑战。为此，我们提出了 IMR-LLM，这是一种新型的由 LLM 驱动的工业多机器人任务规划与程序生成框架。具体而言，我们利用 LLMs 辅助构建析取图（disjunctive graphs），并采用确定性求解方法来获得可行且高效的高层任务计划。在此基础上，我们使用过程树（process tree）引导 LLMs 生成可执行的底层程序。此外，我们创建了 IMR-Bench，这是一个具有挑战性的基准测试，涵盖了三个复杂度级别的多机器人工业任务。实验结果表明，我们的方法在所有评估指标上都显著优于现有方法。

## Abstract
In modern industrial production, multiple robots often collaborate to complete complex manufacturing tasks. Large language models (LLMs), with their strong reasoning capabilities, have shown potential in coordinating robots for simple household and manipulation tasks. However, in industrial scenarios, stricter sequential constraints and more complex dependencies within tasks present new challenges for LLMs. To address this, we propose IMR-LLM, a novel LLM-driven Industrial Multi-Robot task planning and program generation framework. Specifically, we utilize LLMs to assist in constructing disjunctive graphs and employ deterministic solving methods to obtain a feasible and efficient high-level task plan. Based on this, we use a process tree to guide LLMs to generate executable low-level programs. Additionally, we create IMR-Bench, a challenging benchmark that encompasses multi-robot industrial tasks across three levels of complexity. Experimental results indicate that our method significantly surpasses existing methods across all evaluation metrics.

---

## 论文详细总结（自动生成）

### IMR-LLM：基于大语言模型的工业多机器人任务规划与程序生成

#### 1. 核心问题与整体含义（研究动机和背景）
在现代智能制造中，多机器人协作完成复杂工业任务（如抛光、焊接、组装）是核心需求。现有的基于大语言模型（LLM）的机器人规划方法多针对家庭或简单操作场景，其任务顺序较为灵活。然而，**工业场景面临两大严峻挑战**：
*   **严格的时序约束**：工业工序必须遵循特定的先后顺序。
*   **复杂的资源依赖**：多个机器人竞争有限的加工设备（如抛光台、传送带），容易产生冲突。
单纯依靠 LLM 的推理能力难以保证生成计划的可行性和最优性。本文提出 **IMR-LLM** 框架，旨在结合 LLM 的语义理解能力与确定性算法的严谨性，解决工业环境下的多机器人任务规划与程序自动化生成问题。

#### 2. 方法论
IMR-LLM 框架由三个核心模块组成：
*   **高层任务规划（LLM + 析取图）**：
    *   利用 LLM 将自然语言指令分解为操作集（Operations）、分配机器人（Allocations）并确定工件内部的工序顺序。
    *   将上述信息转化为**析取图（Disjunctive Graph）**，显式建模机器人和设备的资源冲突。
    *   使用确定性启发式算法（如 FIFO）求解析取图，生成无冲突、高效率的调度计划（Scheduling Graph）。
*   **底层程序生成（过程树引导）**：
    *   构建**操作过程树（Operation Process Tree）**，将复杂的工业操作（如抛光）分解为标准化的功能节点（如拍照、检测边界、生成轨迹）。
    *   LLM 根据当前场景在过程树中选择路径，并组合预定义的代码片段（Snippets），生成可执行的 Python 程序。这种方法替代了传统的 Few-shot 提示词，增强了代码的鲁棒性和环境适应性。
*   **执行模块**：根据调度计划依次调用生成的程序，驱动机器人完成任务。

#### 3. 实验设计
*   **数据集与基准测试**：创建了 **IMR-Bench**，包含 23 个源自真实工业环境的场景，涵盖 1 到 7 台机器人。
*   **任务复杂度分级**：
    *   **单机器人任务**：简单操作。
    *   **简单多机器人任务**：最多 3 台机器人，包含并行或串行协作。
    *   **复杂多机器人任务**：最多 7 台机器人，包含复杂的并行/串行混合逻辑。
*   **对比方法**：
    *   **SMART-LLM**：直接利用 LLM 进行分解、分配和调度。
    *   **LaMMA-P**：使用 LLM 直接生成调度顺序。
    *   **LiP-LLM**：结合线性规划和依赖图的方法。
*   **评估指标**：操作一致性（OC）、调度效率（SE）、可执行性（Exe）、目标达成率（GCR）和最终成功率（SR）。

#### 4. 资源与算力
*   **模型使用**：实验主要基于 **GPT-4o** 进行评估。
*   **泛化性测试**：使用了国产开源模型 **Qwen3-32B-thinking** 验证框架的通用性。
*   **算力细节**：文中**未明确说明**具体的 GPU 型号、数量或训练时长。由于该框架主要侧重于 LLM 的推理应用（Inference-based）而非模型训练，其算力消耗主要集中在 API 调用或本地大模型的推理部署上。

#### 5. 实验数量与充分性
*   **实验规模**：共进行了 50 组制造任务实验，涵盖了不同难度等级。
*   **消融实验**：针对“析取图”和“过程树”分别设计了对比实验（如 w/o T, w/order 等），验证了各组件对提升调度效率和代码可执行性的贡献。
*   **真实环境验证**：在包含 3 台机器人的真实工业场景中进行了部署测试。
*   **评价**：实验设计较为全面，涵盖了从仿真到实体的跨度，对比基准选择合理，消融实验逻辑严密，能够客观反映该方法在处理复杂逻辑时的优势。

#### 6. 主要结论与发现
*   **IMR-LLM 显著优于现有方法**：尤其在复杂多机器人任务中，成功率（SR）远超直接使用 LLM 规划的方法。
*   **确定性求解的必要性**：在工业场景下，LLM 难以直接处理复杂的资源冲突，引入析取图和确定性算法能显著提升调度效率（SE）。
*   **结构化引导的优势**：过程树比 Few-shot 示例更能有效减少 LLM 的幻觉，提高生成代码的成功率和可重用性。

#### 7. 优点
*   **混合架构**：巧妙结合了 LLM 的柔性推理与经典调度算法的刚性约束，解决了工业场景的痛点。
*   **可解释性强**：通过析取图生成的计划易于工程师审查和验证，符合工业安全要求。
*   **模块化与扩展性**：过程树结构使得添加新工序或适配新设备变得简单，无需重新训练模型。

#### 8. 不足与局限
*   **长上下文压力**：在极度复杂的任务中，随着操作数量增加，LLM 仍可能出现逻辑不一致或幻觉，导致 OC 指标下降。
*   **开环控制**：目前系统主要基于预规划，缺乏实时的感知反馈闭环（如操作失败后的自动重试或动态重规划）。
*   **场景描述依赖**：高度依赖于人工或预定义的 JSON 场景描述，若环境感知出现偏差，规划结果将失效。

（完）
