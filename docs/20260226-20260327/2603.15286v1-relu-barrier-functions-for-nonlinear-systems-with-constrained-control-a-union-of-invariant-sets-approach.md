---
title: "ReLU Barrier Functions for Nonlinear Systems with Constrained Control: A Union of Invariant Sets Approach"
title_zh: 具有约束控制的非线性系统的 ReLU 障碍函数：一种不变集并集方法
authors: "Pouya Samanipour, Hasan A. Poonawala"
date: 2026-03-16
pdf: "https://arxiv.org/pdf/2603.15286v1"
tags: ["query:ra-rl-pe"]
score: 9.0
evidence: 受限控制非线性系统的障碍函数
tldr: 针对具有多面体输入约束的非线性系统安全性验证难题，本文提出一种基于ReLU屏障函数和不变集并集（UIS）的合成框架。该方法通过分段线性代理模型进行凸屏障合成，并利用Leaky ReLU改进扩展K类函数以降低保守性。通过面验证和局部不确定性更新处理非线性误差，最终在保持计算效率的同时，实现了比传统线性设计更大的认证安全不变集。
selection_source: fresh_fetch
motivation: 解决非线性系统在控制输入饱和约束下，难以合成既能保证安全性又具备控制可容许性的控制屏障函数的问题。
method: 采用分段线性代理模型进行凸合成，结合Leaky ReLU改进K类函数，并通过不变集并集和面验证确保原始系统的安全性。
result: 在单摆和倒立摆系统的仿真中，该方法在可接受的计算时间内获得了比线性K类函数设计更大的认证不变集。
conclusion: 该研究证明了结合ReLU代理模型与不变集并集方法能有效提升受限非线性系统的安全控制性能和认证范围。
---

## 摘要
由于控制障碍函数（CBF）的合成必须确保在饱和情况下的控制容许性，为具有多胞体输入约束的非线性系统验证安全性具有挑战性。我们提出了一种“近似-验证”流程，在分段仿射（PWA）代理模型上执行凸障碍合成，并通过逐面验证来认证原始非线性系统的安全性。为了在保持计算可行性的同时降低保守性，我们为扩展 $\mathcal{K}$ 类函数 $\alpha(\cdot)$ 采用了双斜率 Leaky ReLU 代理，并利用不变集并集（UIS）结合了多个证书。反例通过局部不确定性更新进行处理。在具有输入饱和的单摆和倒立摆系统上的仿真表明，与线性 $\alpha$ 设计相比，该方法在可接受的计算时间内获得了更大的认证不变集。

## Abstract
Certifying safety for nonlinear systems with polytopic input constraints is challenging because CBF synthesis must ensure control admissibility under saturation. We propose an approximation--verification pipeline that performs convex barrier synthesis on piecewise-affine (PWA) surrogates and certifies safety for the original nonlinear system via facet-wise verification. To reduce conservatism while preserving tractability, we use a two-slope Leaky ReLU surrogate for the extended class-$\mathcal{K}$ function $α(\cdot)$ and combine multiple certificates using a Union of Invariant Sets (UIS). Counterexamples are handled through local uncertainty updates. Simulations on pendulum and cart-pole systems with input saturation show larger certified invariant sets than linear-$α$ designs with tractable computation time.

---

## 论文详细总结（自动生成）

### 论文结构化深度总结：具有约束控制的非线性系统的 ReLU 障碍函数

#### 1. 核心问题与整体含义（研究动机和背景）
论文针对**具有多面体输入约束（如执行器饱和）的非线性系统**，探讨如何高效地合成并验证**控制障碍函数（CBF）**。
*   **研究动机**：在机器人和自动驾驶等安全关键领域，确保系统轨迹始终保持在安全区域内至关重要。然而，现有的 CBF 合成方法面临两大挑战：
    1.  **非凸性与计算复杂性**：直接为非线性系统合成 CBF 通常涉及复杂的非凸优化。
    2.  **保守性**：为了保持计算简便，通常选择线性的扩展 $\mathcal{K}$ 类函数 $\alpha(\cdot)$，但这会显著缩小认证的安全不变集范围。
    3.  **输入饱和**：输入约束可能导致原本安全的区域因控制力不足而变得不安全。

#### 2. 论文提出的方法论
论文提出了一种“**近似-合成-验证-更新**”的流水线框架，核心思想是利用 ReLU 神经网络与分段仿射（PWA）函数的等价性。
*   **PWA 代理模型合成**：首先利用 ReLU 神经网络将非线性闭环动力学近似为 PWA 代理模型。
*   **Leaky ReLU 代理 $\alpha(\cdot)$**：提出使用双斜率的 Leaky ReLU 函数作为 $\alpha(\cdot)$ 的代理。相比单一线性斜率，这种设计在保持凸优化的同时，能更好地逼近非线性 $\mathcal{K}$ 类函数，从而降低保守性。
*   **不变集并集（UIS）**：这是本文的创新点。通过对不同的 $\alpha$ 参数并行求解多个线性规划（LP），然后利用 **max-composition（极大值复合）** 将得到的所有不变集取并集。这种方法无需额外的优化成本，即可获得比任何单一参数设计都更大的认证区域。
*   **面验证（Facet-wise Verification）**：利用 Farkas 引理，在 PWA 障碍函数的边界分段（面）上，针对原始非线性系统和输入约束进行精确验证。
*   **局部不确定性更新**：如果验证失败（发现反例），算法不会重新训练模型，而是局部更新该区域的不确定性界限并重新求解 LP。

#### 3. 实验设计
论文通过三个典型场景验证了方法的有效性：
*   **场景 1：倒立摆（Inverted Pendulum）**：2 维系统，带有输入饱和限制。对比了单一线性 $\alpha$ 与 UIS 方法生成的集合大小。
*   **场景 2：基准非线性系统验证**：引用自现有文献（如 Zhang 等人, Zhao 等人）的 2 维非线性系统。
*   **场景 3：小车倒立摆（Cart-Pole）**：4 维高维系统，具有复杂的非线性动力学和输入力矩饱和。
*   **对比基准（Benchmark）**：
    *   **SOS（平方和规划）**：传统的非线性系统分析方法。
    *   **SyntheBC**：基于神经网络的障碍函数合成方法。
    *   **线性 $\alpha$ 设计**：作者之前提出的基础方法。

#### 4. 资源与算力
*   **硬件环境**：2.1 GHz CPU，8 GB RAM。
*   **软件环境**：Python 3.11。
*   **计算耗时**：
    *   倒立摆（2D）：总计 46 秒。
    *   基准系统（2D）：总计 80 秒。
    *   小车倒立摆（4D）：总计 1466 秒（约 24 分钟）。
*   **说明**：文中未提及使用 GPU，所有计算似乎均在 CPU 上完成，体现了该方法对算力要求较低且具有较好的实时潜力。

#### 5. 实验数量与充分性
*   **实验规模**：共进行了 3 组主要实验，涵盖了从低维到中等维度（4D）的非线性系统。
*   **充分性评价**：
    *   **对比充分**：实验不仅展示了自身方法的改进，还与 SOS 和 SyntheBC 等主流方法进行了可视化对比（如图 3 所示）。
    *   **客观性**：通过 4D 系统的测试证明了算法在处理更高维度和输入饱和时的扩展能力。
    *   **消融/参数研究**：通过展示不同 $\alpha$ 值对不变集形状的影响，间接验证了 UIS 方法的必要性。

#### 6. 论文的主要结论与发现
*   **UIS 的优越性**：UIS 方法生成的认证安全集始终不小于（通常显著大于）任何单一线性 $\alpha$ 选取的集合，且不增加优化负担。
*   **Leaky ReLU 的有效性**：证明了双斜率 $\alpha$ 函数在保持计算可解性的同时，能有效减少安全分析的保守性。
*   **精确验证的可行性**：通过面验证和局部更新机制，可以将 PWA 代理模型上的保证可靠地转移到原始非线性系统上，确保了形式化的安全性。

#### 7. 优点
*   **凸性保证**：将复杂的非线性安全验证转化为一系列线性规划（LP）问题，保证了全局最优性和计算效率。
*   **降低保守性**：通过 UIS 和 Leaky ReLU 巧妙地解决了传统方法中安全区域过小的问题。
*   **处理输入饱和**：显式考虑了执行器约束，这在实际工程应用中比理想化的 CBF 更具参考价值。
*   **模块化更新**：局部不确定性更新机制避免了昂贵的全局重训练。

#### 8. 不足与局限
*   **维度灾难风险**：虽然在 4D 系统上表现良好，但 PWA 划分的数量随维度增加呈指数级增长，对于更高维（如 10D 以上）的系统，分段数量可能导致计算量激增。
*   **依赖初始控制器**：合成过程需要一个预定义的标称反馈控制律（Nominal Control Law），如果初始控制器性能极差，可能难以找到有效的不变集。
*   **分区细化策略**：虽然提到了自动细化分区，但在极端非线性区域，分区可能会变得非常细碎，影响求解速度。

（完）
