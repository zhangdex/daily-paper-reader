---
title: Online Generation of Collision-Free Trajectories in Dynamic Environments
title_zh: 动态环境下无碰撞轨迹的在线生成
authors: "Nermin Covic, Bakir Lacevic"
date: 2026-02-28
pdf: "https://arxiv.org/pdf/2603.00759v1"
tags: ["query:robot-planning"]
score: 9.0
evidence: 在线生成运动学可行的轨迹
tldr: 本研究提出一种在线轨迹生成方法，能将任意几何路径转换为运动学可行且加加速度受限的轨迹。该方法利用五次/四次样条曲线，支持实时环境自适应和目标状态重规划。在动态环境中，它能保证一定时间内的无碰撞特性，并严格遵守运动学约束。实验证明其在平滑度、计算效率（最高1kHz）及实时性方面优于现有方法，且适用于包含人类的真实场景。
selection_source: fresh_fetch
motivation: 旨在解决如何将离散几何路径实时转化为满足运动学约束且能应对动态环境变化的平滑轨迹。
method: 通过生成一系列五次或四次样条曲线，在满足加加速度限制和几何偏差约束的前提下，实现路径到轨迹的在线转换。
result: 仿真和实机实验表明，该方法在处理频繁目标变更时具有极高的计算效率（1kHz）和优异的平滑度。
conclusion: 该方法为动态环境下的机器人运动规划提供了一种高效、安全且符合运动学特性的实时轨迹生成方案。
---

## 摘要
本文提出了一种在线方法，用于将由任何规划器（例如，基于采样的规划器如 RRT 或 PRM，基于搜索的规划器如 ARA* 等）生成的、由状态序列表示的任意几何路径，转换为相应的运动学可行且加加速度（jerk）受限的轨迹。该方法生成一系列五次/四次样条曲线，这些曲线可以按用户指定的控制频率进行离散化，然后流式传输到底层机器人控制器。我们的方法能够实时适应环境中新捕获的变化。它还可以在任何时间点重新调用，以生成从机器人当前状态到期望目标状态或状态序列的新轨迹。我们能够保证轨迹在动态环境中在一定时间内保持无碰撞，同时允许与原始路径之间存在有界的几何偏差。考虑了运动学约束，包括受限的加加速度。我们通过与竞争方法的对比仿真研究验证了该方法，证明了其在平滑度、计算时间和实时性能方面的优异表现，特别是在目标状态频繁变化（高达 1 [kHz]）的场景中。在真实机器人上的实验表明，所提出的方法可用于包括有人类存在的真实世界场景。

## Abstract
In this paper, we present an online method for converting an arbitrary geometric path represented by a sequence of states, generated by any planner (e.g., sampling-based planners like RRT or PRM, search-based planners like ARA*, etc.), into a corresponding kinematically feasible, jerk-limited trajectory. The method generates a sequence of quintic/quartic splines that can be discretized at a user-specified control rate, and then streamed to a low-level robot controller. Our approach enables real-time adaptation to newly captured changes in the environment. It can also be re-invoked at any time instance to generate a new trajectory from the robot's current to a desired target state or sequence of states. We can guarantee that the trajectory will remain collision-free for a certain amount of time in dynamic environments, while allowing bounded geometric deviation from the original path. The kinematic constraints are taken into account, including limited jerk. We validate the approach in a comparative simulation study against the competing method, demonstrating favorable behavior w.r.t. smoothness, computational time, and real-time performance, particularly in scenarios with frequent changes of target states (up to 1 [kHz]). Experiments on a real robot demonstrate that the proposed approach can be used in real-world scenarios including human presence.

---

## 论文详细总结（自动生成）

以下是对论文《Online Generation of Collision-Free Trajectories in Dynamic Environments》的结构化深度总结：

### 1. 核心问题与整体含义（研究动机和背景）
*   **核心问题**：如何在动态环境下，将离散的几何路径（由 RRT、PRM 等规划器生成）实时转换为满足运动学约束（位置、速度、加速度、加加速度/Jerk）且能保证安全的平滑轨迹。
*   **研究动机**：传统的路径参数化方法（如 TOPP）多为离线计算，难以适应动态障碍物或目标点的频繁变更。现有的在线方法（如 Ruckig）虽然高效，但在处理复杂动态避障和轨迹平滑度（Jerk 优化）方面仍有提升空间。

### 2. 论文提出的方法论：CFS45
该方法被称为 **CFS45**（Collision-Free 4th/5th order Splines），核心思想是通过五次/四次样条曲线实现路径到轨迹的转换。
*   **样条曲线生成**：利用五次多项式描述关节运动，通过初始状态（位置、速度、加速度）和目标状态构建方程组。
*   **二分法 Jerk 选择（算法1）**：在满足 Jerk 限制的范围内，利用二分法搜索最优的 Jerk 系数，以求得最小的运动时间 $t_{fi}$，同时确保不违反任何运动学边界。
*   **路径到轨迹转换（算法2）**：对原始路径进行简化和加密，尝试在路径拐角处进行平滑插值。如果直接插值会导致碰撞，则通过二分搜索寻找最接近原始路径且无碰撞的插值点。
*   **高效碰撞检测（算法3/4）**：引入“自由配置空间气泡（Bubbles）”和“刺（Bur）”的概念。通过计算机器人与障碍物的最小距离，构建安全区域。利用分离平面（Separating Planes）和动态扩展气泡（DEBs）技术，在动态环境下提供安全保证。
*   **紧急制动机制**：当常规轨迹可能发生碰撞时，系统会计算四次样条曲线（Quartic Splines）执行紧急停车，确保机器人始终处于安全状态。

### 3. 实验设计
*   **实验场景**：
    1.  **仿真场景1**：10个随机移动的小型障碍物（速度达 1.6 m/s）。
    2.  **仿真场景2**：4个大型预定义障碍物（速度达 0.3 m/s）。
    3.  **实机实验**：使用 UFactory xArm6 机械臂，在有人类干扰的真实环境中验证。
*   **Benchmark（基准）**：**Ruckig**（目前学术界和工业界最领先的在线轨迹生成库）。
*   **对比指标**：计算耗时、轨迹平滑度（Jerk L1范数）、成功率、路径长度、Frechet 距离（轨迹与原始路径的几何偏差）。

### 4. 资源与算力
*   **硬件环境**：Intel Core i7-9750H CPU @ 2.60 GHz，16 GB RAM。
*   **算力使用**：实验明确指出代码仅在 **单核 CPU** 上运行，**未使用 GPU 加速**。这强调了该算法极高的计算效率，适合嵌入式或实时控制系统。

### 5. 实验数量与充分性
*   **实验规模**：进行了 1000 次随机初始/目标点的仿真运行，涵盖了 19 种不同的规划频率（从 10Hz 到 1kHz）。
*   **数据量**：在执行时间测试中，收集了超过 **2400 万条** 轨迹数据进行统计分析。
*   **充分性评价**：实验设计非常充分且客观。通过大规模随机采样和不同频率下的对比，验证了算法在极端情况下的鲁棒性。同时，实机实验补充了感知不确定性下的表现，增强了说服力。

### 6. 论文的主要结论与发现
*   **计算速度**：CFS45 的平均计算速度比 Ruckig 快约 **3 倍**，能够支持高达 **1 kHz** 的目标更新频率。
*   **平滑度**：在 Jerk 性能指标上，CFS45 比 Ruckig 提升了约 **3.3 倍**，生成的运动更加平滑，有利于保护电机和减速器。
*   **几何跟踪**：CFS45 生成的轨迹与原始几何路径的偏差（Frechet 距离）平均比 Ruckig 小 **2.33 倍**。
*   **安全性**：在动态高频更新场景下，CFS45 配合动态气泡技术展现了更高的任务成功率。

### 7. 优点
*   **极高的实时性**：微秒级的计算耗时使其成为动态避障的理想选择。
*   **运动学严谨**：严格遵守位置、速度、加速度和 Jerk 的限制。
*   **安全保证**：通过 DEBs 和紧急制动策略，在理论上保证了动态环境下的安全性（Type II 碰撞约束）。
*   **易于集成**：可作为任何几何路径规划器的后处理插件。

### 8. 不足与局限
*   **障碍物形状限制**：碰撞检测算法依赖于凸包分解，对于极度复杂的非凸障碍物，计算开销可能会增加。
*   **保守性**：基于气泡（Bubbles）的安全区域计算通常较为保守，可能会导致在极度拥挤的空间内无法找到可行轨迹（尽管成功率已优于对比方法）。
*   **高维扩展性**：虽然在 6-DoF 机械臂上表现优异，但文中未深入探讨在更高自由度（如双臂或多机器人协同）下的计算扩展性。

（完）
