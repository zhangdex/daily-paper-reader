---
title: "SG-CoT: An Ambiguity-Aware Robotic Planning Framework using Scene Graph Representations"
title_zh: SG-CoT：一种基于场景图表示的歧义感知机器人规划框架
authors: "Akshat Rana, Peeyush Agarwal, K. P. S. Rana, Amarjit Malhotra"
date: 2026-03-18
pdf: "https://arxiv.org/pdf/2603.18271v1"
tags: ["query:robot-planning"]
score: 9.0
evidence: 使用场景图的机器人规划框架
tldr: 针对大语言模型在机器人规划中面临的歧义性挑战，本文提出SG-CoT框架。该框架通过构建环境的场景图表示，并结合检索函数让LLM迭代查询相关信息，从而将推理过程与实际观测挂钩。SG-CoT能识别歧义来源并主动提问，显著提升了机器人在单智能体和多智能体环境下的规划准确率和任务成功率。
selection_source: fresh_fetch
motivation: 大语言模型作为机器人规划器时，难以有效识别和处理环境观测中的歧义信息，导致规划可靠性不足。
method: 提出一种两阶段框架，首先构建包含物体属性与关系的场景图，再利用检索函数引导LLM迭代查询场景图以消除歧义并生成指令。
result: "实验显示SG-CoT在提问准确率上提升了至少10%，且在单智能体和多智能体环境下的任务成功率分别提升了4%和15%。"
conclusion: SG-CoT通过场景图增强了LLM的感知接地能力，为实现更具通用性和鲁棒性的机器人规划提供了有效路径。
---

## 摘要
歧义性对作为机器人规划器的大语言模型（LLMs）构成了重大挑战。在本文中，我们提出了场景图思维链（SG-CoT），这是一个两阶段框架，其中 LLM 迭代地查询环境的场景图表示，以检测并澄清歧义。首先，根据输入观测构建环境的结构化场景图表示，捕捉物体、其属性以及与其他物体的关系。其次，LLM 配备了检索函数，用于查询场景图中与给定指令相关的部分。这使 LLM 的推理过程立足于观测，从而提高了机器人在歧义情况下的规划可靠性。SG-CoT 还允许 LLM 识别歧义来源，并向用户或其他机器人提出相关的消歧问题。广泛的实验表明，SG-CoT 一贯优于先前的方法，在提问准确率上至少提高了 10%，在单智能体环境中的成功率至少提高了 4%，在多智能体环境中至少提高了 15%，验证了其在更具泛化性的机器人规划中的有效性。

## Abstract
Ambiguity poses a major challenge to large language models (LLMs) used as robotic planners. In this letter, we present Scene Graph-Chain-of-Thought (SG-CoT), a two-stage framework where LLMs iteratively query a scene graph representation of the environment to detect and clarify ambiguities. First, a structured scene graph representation of the environment is constructed from input observations, capturing objects, their attributes, and relationships with other objects. Second, the LLM is equipped with retrieval functions to query portions of the scene graph that are relevant to the provided instruction. This grounds the reasoning process of the LLM in the observation, increasing the reliability of robotic planners under ambiguous situations. SG-CoT also allows the LLM to identify the source of ambiguity and pose a relevant disambiguation question to the user or another robot. Extensive experimentation demonstrates that SG-CoT consistently outperforms prior methods, with a minimum of 10% improvement in question accuracy and a minimum success rate increase of 4% in single-agent and 15% in multi-agent environments, validating its effectiveness for more generalizable robot planning.

---

## 论文详细总结（自动生成）

这篇论文介绍了一种名为 **SG-CoT（Scene Graph-Chain-of-Thought）** 的机器人规划框架，旨在解决大语言模型（LLM）在面对环境或指令歧义时表现出的过度自信和规划失败问题。

以下是对该论文的结构化总结：

### 1. 核心问题与整体含义（研究动机和背景）
*   **核心问题**：当人类指令模糊（如“递给我那个杯子”，但现场有多个杯子）或环境状态不明（如目标物体不存在）时，现有的 LLM 机器人规划器往往会盲目执行错误动作，而非寻求澄清。
*   **研究背景**：现有的消歧方法多依赖于不确定性估计（如 CLARA）或简单的视觉描述，难以处理涉及物体属性（颜色、状态）或复杂空间关系（“左边的物体”）的深度歧义。
*   **整体含义**：通过引入**场景图（Scene Graph）**作为环境的结构化表示，并结合**迭代式思维链（CoT）**，使机器人能够主动查询环境细节，识别歧义来源，并向用户提出针对性的澄清问题。

### 2. 方法论：SG-CoT 框架
该框架分为两个主要阶段：
*   **阶段一：场景图生成**
    *   利用视觉语言模型（VLM，如 Grounding DINO 或 Qwen-VL）从原始图像中提取物体列表。
    *   通过 Prompt 引导 VLM 生成包含物体属性（颜色、大小、状态）和物体间关系（空间位置、包含关系）的结构化 JSON 场景图。
*   **阶段二：迭代推理与消歧**
    *   **核心思想**：LLM 不再是一次性生成计划，而是通过调用 API 函数（`retrieve_node` 和 `retrieve_edge`）来按需查询场景图。
    *   **算法流程**：
        1.  **初始化**：将指令、物体列表和场景图架构输入 LLM。
        2.  **推理循环**：LLM 生成推理轨迹（Thought），决定是否需要更多信息。
        3.  **函数调用**：如果需要，LLM 调用 API 查询特定属性（如“红色的碗有哪些？”）或关系（“碗里有什么？”）。
        4.  **状态更新**：查询结果反馈给 LLM，更新对话历史，循环继续。
        5.  **决策输出**：当信息充足时，输出最终动作序列或特定的澄清问题（如“你是要黄色块还是蓝色块？”）。

### 3. 实验设计
*   **实验场景**：
    1.  **单智能体环境**：基于 SayCan 模拟器，设置了基础任务、空间关系任务和长程任务。
    2.  **多智能体环境**：基于 LEMMA 基准，测试机器人在部分可观测情况下的协作与通信。
*   **歧义类型**：定义了三种类型：多样性（Multiplicity）、缺失（Absence）和描述不足（Underspecification）。
*   **对比方法（Baselines）**：
    *   **CLARA**：基于不确定性估计的消歧方法。
    *   **Inner Monologue**：基于环境反馈的闭环规划方法。
    *   **ProgPrompt**：基于编程提示词的规划方法。
*   **评估指标**：成功率（SR）和提问准确率（CQR）。

### 4. 资源与算力
*   **模型使用**：实验使用了 **Gemini-2.5-Flash** 和 **Qwen3-VL-2B-Instruct** 作为核心推理引擎和视觉感知模型。
*   **算力说明**：论文**未明确说明**具体的硬件配置（如 GPU 型号、数量）或训练/推理的总时长。由于该方法主要基于 Prompting 和 API 调用，不涉及大规模模型微调，因此算力消耗主要集中在 LLM/VLM 的推理延迟上。

### 5. 实验数量与充分性
*   **实验规模**：在单智能体实验中进行了 **400 次试验**（每种歧义类别 100 次，外加 100 次清晰场景）。
*   **消融实验**：设计了多组消融实验，包括“无迭代直接读取全图”、“仅限一次迭代”以及“去除场景图边信息”，验证了迭代查询和结构化关系的必要性。
*   **充分性评价**：实验覆盖了从简单到复杂的多种歧义场景，并对比了当前主流的几种 LLM 规划框架，实验设计较为客观、公平，样本量足以支撑其结论。

### 6. 主要结论与发现
*   **性能提升**：SG-CoT 在提问准确率（CQR）上比最优基准提升了至少 **10%**。
*   **成功率**：在单智能体任务中成功率提升约 **4%**，在多智能体（部分可观测）任务中成功率显著提升 **15%**。
*   **消歧能力**：通过迭代查询，LLM 能够精准定位歧义点（例如发现有两个符合条件的物体），从而生成更高质量的澄清问题。
*   **模型规模效应**：性能随模型能力增强而提升，Gemini-2.5-Flash 的表现优于较小的 Qwen 模型。

### 7. 优点
*   **结构化接地（Grounding）**：将非结构化的视觉信息转化为结构化场景图，有效减少了 LLM 的幻觉。
*   **主动交互**：赋予机器人“不知道就问”的能力，且提问具有针对性，而非盲目报错。
*   **可扩展性**：API 查询机制允许模型处理复杂的环境，而不会因为输入过长的环境描述而导致上下文溢出。

### 8. 不足与局限
*   **感知依赖**：框架高度依赖 VLM 生成场景图的准确性。如果 VLM 在第一阶段识别物体关系出错（幻觉），后续推理将受到误导。
*   **延迟与成本**：迭代推理意味着多次 LLM 调用，这会增加系统的响应延迟和 Token 消耗成本。
*   **实验局限**：目前仅在**模拟环境**中验证，缺乏真实物理环境（Real-world）的复杂噪声和动态变化测试。
*   **扩展性挑战**：对于极其复杂的指令，迭代次数可能会显著增加，影响实时性。

（完）
