---
title: Multi-Robot Coordination for Planning under Context Uncertainty
title_zh: 上下文不确定性下的多机器人协作规划
authors: "Pulkit Rustagi, Kyle Hollins Wray, Sandhya Saisubramanian"
date: 2026-03-14
pdf: "https://arxiv.org/pdf/2603.13748v2"
tags: ["query:robot-planning"]
score: 9.0
evidence: 不确定性下的多机器人协同与规划
tldr: 本文针对多机器人系统在上下文不确定环境下的规划挑战，提出了MR-CUSSP模型。该研究通过两阶段方法解决问题：首先利用CIMOP算法协调机器人前往关键地标收集观测数据以推断真实上下文，随后使用LCBS算法在推断出的偏好顺序下进行无冲突路径规划。该方法在仿真和真实机器人实验中均表现出高效的推理能力和任务执行性能。
selection_source: fresh_fetch
motivation: 机器人任务的优先级通常依赖于未知的上下文，若不能准确推断上下文，可能导致行为与目标不一致或产生安全风险。
method: 提出了一种结合协调推理（CIMOP）与词典序冲突搜索（LCBS）的两阶段框架，用于处理上下文推断与多目标路径规划。
result: 通过三个仿真场景和五个移动机器人的实地测试，验证了该算法在复杂不确定环境下的协调效率和实用性。
conclusion: 该研究成功实现了不确定上下文下的多机器人高效协作，为复杂环境下的安全多目标规划提供了有力支持。
---

## 摘要
现实世界中的机器人通常在目标优先级取决于底层操作上下文的环境中运行。当底层上下文先验未知时，多个机器人可能需要协作收集信息性观测以推断上下文，因为基于错误的上下文采取行动可能导致目标不一致和不安全的行为。一旦推断出底层真实上下文，机器人将按照上下文诱导的偏好顺序优化其特定任务目标。我们将该问题形式化为多机器人上下文不确定随机最短路径（MR-CUSSP），它通过联合观测在路标状态捕获上下文相关信息。我们的两阶段解决方案包括：(1) CIMOP（多目标规划的协作推理），用于计算引导机器人前往信息丰富的路标以高效推断真实上下文的规划；(2) LCBS（词典序冲突搜索），用于在上下文诱导的词典序目标偏好下进行无碰撞多机器人路径规划。我们在三个模拟领域评估了该算法，并在 salp 领域设置中使用五个移动机器人展示了其实际适用性。

## Abstract
Real-world robots often operate in settings where objective priorities depend on the underlying context of operation. When the underlying context is unknown apriori, multiple robots may have to coordinate to gather informative observations to infer the context, since acting based on an incorrect context can lead to misaligned and unsafe behavior. Once the underlying true context is inferred, the robots optimize their task-specific objectives in the preference order induced by the context. We formalize this problem as a Multi-Robot Context-Uncertain Stochastic Shortest Path (MR-CUSSP), which captures context-relevant information at landmark states through joint observations. Our two-stage solution approach is composed of: (1) CIMOP (Coordinated Inference for Multi-Objective Planning) to compute plans that guide robots toward informative landmarks to efficiently infer the true context, and (2) LCBS (Lexicographic Conflict-Based Search) for collision-free multi-robot path planning with lexicographic objective preferences, induced by the context. We evaluate the algorithms using three simulated domains and demonstrate its practical applicability using five mobile robots in the salp domain setup.

---

## 论文详细总结（自动生成）

### 论文总结：上下文不确定性下的多机器人协作规划

#### 1. 核心问题与整体含义
论文探讨了多机器人系统在**上下文（Context）不确定**环境下的多目标规划问题。在现实场景中，任务目标的优先级（如速度、节能、安全性）往往取决于底层环境上下文（如天气、地形、洋流），而这些上下文在任务开始前可能是未知的。
*   **核心挑战**：某些关键上下文信息无法通过单个机器人独立获取，必须由多个机器人协作形成特定配置（如环形、链式）进行**联合观测**。
*   **研究动机**：如果机器人基于错误的上下文执行任务，会导致行为失调或产生安全风险。因此，需要一种机制先协调机器人推断上下文，再根据推断出的偏好顺序执行任务。

#### 2. 方法论
论文提出了 **MR-CUSSP**（多机器人上下文不确定随机最短路径）框架，并采用**两阶段**解决方案：

*   **第一阶段：CIMOP（协作推理算法）**
    *   **核心思想**：通过最小化信念熵（Belief Entropy）来引导机器人。
    *   **流程**：计算访问各个“路标状态（Landmarks）”所需的最小机器人数；根据当前共享信念，优先选择能最大程度降低熵的路标；指派机器人组前往路标进行联合观测，直到信念坍缩（确定真实上下文）。
*   **第二阶段：LCBS（词典序冲突搜索算法）**
    *   **核心思想**：在已知上下文诱导的词典序偏好（Lexicographic Preferences）下，进行无冲突路径规划。
    *   **技术细节**：
        *   **底层搜索（LA*）**：扩展 A* 算法，使用词典序比较算子处理多维代价向量，直接剪枝非优路径。
        *   **高层搜索**：基于冲突搜索（CBS）框架，检测机器人间的时空冲突并添加约束，确保最终生成的联合规划既符合偏好又无碰撞。

#### 3. 实验设计
论文在仿真和实物两个层面进行了评估：
*   **实验场景**：
    1.  **Salp（海鞘机器人）领域**：水下采样，需平衡能源、珊瑚保护和时间。
    2.  **重载仓库**：货架运输，需平衡时间、拥堵和避开人类区域。
    3.  **森林灭火**：无人机监测，需平衡时间、能源和路径长度。
*   **Benchmark 与对比方法**：
    *   **推理阶段对比**：ARVI（主动信息获取）、SAIA（采样推理）。
    *   **规划阶段对比**：MO-CBS（多目标冲突搜索）、BB-MO-CBS-k（二进制分支多目标搜索）、标量化 CBS（将多目标加权合并）。
*   **硬件验证**：在 Robotarium 平台上使用 5 台 GTernal 移动机器人进行实地测试。

#### 4. 资源与算力
*   **硬件环境**：论文提到算法在配备 **18 GB RAM 的 macOS 机器**上运行。
*   **软件实现**：使用 **C++** 编写。
*   **算力说明**：文中未提及使用了 GPU 加速，主要依赖 CPU 进行启发式搜索规划。对于 35 个机器人的大规模场景，CIMOP 能在 2 分钟内完成规划，显示出较好的计算效率。

#### 5. 实验数量与充分性
*   **实验规模**：每个领域均进行了 5 组随机实例测试，机器人数量从 5 台扩展到 35 台。
*   **充分性**：
    *   涵盖了信念熵下降速度、累积熵、规划耗时、时间受限下的成功率等多个维度。
    *   对比了当前最先进的（SOTA）多目标规划和信息采集基准。
    *   **客观性**：通过实物机器人实验验证了仿真结果的一致性，实验设计较为全面，考虑了冗余路标对推理效率的影响。

#### 6. 主要结论与发现
*   **推理效率**：CIMOP 在所有领域中降低信念熵的速度均显著快于基准方法，尤其在机器人数量较多时优势更明显。
*   **规划性能**：LCBS 在处理具有严格优先级（词典序）的任务时，比传统的帕累托（Pareto）前沿搜索方法（如 MO-CBS）更高效，因为它能利用偏好信息进行有效剪枝。
*   **实时性**：两阶段法成功分离了推理与执行的复杂性，使得系统能够在紧迫的时间预算内（如 5 秒内）找到高质量解。

#### 7. 优点
*   **模型创新**：首次形式化了需要“联合观测”才能推断上下文的多机器人规划问题。
*   **计算高效**：LCBS 避免了构建完整的帕累托前沿，极大地提升了多目标冲突搜索的速度。
*   **实用性强**：通过 Robotarium 硬件实验证明了算法在真实物理约束（如非完整约束、通信同步）下的可行性。

#### 8. 不足与局限
*   **路标依赖**：假设环境中存在预定义的、已知位置的路标状态，且观测模型是确定的（信念坍缩模型）。
*   **静态上下文**：假设上下文在任务执行期间是固定不变的，无法处理动态变化的上下文（如突发的极端天气）。
*   **通信假设**：依赖于机器人间的共享信念，未深入探讨在通信受限或延迟环境下的去中心化推理表现。

（完）
