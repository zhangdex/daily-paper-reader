---
title: The FABRIC Strategy for Verifying Neural Feedback Systems
title_zh: 验证神经反馈系统的 FABRIC 策略
authors: "I. Samuel Akinwande, Sydney M. Katz, Mykel J. Kochenderfer, Clark Barrett"
date: 2026-03-09
pdf: "https://arxiv.org/pdf/2603.08964v1"
tags: ["query:ra-rl-pe"]
score: 9.0
evidence: 使用后向可达性分析验证到达-规避规范
tldr: 本研究针对由神经网络控制的动力系统（神经反馈系统），解决了其在验证到达-规避规范时后向可达性分析扩展性不足的问题。作者提出了计算非线性神经反馈系统后向可达集超近似和欠近似的新算法，并将其与现有的前向分析技术相结合，构建了名为FaBRIC的集成验证策略。该方法在多个基准测试中表现优异，显著提升了验证效率和性能，为神经反馈系统的安全认证提供了新思路。
selection_source: fresh_fetch
motivation: 由于计算扩展性受限，神经反馈系统的后向可达性分析相比前向分析受到的关注较少，限制了复杂规范的验证。
method: 提出了FaBRIC策略，通过集成新开发的非线性系统后向可达集超/欠近似算法与现有的前向分析技术来增强验证能力。
result: 实验评估显示，FaBRIC在代表性基准测试中显著优于现有的最先进验证技术。
conclusion: 将前向与后向可达性分析相结合是提高神经反馈系统安全性验证效率和准确性的有效手段。
---

## 摘要
前向可达性分析是验证神经反馈系统（即由神经网络控制的动力系统）中“到达-规避”规范的主流方法，目前已有多个研究方向被提出并得到探讨。相比之下，针对这些系统的后向可达性分析受到的关注要少得多，部分原因是已知技术的可扩展性有限。在这项工作中，我们通过引入计算非线性神经反馈系统后向可达集的超近似（over-approximation）和欠近似（under-approximation）的新算法，开始填补这一空白。我们还描述并实现了一种将这些后向可达性技术与现有前向分析技术相结合的方法。我们将生成的算法称为“用于认证的前向与后向可达性集成”（FaBRIC）。我们在代表性的基准测试集上评估了我们的算法，并证明其性能显著优于现有的最先进技术。

## Abstract
Forward reachability analysis is a dominant approach for verifying reach-avoid specifications in neural feedback systems, i.e., dynamical systems controlled by neural networks, and a number of directions have been proposed and studied. In contrast, far less attention has been given to backward reachability analysis for these systems, in part because of the limited scalability of known techniques. In this work, we begin to address this gap by introducing new algorithms for computing both over- and underapproximations of backward reachable sets for nonlinear neural feedback systems. We also describe and implement an integration of these backward reachability techniques with existing ones for forward analysis. We call the resulting algorithm Forward and Backward Reachability Integration for Certification (FaBRIC). We evaluate our algorithms on a representative set of benchmarks and show that they significantly outperform the prior state of the art.

---

## 论文详细总结（自动生成）

这篇论文介绍了一种名为 **FaBRIC** 的新策略，旨在解决神经反馈系统（Neural Feedback Systems, NFS）在非线性动力学环境下的形式化验证难题。以下是对该论文的深度结构化总结：

### 1. 核心问题与整体含义
*   **研究背景**：神经反馈系统是指由神经网络（NN）作为控制器的动力系统。为了确保这类系统在安全关键任务（如自动驾驶、无人机控制）中的可靠性，需要验证其是否满足“到达-规避”（Reach-Avoid）规范。
*   **核心问题**：现有的验证方法大多集中在**前向可达性分析**，但前向分析往往具有保守性且计算开销大。**后向可达性分析**虽然理论上互补，但在处理非线性动力学和神经网络预映射（Pre-image）时面临严重的扩展性挑战。
*   **整体含义**：论文填补了非线性神经反馈系统后向分析技术的空白，通过结合前向与后向分析，显著提升了验证的效率和精度。

### 2. 方法论：核心思想与关键技术
论文提出了 **FaBRIC**（Forward and Backward Reachability Integration for Certification）框架，包含以下核心组件：

*   **后向超近似（Outer Sets）计算 - DRiPy 算法**：
    *   扩展了现有的域细化（Domain Refinement）技术，使其适用于非线性系统。
    *   利用**多面体包围（Polyhedral Enclosures）**技术将非线性动力学转化为混合整数线性规划（MILP）约束。
    *   通过迭代细化状态域，逐步收紧后向可达集的边界。
*   **后向欠近似（Inner Sets）计算 - FITS 框架**：
    *   提出了三种算法来寻找目标集合的子集：
        1.  **SHARP**：基于中心缩放的超矩形近似。
        2.  **CRISP**：利用采样点和凸包寻找最大轴对齐超矩形。
        3.  **CLEAN**：针对不规则集合，通过排除负样本点来构建欠近似集。
*   **FaBRIC 集成策略**：
    *   将总时间步 $T$ 划分为前向步数 $F$ 和后向步数 $B$。
    *   **验证逻辑**：如果前向 $F$ 步的可达集被包含在后向 $B$ 步的“必须到达”（Must-reach）欠近似集中，则证明系统必能到达目标；如果前向集与后向“可能到达”（May-reach）超近似集不相交，则证明系统安全。

### 3. 实验设计
*   **基准测试（Benchmarks）**：
    *   **TORA**：受控推车系统（4维状态）。
    *   **Unicycle**：单轮车汽车模型（4维状态，非线性强）。
    *   **Attitude**：飞行器姿态控制模型（6维状态，高度非线性）。
*   **对比方法**：
    *   **超近似对比**：HyBreach-MILP（最先进的非线性后向分析工具）。
    *   **欠近似对比**：BURNS（现有的非线性欠近似工具）。
    *   **整体策略对比**：基于 OVERT 的纯前向可达性分析。
*   **控制器变量**：针对每个模型训练了不同规模（Small, Medium, Large）的神经网络控制器。

### 4. 资源与算力
*   **硬件环境**：所有实验均在 **AMD Ryzen 9 7950X** 处理器上运行。
*   **软件工具**：使用 **Gurobi** 作为 MILP 求解器。
*   **算力细节**：论文设定了严格的超时限制（如单次求解 600-900 秒），并未提及使用 GPU，这符合符号化验证/MILP 求解通常依赖 CPU 单核性能的特征。

### 5. 实验数量与充分性
*   **实验规模**：涵盖了 3 个经典动力学模型，每个模型对应 3 种不同规模的 NN 控制器，共计 9 个核心场景。
*   **充分性**：针对超近似和欠近似分别进行了消融实验和对比实验。欠近似实验中进行了 10 次独立尝试以确保结果的稳健性。
*   **客观性**：作者重新实现了对比基线（如 BURNS），并在相同的硬件和误差容限下进行测试，实验设计较为公平。

### 6. 主要结论与发现
*   **性能提升**：在 Unicycle 和 Attitude 等复杂基准测试中，DRiPy 计算出的超近似集体积比 HyBreach-MILP 小 **40 倍到 2900 倍**，且速度更快。
*   **欠近似突破**：在 BURNS 无法找到有效解的高维场景下，FITS 框架（尤其是 CRISP 变体）能够成功计算出有效的欠近似集。
*   **集成优势**：FaBRIC 策略在复杂任务中比纯前向分析实现了高达 **7 倍** 的加速，证明了前后向结合能有效缓解“维度灾难”。

### 7. 优点
*   **理论完备性**：同时解决了非线性系统的后向超近似和欠近似问题，这在现有文献中非常罕见。
*   **灵活性**：FaBRIC 允许用户根据计算资源灵活分配前后向分析的比例。
*   **实用性**：通过多面体包围技术，将复杂的非线性验证问题转化为成熟的 MILP 求解问题，易于工程实现。

### 8. 不足与局限
*   **维度限制**：尽管有所改进，但面对更高维度（如 >10 维）的系统，MILP 的求解规模仍可能爆炸。
*   **参数敏感性**：$F$ 和 $B$ 的划分比例目前依赖人工经验，缺乏自动化的最优分配机制。
*   **形状约束**：欠近似计算目前主要依赖轴对齐超矩形（Hyperrectangles），对于极度扭曲或非凸的可达集，可能会损失较多精度。

（完）
