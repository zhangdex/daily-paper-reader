---
title: Graph-of-Constraints Model Predictive Control for Reactive Multi-agent Task and Motion Planning
title_zh: 针对反应式多智能体任务与运动规划的约束图模型预测控制
authors: "Anastasios Manganaris, Jeremy Lu, Ahmed H. Qureshi, Suresh Jagannathan"
date: 2026-03-19
pdf: "https://arxiv.org/pdf/2603.18400v1"
tags: ["query:robot-planning"]
score: 10.0
evidence: 基于 MPC 的反应式多智能体任务与运动规划
tldr: 针对多智能体任务与运动规划（TAMP）中约束序列处理困难、无法应对动态任务分配和环境扰动的问题，本文提出了GoC-MPC框架。该方法将约束图与模型预测控制相结合，通过跟踪3D关键点定义约束，支持部分有序任务和动态智能体协作。无需训练数据或环境模型，仅凭视觉观测即可实现高效、鲁棒的多智能体操纵，显著提升了计算速度和任务成功率。
selection_source: fresh_fetch
motivation: 现有的多智能体TAMP方法在处理部分有序任务和动态智能体分配时缺乏灵活性，且难以应对实时扰动。
method: 提出了一种集成约束图的模型预测控制框架（GoC-MPC），利用3D关键点定义几何约束，实现动态任务协调。
result: 实验表明，该方法在多智能体操纵任务中比现有基准具有更高的成功率、更快的计算速度和更短的路径。
conclusion: GoC-MPC为现实世界扰动下的多智能体协作操纵提供了一种高效、鲁棒且无需预训练的解决方案。
---

## 摘要
相互依赖的几何约束序列是许多多智能体任务与运动规划（TAMP）问题的核心。然而，现有的处理此类约束序列的方法在处理偏序任务和动态智能体分配方面存在困难。它们通常假设静态分配，且当扰动改变任务分配时无法进行调整。为了克服这些局限性，我们引入了约束图模型预测控制（GoC-MPC），这是一种与 MPC 集成的通用约束序列框架。GoC-MPC 自然地支持偏序任务、动态智能体协作以及扰动恢复。通过在跟踪的 3D 关键点上定义约束，我们的方法能够稳健地解决多种多智能体操纵任务——仅凭视觉观测即可协调智能体并进行在线调整，而不依赖于训练数据或环境模型。实验表明，与最近的基准方法相比，GoC-MPC 实现了更高的成功率、显著更快的 TAMP 计算速度以及更短的总路径，证明了它是现实世界扰动下多智能体操纵的一种高效且稳健的解决方案。我们的补充视频和代码可在 https://sites.google.com/view/goc-mpc/home 找到。

## Abstract
Sequences of interdependent geometric constraints are central to many multi-agent Task and Motion Planning (TAMP) problems. However, existing methods for handling such constraint sequences struggle with partially ordered tasks and dynamic agent assignments. They typically assume static assignments and cannot adapt when disturbances alter task allocations. To overcome these limitations, we introduce Graph-of-Constraints Model Predictive Control (GoC-MPC), a generalized sequence-of-constraints framework integrated with MPC. GoC-MPC naturally supports partially ordered tasks, dynamic agent coordination, and disturbance recovery. By defining constraints over tracked 3D keypoints, our method robustly solves diverse multi-agent manipulation tasks-coordinating agents and adapting online from visual observations alone, without relying on training data or environment models. Experiments demonstrate that GoC-MPC achieves higher success rates, significantly faster TAMP computation, and shorter overall paths compared to recent baselines, establishing it as an efficient and robust solution for multi-agent manipulation under real-world disturbances. Our supplementary video and code can be found at https://sites.google.com/view/goc-mpc/home .

---

## 论文详细总结（自动生成）

以下是对论文《Graph-of-Constraints Model Predictive Control for Reactive Multi-agent Task and Motion Planning》的结构化总结：

### 1. 核心问题与研究背景
*   **研究动机**：在多机器人协作的任务与运动规划（TAMP）中，现有的基于约束序列的方法（如 ReKep）存在两个主要局限：
    1.  **全序限制**：强制要求任务步骤按线性顺序执行，无法处理可并行的任务，导致机器人不必要的等待。
    2.  **静态分配**：预先设定机器人与任务的对应关系，在发生外部扰动或环境变化时，无法动态重新分配任务。
*   **核心问题**：如何构建一个既能支持任务并行执行，又能根据实时反馈动态调整机器人分工的反应式规划框架。

### 2. 方法论
论文提出了 **GoC-MPC（约束图模型预测控制）**，其核心思想是将任务表示为有向无环图（DAG），并将其集成到 MPC 循环中。
*   **约束图 (Graph-of-Constraints, GoC)**：
    *   **节点与边**：节点代表路标点约束（Waypoints），边代表路径约束（Path constraints）。DAG 结构天然支持任务的偏序关系（并行或同步）。
    *   **动态分配矩阵 ($A$)**：引入二进制决策变量矩阵，通过优化该矩阵实现机器人与子任务的实时匹配。
*   **问题分解与求解流程**：为了达到实时性，将复杂的混合整数非线性规划问题分解为三个子问题：
    1.  **路标点与分配子问题**：求解最优任务分配 $A$ 和关键路标点位置 $W$。
    2.  **智能体样条曲线子问题**：基于分配结果，为每个机器人生成穿过路标点的三次样条曲线，并优化时间分配（$\Delta t$）以确保多机同步或先后顺序。
    3.  **短时域收敛子问题**：在 MPC 循环中，通过二次规划（QP）跟踪参考轨迹，同时处理避障和动力学约束。
*   **感知基础**：基于 3D 关键点（Keypoints）定义几何约束，无需预先训练模型或精确的环境动力学模型。

### 3. 实验设计
*   **实验场景**：
    1.  **积木堆叠 (Block-Stacking)**：涉及并行抓取和顺序放置。
    2.  **倒水任务 (Pick-and-Pour)**：涉及两个机器人之间的精确空间协调。
    3.  **布料折叠 (Cloth-Folding)**：涉及双臂的高度同步操作。
*   **基准方法 (Benchmark)**：主要对比了 **ReKep**（目前最先进的基于关键点的反应式规划方法）。
*   **测试环境**：仿真环境使用 Isaac-Sim、OmniGibson 和 Drake；物理实验使用两台 UR5e 机械臂。

### 4. 资源与算力
*   **硬件配置**：实验在一台配备 **第12代 Intel i7-12700F CPU** 和 **32 GB RAM** 的本地工作站上运行。
*   **算力特点**：该方法属于基于优化的方法，**不需要 GPU 进行大规模模型训练**。视觉感知部分使用了 SAM2 和 KLT 跟踪器，但核心规划算法主要依赖 CPU 上的数值优化器（如 Ipopt 和 MOSEK）。
*   **运行效率**：初始解计算约 0.37 秒，后续 MPC 循环平均仅需 **0.065 秒**。

### 5. 实验数量与充分性
*   **实验规模**：
    *   静态场景对比：每个任务进行 10 次试验。
    *   扰动恢复实验：在积木堆叠中引入外部干扰，测试回溯能力。
    *   **可扩展性分析**：测试了不同物体数量（5/8/11个）和机器人数量（2/3/4个）的组合，共进行多组随机初始化试验。
    *   物理实验：三个任务各进行 5 次试验。
*   **充分性评价**：实验设计较为全面，涵盖了从仿真到实体的迁移，并专门针对多智能体特有的“可扩展性”和“扰动恢复”进行了定量分析，实验结果具有较强的说服力。

### 6. 主要结论与发现
*   **性能飞跃**：GoC-MPC 的计算速度比 ReKep 快约 **70 倍**，且路径长度更短。
*   **高成功率**：在静态和扰动场景下均保持了极高的成功率（多为 100%），而基准方法在复杂协调任务中易失败。
*   **动态适应性**：证明了通过约束图的回溯机制（Backtracking），系统可以在任务失败（如物体滑落）时自动返回之前的阶段，并让其他机器人协同等待或调整。

### 7. 优点
*   **灵活性**：打破了线性任务序列的限制，支持复杂的并行和同步逻辑。
*   **高效性**：通过三级问题分解，将复杂的 TAMP 问题提升到了实时控制的频率。
*   **零样本/无模型**：不需要针对特定任务的训练数据，仅需定义几何约束即可泛化到不同操纵任务。
*   **鲁棒性**：天然支持动态任务重分配和故障恢复。

### 8. 不足与局限
*   **感知依赖**：高度依赖外部视觉跟踪器的准确性。在物理实验中，严重的遮挡会导致任务失败。
*   **人工干预**：虽然不需要训练，但任务的“约束图骨架”（Task Skeleton）仍需要人工预设或由高级逻辑规划器提供。
*   **局部最优风险**：由于使用了非线性优化器的局部求解，在极度复杂的约束环境下可能会陷入局部最优，导致路标点更新停滞。

（完）
