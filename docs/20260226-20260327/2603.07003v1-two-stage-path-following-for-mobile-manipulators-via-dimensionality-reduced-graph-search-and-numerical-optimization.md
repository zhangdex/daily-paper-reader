---
title: Two-Stage Path Following for Mobile Manipulators via Dimensionality-Reduced Graph Search and Numerical Optimization
title_zh: 基于降维图搜索与数值优化的移动机械臂两阶段路径跟踪
authors: "Fuyu Guo, Yuting Mei, Yuyao Zhang, Qian Tang"
date: 2026-03-07
pdf: "https://arxiv.org/pdf/2603.07003v1"
tags: ["query:robot-planning"]
score: 9.0
evidence: 移动机械臂的两阶段路径跟踪与构型规划
tldr: 针对移动机械臂在高维配置空间和运动学约束下的路径跟踪难题，本文提出了一种两阶段规划框架。该方法将8自由度问题简化为2自由度底座优化，首先通过逆可达图（IRM）和Dijkstra算法在离散图中搜索初始可行路径，随后利用凸包转换和L-BFGS算法进行连续优化，以提升轨迹平滑度并确保可达性。实验证明该方法具有亚毫米级精度和良好的鲁棒性。
selection_source: fresh_fetch
motivation: 解决移动机械臂在高维空间中进行路径跟踪时面临的计算复杂性和运动学约束限制。
method: 采用两阶段法，先通过降维图搜索获取初始路径，再利用数值优化算法对底座轨迹进行平滑处理。
result: 仿真结果显示运动学精度达到亚毫米级，并在全向移动机械臂的物理实验中验证了其有效性。
conclusion: 该框架通过解耦和分阶段优化，有效平衡了移动机械臂路径规划的计算效率、全局最优性与轨迹平滑度。
---

## 摘要
移动机械臂的高效路径跟踪常受限于高维构型空间和运动学约束。本文提出了一种鲁棒的两阶段构型规划框架，在底盘偏航角固定的规划假设下，将 8 自由度规划问题解耦为易于处理的 2 自由度底盘优化问题。在第一阶段，所提方法利用逆可达性图（IRM）将任务空间路径离散化为多层图，并通过基于 Dijkstra 的动态规划方法提取初始可行路径，以确保离散图内的计算效率和全局最优性。在第二阶段，为克服离散搜索的量化误差，将可行底盘区域转换为凸包，随后通过 L-BFGS 算法进行连续优化，在严格执行可达性约束的同时最大化轨迹平滑度。仿真结果表明，该方法在仿真中达到了亚毫米级的运动学精度，证明了其理论精确性；在全向移动机械臂上的物理实验进一步验证了该框架的鲁棒性和实际应用价值。

## Abstract
Efficient path following for mobile manipulators is often hindered by high-dimensional configuration spaces and kinematic constraints. This paper presents a robust two-stage configuration planning framework that decouples the 8-DoF planning problem into a tractable 2-DoF base optimization under a yaw-fixed base planning assumption. In the first stage, the proposed approach utilizes IRM to discretize the task-space path into a multi-layer graph, where an initial feasible path is extracted via a Dijkstra-based dynamic programming approach to ensure computational efficiency and global optimality within the discretized graph. In the second stage, to overcome discrete search quantization, feasible base regions are transformed into convex hulls, enabling subsequent continuous refinement via the L-BFGS algorithm to maximize trajectory smoothness while strictly enforcing reachability constraints. Simulation results demonstrate the theoretical precision of the proposed method by achieving sub-millimeter kinematic accuracy in simulation, and physical experiments on an omnidirectional mobile manipulator further validate the framework's robustness and practical applicability.

---

## 论文详细总结（自动生成）

### 基于降维图搜索与数值优化的移动机械臂两阶段路径跟踪总结

#### 1. 核心问题与研究背景
*   **核心问题**：移动机械臂（Mobile Manipulator, MM）结合了移动底盘的灵活性和机械臂的多自由度，但其路径跟踪面临高维配置空间（C-space）耦合、运动学约束严格以及计算效率低下等挑战。
*   **研究背景**：传统的逆运动学（IK）方法在处理高维耦合运动时难以平衡计算效率与全局最优性。现有的解耦规划或整体反应式控制往往在离散搜索的量化误差或局部最优陷阱中挣扎。本文旨在提出一种既能保证全局可达性，又能实现轨迹平滑的高效规划框架。

#### 2. 方法论
该论文提出了一个**两阶段规划框架**，核心思想是将 8 自由度的复杂规划解耦为 2 自由度的底座位置优化：
*   **第一阶段：基于逆可达图（IRM）的离散图搜索**
    *   **预计算**：离线生成机械臂的可达性图（RM）并反转为 IRM，建立末端位姿与底座可行位置的映射。
    *   **降维假设**：固定底座的偏航角（Yaw），将问题简化为平面 $x, y$ 规划。
    *   **图构建与搜索**：将任务路径离散化，利用 IRM 提取每一点的可行底座配置集，构建多层图。采用 **Dijkstra 结合动态规划（DP）** 算法提取一条在离散空间内长度和能量最优的初始路径。
*   **第二阶段：基于 L-BFGS 的连续数值优化**
    *   **区域转换**：将离散的可行点集通过滤波、聚类和凸包生成算法，转换为连续的**凸多边形可行区域**。
    *   **优化目标**：构建包含路径长度、平滑度（二阶差分）和可达性惩罚的代价函数。
    *   **关键技术**：使用 **L-BFGS 算法**进行迭代。引入**指数惩罚函数**处理可达性约束，相比二次惩罚，它在可行域内几乎不产生偏差，而在边界处提供极强的梯度，确保机械臂始终处于高可操纵性区域。

#### 3. 实验设计
*   **仿真实验**：
    *   **场景**：设计了 5 条具有不同几何复杂度的末端轨迹（T1-T5）。
    *   **对比方法（Benchmark）**：
        1.  **整体反应式控制（HRC）**：基于 Python Robotics Toolbox 实现。
        2.  **CHOMP 优化算法**：一种经典的基于梯度的运动规划器。
    *   **评估指标**：底座路径长度 ($L_b$)、路径平滑度 ($S_b$)、末端运动学精度 ($E_{ee}$)。
*   **物理实验**：
    *   **硬件**：Unitree Z1 机械臂 + Agilex Scout Mini 麦克纳姆轮底盘。
    *   **环境**：使用 NOKOV Mars 光学 3D 运动捕捉系统进行高精度标定和轨迹记录。
    *   **路径**：双纽线（Lemniscate）、胶囊形曲线和多边形路径。

#### 4. 资源与算力
*   论文中**未明确说明**具体的 GPU 型号或训练时长。
*   由于该方法属于传统的运动规划与数值优化范畴（非深度学习训练），其核心计算压力在于离线 IRM 的构建和在线的 L-BFGS 迭代。实验结果显示其在实时性与精度之间取得了较好的平衡。

#### 5. 实验数量与充分性
*   **实验数量**：进行了 5 组仿真对比实验和 3 组不同形状的物理轨迹跟踪实验。
*   **充分性与客观性**：
    *   实验涵盖了从简单到复杂的多种轨迹，对比了反应式控制和全局优化两种主流思路，评估指标全面。
    *   物理实验中详细描述了底座中心和机械臂安装位置的标定过程，增强了实验的可重复性和客观性。
    *   虽然物理实验样本量不算巨大，但结合仿真数据的亚毫米级精度验证，足以证明算法的有效性。

#### 6. 主要结论与发现
*   **高精度**：仿真中末端跟踪误差保持在亚毫米级（约 $10^{-3}$ mm），远优于 HRC（毫米级）和 CHOMP（易产生漂移）。
*   **平滑性**：通过 L-BFGS 优化，路径平滑度比基准方法提升了 1-2 个数量级，消除了离散搜索带来的“锯齿”感。
*   **物理可行性**：在实际机器人上，尽管受麦克纳姆轮打滑影响，平均跟踪误差仍控制在 21-23 mm 范围内，验证了框架的实用性。

#### 7. 优点
*   **解耦高效**：通过固定底座偏航角和利用 IRM，将高维搜索降维，显著降低了计算复杂度。
*   **两阶段结合**：结合了 Dijkstra 的全局最优性（离散层）和 L-BFGS 的局部平滑性（连续层）。
*   **约束处理**：提出的指数惩罚函数能更严格地强制执行运动学可达性约束，避免了 CHOMP 等方法常见的“切角”导致失真问题。

#### 8. 不足与局限
*   **离线限制**：目前框架主要针对已知轨迹的离线规划，尚未实现针对动态障碍物的实时在线重规划。
*   **简化假设**：固定底座偏航角的假设虽然简化了计算，但在某些极端狭窄空间或需要底座大幅度转向的场景下，可能会限制系统的灵活性。
*   **硬件偏差**：物理实验中麦克纳姆轮的打滑对精度影响较大，论文提到未来需在四轮独立转向（4WIS）平台上进一步验证。

（完）
