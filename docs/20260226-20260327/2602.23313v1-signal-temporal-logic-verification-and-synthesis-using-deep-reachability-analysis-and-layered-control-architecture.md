---
title: Signal Temporal Logic Verification and Synthesis Using Deep Reachability Analysis and Layered Control Architecture
title_zh: 基于深度可达性分析与分层控制架构的信号时序逻辑验证与综合
authors: "Joonwon Choi, Kartik Anand Pant, Youngim Nam, Henry Hellmann, Karthik Nune, Inseok Hwang"
date: 2026-02-26
pdf: "https://arxiv.org/pdf/2602.23313v1"
tags: ["query:ra-rl-pe"]
score: 10.0
evidence: 用于多重到达-规避问题的深度可达性分析
tldr: 本研究提出一个基于信号时序逻辑（STL）的框架，用于验证任务可行性并合成安全控制策略。该框架首先利用深度神经网络加速计算后向可达管（BRT），以评估STL任务在多重到达-规避（MRA）场景下的可行性，计算速度较传统方法提升约1000倍。随后，通过结合混合整数线性规划（MILP）全局规划与模型预测控制（MPC）局部控制的分层架构，显著增强了系统应对未预见障碍物的鲁棒性，确保了复杂任务的安全执行。
selection_source: fresh_fetch
motivation: 旨在解决信号时序逻辑任务验证中计算效率低下以及系统难以应对环境内未预见动态障碍物的挑战。
method: 采用深度学习加速的后向可达性分析进行任务验证，并结合MILP与MPC构建分层规划与控制架构。
result: 实验表明该方法在保持任务可靠性的同时，将可达性分析计算时间缩短了三个数量级，并能成功规避未知障碍。
conclusion: 该框架为复杂STL任务提供了高效的可行性评估与鲁棒的实时控制方案，确保了任务执行的安全与可靠。
---

## 摘要
我们提出了一种基于信号时序逻辑（STL）的框架，该框架能够严格验证以 STL 描述的任务的可行性，并综合控制策略以安全地执行该任务。所提出的框架通过两个阶段确保安全可靠的运行。首先，该框架通过计算后向可达管（BRT）来评估 STL 的可行性，BRT 捕捉了无论初始状态如何都能满足给定 STL 的所有状态。该框架考虑了多重到达-规避（MRA）问题以处理更通用的 STL 规范，并利用深度神经网络减轻可达性分析的计算负担，与基准方法相比，计算时间缩短了约 1000 倍。我们进一步提出了一种分层规划与控制架构，该架构将用于全局规划的混合整数线性规划（MILP）与作为已验证 STL 局部控制器的模型预测控制（MPC）相结合。因此，所提出的框架能够鲁棒地处理环境信息或 STL 中未描述的障碍物的意外行为，从而提供可靠的任务性能。数值模拟表明，所提出的框架能够成功计算给定 STL 的 BRT 并执行任务。

## Abstract
We propose a signal temporal logic (STL)-based framework that rigorously verifies the feasibility of a mission described in STL and synthesizes control to safely execute it. The proposed framework ensures safe and reliable operation through two phases. First, the proposed framework assesses the feasibility of STL by computing a backward reachable tube (BRT), which captures all states that can satisfy the given STL, regardless of the initial state. The proposed framework accommodates the multiple reach-avoid (MRA) problem to address more general STL specifications and leverages a deep neural network to alleviate the computation burden for reachability analysis, reducing the computation time by about 1000 times compared to a baseline method. We further propose a layered planning and control architecture that combines mixed-integer linear programming (MILP) for global planning with model predictive control (MPC) as a local controller for the verified STL. Consequently, the proposed framework can robustly handle unexpected behavior of obstacles that are not described in the environment information or STL, thereby providing reliable mission performance. Our numerical simulations demonstrate that the proposed framework can successfully compute BRT for a given STL and perform the mission.

---

## 论文详细总结（自动生成）

### 论文结构化总结：基于深度可达性分析与分层控制架构的信号时序逻辑验证与综合

#### 1. 核心问题与整体含义（研究动机和背景）
本文旨在解决自主系统在执行由**信号时序逻辑（STL）**描述的复杂任务时的两个关键挑战：
*   **可行性验证的效率与全面性**：传统的 STL 验证通常基于特定初始状态的点分析，而基于 Hamilton-Jacobi (HJ) 可达性分析的集合分析虽严谨，但受限于“维度灾难”，计算极其耗时。
*   **动态环境下的鲁棒执行**：预先规划的 STL 路径在面对未预见的障碍物或环境变动时容易失效，系统需要一种既能满足长期逻辑约束又能实现短期安全避障的机制。

#### 2. 方法论：核心思想与关键技术
论文提出了一个双层框架，结合了深度学习加速的验证与分层控制：
*   **DeepSTLReach（验证层）**：
    *   **核心思想**：将 STL 公式转化为后向可达管（BRT）的计算问题。
    *   **MRA 处理**：引入多重到达-规避（Multiple Reach-Avoid）公式，解决 STL 中多个无序目标点（如“最终访问 A 且最终访问 B”）导致的逻辑冲突，通过迭代计算 BRT 覆盖所有可能的访问顺序。
    *   **深度近似**：使用多层感知机（MLP）拟合 Bellman 价值函数。损失函数 $L(\theta)$ 由均方误差（MSE）和**保守性惩罚项**组成，后者专门惩罚在不安全区域预测为安全的行为，确保边界预测的安全性。
*   **分层控制架构（执行层）**：
    *   **上层（MILP 全局规划）**：将 STL 规范编码为混合整数线性规划约束，在 DeepSTLReach 确定的可行域内生成参考轨迹。
    *   **下层（MPC 局部控制）**：采用非线性模型预测控制跟踪参考轨迹，并实时处理 MILP 未考虑的突发障碍物，确保运行时安全。

#### 3. 实验设计
*   **实验场景**：
    1.  **场景 1（意外移动障碍物）**：验证系统在全局规划后，面对路径上突然出现的移动障碍物时的避障能力。
    2.  **场景 2（交叉路口）**：包含复杂的区域进入限制（如“除非先到达 G1，否则不能进入 T1”）。
    3.  **场景 3（门与钥匙）**：测试具有严格先后逻辑依赖的任务。
*   **Benchmark（基准方法）**：使用传统的 `hj-reachability` 工具包（基于网格的数值解法）作为计算精度和时间的对比基准。
*   **对比指标**：计算耗时（秒）、初始状态可行性识别的准确率、动态避障成功率。

#### 4. 资源与算力
*   **硬件配置**：AMD Ryzen 7 8845HS 处理器，NVIDIA RTX 4050 GPU。
*   **模型细节**：4 层隐藏层，每层 128 个神经元，使用 ReLU 激活函数。
*   **训练与求解**：使用 Adam 优化器进行监督学习训练；MILP 部分使用 Gurobi 求解器。文中未明确给出具体的训练时长，但强调了推理阶段的实时性。

#### 5. 实验数量与充分性
*   **实验规模**：针对 3 个不同复杂度的场景进行了仿真。在场景 1 中进行了 **100 组蒙特卡洛模拟**，随机采样初始状态以验证 BRT 边界的准确性。
*   **充分性评价**：实验设计较为全面，涵盖了从简单的时空约束到复杂的逻辑依赖。通过与基准数值方法的直接对比（见附录图 5），证明了神经网络近似的准确性。实验对比了计算时间，客观展示了深度学习带来的性能提升。

#### 6. 主要结论与发现
*   **计算效率飞跃**：DeepSTLReach 将可达性分析的计算时间从数十秒缩短至毫秒级（**加速约 1000 倍**），使在线监控成为可能。
*   **验证准确性**：深度学习生成的 BRT 能够准确区分可行与不可行的初始状态，蒙特卡洛测试显示其边界与 MILP 求解结果高度一致。
*   **鲁棒性增强**：分层架构成功解决了全局规划的滞后性问题，MPC 能够有效补足 MILP 在应对未预见动态障碍时的短板。

#### 7. 优点与亮点
*   **集合化验证**：不同于点轨迹验证，该方法能给出整个“可行初始区域”，为任务部署提供更宏观的指导。
*   **保守性损失函数**：在神经网络训练中加入惩罚项，有效降低了安全关键应用中的误报风险。
*   **逻辑与控制结合**：成功将高层逻辑推理（STL/MILP）与底层物理安全（MPC/可达性）无缝集成。

#### 8. 不足与局限
*   **离线依赖**：深度可达性分析仍依赖于离线生成的大量训练数据，对于动力学模型发生重大变化的场景，需要重新训练。
*   **环境规模限制**：虽然计算速度快，但在极大规模环境或极高维状态空间下的泛化能力尚未在文中深入探讨。
*   **重规划能力**：目前下层 MPC 主要负责避障，若环境变化导致全局 STL 逻辑彻底无法满足，系统尚缺乏高效的全局重规划机制（作者将其列为未来工作）。

（完）
