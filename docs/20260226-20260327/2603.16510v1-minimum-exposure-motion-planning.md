---
title: Minimum Exposure Motion Planning
title_zh: 最小暴露运动规划
authors: "Sarita de Berg, Joachim Gudmundsson, Peter Kramer, Christian Rieck, Sampson Wong"
date: 2026-03-17
pdf: "https://arxiv.org/pdf/2603.16510v1"
tags: ["query:robot-planning"]
score: 9.0
evidence: 具有新暴露目标的多个机器人协调运动规划
tldr: 本文研究了平面 L1 度量下单位正方形机器人的协调运动规划（CMP）问题。研究者引入了“最小暴露量”（Min-Exposure）这一新目标函数，旨在最小化机器人处于指定覆盖区域外的时间。针对该问题，论文提出了 k=2 时的 O(n^4 log n) 算法及任意 k 时的 XP 算法。此外，研究还证明了 Min-Makespan 和 Min-Sum 目标在以机器人数量为参数时是固定参数可解（FPT）的，将现有结论从网格图推广到了平面空间。
selection_source: fresh_fetch
motivation: 旨在解决机器人在移动过程中需要尽量减少在无遮蔽或危险区域暴露时间的新型路径规划需求。
method: 通过定义多边形覆盖区域并优化暴露时间，结合几何算法与参数化复杂性分析，设计了针对不同机器人数量的调度算法。
result: 实现了 k=2 机器人的多项式时间算法，并证明了多种 CMP 目标函数在机器人数量固定时具有 FPT 性质。
conclusion: 该研究不仅丰富了 CMP 的目标函数体系，还通过推广 FPT 结果提升了对复杂平面运动规划问题的理论认知。
---

## 摘要
我们研究了在 $L_1$ 度量下，平面内单位正方形机器人的经典协调运动规划（CMP）问题的多个基本变体。在协调运动规划中，给定 $k$ 个机器人的两种排列，任务是寻找一个最小化特定目标函数的移动调度。两个最著名的目标函数是总行驶距离（Min-Sum）和最晚到达时间（Min-Makespan）。这两个目标此前都已被广泛研究。我们为平面内的 CMP 引入了一个新的目标函数。所提出的最小暴露（Min-Exposure）目标函数在平面内定义了一组提供掩护的多边形区域，并要求寻找一个调度方案，使得至少有一个机器人部分或全部位于这些区域之外的累计时间最短。我们给出了一个时间复杂度为 $\mathcal{O}(n^4\log n)$ 的算法，用于计算 $k=2$ 个机器人的最小暴露调度，以及一个针对任意 $k$ 的 XP 算法。作为一个具有独立研究价值的结果，我们利用新的见解证明了以机器人数量为参数时，Min-Makespan 和 Min-Sum 目标都是固定参数可解（FPT）的。我们的参数化复杂度结果推广了矩形网格图已知的 FPT 结果 [Eiben, Ganian, and Kanj, SoCG'23]。

## Abstract
We investigate multiple fundamental variants of the classic coordinated motion planning (CMP) problem for unit square robots in the plane under the $L_1$ metric. In coordinated motion planning, we are given two arrangements of $k$ robots and are tasked with finding a movement schedule that minimizes a certain objective function. The two most prominent objective functions are the sum of distances traveled (Min-Sum) and the latest time of arrival (Min-Makespan). Both objectives have previously been studied extensively.   We introduce a new objective function for CMP in the plane. The proposed Min-Exposure objective function defines a set of polygonal regions in the plane that provide cover and asks for a schedule with minimal elapsed time during which at least one robot is partially or fully outside of these regions. We give an $\mathcal{O}(n^4\log n)$ time algorithm that computes exposure-minimal schedules for $k=2$ robots, and an XP algorithm for arbitrary $k$. As a result of independent interest, we leverage new insights to prove that both the Min-Makespan and Min-Sum objectives are fixed-parameter tractable (FPT) parameterized by the number of robots. Our parameterized complexity results generalize known FPT results for rectangular grid graphs [Eiben, Ganian, and Kanj, SoCG'23].

---

## 论文详细总结（自动生成）

这篇论文由 Sarita de Berg 等人撰写，发表于 2026 年（预印本），主要探讨了平面空间中多个机器人协调运动规划（Coordinated Motion Planning, CMP）的新变体。

### 1. 核心问题与整体含义（研究动机和背景）
*   **研究背景**：协调运动规划（CMP）是计算几何和机器人学的经典问题，即在避免碰撞的前提下，将 $k$ 个机器人从起始配置移动到目标配置。
*   **研究动机**：传统的优化目标通常是“总行驶距离最小”（Min-Sum）或“完成时间最小”（Min-Makespan）。但在现实场景（如搜索救援、危险品处理、无线网络覆盖）中，机器人往往需要尽可能留在“安全区”或“覆盖区”内。
*   **核心问题**：论文引入了**最小暴露量（Min-Exposure）**目标函数。给定一组多边形覆盖区域，目标是寻找一个调度方案，使得“至少有一个机器人处于覆盖区之外”的累计时间最短。

### 2. 方法论：核心思想与算法流程
论文采用了几何离散化和参数化复杂度分析的方法：
*   **状态转移图（Transition Graph）**：将连续的配置空间根据机器人之间的相对位置（左、右、上、下）划分为不同的“序（Orderings）”。证明了在 $L_1$ 度量下，最优路径可以通过在转移图中的简单路径来表示。
*   **$k=2$ 机器人的最小暴露算法**：
    1.  **空间分解**：对覆盖区域进行水平梯形分解，构建由“梯形对+相对顺序约束”组成的状态图 $G$。
    2.  **边权计算**：图中的边分为“零暴露边”（利用 $O(n^2)$ 的可达性数据结构判断）和“正暴露边”（利用线性规划 LP 求解两状态间的最小完成时间）。
    3.  **最短路求解**：在状态图上运行 Dijkstra 算法，复杂度为 $O(n^4 \log n)$。
*   **参数化复杂度（FPT）**：证明了对于固定数量的机器人 $k$，Min-Sum 和 Min-Makespan 问题在平面上是**固定参数可解（FPT）**的。这通过将连续路径优化转化为有限个 LP 问题的组合来实现。
*   **任意 $k$ 的算法**：提出了一个 **XP 算法**（复杂度为 $O(n^{g(k)})$），将 $k=2$ 的状态图方法推广到多机器人情形。

### 3. 实验设计
*   **性质说明**：本论文属于**理论算法研究**，未包含基于真实数据集的实证实验。
*   **Benchmark（基准）**：其对比对象是现有的理论复杂度界限。例如，将 FPT 结果从之前的“整数网格图”推广到了“连续平面空间”，并改进了 Eiben 等人（SoCG'23）的结论。
*   **验证方式**：通过严谨的数学证明（如 Lemma 13 关于转向次数的界限）来验证算法的正确性和最优性。

### 4. 资源与算力
*   **算力说明**：论文未提及具体的 GPU/CPU 硬件实验或训练时长。
*   **复杂度分析**：重点在于渐进复杂度分析。对于 $k=2$ 的情况，算法在多项式时间内运行；对于 $k>2$，复杂度随 $k$ 指数级增长。

### 5. 实验数量与充分性
*   **理论完备性**：论文对 $k=2$ 和任意 $k$ 的情况都给出了算法，并涵盖了 Min-Exposure、Min-Sum、Min-Makespan 三种目标函数。
*   **逻辑严密性**：通过对配置空间的穷举分类和线性规划的结合，证明了在 $L_1$ 度量下离散化搜索的充分性。

### 6. 主要结论与发现
*   **最小暴露问题**：对于两个机器人，可以在 $O(n^4 \log n)$ 时间内找到最优暴露调度。
*   **FPT 性质**：首次证明了平面上单位正方形机器人的 Min-Sum 和 Min-Makespan CMP 是 FPT 的（以 $k$ 为参数）。
*   **路径特性**：在 $L_1$ 空间中，两个机器人之间的最优调度可以通过极少的转向（每个机器人每段最多转一次）来实现。

### 7. 优点
*   **新颖的目标函数**：Min-Exposure 具有很强的实际应用潜力，填补了 CMP 领域在“区域覆盖”优化上的空白。
*   **理论突破**：将 CMP 的参数化可解性从受限的网格环境推广到了更通用的平面多边形环境。
*   **算法简洁性**：利用 $L_1$ 度量的几何特性，将复杂的连续运动规划简化为图搜索和线性规划问题。

### 8. 不足与局限
*   **度量限制**：研究主要基于 $L_1$ 度量，对于更常见的欧几里得度量（$L_2$），由于最优路径可能包含曲线或复杂的代数数，结论难以直接推广。
*   **可扩展性**：针对 $k$ 个机器人的 XP 算法在 $k$ 较大时计算代价极高，实际应用中仍需启发式算法。
*   **形状限制**：仅针对单位正方形机器人，未深入探讨旋转或更复杂几何形状机器人的暴露优化。

（完）
