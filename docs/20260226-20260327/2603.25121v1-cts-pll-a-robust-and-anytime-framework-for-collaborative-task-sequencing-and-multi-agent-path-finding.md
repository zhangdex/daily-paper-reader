---
title: "CTS-PLL: A Robust and Anytime Framework for Collaborative Task Sequencing and Multi-Agent Path Finding"
title_zh: CTS-PLL：一种用于协作任务排序与多智能体路径规划的鲁棒且随时框架
authors: "Junkai Jiang, Yitao Xu, Ruochen Li, Shaobing Xu, Jianqiang Wang"
date: 2026-03-26
pdf: "https://arxiv.org/pdf/2603.25121v1"
tags: ["query:robot-planning"]
score: 9.0
evidence: 协作任务排序与多智能体路径规划
tldr: 针对协作任务排序与多智能体路径规划（CTS-MAPF）中的组合复杂性难题，本文提出CTS-PLL分层框架。该框架引入了基于完备规划方法的死锁检测与释放机制，并结合大邻域搜索（LNS）实现解的随时优化。实验证明，该方法在稠密环境下具有极高的鲁棒性，能显著提升任务成功率和解质量，并已在真实机器人实验中得到验证。
selection_source: fresh_fetch
motivation: 旨在解决CTS-MAPF问题在复杂稠密环境下因组合爆炸导致的规划失败和解质量低下的挑战。
method: 提出了一种结合死锁检测释放机制与大邻域搜索（LNS）优化的分层规划框架。
result: 在稀疏和稠密基准测试中，该方法在成功率和解质量上均优于现有算法，且具备良好的运行效率。
conclusion: CTS-PLL是一个鲁棒且高效的随时规划框架，为解决实际场景中的多智能体协作任务提供了可行方案。
---

## 摘要
协作任务排序与多智能体路径规划（CTS-MAPF）问题要求智能体在避免碰撞的同时完成一系列任务，由于其组合复杂性，这带来了巨大的挑战。本研究提出了 CTS-PLL，这是一个分层框架，它通过两项关键改进扩展了基于配置的 CTS-MAPF 规划范式：一种利用完备规划方法进行局部重规划的锁定智能体检测与释放机制，以及一种基于大邻域搜索（LNS）的随时优化程序。这些改进确保了在密集环境中的鲁棒性，并能够持续提升解的质量。在稀疏和密集基准测试上的广泛评估表明，与现有方法相比，CTS-PLL 在保持竞争力的运行效率的同时，实现了更高的成功率和解质量。真实世界的机器人实验进一步证明了该方法在实践中的可行性。

## Abstract
The Collaborative Task Sequencing and Multi-Agent Path Finding (CTS-MAPF) problem requires agents to accomplish sequences of tasks while avoiding collisions, posing significant challenges due to its combinatorial complexity. This work introduces CTS-PLL, a hierarchical framework that extends the configuration-based CTS-MAPF planning paradigm with two key enhancements: a lock agents detection and release mechanism leveraging a complete planning method for local re-planning, and an anytime refinement procedure based on Large Neighborhood Search (LNS). These additions ensure robustness in dense environments and enable continuous improvement of solution quality. Extensive evaluations across sparse and dense benchmarks demonstrate that CTS-PLL achieves higher success rates and solution quality compared with existing methods, while maintaining competitive runtime efficiency. Real-world robot experiments further demonstrate the feasibility of the approach in practice.

---

## 论文详细总结（自动生成）

以下是对论文《CTS-PLL: A Robust and Anytime Framework for Collaborative Task Sequencing and Multi-Agent Path Finding》的结构化深入总结：

### 1. 核心问题与研究背景
*   **核心问题**：论文探讨了**协作任务排序与多智能体路径规划（CTS-MAPF）**。这比传统的 MAPF 更复杂，因为它要求多个智能体不仅要到达终点，还要按照特定顺序协同完成一系列中间任务。
*   **研究动机**：
    *   **组合爆炸**：任务顺序决策与路径规划的耦合导致搜索空间呈指数级增长。
    *   **现有方法局限**：基于冲突搜索（CBS）的方法在稠密环境下扩展性差；基于配置的方法（如 CTS-PIBT）虽然高效，但由于缺乏完备性，容易陷入死锁（Deadlock）或活锁（Livelock），且无法持续优化解的质量。
*   **整体含义**：提出一个名为 **CTS-PLL** 的分层框架，旨在通过引入死锁检测释放机制和随时（Anytime）优化机制，在保证高效性的同时，提升算法的鲁棒性和解的质量。

### 2. 方法论
CTS-PLL 延续了任务排序与路径规划的分层结构，核心改进在于以下两个模块：
*   **锁定智能体检测与释放（Lock Detection & Release）**：
    *   **检测**：当底层规划器（Extended-PIBT）在规定步数内无法完成任务时，识别出未完成任务的智能体以及阻碍它们的智能体（锁定集合 $A_{lock}$）。
    *   **释放**：利用具备完备性的 MAPF 求解器（如 **LaCAM**）进行局部重规划。为了提高效率，该模块通过随机置换锁定智能体的位置来打破对称性，从而引导系统跳出死锁或活锁状态。
*   **基于大邻域搜索（LNS）的随时优化**：
    *   在获得初始可行解后，利用 LNS 进行迭代优化。
    *   **破坏（Destroy）**：采用随机采样、基于交集的采样和随机游走三种策略选择一组智能体并移除其路径。
    *   **修复（Repair）**：使用优先级规划（PP）为这些智能体重新规划路径，以降低总流失时间（Flowtime）。
    *   **自适应权重**：通过指数移动平均值动态调整三种破坏策略的权重，优先选择优化效果更好的策略。

### 3. 实验设计
*   **实验场景**：采用了四种标准网格地图：`random`（随机）、`room`（房间）、`maze`（迷宫）和 `empty`（空旷）。
*   **实验设置**：
    *   **稀疏场景**：智能体数量 5-20，任务数量 10-50。
    *   **稠密场景**：在最复杂的 `maze` 地图上设置 50 个智能体，任务数量 60-180。
*   **对比基准（Baselines）**：
    *   **CTS-CBS** 与 **CBSS**：基于冲突搜索的组合优化方法。
    *   **CTS-PIBT**：现有的高效配置类方法。
*   **评估指标**：成功率（Success Rate）、运行时间（Runtime）、解质量（Flowtime）。

### 4. 资源与算力
*   **硬件环境**：Intel Core i7-12700KF CPU，32GB RAM。
*   **软件环境**：Linux 系统，C++ 实现。
*   **算力说明**：文中未提及使用 GPU，这符合此类离散格点规划算法通常依赖单核或多核 CPU 性能的特征。实验设置了明确的时间限制（稀疏场景 60s/180s，稠密场景 180s）。

### 5. 实验数量与充分性
*   **实验规模**：针对每种（智能体数，任务数）的组合，均生成了 **50 个随机实例**进行测试，样本量足以支撑统计显著性。
*   **消融实验**：设计了三个变体（v1: 仅锁定释放；v2: 锁定释放+序列搜索；v3: 全功能版），清晰地展示了各模块对成功率和解质量的贡献。
*   **客观性**：在相同硬件环境下对比了多种 SOTA 算法，并涵盖了从简单到极高难度的多种地图，实验设计较为客观、公平。

### 6. 主要结论与发现
*   **鲁棒性显著提升**：在稀疏场景下，CTS-PLL 达到了 **100% 的成功率**，远超 CTS-CBS 和 CBSS。在稠密场景下，相比 CTS-PIBT，成功率提升了 **28.95% 至 89.47%**。
*   **解质量优化**：通过 LNS 机制，CTS-PLL 在相同时间内产出的解质量（Flowtime）比 CTS-PIBT 平均提升了约 **10.4%**。
*   **现实可行性**：通过 **toio 机器人** 的物理实验验证了算法在处理真实世界碰撞和活锁问题时的有效性。

### 7. 优点
*   **分层解耦**：巧妙地结合了配置类算法的高效性和搜索类算法的完备性。
*   **随时特性（Anytime）**：能够快速给出初始解，并随时间推移不断优化，非常适合对实时性有要求的工业场景。
*   **局部重规划策略**：引入 LaCAM 处理局部死锁，避免了全局重规划的高昂代价。

### 8. 不足与局限
*   **随机性依赖**：锁定释放机制中的目标位置生成依赖随机置换，虽然实验证明有效，但在极端严苛的拓扑结构下可能仍需多次尝试才能成功。
*   **静态环境假设**：实验主要在静态网格地图中进行，未深入探讨动态障碍物或任务实时变更的动态环境。
*   **计算开销**：虽然比 CBS 类方法快，但在智能体和任务规模达到极大规模（如数千个）时，LNS 的收敛速度可能会变慢。

（完）
