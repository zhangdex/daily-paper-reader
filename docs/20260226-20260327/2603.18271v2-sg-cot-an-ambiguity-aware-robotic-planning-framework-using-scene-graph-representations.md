---
title: "SG-CoT: An Ambiguity-Aware Robotic Planning Framework using Scene Graph Representations"
title_zh: SG-CoT：一种基于场景图表示的歧义感知机器人规划框架
authors: "Akshat Rana, Peeyush Agarwal, K. P. S. Rana, Amarjit Malhotra"
date: 2026-03-18
pdf: "https://arxiv.org/pdf/2603.18271v2"
tags: ["query:robot-planning"]
score: 9.0
evidence: 基于大语言模型和场景图的机器人规划
tldr: 针对大语言模型在机器人规划中面临的歧义性挑战，本文提出SG-CoT框架。该框架通过构建环境的场景图表示，并利用检索函数让LLM迭代查询相关信息，从而将推理过程与实际观测结合。SG-CoT能识别歧义并主动提问澄清，显著提升了机器人在单智能体和多智能体环境下的规划成功率和任务可靠性。
selection_source: fresh_fetch
motivation: 大语言模型作为机器人规划器时，难以有效识别和处理环境及指令中的歧义信息，导致规划可靠性不足。
method: 提出一种两阶段框架，先构建包含物体属性与关系的场景图，再利用检索函数引导LLM迭代查询场景图以消除歧义。
result: "实验显示SG-CoT在提问准确率上提升超10%，且在单智能体和多智能体环境的任务成功率分别至少提升了4%和15%。"
conclusion: 通过将场景图表示与思维链推理相结合，SG-CoT显著增强了机器人在复杂、模糊环境下的规划能力和泛化性。
---

## 摘要
歧义性对作为机器人规划器的大语言模型（LLMs）构成了重大挑战。在本文中，我们提出了场景图思维链（SG-CoT），这是一个两阶段框架，其中 LLM 迭代地查询环境的场景图表示，以检测并澄清歧义。首先，根据输入观测构建环境的结构化场景图表示，捕捉物体、其属性以及与其他物体的关系。其次，LLM 配备了检索函数，用于查询场景图中与给定指令相关的部分。这使 LLM 的推理过程立足于观测，从而提高了机器人在歧义情况下的规划可靠性。SG-CoT 还允许 LLM 识别歧义来源，并向用户或其他机器人提出相关的消歧问题。广泛的实验表明，SG-CoT 一贯优于先前的方法，在提问准确率上至少提高了 10%，在单智能体环境中的成功率至少提高了 4%，在多智能体环境中至少提高了 15%，验证了其在更具泛化性的机器人规划中的有效性。

## Abstract
Ambiguity poses a major challenge to large language models (LLMs) used as robotic planners. In this letter, we present Scene Graph-Chain-of-Thought (SG-CoT), a two-stage framework where LLMs iteratively query a scene graph representation of the environment to detect and clarify ambiguities. First, a structured scene graph representation of the environment is constructed from input observations, capturing objects, their attributes, and relationships with other objects. Second, the LLM is equipped with retrieval functions to query portions of the scene graph that are relevant to the provided instruction. This grounds the reasoning process of the LLM in the observation, increasing the reliability of robotic planners under ambiguous situations. SG-CoT also allows the LLM to identify the source of ambiguity and pose a relevant disambiguation question to the user or another robot. Extensive experimentation demonstrates that SG-CoT consistently outperforms prior methods, with a minimum of 10% improvement in question accuracy and a minimum success rate increase of 4% in single-agent and 15% in multi-agent environments, validating its effectiveness for more generalizable robot planning.

---

## 论文详细总结（自动生成）

以下是对论文《SG-CoT: An Ambiguity-Aware Robotic Planning Framework using Scene Graph Representations》的结构化总结：

### 1. 核心问题与整体含义
该论文针对大语言模型（LLM）在担任机器人规划器时难以处理**歧义性（Ambiguity）**的问题展开研究。
*   **研究动机**：LLM 在处理指令时往往表现出“过度自信”，且缺乏对物理环境的深度理解。当指令不明确（如“拿那个杯子”但有多个杯子）或环境状态不支持指令（如目标物体不存在）时，传统规划器容易产生幻觉或执行错误动作。
*   **核心目标**：开发一种能够感知歧义、主动寻求澄清，并将推理过程与环境观测深度绑定的机器人规划框架。

### 2. 方法论
论文提出了 **SG-CoT（场景图思维链）** 框架，分为两个主要阶段：
*   **场景图生成（Scene Graph Generation）**：
    *   利用开放集目标检测器（如 Grounding DINO）识别物体。
    *   利用视觉语言模型（VLM）通过 Prompting 方式生成物体的属性（颜色、状态等）和物体间的空间/语义关系（左边、内部等），构建结构化的 JSON 场景图。
*   **迭代推理过程（Reasoning Process）**：
    *   **工具使用（Tool-use）**：LLM 不直接生成动作，而是通过调用 API 函数（如 `retrieve_node` 和 `retrieve_edge`）来查询场景图。
    *   **思维链迭代**：LLM 遵循“观察-推理-函数调用”的循环。它先思考需要什么信息，调用 API 获取场景局部细节，更新对话历史，再决定是继续查询、执行动作，还是因发现歧义而向用户提问。
    *   **歧义分类**：框架将歧义分为指令欠指定（Underspecification）、物体缺失（Absence）、多样性冲突（Multiplicity）以及多智能体中的局部观测歧义。

### 3. 实验设计
*   **实验场景**：
    1.  **单智能体环境**：基于 SayCan 的模拟桌面环境（PyBullet 模拟器），包含基础任务、空间关系任务和长程任务。
    2.  **多智能体环境**：基于 LEMMA 基准测试，涉及两个机器人在部分可观测环境下的协作（Stack 和 Pass 任务）。
*   **对比基准（Baselines）**：
    *   **CLARA**：基于不确定性估计的方法。
    *   **Inner Monologue**：基于环境反馈的闭环规划方法。
    *   **ProgPrompt**：基于程序化 Prompt 的规划方法。
*   **评估指标**：成功率（SR）和正确提问率（CQR，衡量机器人是否准确识别了歧义来源）。

### 4. 资源与算力
*   **模型使用**：实验使用了 **Gemini-2.5-Flash** 和 **Qwen3-VL-2B-Instruct** 作为核心 LLM/VLM。
*   **算力说明**：论文**未明确说明**具体的硬件配置（如 GPU 型号、数量）或训练时长。由于该框架主要基于 Prompting 和 API 调用，不涉及大规模模型微调，因此算力消耗主要集中在推理阶段的 Token 消耗和延迟上。

### 5. 实验数量与充分性
*   **实验规模**：在单智能体实验中进行了 **400 次试验**（每种歧义类型 100 次，外加 100 次无歧义场景）。
*   **消融实验**：对比了“无迭代（直接给出全图）”、“单次迭代”以及“无边信息（仅有物体列表）”的情况，验证了迭代查询和关系建模的必要性。
*   **充分性评价**：实验覆盖了从简单到复杂的多种歧义类型，并跨越了单/多智能体场景，对比了多种主流基准，实验设计较为客观、全面。

### 6. 主要结论与发现
*   **性能提升**：SG-CoT 在提问准确率（CQR）上比现有最佳方法提升了至少 **10%**。
*   **成功率**：在单智能体任务中成功率提升了 **4%** 以上，在多智能体协作任务中提升了 **15%** 以上。
*   **接地能力**：通过迭代查询场景图，LLM 能够有效抑制幻觉，精准定位导致歧义的物体或关系。
*   **模型规模效应**：性能随 LLM 能力（如 Gemini vs Qwen）的增强而显著提升。

### 7. 优点
*   **结构化接地**：将非结构化的视觉信息转化为结构化场景图，使 LLM 的推理有据可查。
*   **主动消歧**：不仅能完成任务，还能在信息不足时“知道自己不知道”，并针对性地提问。
*   **可扩展性**：通过 API 局部查询而非输入全图，能够处理更复杂的环境，减少了上下文窗口的压力。

### 8. 不足与局限
*   **感知瓶颈**：严重依赖 VLM 生成场景图的质量。如果 VLM 对空间关系的判断出错（幻觉），会直接导致后续规划失败。
*   **延迟与成本**：迭代推理需要多次调用 LLM，增加了 Token 消耗和任务执行的端到端延迟。
*   **环境限制**：目前仅在**模拟环境**中验证，缺乏真实物理世界（Real-world）的实验数据，未考虑传感器噪声和动态环境的影响。
*   **应用范围**：主要集中在桌面操纵任务，对于更广阔的导航或复杂交互场景尚待验证。

（完）
