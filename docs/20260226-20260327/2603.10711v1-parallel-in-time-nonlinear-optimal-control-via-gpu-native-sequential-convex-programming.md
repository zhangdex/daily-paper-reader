---
title: Parallel-in-Time Nonlinear Optimal Control via GPU-native Sequential Convex Programming
title_zh: 基于 GPU 原生序列凸规划的时间并行非线性最优控制
authors: "Yilin Zou, Zhong Zhang, Fanghua Jiang"
date: 2026-03-11
pdf: "https://arxiv.org/pdf/2603.10711v1"
tags: ["query:robot-planning"]
score: 9.0
evidence: 针对非线性约束自主系统的GPU原生轨迹优化
tldr: "针对非线性约束自主系统的实时轨迹优化，本文提出了一种完全基于GPU原生的求解框架。该框架结合了序列凸规划（SCP）与基于共识的交替方向乘子法（ADMM），通过时间分割策略将优化问题解耦为可大规模并行执行的子问题。实验证明，该方法在无人机敏捷飞行和火星着陆任务中，相比高性能CPU基准实现了4倍吞吐量提升和51%的能耗降低，支持超过100Hz的规划频率。"
selection_source: fresh_fetch
motivation: 传统的CPU串行求解器受限于全局稀疏线性代数或动态规划的串行特性，难以充分利用GPU的大规模并行计算能力。
method: 提出一种结合SCP与共识ADMM的GPU原生框架，通过时间分割将优化时域解耦为独立的并行子问题，并消除内存传输开销。
result: "在嵌入式平台上实现了超过100Hz的规划速率，相比12核CPU基准，吞吐量提升4倍，能耗降低51%，且GPU利用率超过96%。"
conclusion: 该框架通过充分利用硬件并行性，为非线性最优控制和鲁棒模型预测控制提供了一种高效、节能且可扩展的解决方案。
---

## 摘要
非线性约束自主系统的实时轨迹优化至关重要，通常由基于 CPU 的序列求解器执行。具体而言，对全局稀疏线性代数的依赖或动态规划算法的串行特性限制了对 GPU 等大规模并行计算架构的利用。为弥补这一差距，我们引入了一个完全 GPU 原生的轨迹优化框架，该框架将序列凸规划与基于共识的交替方向乘子法（ADMM）相结合。通过应用时间拆分策略，我们的算法将优化时域解耦为独立的、按节点划分的子问题，从而实现大规模并行执行。整个过程完全在 GPU 上运行，消除了昂贵的内存传输和大规模稀疏分解。该架构可自然扩展到多轨迹优化。我们在边缘计算平台上，通过四旋翼飞行器敏捷飞行任务和火星动力下降问题验证了该求解器。基准测试显示，与经过深度优化的 12 核 CPU 基准相比，吞吐量持续提升了 4 倍，能耗降低了 51%。至关重要的是，该框架使硬件达到饱和状态，保持 96% 以上的 GPU 活跃利用率，从而实现超过 100 Hz 的规划频率。此外，我们通过联合优化随机扰动下的动态耦合场景，展示了该求解器在鲁棒模型预测控制（MPC）方面的可扩展性，从而实现了可扩展且安全的自主性。

## Abstract
Real-time trajectory optimization for nonlinear constrained autonomous systems is critical and typically performed by CPU-based sequential solvers. Specifically, reliance on global sparse linear algebra or the serial nature of dynamic programming algorithms restricts the utilization of massively parallel computing architectures like GPUs. To bridge this gap, we introduce a fully GPU-native trajectory optimization framework that combines sequential convex programming with a consensus-based alternating direction method of multipliers. By applying a temporal splitting strategy, our algorithm decouples the optimization horizon into independent, per-node subproblems that execute massively in parallel. The entire process runs fully on the GPU, eliminating costly memory transfers and large-scale sparse factorizations. This architecture naturally scales to multi-trajectory optimization. We validate the solver on a quadrotor agile flight task and a Mars powered descent problem using an on-board edge computing platform. Benchmarks reveal a sustained 4x throughput speedup and a 51% reduction in energy consumption over a heavily optimized 12-core CPU baseline. Crucially, the framework saturates the hardware, maintaining over 96% active GPU utilization to achieve planning rates exceeding 100 Hz. Furthermore, we demonstrate the solver's extensibility to robust Model Predictive Control by jointly optimizing dynamically coupled scenarios under stochastic disturbances, enabling scalable and safe autonomy.

---

## 论文详细总结（自动生成）

这篇论文介绍了一个名为 **ucenter** 的完全 GPU 原生轨迹优化框架，旨在解决非线性约束自主系统在实时规划中的计算瓶颈。以下是对该论文的结构化总结：

### 1. 核心问题与整体含义（研究动机和背景）
*   **核心问题**：传统的非线性最优控制（OCP）求解器（如基于 iLQR 或 IPOPT 的方法）高度依赖 CPU 的串行逻辑或全局稀疏线性代数运算。这种“串行性”导致其难以利用现代 GPU 的大规模并行能力，成为复杂机器人任务（如敏捷飞行、火星着陆）实时规划的瓶颈。
*   **研究背景**：随着机器人任务复杂化，对大规模、非凸 OCP 的实时求解需求激增。现有的 GPU 加速方案往往在非线性前向模拟或硬约束处理上存在局限。

### 2. 方法论：核心思想与关键技术
*   **核心思想**：结合**序列凸规划（SCP）**与**基于共识的交替方向乘子法（ADMM）**，通过“时间分割”策略将长时域优化问题解耦为多个在时间点上独立的子问题。
*   **关键技术细节**：
    *   **外层循环（SCP）**：将非线性动力学和非凸成本函数进行局部线性化和二次近似，转化为一系列凸二次规划（QP）子问题。
    *   **内层循环（并行共识 ADMM）**：引入三种变量（物理变量、动力学辅助变量、几何镜像变量）来解耦时间耦合。
    *   **GPU 原生实现**：利用 JAX 框架的 `vmap`（矢量化映射）和 `jit`（即时编译），将每个时间节点的计算映射到 GPU 线程块，避免了昂贵的 CPU-GPU 数据传输和全局稀疏矩阵分解。
    *   **算子分裂**：将硬约束转化为近端投影（Proximal Projection）操作，适合 GPU 的 SIMT（单指令多线程）架构。

### 3. 实验设计
*   **实验场景**：
    1.  **四旋翼敏捷飞行**：6 自由度模型，包含障碍物规避和严格的执行器限制。
    2.  **火星动力下降着陆**：6 自由度模型，涉及质量消耗、推力矢量角限制、下滑道约束等复杂物理限制。
*   **Benchmark（对比基准）**：
    *   **硬件对比**：与高性能 12 核 CPU（利用多进程和 numba JIT 优化的 iLQR 算法）进行对比。
    *   **性能指标**：吞吐量（Hz）、计算耗时、能耗（焦耳）、GPU 利用率、成功率。
*   **对比方法**：主要对比了分布式 CPU 求解方案与所提 GPU 原生方案在不同批量（Batch Size）下的扩展性。

### 4. 资源与算力
*   **硬件平台**：使用了 **Nvidia Jetson AGX Orin 64GB** 边缘计算平台（常用于无人机和自动驾驶的嵌入式设备）。
*   **软件环境**：基于 Python 和 **JAX** 框架开发。
*   **算力表现**：在处理批量为 5000 的轨迹优化时，GPU 达到了 **96% 以上的活跃利用率**，吞吐量超过 **100 Hz**。

### 5. 实验数量与充分性
*   **实验规模**：
    *   进行了**消融实验**，测试了惩罚参数 $\rho$ 和 ADMM 迭代次数对收敛性的影响。
    *   进行了**扩展性基准测试**，批量大小从 1 跨越到 5000，验证了在大规模并行下的加速比。
    *   进行了 **1000 组随机障碍物场景**的成功率测试（成功率 93.9%）和 **1000 组火星着陆扰动测试**（成功率 99.8%）。
*   **充分性评价**：实验设计较为充分，涵盖了从单轨迹实时性到多轨迹鲁棒性的多维度验证，且使用了真实的嵌入式硬件而非仅在桌面级 GPU 上运行，具有较强的工程参考价值。

### 6. 主要结论与发现
*   **高性能**：相比优化的 12 核 CPU 基准，该方法实现了 **4.1 倍的吞吐量提升**。
*   **高能效**：在完成相同规模的任务时，**能耗降低了 51%**，这对电池驱动的机器人至关重要。
*   **实时鲁棒性**：通过“场景优化（Scenario Optimization）”，该框架能在 200ms 内同时优化 15 个耦合场景，实现了鲁棒 MPC 的实时化。
*   **硬件饱和**：证明了通过时间分割策略可以有效消除分支预测和线程发散，使 GPU 算力趋于饱和。

### 7. 优点
*   **完全解耦**：通过 ADMM 将时间序列问题转化为空间并行问题，彻底避开了传统求解器中难以并行的稀疏因子分解。
*   **端到端 GPU**：算法全流程在 GPU 上完成，极大减少了数据同步开销。
*   **通用性**：不仅适用于单轨迹生成，还天然支持大规模数据集生成和鲁棒/随机 MPC。

### 8. 不足与局限
*   **收敛性理论**：论文提到内层 ADMM 的部分收敛与外层 SCP 迭代之间的相互作用仍需更深入的理论分析。
*   **参数敏感性**：ADMM 算法通常对惩罚参数（$\rho$）较为敏感，虽然文中给出了调优参考，但在不同动力学系统下的通用调参规则尚不明确。
*   **硬件依赖**：该方法依赖于支持高效张量运算的硬件和软件栈（如 JAX），在不支持这些特性的低端嵌入式芯片上难以部署。

（完）
