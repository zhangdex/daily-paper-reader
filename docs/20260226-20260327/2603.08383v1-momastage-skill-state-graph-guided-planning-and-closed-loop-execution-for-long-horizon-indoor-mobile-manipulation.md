---
title: "MoMaStage: Skill-State Graph Guided Planning and Closed-Loop Execution for Long-Horizon Indoor Mobile Manipulation"
title_zh: MoMaStage：面向长程室内移动操作的技能-状态图引导规划与闭环执行
authors: "Chenxu Li, Zixuan Chen, Yetao Li, Jiapeng Xu, Hongyu Ding, Jieqi Shi, Jing Huo, Yang Gao"
date: 2026-03-09
pdf: "https://arxiv.org/pdf/2603.08383v1"
tags: ["query:robot-planning"]
score: 9.0
evidence: 基于技能状态图的移动操作规划
tldr: MoMaStage是一个针对长程室内移动操作的视觉语言框架。它通过分层技能库和拓扑感知的技能状态图，将视觉语言模型（VLM）的规划约束在可行空间内，无需显式场景建模。结合闭环执行与语义重规划机制，该方法有效解决了长程任务中的逻辑一致性和累积误差问题，显著提升了复杂环境下的任务成功率。
selection_source: fresh_fetch
motivation: 现有的长程移动操作方法在逻辑一致性、环境泛化能力及应对执行误差方面存在局限。
method: 提出一种基于技能状态图引导的规划框架，利用分层技能库约束VLM生成逻辑一致的计划，并结合闭环反馈进行动态重规划。
result: 在物理仿真和真实世界实验中，该方法在规划成功率、Token开销和整体任务成功率上均优于现有最先进基准。
conclusion: MoMaStage通过结构化约束和闭环机制，为实现鲁棒且泛化性强的长程室内移动操作提供了有效方案。
---

## 摘要
室内移动操作（MoMA）使机器人能够将自然语言指令转化为物理动作，但由于级联误差和在多样化环境中的泛化能力有限，长程执行仍然具有挑战性。基于学习的方法通常难以在长时程内保持逻辑一致性，而依赖显式场景表示的方法则引入了僵化的结构假设，降低了在动态环境中的适应性。为了解决这些局限性，我们提出了 MoMaStage，这是一个用于长程 MoMA 的结构化视觉-语言框架，它消除了对显式场景建图的需求。MoMaStage 将视觉-语言模型（VLM）植根于分层技能库和拓扑感知的技能-状态图中，在可行的转换空间内约束任务分解和技能组合。这种结构化植根确保了生成的规划在逻辑上保持一致，并且相对于智能体不断变化的物理状态在拓扑上是有效的。为了增强鲁棒性，MoMaStage 结合了闭环执行机制，该机制监控本体感受反馈，并在检测到偏差时触发受图约束的语义重规划，从而保持规划技能与物理结果之间的一致性。在物理特性丰富的仿真和真实世界环境中的广泛实验表明，MoMaStage 优于最先进的基准方法，在长程移动操作中实现了显著更高的规划成功率，减少了 token 开销，并显著提高了整体任务成功率。视频演示可在项目网站上找到：https://chenxuli-cxli.github.io/MoMaStage/。

## Abstract
Indoor mobile manipulation (MoMA) enables robots to translate natural language instructions into physical actions, yet long-horizon execution remains challenging due to cascading errors and limited generalization across diverse environments. Learning-based approaches often fail to maintain logical consistency over extended horizons, while methods relying on explicit scene representations impose rigid structural assumptions that reduce adaptability in dynamic settings. To address these limitations, we propose MoMaStage, a structured vision-language framework for long-horizon MoMA that eliminates the need for explicit scene mapping. MoMaStage grounds a Vision-Language Model (VLM) within a Hierarchical Skill Library and a topology-aware Skill-State Graph, constraining task decomposition and skill composition within a feasible transition space. This structured grounding ensures that generated plans remain logically consistent and topologically valid with respect to the agent's evolving physical state. To enhance robustness, MoMaStage incorporates a closed-loop execution mechanism that monitors proprioceptive feedback and triggers graph-constrained semantic replanning when deviations are detected, maintaining alignment between planned skills and physical outcomes. Extensive experiments in physics-rich simulations and real-world environments demonstrate that MoMaStage outperforms state-of-the-art baselines, achieving substantially higher planning success, reducing token overhead, and significantly improving overall task success rates in long-horizon mobile manipulation. Video demonstrations are available on the project website: https://chenxuli-cxli.github.io/MoMaStage/.

---

## 论文详细总结（自动生成）

以下是对论文《MoMaStage: Skill-State Graph Guided Planning and Closed-Loop Execution for Long-Horizon Indoor Mobile Manipulation》的结构化总结：

### 1. 核心问题与整体含义（研究动机和背景）
*   **核心问题**：室内长程移动操作（MoMA）要求机器人执行复杂的导航与物体交互序列。现有方法面临两大挑战：
    1.  **逻辑一致性缺失**：纯视觉语言模型（VLM）驱动的规划器容易产生“幻觉”，生成语义合理但物理上不可行的指令（如在手部已满时尝试抓取）。
    2.  **误差累积与环境泛化**：端到端学习方法在长程任务中容易产生级联误差，而基于显式地图的方法则过于僵化，难以适应动态环境。
*   **整体含义**：本文提出 **MoMaStage** 框架，旨在不依赖显式场景建图的情况下，通过结构化的“技能-状态图”约束 VLM 的推理，并结合闭环执行机制实现鲁棒的长程任务处理。

### 2. 方法论
MoMaStage 由三个核心模块组成：
*   **结构化技能植根（Structured Skill Grounding）**：
    *   **分层技能库**：分为动作级（底层控制）和语义级（与场景语义耦合，如 Pick/Place）。
    *   **技能-状态图（Skill-State Graph）**：定义为 $G = (V, E)$。每个节点包含**前置状态 $C$**（位置、机械臂状态）和**状态变化量 $\Delta$**（如移动、增加/减少物体）。这为 VLM 提供了一个轻量级的物理约束接口。
*   **图约束规划与验证**：
    *   **拓扑感知规划**：VLM 根据指令和视觉输入，在技能图的拓扑约束下生成候选序列。
    *   **状态驱动验证**：通过递归应用 $\Delta$ 检查全局状态一致性，剔除逻辑错误的规划（如违反机械臂负载限制）。
*   **闭环执行与重规划**：
    *   **双层监控**：结合本体感受（关节/触觉传感器）和语义验证（VLM 检查环境变化）。
    *   **动态重规划**：一旦检测到偏差，以当前观测状态为起点，在技能图中搜索纠正路径，而非简单重启任务。

### 3. 实验设计
*   **实验场景**：
    *   **真实世界**：3×3m 空间，包含橱柜、餐桌、储藏室。使用 Agilex Cobot Magic 平台（4 个 Piper 机械臂 + Tracer 移动底座）。
    *   **仿真环境**：基于 **mshab***（ManiSkill-Hab 的扩展版），包含 Tidy House、Prepare Groceries、Set Table 三个复杂场景。
*   **Benchmark 与对比方法**：
    *   **GT Sequencing**：人工编写的最优序列（执行上限）。
    *   **DeCo***：最先进的基于 VLM 的任务分解框架。
    *   **End-to-End ACT**：端到端模仿学习基准。
    *   **消融实验**：对比了不同技能配置（RL/BC/Per-Object）以及有无图约束的规划效率。

### 4. 资源与算力
*   **推理引擎**：仿真实验使用了 **Gemini 2.5 Flash** 作为 VLM 推理引擎。
*   **训练细节**：真实世界中的语义操作技能使用 ACT 算法在 50 个演示轨迹上训练；仿真中的底层技能使用 SAC、PPO 和 BC 预训练。
*   **算力说明**：文中**未明确提及**具体的 GPU 型号、数量或总训练时长，但强调了 MoMaStage 在 Token 消耗和推理延迟上的优化（见表 III）。

### 5. 实验数量与充分性
*   **实验规模**：
    *   仿真实验极其充分：涵盖 3 种场景、3 种技能配置，共执行了 **8,100 条**轨迹。
    *   真实世界实验：设计了 100 个不同长度的任务，重点展示了 17 个子任务的极长程案例。
*   **客观性与公平性**：通过引入 GT（地面真值）作为上限，并对比了 Token 消耗、规划成功率和执行成功率等多个维度，实验设计较为客观。

### 6. 主要结论与发现
*   **规划鲁棒性**：MoMaStage 在所有仿真场景中实现了 **79%-94%** 的规划成功率，远超 DeCo*。
*   **闭环优势**：在长程任务后期，MoMaStage 的成功率超过了开环的 GT 序列，证明了重规划机制在应对物理异常时的必要性。
*   **效率提升**：通过图约束，VLM 的推理 Token 消耗显著降低，且消除了逻辑幻觉。
*   **瓶颈转移**：实验发现当前的失败主要源于底层物理模拟误差（如碰撞、物理引擎计算错误），而非高层语义推理。

### 7. 优点
*   **无需建图**：摆脱了对 3D 场景图或显式地图的依赖，降低了系统复杂性。
*   **逻辑严密**：通过技能-状态图将 VLM 的生成能力与物理约束（如机械臂是否空闲）硬性绑定。
*   **高效重规划**：闭环监控与局部重规划机制显著提升了长程任务的生存率。
*   **Token 友好**：结构化引导减少了 VLM 的盲目搜索，降低了 API 调用成本。

### 8. 不足与局限
*   **底层技能依赖**：系统的最终表现高度依赖于底层动作原语的鲁棒性，若底层技能无法完成物理纠正，高层规划也无能为力。
*   **仿真与现实差距**：仿真中的物理限制（如 FLE 错误）在现实中表现不同，且现实实验的场景多样性（3×3m）仍有提升空间。
*   **状态变量简化**：目前的技能-状态图仅追踪了位置和抓取状态，对于更复杂的物理属性（如物体的温度、容器内的液体量）尚未建模。

（完）
