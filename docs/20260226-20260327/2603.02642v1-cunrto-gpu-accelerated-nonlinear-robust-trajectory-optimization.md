---
title: "cuNRTO: GPU-Accelerated Nonlinear Robust Trajectory Optimization"
title_zh: cuNRTO：GPU加速的非线性鲁棒轨迹优化
authors: "Jiawei Wang, Arshiya Taj Abdul, Evangelos A. Theodorou"
date: 2026-03-03
pdf: "https://arxiv.org/pdf/2603.02642v1"
tags: ["query:robot-planning"]
score: 9.0
evidence: GPU加速的非线性鲁棒轨迹优化
tldr: 本研究针对自主系统在不确定性下的安全运行问题，提出了cuNRTO框架，旨在加速非线性鲁棒轨迹优化。该框架通过CUDA实现了两种核心架构：NRTO-DR利用Douglas-Rachford分裂法并行处理二阶锥规划（SOCP）子问题，而NRTO-FullADMM则通过ADMM进一步提升扩展性。利用自定义CUDA内核和cuBLAS优化，该方法在处理复杂机器人模型时表现优异，显著降低了计算负担。
selection_source: fresh_fetch
motivation: 鲁棒轨迹优化涉及的大规模二阶锥规划（SOCP）约束计算成本极高，限制了其在实时自主系统中的应用。
method: 开发了基于CUDA的cuNRTO框架，引入NRTO-DR和NRTO-FullADMM两种架构，利用并行投影和高效矩阵运算加速求解。
result: 在单轮车、四旋翼和机械臂等模型上的仿真实验显示，该框架相比传统方法实现了最高139.6倍的加速。
conclusion: cuNRTO通过GPU加速显著提升了非线性鲁棒轨迹优化的效率，为复杂机器人在不确定环境下的实时决策提供了可能。
---

## 摘要
鲁棒轨迹优化通过计算满足所有有界扰动约束的控制策略，使自主系统能够在不确定性下安全运行。然而，这类问题通常涉及大规模的二阶锥规划（SOCP）约束，计算成本极高。在这项工作中，我们提出了 CUDA 非线性鲁棒轨迹优化（cuNRTO）框架，引入了两种直接应用于鲁棒决策并在 CUDA 上实现的动态优化架构。第一种架构 NRTO-DR 利用 Douglas-Rachford (DR) 分裂法求解 NRTO 的 SOCP 内部子问题，通过并行 SOCP 投影和稀疏直接求解显著减轻了计算负担。第二种架构 NRTO-FullADMM 是一种新型变体，利用交替方向乘子法（ADMM）进一步挖掘问题结构以提升可扩展性。最后，我们提供了所提方法的 GPU 实现，采用自定义 CUDA 内核处理 SOC 投影步骤，并使用 cuBLAS GEMM 链进行反馈增益更新。我们通过在单轮车、四旋翼飞行器和 Franka 机械臂模型上的仿真实验验证了 cuNRTO 的性能，结果显示其加速比高达 139.6 倍。

## Abstract
Robust trajectory optimization enables autonomous systems to operate safely under uncertainty by computing control policies that satisfy the constraints for all bounded disturbances. However, these problems often lead to large Second Order Conic Programming (SOCP) constraints, which are computationally expensive. In this work, we propose the CUDA Nonlinear Robust Trajectory Optimization (cuNRTO) framework by introducing two dynamic optimization architectures that have direct application to robust decision-making and are implemented on CUDA. The first architecture, NRTO-DR, leverages the Douglas-Rachford (DR) splitting method to solve the SOCP inner subproblems of NRTO, thereby significantly reducing the computational burden through parallel SOCP projections and sparse direct solves. The second architecture, NRTO-FullADMM, is a novel variant that further exploits the problem structure to improve scalability using the Alternating Direction Method of Multipliers (ADMM). Finally, we provide GPU implementation of the proposed methodologies using custom CUDA kernels for SOC projection steps and cuBLAS GEMM chains for feedback gain updates. We validate the performance of cuNRTO through simulated experiments on unicycle, quadcopter, and Franka manipulator models, demonstrating speedup up to 139.6$\times$.

---

## 论文详细总结（自动生成）

### cuNRTO: GPU加速的非线性鲁棒轨迹优化论文总结

#### 1. 核心问题与整体含义（研究动机和背景）
在机器人领域，**鲁棒轨迹优化（Robust Trajectory Optimization）**是确保系统在存在不确定性（如模型误差、外部扰动）时仍能安全运行的关键。然而，为了保证在所有可能的扰动下都能满足约束，该问题通常被转化为复杂的**二阶锥规划（SOCP）**。
*   **痛点**：传统的求解方法（如基于CPU的内点法）在处理高维系统或大量障碍物约束时，计算量呈爆炸式增长，难以满足实时性要求。
*   **目标**：本文旨在利用GPU的大规模并行计算能力，通过重新设计优化算法架构，显著提升非线性鲁棒轨迹优化的求解速度。

#### 2. 方法论：核心思想与技术细节
论文提出了 **cuNRTO** 框架，包含两种主要的并行优化架构：
*   **双层优化结构**：外层采用**相继线性化（Successive Linearization, SL）**将非线性问题转化为一系列凸子问题；内层则在GPU上高效求解这些子问题。
*   **NRTO-DR 架构**：
    *   利用 **Douglas-Rachford (DR) 分裂法** 求解SOCP子问题。
    *   核心在于将复杂的约束投影分解为独立的、可并行的SOC（二阶锥）投影。
    *   使用稀疏直接求解器（cuDSS）处理KKT系统，并在迭代中复用矩阵分解结果。
*   **NRTO-FullADMM 架构**：
    *   这是一种更彻底的并行方案，将整个内层循环（包括变量更新和对偶更新）完全移至GPU。
    *   通过**交替方向乘子法（ADMM）**进一步解耦问题结构，消除CPU-GPU之间频繁的数据传输瓶颈。
*   **关键技术实现**：
    *   **自定义CUDA内核**：专门用于并行的SOC投影计算。
    *   **cuBLAS GEMM链**：用于高效更新高维的反馈增益矩阵（Feedback Gain）。
    *   **矩阵自由（Matrix-free）更新**：在更新过程中避免显式形成巨大的稠密矩阵，节省显存并提升速度。

#### 3. 实验设计
论文在三个具有代表性的机器人仿真场景中验证了算法：
*   **场景模型**：
    1.  **单轮车（Unicycle）**：基础非线性动力学。
    2.  **四旋翼飞行器（Quadcopter）**：12维状态空间的高动态系统。
    3.  **Franka 机械臂（7-DoF）**：14维状态、7维控制的高维操作任务。
*   **Benchmark（基准方法）**：
    *   **NRTO (Baseline)**：在CPU上使用多线程 **MOSEK**（商业级内点法求解器）求解。
*   **对比维度**：壁面时间（Wall-clock time）、约束满足率（通过1000次随机扰动和1000次边界情况的蒙特卡洛模拟验证）。

#### 4. 资源与算力
*   **硬件配置**：
    *   **GPU**：NVIDIA A100 (80GB 显存)。
    *   **CPU**：Intel Xeon W-3500 (60核心，3.06 GHz)。
*   **软件环境**：Ubuntu 22.04, CUDA 12.9, C++。
*   **训练/求解时长**：论文未提及离线训练（因为是基于优化的方法而非学习方法），主要关注在线求解的推理时间，单次优化从几毫秒到几百秒不等（取决于复杂度）。

#### 5. 实验数量与充分性
*   **实验规模**：针对不同时间步长（Horizon $T$）、不确定性水平（$\tau$）和障碍物数量（$Obs$）进行了独立的三组参数扫描（Sweeps）。
*   **充分性**：实验覆盖了从低维到高维的动力学模型，并对比了不同复杂度的约束场景。
*   **公平性**：所有方法使用相同的物理参数和收敛阈值，且基准方法MOSEK已开启多线程优化，对比具有说服力。此外，还包含了在真实硬件（Robotarium平台）上的部署验证。

#### 6. 主要结论与发现
*   **显著加速**：相比于基于CPU的MOSEK求解器，cuNRTO 实现了巨大的加速。在障碍物较多的单轮车任务中，加速比最高达到 **139.6倍**。
*   **可扩展性**：随着障碍物数量增加（即SOCP约束增多），GPU的并行优势愈发明显。
*   **鲁棒性保证**：结合线性化误差补偿（NRTO-LE），该框架在所有测试场景下均实现了 **100% 的约束满足率**，证明了其在安全关键任务中的可靠性。
*   **架构优劣**：NRTO-FullADMM 在处理复杂约束和高维反馈增益时表现优于 NRTO-DR，因为它减少了数据传输开销。

#### 7. 优点（亮点）
*   **算法创新**：成功将一阶分裂方法（DR和ADMM）应用于复杂的非线性鲁棒控制问题，并实现了高效的GPU映射。
*   **工程实现**：提供了细粒度的并行化方案（如Warp级别的SOC投影），充分利用了现代GPU的硬件特性。
*   **实用性强**：不仅在仿真中表现优异，还通过了真实机器人的实验验证，证明了其在实时控制中的潜力。

#### 8. 不足与局限
*   **局部最优风险**：由于外层采用相继线性化（SL），算法本质上是寻找局部最优解，在极度非凸的环境中可能陷入局部极值。
*   **参数敏感性**：ADMM和DR算法的收敛速度高度依赖于惩罚参数（$\rho$）的选择，论文中这些参数需针对不同任务进行手动调优。
*   **不确定性模型限制**：目前主要针对有界椭球体扰动（Ellipsoidal Uncertainty），对于更复杂的非凸不确定性集合的扩展性有待研究。

（完）
