---
title: Multi-Robot Coordination for Planning under Context Uncertainty
title_zh: 上下文不确定性下的多机器人协作规划
authors: "Pulkit Rustagi, Kyle Hollins Wray, Sandhya Saisubramanian"
date: 2026-03-14
pdf: "https://arxiv.org/pdf/2603.13748v1"
tags: ["query:robot-planning"]
score: 9.0
evidence: 不确定性下的多机器人协作与规划
tldr: 针对上下文不确定环境下的多机器人协作规划问题，本文提出了MR-CUSSP模型及两阶段解决方案。该方法首先通过CIMOP算法引导机器人前往关键地标以协同推断真实上下文，随后利用LCBS算法在特定偏好下进行无碰撞路径规划。研究通过仿真和实物机器人实验证明，该框架能有效减少因上下文误判导致的安全风险，实现高效的任务执行。
selection_source: fresh_fetch
motivation: 在现实场景中，机器人的任务优先级取决于未知的上下文环境，若无法准确推断上下文，将导致行为失准或产生安全隐患。
method: 提出了一种结合协同推理（CIMOP）与字典序冲突搜索（LCBS）的两阶段框架，分别用于高效推断上下文和执行多目标无碰撞路径规划。
result: 在三个仿真领域及五个移动机器人的实物实验中，该方法成功验证了其在处理上下文不确定性及多机器人冲突规避方面的有效性。
conclusion: 该研究为不确定环境下的多机器人协同任务提供了形式化模型和求解算法，显著提升了多机器人系统在复杂任务中的适应能力和安全性。
---

## 摘要
现实世界的机器人通常在目标优先级取决于底层操作上下文的环境中运行。当底层上下文先验未知时，多个机器人可能需要协作以收集信息性观测来推断上下文，因为基于错误的上下文采取行动可能导致目标不一致和不安全的行为。一旦推断出真实的底层上下文，机器人将按照上下文引导的偏好顺序优化其特定任务目标。我们将该问题形式化为多机器人上下文不确定随机最短路径（MR-CUSSP），它通过联合观测在路标状态下捕获与上下文相关的信息。我们的两阶段解决方案包括：(1) CIMOP（多目标规划的协作推理），用于计算引导机器人前往信息丰富的路标以高效推断真实上下文的规划；(2) LCBS（词典序冲突搜索），用于在上下文引导的词典序目标偏好下进行无碰撞多机器人路径规划。我们在三个模拟领域评估了该算法，并在 salp 领域设置中使用五个移动机器人展示了其实际适用性。

## Abstract
Real-world robots often operate in settings where objective priorities depend on the underlying context of operation. When the underlying context is unknown apriori, multiple robots may have to coordinate to gather informative observations to infer the context, since acting based on an incorrect context can lead to misaligned and unsafe behavior. Once the underlying true context is inferred, the robots optimize their task-specific objectives in the preference order induced by the context. We formalize this problem as a Multi-Robot Context-Uncertain Stochastic Shortest Path (MR-CUSSP), which captures context-relevant information at landmark states through joint observations. Our two-stage solution approach is composed of: (1) CIMOP (Coordinated Inference for Multi-Objective Planning) to compute plans that guide robots toward informative landmarks to efficiently infer the true context, and (2) LCBS (Lexicographic Conflict-Based Search) for collision-free multi-robot path planning with lexicographic objective preferences, induced by the context. We evaluate the algorithms using three simulated domains and demonstrate its practical applicability using five mobile robots in the salp domain setup.

---

## 论文详细总结（自动生成）

### 论文总结：上下文不确定性下的多机器人协作规划

#### 1. 核心问题与研究动机
在现实世界中，多机器人系统（MRS）执行任务时往往面临多个相互冲突的目标（如速度、能耗、安全性）。这些目标的优先级通常取决于当前的“上下文”（Context），例如环境地形、资源可用性或天气状况。
*   **研究动机**：上下文往往是先验未知的，且某些关键信息需要多个机器人协作（如形成特定阵型）才能观测到。若机器人基于错误的上下文执行任务，会导致行为失准或产生安全隐患。
*   **核心问题**：如何有效地协调多个机器人，通过联合观测推断出真实的上下文，并在此基础上计算出符合特定偏好顺序且无碰撞的路径。

#### 2. 方法论
论文提出了 **MR-CUSSP（多机器人上下文不确定随机最短路径）** 框架，并采用两阶段解决方案：

*   **第一阶段：CIMOP（多目标规划的协作推理）**
    *   **核心思想**：通过最小化信念熵（Belief Entropy）来选择观测效率最高的路标点（Landmarks）。
    *   **技术细节**：算法计算每个路标点所需的最小机器人数量，并根据当前共享信念（Shared Belief）对路标点进行优先级排序。它引导机器人组前往这些点进行联合感知，直到信念坍缩（即确定了唯一的真实上下文）。
*   **第二阶段：LCBS（词典序冲突搜索）**
    *   **核心思想**：在已知上下文诱导的词典序偏好（Lexicographic Preferences）下，解决多机器人路径冲突。
    *   **技术细节**：
        *   **底层搜索（LA*）**：改进的 A* 算法，使用词典序比较算子来剪枝被支配的路径，直接优化优先级目标。
        *   **高层搜索**：基于冲突搜索（CBS）框架，通过构建约束树（Constraint Tree）来消除机器人间的时空冲突，确保最终路径在满足偏好的同时实现无碰撞。

#### 3. 实验设计
研究在三个仿真领域和物理硬件上进行了评估：
*   **仿真领域**：
    1.  **Salp（海鞘机器人）采样**：水下机器人需根据水流和珊瑚分布调整能量、保护和速度的优先级。
    2.  **重载仓库**：机器人需在拥堵、避开人类区域和完成时间之间权衡。
    3.  **森林灭火**：无人机需根据风向和烟雾状况优化能耗、路径长度和时间。
*   **对比基准（Baselines）**：
    *   **推理阶段**：对比了 ARVI 和 SAIA（均修改为支持联合观测）。
    *   **规划阶段**：对比了 BB-MO-CBS-k、标量化 CBS（Scalarized CBS）和 MO-CBS（帕累托最优基准）。
*   **硬件实验**：在 Robotarium 平台上使用 5 台 GTernal 移动机器人进行实测。

#### 4. 资源与算力
*   **硬件环境**：实验在一台配备 **18 GB RAM 的 macOS 设备**上运行。
*   **软件实现**：算法使用 **C++** 编写。
*   **算力说明**：文中未提及使用 GPU，主要依赖 CPU 进行启发式搜索和逻辑计算。由于是离线/在线规划算法，重点在于算法的时间复杂度和实时响应能力。

#### 5. 实验数量与充分性
*   **实验规模**：每个领域均测试了不同数量的机器人（从 5 台到 35 台），并针对每种设置运行了 **5 组随机实例**。
*   **充分性分析**：
    *   实验涵盖了从低维到高维（35 台机器人）的扩展性测试。
    *   包含了消融性质的分析，如冗余路标点比例对推理效率的影响。
    *   通过物理机器人实验验证了算法从仿真到现实的迁移能力。
    *   **客观性**：对比了当前多目标路径规划（MO-MAPF）领域的最先进算法，实验设计较为公平。

#### 6. 主要结论与发现
*   **推理效率**：CIMOP 在所有领域中推断上下文的速度均显著快于基准算法，尤其在机器人数量较多时，能更早实现信念坍缩。
*   **规划性能**：LCBS 在时间受限的情况下表现优异。当规划预算缩减至 5 秒时，LCBS 仍能保持 100% 的成功率，而基于帕累托前沿的方法（如 MO-CBS）成功率大幅下降。
*   **扩展性**：CIMOP + LCBS 组合在处理 35 台机器人时，规划时间保持在 2 分钟以内，而基准方法往往需要 40-80 分钟。

#### 7. 优点
*   **解耦设计**：将复杂的“信息收集”与“任务执行”分离，使每个阶段都能使用针对性优化的算法。
*   **词典序优化**：相比于寻找整个帕累托前沿，LCBS 利用已知的偏好顺序极大地缩小了搜索空间，提高了计算效率。
*   **联合感知建模**：填补了现有研究中关于“必须协作才能获得观测信息”的建模空白。

#### 8. 不足与局限
*   **路标点依赖**：算法假设环境中存在预定义的、具有特定观测能力的路标点，这在完全未知的探索任务中可能难以满足。
*   **确定性简化**：在规划时使用了“最可能结果确定化”（Most-likely outcome determinization），虽然提高了速度，但在高度随机的环境中可能需要频繁重规划。
*   **通信假设**：默认机器人之间存在完美的共享信念同步，未深入探讨通信延迟或中断对协作推理的影响。

（完）
