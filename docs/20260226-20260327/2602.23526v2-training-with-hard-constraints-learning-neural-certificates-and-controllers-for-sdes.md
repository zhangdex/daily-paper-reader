---
title: "Training with Hard Constraints: Learning Neural Certificates and Controllers for SDEs"
title_zh: 带硬约束的训练：学习随机微分方程的神经证书与控制器
authors: "Chun-Wei Kong, Sebastian Escobar, Ibon Gracia, Jay McMahon, Morteza Lahijanian"
date: 2026-02-26
pdf: "https://arxiv.org/pdf/2602.23526v2"
tags: ["query:ra-rl-pe"]
score: 9.0
evidence: 随机系统的Reach-avoid证书生成与控制器综合
tldr: 针对随机微分方程（SDE）系统的到达-规避证书生成，本文提出了两种带硬约束保证的神经网络训练框架。第一种方法结合领域离散化与基于边界的损失函数，确保损失归零时证书全局有效；第二种方法采用场景化训练，为高维系统提供PAC概率保证。研究实现了证书与控制器的联合合成，在5维到10维系统上展现了优于现有技术的扩展性。
selection_source: fresh_fetch
motivation: 神经网络在处理SDE系统的功能优化时表现出色，但如何确保其生成的证书严格满足硬约束仍是一个重大挑战。
method: 提出了基于领域离散化的边界损失法和面向高维系统的场景化训练法，分别提供确定性全局保证和PAC概率保证。
result: 实验表明边界法在5维系统上优于现有技术，而场景化方法可扩展至10维系统并保持高置信度保证。
conclusion: 该框架有效解决了SDE系统神经证书训练中的硬约束满足问题，提升了高维随机系统的安全验证与控制能力。
---

## 摘要
由于其强大的表达能力，神经网络 (NN) 是解决泛函优化问题的极具前景的模板，特别是对于受随机微分方程 (SDE) 支配系统的到达-规避 (reach-avoid) 证书生成。然而，确保硬约束的满足仍然是一个重大挑战。在这项工作中，我们提出了两个具有保证的约束驱动训练框架，用于基于超鞅 (supermartingale) 的 SDE 神经证书构建和控制器合成。第一种方法通过区域离散化和基于边界的损失来强制执行证书不等式，一旦损失达到零，即可保证全局有效性。我们证明了该方法还能够实现具有硬保证的神经网络控制器-证书联合合成。对于离散化变得不可行的高维系统，我们引入了一种无划分的、基于场景的训练方法，为证书约束的满足提供任意紧致的 PAC 保证。基准测试表明，基于边界的方法在高达 5 维的系统上具有可扩展性，优于现有技术；而基于场景的方法在至少 10 维的系统上具有可扩展性，并提供高置信度保证。

## Abstract
Due to their expressive power, neural networks (NNs) are promising templates for functional optimization problems, particularly for reach-avoid certificate generation for systems governed by stochastic differential equations (SDEs). However, ensuring hard-constraint satisfaction remains a major challenge. In this work, we propose two constraint-driven training frameworks with guarantees for supermartingale-based neural certificate construction and controller synthesis for SDEs. The first approach enforces certificate inequalities via domain discretization and a bound-based loss, guaranteeing global validity once the loss reaches zero. We show that this method also enables joint NN controller-certificate synthesis with hard guarantees. For high-dimensional systems where discretization becomes prohibitive, we introduce a partition-free, scenario-based training method that provides arbitrarily tight PAC guarantees for certificate constraint satisfaction. Benchmarks demonstrate scalability of the bound-based method up to 5D, outperforming the state of the art, and scalability of the scenario-based approach to at least 10D with high-confidence guarantees.

---

## 论文详细总结（自动生成）

### 论文总结：带硬约束的训练——学习随机微分方程的神经证书与控制器

#### 1. 核心问题与整体含义
本文针对受**随机微分方程 (SDE)** 支配的连续时间随机系统，探讨如何利用神经网络 (NN) 生成**到达-规避 (Reach-Avoid) 证书**（即证明系统能以高概率到达目标区域并避开危险区域的函数）以及合成相应的控制器。
*   **研究动机**：虽然神经网络表达能力强，但在安全关键型应用中，仅靠经验性能是不够的。现有的神经证书方法通常依赖后验验证，且难以处理 SDE 中的微分不等式约束。
*   **核心挑战**：如何在训练过程中直接嵌入“硬约束”，确保生成的证书在整个连续域上全局有效，并解决高维系统下的计算扩展性问题。

#### 2. 方法论
论文提出了两种互补的训练框架：

*   **Hard-SAT 框架（基于边界的训练）**：
    *   **核心思想**：利用区间算术（如 `auto-LiRPA`）推导 NN 及其导数在特定区域内的解析边界。
    *   **技术细节**：将状态空间划分为多个网格（Cells），构造一个基于边界的损失函数 $L_{bound}$。该损失函数聚合了所有网格中违反约束的最坏情况。
    *   **保证**：证明了当 $L_{bound}$ 降至零时，证书在全局范围内严格满足超鞅（Supermartingale）约束。
    *   **优化策略**：引入了**自适应细化（Adaptive Refinement）**和**合并（Merging）**策略，通过 Top-K 规则细化违反约束的网格，并合并满足约束的网格以控制计算量。
    *   **联合合成**：通过对闭环 SDE 动力学的计算图进行反向传播，实现了控制器和证书的同步训练。

*   **基于场景的训练（PAC 保证）**：
    *   **核心思想**：针对高维系统（离散化不可行时），采用场景优化理论。
    *   **技术细节**：仅优化神经网络最后一层的参数，将约束满足问题转化为**线性规划 (LP)** 问题。
    *   **保证**：提供**概率近似正确 (PAC)** 保证。即在给定的置信度 $1-\delta$ 下，约束在除了测度极小的集合 $D_\epsilon$ 之外的所有地方均成立。

#### 3. 实验设计
*   **实验场景**：
    1.  **2D 随机倒立摆**：验证非线性动力学下的证书生成。
    2.  **n 维几何布朗运动 (GBM)**：用于测试从 2D 到 10D 的扩展性。
    3.  **3D 随机 Lorenz 系统**：测试混沌动力学下的控制与验证。
    4.  **3D XV15 倾转旋翼机**：复杂的空气动力学模型，测试从悬停到前飞的转换。
*   **Benchmark（基准）**：对比了 Neustroev 等人 (2025) 的 SOTA 方法（该方法采用软约束训练加后验验证）。
*   **对比指标**：运行时间、分区数量 $|Q|$、到达-规避概率 $p_{RA}$。

#### 4. 资源与算力
*   **Hard-SAT 方法**：使用 AMD Ryzen 9 7950X CPU (16核/32线程)，128 GB RAM。
*   **场景化方法**：使用 Intel Core i7 3.6GHz CPU，32 GB RAM（单线程运行）。
*   **训练时长**：
    *   低维系统（2D）：数百秒。
    *   高维系统（5D GBM）：约 31 小时（114,282 秒）。
    *   复杂 3D 系统（XV15）：约 22 小时。
    *   10D 场景化方法：仅需约 94 秒（得益于 LP 形式）。

#### 5. 实验数量与充分性
*   **实验规模**：涵盖了 6 组验证实验和 4 组联合合成实验。
*   **充分性**：
    *   通过 GBM 模型系统地测试了维度扩展性（2D, 3D, 4D, 5D, 10D）。
    *   每组实验运行 5 次取平均值，保证了结果的客观性。
    *   进行了蒙特卡洛模拟验证，结果显示所有合成控制器的实测成功率均为 1.0，证明了理论保证的有效性。
*   **公平性**：在对比实验中，对基准代码进行了适配修改以确保在相同的 SDE 任务下进行公平竞争。

#### 6. 主要结论与发现
*   **硬保证的可行性**：Hard-SAT 框架能有效处理 5D 及以下的 SDE 系统，且在分区效率上远优于现有技术（Neustroev et al.）。
*   **高维突破**：基于场景的方法成功扩展到了 10D 系统，并提供了极高置信度（$1-10^{-9}$）的 PAC 保证。
*   **联合训练优势**：直接在损失函数中耦合控制器和证书，比传统的“先训练控制器、后验证”模式更高效，能引导控制器向“易于验证”的方向优化。

#### 7. 优点
*   **理论严谨**：首次为 SDE 系统的神经证书提供了零损失即全局有效的硬保证。
*   **自适应性强**：通过网格合并和细化技术，显著缓解了状态空间划分带来的“维度灾难”。
*   **扩展性好**：场景化训练方法利用线性规划的特性，解决了高维神经证书训练的难题。

#### 8. 不足与局限
*   **Hard-SAT 的维度瓶颈**：尽管有优化，但基于分区的 Hard-SAT 在超过 5 维时内存和计算开销依然巨大（指数级增长）。
*   **场景法的概率性质**：场景化方法虽然快，但它允许存在一个极小的“违规集”，在极端安全敏感的场景下可能仍需额外处理。
*   **模型依赖**：方法依赖于已知的 SDE 动力学模型（漂移项和扩散项需已知且满足 Lipschitz 连续），对于完全未知的黑盒系统适用性有限。

（完）
