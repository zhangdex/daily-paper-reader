---
title: Shielded Reinforcement Learning Under Dynamic Temporal Logic Constraints
title_zh: 动态时序逻辑约束下的屏蔽强化学习
authors: "Sadık Bera Yüksel, Ali Tevfik Buyukkocak, Derya Aksaray"
date: 2026-03-17
pdf: "https://arxiv.org/pdf/2603.17152v1"
tags: ["query:ra-rl-pe"]
score: 9.0
evidence: 动态时序逻辑约束下的安全强化学习
tldr: 针对强化学习在处理复杂时空约束（如定期充电、限时访问）时的局限性，本文提出了一种基于屏蔽强化学习的新框架。该方法结合了信号时序逻辑（STL）与序列控制屏障函数（CBF），能够在学习过程中强制执行复杂的时空任务规范，甚至支持对未知轨迹动态目标的追踪。研究通过仿真验证了该框架在保证系统安全与任务合规性方面的有效性，为复杂约束下的机器人部署提供了新方案。
selection_source: fresh_fetch
motivation: 传统的安全强化学习难以处理如定期任务或限时访问等复杂的时空逻辑约束。
method: 提出一种结合序列控制屏障函数（CBF）与无模型强化学习的框架，利用STL表达并强制执行复杂的任务规范。
result: 仿真实验证明，该方法能确保智能体在学习过程中满足包括动态目标访问在内的多种STL约束。
conclusion: 该框架扩展了安全强化学习的应用范围，使其能够处理具有动态目标和复杂时序要求的实际任务。
---

## 摘要
强化学习 (RL) 在各种机器人应用中展现出巨大潜力，但由于安全和操作约束，其在真实系统上的部署仍然受到限制。近年来，安全强化学习领域受到了广泛关注，该领域专注于在整个学习过程中施加安全约束。然而，真实系统通常需要比单纯的安全约束更复杂的约束，例如定期充电或在限定时间内访问特定区域。在学习过程中施加此类时空任务仍然是一个挑战。信号时序逻辑 (STL) 是一种用于指定实值信号时序属性的形式化语言，并提供了一种表达此类复杂任务的方法。在本文中，我们提出了一个框架，利用序列控制屏障函数和无模型强化学习，以确保在整个学习过程中满足给定的 STL 任务。我们的方法通过执行丰富的 STL 规范扩展了传统的安全约束，这些规范可以涉及对轨迹未知的动态目标的访问。我们还通过各种仿真证明了我们框架的有效性。

## Abstract
Reinforcement Learning (RL) has shown promise in various robotics applications, yet its deployment on real systems is still limited due to safety and operational constraints. The safe RL field has gained considerable attention in recent years, which focuses on imposing safety constraints throughout the learning process. However, real systems often require more complex constraints than just safety, such as periodic recharging or time-bounded visits to specific regions. Imposing such spatio-temporal tasks during learning still remains a challenge. Signal Temporal Logic (STL) is a formal language for specifying temporal properties of real-valued signals and provides a way to express such complex tasks. In this paper, we propose a framework that leverages sequential control barrier functions and model-free RL to ensure that the given STL tasks are satisfied throughout the learning process. Our method extends beyond traditional safety constraints by enforcing rich STL specifications, which can involve visits to dynamic targets with unknown trajectories. We also demonstrate the effectiveness of our framework through various simulations.

---

## 论文详细总结（自动生成）

以下是对论文《Shielded Reinforcement Learning Under Dynamic Temporal Logic Constraints》（动态时序逻辑约束下的屏蔽强化学习）的结构化总结：

### 1. 核心问题与整体含义
*   **研究动机**：虽然强化学习（RL）在机器人领域表现出色，但其随机探索机制在现实部署中存在安全隐患。现有的“安全RL”大多仅关注简单的碰撞避免（静态安全约束），难以处理复杂的**时空任务约束**（例如：每隔一段时间必须充电、在特定时间窗口内访问移动目标等）。
*   **核心问题**：如何在无模型强化学习的训练过程中，强制执行复杂的、包含动态目标的信号时序逻辑（STL）规范，确保智能体在学习阶段就满足这些硬性约束，而不仅仅是在学习结束后。

### 2. 方法论
*   **核心思想**：采用“屏蔽”（Shielding）机制，在RL智能体与环境之间加入一个基于**序列控制屏障函数（Sequential CBFs）**的修正层。
*   **关键技术细节**：
    *   **STL 任务建模**：利用 STL 表达复杂的逻辑，如 $G_{[a,b]} F_{[c,d]} \phi$（在 a 到 b 时间段内，每隔 c 到 d 时间必须完成任务 $\phi$）。
    *   **动态目标处理**：针对轨迹未知的移动目标，定义“最坏情况距离”（Worst-Case Distance），利用目标的最大速度界限进行保守估计，确保即使目标逃逸也能达成任务。
    *   **序列 CBF 构造**：将复杂的 STL 任务分解为一系列具有先后顺序的子任务，并为每个子任务构造时间相关的 CBF。
    *   **二次规划（QP）求解**：在每个时间步，RL 策略输出原始动作 $u_{RL}$，系统通过求解一个 QP 问题计算最小修正量 $u_{CBF}$，最终执行动作 $\tilde{u} = u_{RL} + u_{CBF}$。
    *   **理论保证**：论文证明了通过引入松弛变量 $\epsilon$，即使在输入受限的情况下，任务违反量（时间延迟）也是有界的。
*   **算法流程**：在每个 episode 开始前采样可行初始状态；在训练每一步，根据当前最紧迫的 STL 任务更新 CBF 约束，修正 RL 动作，并将修正后的经验存入回放池进行学习。

### 3. 实验设计
*   **实验场景**：
    *   **Case 1（周期性充电）**：智能体在执行高奖励任务的同时，必须每 90 步内访问两个移动充电站中的任意一个。
    *   **Case 2（复杂组合任务）**：包含两个随机移动目标的限时访问（需停留一定时间）以及一个周期性移动充电站的访问。
*   **Benchmark（基准）**：对比了标准的、无约束的 **Soft Actor-Critic (SAC)** 算法。
*   **评估指标**：学习曲线（累积奖励）、STL 任务满足率。

### 4. 资源与算力
*   **硬件**：使用单块 **NVIDIA RTX 4070 GPU**。
*   **软件**：Python 3.10 环境。
*   **模型结构**：Actor 和 Critic 网络均为 4 层隐藏层、每层 256 个神经元的反馈神经网络。
*   **训练细节**：文中未明确给出总训练时长（小时数），但指出了 episode 长度为 300 步，并展示了约 1000 个 episode 的收敛过程。

### 5. 实验数量与充分性
*   **实验规模**：针对两个不同复杂度的案例进行了测试，每个案例的结果均基于 **10 次独立运行**的平均值，并给出了两个标准差的置信区间。
*   **充分性评价**：实验充分验证了该框架在处理动态目标和复杂时序逻辑方面的有效性。然而，实验主要侧重于与“无约束”方法的对比，**缺乏与其他安全 RL 算法**（如基于拉格朗日乘子法或 CPO 的方法）的横向对比。

### 6. 主要结论与发现
*   **约束有效性**：该框架在学习过程中实现了极高的任务满足率（Case 1 为 99.56%，Case 2 为 95.2%）。
*   **性能权衡**：加入 STL 约束后，智能体的最终累积奖励低于无约束版本。这是因为智能体必须牺牲部分效率（偏离最优路径）去满足充电或访问目标的硬性时空约束。
*   **鲁棒性**：即使在存在未知扰动和目标轨迹未知的情况下，通过保守的 CBF 设计仍能保证任务的基本达成。

### 7. 优点
*   **表达力强**：能够处理比传统安全 RL 复杂得多的时空逻辑任务。
*   **训练即安全**：通过屏蔽机制，确保了智能体在探索阶段的行为合规，降低了实机训练的风险。
*   **动态适应**：创新的序列 CBF 设计使其能够应对移动目标，且不需要预知目标的未来轨迹。
*   **数学严谨**：提供了关于任务违反量（时间松弛）的理论下界证明。

### 8. 不足与局限
*   **动力学限制**：目前仅在**单积分器动力学**（一阶系统）上进行了验证，对于具有高阶非线性动力学（如多旋翼、双足机器人）的系统，构造有效的 CBF 和进行到达性分析会更加困难。
*   **计算开销**：每一步都需要求解一个 QP 问题，虽然对于单智能体压力不大，但在大规模多智能体系统或高频控制场景下可能存在实时性瓶颈。
*   **保守性风险**：由于对动态目标采用“最坏情况”估计，当目标运动非常剧烈时，CBF 可能会过于保守，导致 RL 无法有效探索高奖励区域。

（完）
