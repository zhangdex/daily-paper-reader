---
title: Safe Reinforcement Learning with Preference-based Constraint Inference
title_zh: 基于偏好约束推理的安全强化学习
authors: "Chenglin Li, Guangchun Ruan, Hua Geng"
date: 2026-03-24
pdf: "https://arxiv.org/pdf/2603.23565v1"
tags: ["query:ra-rl-pe"]
score: 10.0
evidence: 带有约束推理的安全强化学习
tldr: 针对安全强化学习中约束难以显式定义的问题，本文提出了一种基于偏好的约束推理方法（PbCRL）。研究发现传统的Bradley-Terry模型在处理安全成本时存在风险低估问题，为此引入了“死区”机制以模拟重尾分布，并结合信噪比损失优化探索。该方法通过两阶段训练降低了标注负担，在保证安全性的同时实现了更高的奖励，为复杂环境下的安全决策提供了新思路。
selection_source: fresh_fetch
motivation: 现实世界中的安全约束往往复杂且主观，难以显式指定，而现有的约束推理方法依赖强假设或大量专家演示。
method: 提出PbCRL框架，引入死区机制改进偏好建模以捕捉重尾安全成本，并利用信噪比损失和两阶段训练策略优化策略学习。
result: 实验结果表明，PbCRL在约束对齐、安全性和奖励获取方面均优于现有的最先进基准方法。
conclusion: 本研究通过偏好学习有效地解决了安全强化学习中的约束推理难题，在安全关键型应用中具有巨大的应用潜力。
---

## 摘要
安全强化学习（Safe RL）是安全关键型决策的标准范式。然而，现实世界的安全约束可能非常复杂、主观，甚至难以明确指定。现有的约束推理工作依赖于限制性假设或大量的专家演示，这在许多实际应用中并不现实。如何廉价且可靠地学习这些约束是本研究关注的主要挑战。虽然从人类偏好中推理约束提供了一种数据高效的替代方案，但我们发现流行的 Bradley-Terry (BT) 模型无法捕捉安全成本的不对称和重尾特性，从而导致风险低估。目前文献中仍鲜有研究探讨 BT 模型对下游策略学习的影响。为了填补上述知识空白，我们提出了一种名为“基于偏好的约束强化学习”（PbCRL）的新方法。我们在偏好建模中引入了一种新颖的死区（dead zone）机制，并从理论上证明了它能促进重尾成本分布，从而实现更好的约束对齐。此外，我们结合了信噪比（SNR）损失，通过成本方差来鼓励探索，并发现这有利于策略学习。此外，我们采用了两阶段训练策略，以减轻在线标注负担，同时自适应地增强约束满足。实验结果表明，PbCRL 实现了与真实安全要求的卓越对齐，并在安全性和奖励方面优于最先进的基准方法。我们的工作探索了一种在安全强化学习中进行约束推理的有前景且有效的方法，在各种安全关键型应用中具有巨大潜力。

## Abstract
Safe reinforcement learning (RL) is a standard paradigm for safety-critical decision making. However, real-world safety constraints can be complex, subjective, and even hard to explicitly specify. Existing works on constraint inference rely on restrictive assumptions or extensive expert demonstrations, which is not realistic in many real-world applications. How to cheaply and reliably learn these constraints is the major challenge we focus on in this study. While inferring constraints from human preferences offers a data-efficient alternative, we identify the popular Bradley-Terry (BT) models fail to capture the asymmetric, heavy-tailed nature of safety costs, resulting in risk underestimation. It is still rare in the literature to understand the impacts of BT models on the downstream policy learning. To address the above knowledge gaps, we propose a novel approach namely Preference-based Constrained Reinforcement Learning (PbCRL). We introduce a novel dead zone mechanism into preference modeling and theoretically prove that it encourages heavy-tailed cost distributions, thereby achieving better constraint alignment. Additionally, we incorporate a Signal-to-Noise Ratio (SNR) loss to encourage exploration by cost variances, which is found to benefit policy learning. Further, two-stage training strategy are deployed to lower online labeling burdens while adaptively enhancing constraint satisfaction. Empirical results demonstrate that PbCRL achieves superior alignment with true safety requirements and outperforms the state-of-the-art baselines in terms of safety and reward. Our work explores a promising and effective way for constraint inference in Safe RL, which has great potential in a range of safety-critical applications.

---

## 论文详细总结（自动生成）

### 论文总结：基于偏好约束推理的安全强化学习 (PbCRL)

#### 1. 核心问题与整体含义（研究动机和背景）
在安全强化学习（Safe RL）中，传统的受限马尔可夫决策过程（CMDP）依赖于显式定义的成本函数和阈值。然而，在现实场景（如机器人导航、自动驾驶）中，安全约束往往是**隐式的、主观的且难以量化**的。
*   **现有挑战**：现有的约束推理方法（如逆强化学习）通常需要昂贵的专家演示。虽然基于偏好的学习（PbRL）更具数据效率，但常用的 **Bradley-Terry (BT) 模型** 倾向于推导对称的成本分布，而真实的安全性成本通常具有**重尾特性**（级联效应：一次违规常导致后续一系列失败）。
*   **后果**：直接使用 BT 模型会导致对预期成本的低估（风险低估），从而产生过于激进或危险的策略。

#### 2. 论文提出的方法论：PbCRL 框架
论文提出了一种名为 **PbCRL** 的新框架，旨在通过人类偏好反馈准确推理安全约束。
*   **核心思想**：通过改进偏好建模，使其能够捕捉安全成本的非对称重尾分布，并为下游策略提供更具信息量的梯度信号。
*   **关键技术细节**：
    1.  **死区机制 (Dead Zone Mechanism)**：在安全损失函数中引入偏移量 $\delta$。对于不安全轨迹，要求其估计成本 $\hat{C}$ 不仅要大于 0，还要大于 $\delta$。理论证明该机制能诱导重尾分布，缓解风险低估。
    2.  **信噪比 (SNR) 损失**：定义为成本方差与标签分布熵的比值。通过最小化该损失，鼓励成本模型产生更具辨别力的输出（增加信号强度），从而为策略梯度提供更强的探索信号。
    3.  **两阶段训练策略**：
        *   **离线预训练**：利用现有的偏好数据集初步训练成本模型。
        *   **在线微调**：在策略优化过程中，少量采样在线轨迹进行标注，并根据预测违规率与实际违规率的差异，**自适应调整死区参数 $\delta$**。
*   **算法流程**：先离线学习成本函数，再在 PPO-Lagrangian 框架下交替优化策略参数 $\theta$、拉格朗日乘子 $\lambda$ 和成本模型参数 $\psi$。

#### 3. 实验设计
*   **实验场景**：
    *   **Safety Gymnasium**：包含机器人运动（HalfCheetah, Walker2d, Humanoid）和导航任务（Goal, Push, Button）。
    *   **自动驾驶**：Blocked Road（路障）和 Lane Change（换道）场景。
    *   **大语言模型 (LLM) 对齐**：使用 Llama-3.2-1B 在 Safe RLHF 数据集上进行微调。
*   **Benchmark 与对比方法**：
    *   **PPO-Lag (Oracle)**：使用真实成本函数的上限基准。
    *   **RLSF**：基于片段（Segment-level）偏好学习的最先进基准。
    *   **PPO-BT**：使用标准 Bradley-Terry 模型的偏好学习方法。
*   **评估指标**：平均奖励（Return）、真实成本（True Cost）、成本分布的 2-Wasserstein 距离（衡量分布对齐度）。

#### 4. 资源与算力
*   **硬件环境**：Ubuntu 20.04，24核 CPU，**NVIDIA RTX 4090 GPU**，128GB RAM。
*   **LLM 实验**：使用了 **4 张 NVIDIA A800 GPU**。
*   **训练时长**：单个任务运行约 **4 至 6 小时**。
*   **标注预算**：总查询预算限制为 20,000 次（18,000 离线 + 2,000 在线）。

#### 5. 实验数量与充分性
*   **实验规模**：涵盖了从低维连续控制到高维离散动作空间（LLM）的 9 个不同任务。
*   **消融实验**：非常充分。分别针对 **SNR 损失权重 $\zeta$**、**两阶段策略的必要性**、**死区参数 $\delta$ 的敏感性** 以及 **标签噪声的鲁棒性** 进行了详细测试。
*   **客观性**：使用了 5 个随机种子取平均值，并引入了 2-Wasserstein 距离等量化指标来解释“为什么 PbCRL 有效”，实验设计逻辑严密且公平。

#### 6. 主要结论与发现
*   **约束对齐**：PbCRL 学习到的成本分布与真实重尾分布的 W2 距离最小，证明了死区机制对对齐真实约束的有效性。
*   **性能表现**：在所有任务中，PbCRL 的奖励接近 Oracle（真实成本模型），且真实成本严格控制在安全阈值以下，而 PPO-BT 经常违反约束，RLSF 则过于保守。
*   **SNR 作用**：SNR 损失能显著加快安全约束的收敛速度，避免了成本模型输出“平坦化”导致的梯度消失。

#### 7. 优点（亮点）
*   **理论支撑**：不仅提出了直觉上的改进，还通过数学证明了死区机制与重尾分布、SNR 与策略梯度范数之间的联系。
*   **数据效率**：通过两阶段训练和离线数据利用，大幅降低了对实时人类标注的依赖。
*   **跨领域通用性**：方法不局限于机器人控制，在自动驾驶和 LLM 安全对齐上也表现出良好的泛化能力。

#### 8. 不足与局限
*   **反馈质量依赖**：虽然进行了噪声鲁棒性测试，但当人类偏好存在严重矛盾或高强度噪声（>30%）时，模型性能下降明显。
*   **超参数敏感性**：SNR 损失的权重 $\zeta$ 需要精细调优，过大会导致训练不稳定。
*   **计算开销**：相比标准 Safe RL，增加了一个持续更新的成本模型和两阶段流程，增加了系统实现的复杂性。

（完）
