---
title: "Dynamic Control Barrier Function Regulation with Vision-Language Models for Safe, Adaptive, and Realtime Visual Navigation"
title_zh: 基于视觉语言模型的动态控制障碍函数调节，用于安全、自适应且实时的视觉导航
authors: "Jeffrey Chen, Rohan Chandra"
date: 2026-03-22
pdf: "https://arxiv.org/pdf/2603.21142v1"
tags: ["query:space-defense"]
score: 9.0
evidence: 用于安全视觉导航的动态控制障碍函数
tldr: 针对机器人在动态环境中的导航安全与效率平衡问题，本文提出AlphaAdj框架。该框架利用视觉语言模型（VLM）分析第一视角RGB图像并评估风险，动态调整控制障碍函数（CBF）的参数，以实时调节避障的保守程度。通过结合几何限幅和陈旧度门控融合策略，解决了VLM推理延迟问题。实验表明，该方法在保证零碰撞的同时，显著提升了导航效率和成功率。
selection_source: fresh_fetch
motivation: 传统的控制障碍函数（CBF）参数固定，难以在复杂动态环境中兼顾避障的安全性与导航的效率。
method: 利用VLM从视觉输入中提取风险标量，动态更新CBF参数，并引入速度感知的动态限幅和异步融合机制以应对推理延迟。
result: "在多种静态和动态场景中，AlphaAdj相比固定参数方法提升了高达18.5%的导航效率，并增强了系统的鲁棒性。"
conclusion: 结合大模型视觉理解能力与经典控制理论，能够有效实现安全、自适应且实时的机器人视觉导航。
---

## 摘要
在动态、非结构化环境中运行的机器人必须在感知可能受限的情况下平衡安全性和效率。虽然控制障碍函数（CBF）通过安全滤波提供了一种原则性的避障方法，但其行为通常受固定参数控制，这可能导致在温和场景中过于保守，或在危险附近过于激进。我们提出了 AlphaAdj，这是一个视觉到控制的导航框架，它利用第一视角 RGB 输入实时调整 CBF 安全滤波器的保守程度。视觉语言模型（VLM）根据当前摄像机视图生成一个有界的标量风险估计值，我们将其映射以动态更新 CBF 参数，从而调节安全约束的执行强度。为了解决实际应用中的异步推理和显著的 VLM 延迟问题，我们结合了几何速度感知动态上限和陈旧度门控融合策略，并采用轻量化实现方案以降低端到端推理开销。我们在多种环境下的多个静态和动态障碍物场景中评估了 AlphaAdj，并与固定参数和无上限的消融实验进行了对比。结果表明，AlphaAdj 在保持无碰撞导航的同时，相较于固定设置，其效率（在路径长度和到达目标时间方面）提升了高达 18.5%，且相较于无上限基准提升了鲁棒性和成功率。

## Abstract
Robots operating in dynamic, unstructured environments must balance safety and efficiency under potentially limited sensing. While control barrier functions (CBFs) provide principled collision avoidance via safety filtering, their behavior is often governed by fixed parameters that can be overly conservative in benign scenes or overly permissive near hazards. We present AlphaAdj, a vision-to-control navigation framework that uses egocentric RGB input to adapt the conservativeness of a CBF safety filter in real time. A vision-language model(VLM) produces a bounded scalar risk estimate from the current camera view, which we map to dynamically update a CBF parameter that modulates how strongly safety constraints are enforced. To address asynchronous inference and non-trivial VLM latency in practice, we combine a geometric, speed-aware dynamic cap and a staleness-gated fusion policy with lightweight implementation choices that reduce end-to-end inference overhead. We evaluate AlphaAdj across multiple static and dynamic obstacle scenarios in a variety of environments, comparing against fixed-parameter and uncapped ablations. Results show that AlphaAdj maintains collision-free navigation while improving efficiency (in terms of path length and time to goal) by up to 18.5% relative to fixed settings and improving robustness and success rate relative to an uncapped baseline.

---

## 论文详细总结（自动生成）

这篇论文介绍了一个名为 **AlphaAdj** 的导航框架，旨在通过视觉语言模型（VLM）动态调节控制障碍函数（CBF）的参数，以实现机器人在复杂动态环境中的安全且高效的导航。以下是对该论文的深度结构化总结：

### 1. 核心问题与研究动机
*   **核心问题**：如何在动态、非结构化的环境中平衡机器人的**安全性**（避障）与**效率**（到达目标的时间和路径长度）。
*   **研究动机**：
    *   传统的控制障碍函数（CBF）通常使用固定的保守参数（$\alpha$）。在空旷区域，固定参数会导致机器人过于迟疑；在危险区域，若参数过大则可能导致碰撞。
    *   视觉语言模型（VLM）具备强大的语义感知能力，可以理解场景风险，但其巨大的计算开销导致的高延迟（Latency）使其难以直接用于实时闭环控制。
    *   现有的视觉导航方案往往缺乏对环境语义风险的实时自适应调节能力。

### 2. 方法论
AlphaAdj 的核心思想是将 VLM 的高级语义理解与 CBF 的低级安全过滤相结合，通过异步推理机制解决延迟问题。
*   **风险评估（VLM）**：使用 LLaVA-1.5 7B 模型。机器人每隔 0.5 秒捕捉一张 RGB 图像，结合当前速度和距离障碍物的最小距离作为 Prompt，输入 VLM。VLM 输出一个 [0, 1] 之间的风险标量 $r$。
*   **风险到参数的映射**：使用幂律函数将风险 $r$ 映射为 CBF 参数 $\alpha_{vlm}$。风险越高，$\alpha$ 越小（越保守）；风险越低，$\alpha$ 越大（越激进）。
*   **延迟感知与动态限幅（Dynamic Cap）**：
    *   **异步推理**：控制频率为 60Hz，而 VLM 更新频率约为 2Hz。
    *   **几何限幅**：设计了一个基于几何距离和当前速度的动态上限 $\alpha_{cap}$。即使 VLM 给出错误或过时的低风险评估，几何限幅也能确保机器人在靠近障碍物或高速行驶时保持保守。
    *   **融合策略**：引入“陈旧度门控”。如果 VLM 更新及时，则取 $\alpha_{vlm}$ 和软限幅的最小值；如果 VLM 更新超时（Stale），则回退到更保守的硬限幅模式。
*   **安全控制**：将调节后的 $\alpha$ 带入 CBF-QP（二次规划）问题，在满足安全约束的前提下，求解最接近名义控制指令（指向目标）的安全速度指令。

### 3. 实验设计
*   **实验环境**：使用 NVIDIA IsaacSim 仿真平台。
*   **场景设置**：
    *   **环境**：包括逼真的仓库（Warehouse）和简单的玩具场景（Toy）。
    *   **任务**：单障碍物阻挡、杂乱障碍物群、动态障碍物横穿、动态障碍物迎面移动。
*   **对比基准（Benchmarks）**：
    *   **固定参数法**：固定最小 $\alpha$（极度保守）和固定最大 $\alpha$（极度激进）。
    *   **消融实验**：AlphaAdj 无动态限幅版本（No-cap）。
*   **评估指标**：成功率、到达目标时间、路径长度、最小安全间距。

### 4. 资源与算力
*   **硬件配置**：AMD Ryzen Threadripper PRO 7955WX 处理器，NVIDIA RTX 6000 Ada GPU。
*   **模型优化**：
    *   使用 LLaVA-1.5 7B 模型，并进行了 **4-bit 量化**。
    *   通过本地 FastAPI 部署和输出格式限制（仅 JSON），将端到端延迟从 1482ms 降低至 **695ms**（优化率 53%）。
*   **运行状态**：VLM 与 IsaacSim 仿真器运行在不同的 GPU 资源上。

### 5. 实验数量与充分性
*   **实验规模**：在 2 种环境、4 种场景下进行了测试，每种配置重复运行 **25 次**，总计超过 200 组实验记录。
*   **充分性评价**：实验设计较为充分。涵盖了静态与动态、简单与复杂的对比；通过消融实验验证了动态限幅在应对延迟时的必要性；对比了固定参数的极端情况，客观展示了自适应调节在效率提升上的优势。

### 6. 主要结论与发现
*   **安全性**：AlphaAdj 在所有测试场景中均实现了 **100% 的成功率（零碰撞）**。
*   **效率提升**：相比于固定保守参数的方法，AlphaAdj 将路径长度缩短了约 **18.5%**，到达目标的时间缩短了约 **10.8%**。
*   **鲁棒性**：动态限幅机制成功解决了 VLM 异步推理带来的陈旧数据问题。在没有限幅的情况下，系统会出现碰撞，证明了纯语义驱动在实时控制中的风险。
*   **实时性**：系统能够维持 60Hz 的控制频率，无需像同步 VLM 方案那样停下来等待推理结果。

### 7. 优点
*   **跨层级融合**：成功将大模型的语义推理（高层）与控制理论的安全约束（底层）解耦并高效结合。
*   **延迟处理机制**：提出的“几何限幅 + 陈旧度门控”是解决大模型上车（机器人）延迟问题的务实且有效的方案。
*   **自适应性强**：能够根据场景复杂度自动切换“激进”与“保守”模式，比手动调参更具泛化性。

### 8. 不足与局限
*   **仿真局限**：所有实验均在 IsaacSim 中完成，尚未在真实物理机器人和复杂光照/传感器噪声环境下验证。
*   **风险定义模糊**：VLM 输出的风险值 $r$ 缺乏严格的概率学解释，更多是一种基于提示词的经验估计。
*   **环境多样性**：虽然有仓库场景，但测试的障碍物类型仍相对单一（主要是圆柱体或简单几何体），未涉及行人社交导航等更复杂的语义场景。

（完）
