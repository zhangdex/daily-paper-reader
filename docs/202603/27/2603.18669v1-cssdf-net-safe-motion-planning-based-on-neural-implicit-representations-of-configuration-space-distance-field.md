---
title: "CSSDF-Net: Safe Motion Planning Based on Neural Implicit Representations of Configuration Space Distance Field"
title_zh: CSSDF-Net：基于构型空间距离场神经隐式表示的安全运动规划
authors: "Haohua Chen, Yixuan Zhou, Yifan Zhou, Hesheng Wang"
date: 2026-03-19
pdf: "https://arxiv.org/pdf/2603.18669v1"
tags: ["query:robot-planning"]
score: 9.0
evidence: 基于神经隐式表示的安全运动规划
tldr: 针对高维机械臂在非结构化环境中的避障难题，本文提出CSSDF-Net，一种直接在配置空间（C-space）学习连续有符号距离场的神经隐式表示方法。该方法通过空间哈希数据生成流程实现零样本泛化，无需针对特定环境重训。CSSDF-Net提供可微的距离和梯度查询，统一处理自碰撞与环境碰撞，并成功集成于轨迹优化和MPC中，在静态及动态场景下表现出优异的避障性能和实时性。
selection_source: fresh_fetch
motivation: 现有的几何碰撞检测器不可微，且基于工作空间的隐式模型在映射到配置空间时存在非线性强、收敛性差及碰撞约束处理分散的问题。
method: 提出CSSDF-Net，利用空间哈希编码机器人几何先验，直接在配置空间学习统一的有符号距离场，并将其集成到安全约束轨迹优化和MPC框架中。
result: 在平面臂和7自由度机械臂上的实验表明，该方法能提供稳定的梯度，在未知动静态环境中实现高效避障，且推理延迟满足实时部署需求。
conclusion: CSSDF-Net通过在配置空间构建神经隐式距离场，为复杂环境下的机械臂安全运动规划提供了一种高效、可微且具备泛化能力的解决方案。
---

## 摘要
非结构化环境中的高维机械臂操作需要一种可微的、与场景无关的距离查询机制，以引导安全运动的生成。现有的几何碰撞检测器通常是不可微的，而基于工作空间的隐式距离模型受限于高度非线性的工作空间-构型映射，且往往面临收敛性差的问题；此外，自碰撞和环境碰撞通常被作为独立的约束处理。我们提出了构型空间有向距离场网络（CSSDF-Net），它直接在构型空间中学习连续的有向距离场，在统一的几何安全概念下提供关节空间的距离和梯度查询。为了实现无需针对特定环境重新训练的零样本泛化，我们引入了一种基于空间哈希的数据生成流水线，该流水线编码了以机器人为中心的几何先验，并支持针对任意障碍物点集的高效风险构型检索。学习到的距离场被集成到受安全约束的轨迹优化和收缩时界 MPC 中，从而实现离线规划和在线反应式避障。在平面机械臂和 7 自由度机械臂上的实验表明，该方法具有稳定的梯度、在静态和动态场景中有效的避障能力，以及针对大规模点云查询的实用推理延迟，支持在先前未见过的环境中进行部署。

## Abstract
High-dimensional manipulator operation in unstructured environments requires a differentiable, scene-agnostic distance query mechanism to guide safe motion generation. Existing geometric collision checkers are typically non-differentiable, while workspace-based implicit distance models are hindered by the highly nonlinear workspace--configuration mapping and often suffer from poor convergence; moreover, self-collision and environment collision are commonly handled as separate constraints. We propose Configuration-Space Signed Distance Field-Net (CSSDF-Net), which learns a continuous signed distance field directly in configuration space to provide joint-space distance and gradient queries under a unified geometric notion of safety. To enable zero-shot generalization without environment-specific retraining, we introduce a spatial-hashing-based data generation pipeline that encodes robot-centric geometric priors and supports efficient retrieval of risk configurations for arbitrary obstacle point sets. The learned distance field is integrated into safety-constrained trajectory optimization and receding-horizon MPC, enabling both offline planning and online reactive avoidance. Experiments on a planar arm and a 7-DoF manipulator demonstrate stable gradients, effective collision avoidance in static and dynamic scenes, and practical inference latency for large-scale point-cloud queries, supporting deployment in previously unseen environments.

---

## 论文详细总结（自动生成）

以下是对论文《CSSDF-Net: Safe Motion Planning Based on Neural Implicit Representations of Configuration Space Distance Field》的结构化深度总结：

### 1. 核心问题与整体含义（研究动机和背景）
在高维机械臂的运动规划中，如何在非结构化和动态环境中生成既安全又平滑的轨迹是一个核心挑战。
*   **现有痛点**：传统的几何碰撞检测器（如 GJK）通常不可微，难以提供引导优化算法脱离不安全区域的梯度信息；而基于工作空间（Workspace）的隐式距离模型在映射到构型空间（C-space）时，由于高度非线性的逆运动学投影，容易陷入局部最优。
*   **研究目标**：提出一种直接在构型空间学习连续有符号距离场（SDF）的神经隐式表示方法，统一处理自碰撞与环境碰撞，并提供高效、可微的距离与梯度查询，以支持实时避障和轨迹优化。

### 2. 方法论：核心思想与关键技术
论文提出了 **CSSDF-Net** 框架，其核心组成部分包括：
*   **统一的安全表示**：将自碰撞视为一种特殊的外部碰撞（障碍物点在可达空间外），通过一个复合有符号距离函数 $d(C, q)$ 统一描述。
*   **网络架构**：采用 5 层 MLP（带残差连接），输入为机械臂构型 $q$ 和障碍物点 $p$ 的拼接向量，输出为该点相对于机械臂的 SDF 值。
*   **空间哈希数据生成流水线**：
    *   **离线阶段**：利用空间哈希（Spatial Hashing）建立工作空间体素与风险构型的关联映射，编码机器人自身的几何先验。
    *   **在线/训练阶段**：通过该映射快速检索任意障碍物点集对应的冲突构型，实现“场景无关”的训练，从而支持零样本（Zero-shot）泛化到新环境。
*   **复合损失函数**：结合了 MSE 回归损失、Eikonal 正则化损失（强制梯度模长为 1）和梯度方向一致性损失（提高下游优化稳定性）。
*   **规划集成**：将学习到的 SDF 及其梯度集成到基于二次规划（QP）的收缩时界控制（MPC）和基于样条的轨迹优化框架中。

### 3. 实验设计
实验涵盖了从简单到复杂的多个场景：
*   **实验场景**：
    1.  **2D 平面臂（2-DoF）**：用于直观验证梯度引导的有效性。
    2.  **7 自由度机械臂（7-DoF）**：验证高维空间下的性能，包括静态障碍物和人体驱动的动态障碍物。
*   **Benchmark 与对比方法**：
    *   **采样类算法**：RRT, RRT-Connect, RRT*, Informed RRT*, BiEST, KPIECE 等。
    *   **优化类算法**：CHOMP, STOMP, TrajOpt。
*   **评估指标**：碰撞风险（CR）、轨迹长度（TL）、平均耗时（AT）、推理延迟、梯度相似度等。

### 4. 资源与算力
*   **硬件环境**：实验在一台配备 **NVIDIA RTX 3060 GPU** 的计算机上运行。
*   **训练细节**：使用了 $1 \times 10^4$ 个样本进行训练，按 8:2 划分训练集和验证集。
*   **推理性能**：在 GPU 上，针对 $10^4$ 个点的距离+梯度查询延迟约为 **4.302 ms**；在 CPU 上处理 $10^3$ 个点约需 **5.846 ms**，满足实时性要求。

### 5. 实验数量与充分性
*   **实验规模**：论文进行了 2D 仿真、7-DoF 仿真以及真实机械臂实验。
*   **消融实验**：针对“数据集构建策略”和“损失函数组成”进行了详细的消融研究，验证了边界挖掘（Boundary Mining）和方向损失对提升模型精度的重要性。
*   **客观性**：对比了超过 10 种主流路径规划算法，涵盖了采样和优化两大类，实验对比相对全面、客观。

### 6. 主要结论与发现
*   **高效性**：CSSDF-Net 提供的 C-space 梯度允许机械臂通过单步线性迭代逃离碰撞区，效率高于需多次逆运动学投影的工作空间方法。
*   **泛化性**：通过空间哈希编码的机器人先验，模型无需针对新环境重新训练即可直接部署。
*   **性能优异**：在 7-DoF 任务中，CSSDF-Net 引导的优化算法在保持极低碰撞率（0.561%）的同时，生成的轨迹长度显著短于纯采样算法（缩短约 50%），且计算耗时远低于 CHOMP 和 STOMP。

### 7. 优点与亮点
*   **直接 C-space 建模**：避开了复杂的逆运动学投影，解决了工作空间 SDF 易导致局部极小值的问题。
*   **零样本泛化**：空间哈希机制巧妙地解耦了机器人几何与环境信息，极大地提升了算法的实用性。
*   **统一性**：一个网络同时处理自碰撞和环境碰撞，简化了运动规划系统的约束处理逻辑。
*   **数学严谨性**：引入 Eikonal 损失确保了 SDF 的几何特性，提供了高质量的连续梯度。

### 8. 不足与局限
*   **时空建模缺失**：目前模型主要基于空间位置，未显式考虑时间维度（4D），在处理极高速动态障碍物时可能存在滞后。
*   **感知鲁棒性**：实验主要基于点云输入，对传感器噪声、遮挡或标定误差的容忍度尚未进行深入的量化压力测试。
*   **多机协作限制**：目前侧重于单臂避障，尚未扩展到多机器人共享空间的协同避障场景。

（完）
