---
title: Planning over MAPF Agent Dependencies via Multi-Dependency PIBT
title_zh: 通过多重依赖 PIBT 在 MAPF 智能体依赖关系上进行规划
authors: "Zixiang Jiang, Yulun Zhang, Rishi Veerapaneni, Jiaoyang Li"
date: 2026-03-24
pdf: "https://arxiv.org/pdf/2603.23405v1"
tags: ["query:robot-planning"]
score: 9.0
evidence: 多智能体路径规划算法
tldr: 针对大规模多智能体路径规划（MAPF）中PIBT算法受限于单一冲突处理的局限性，本文提出MD-PIBT框架。该框架通过定义智能体依赖关系，允许在多个依赖项上进行搜索，不仅涵盖了PIBT和EPIBT，还支持更复杂的规划策略。实验证明其能高效处理万级规模智能体及多种动力学约束，在大型智能体场景下表现尤为出色。
selection_source: fresh_fetch
motivation: 现有的PIBT及其扩展算法在处理智能体冲突时搜索范围有限，难以应对复杂的依赖场景。
method: 提出MD-PIBT框架，通过在智能体依赖关系上进行搜索，实现了比传统PIBT更具通用性的规划策略。
result: "实验证明该算法能为多达10,000个具有多种动力学约束的智能体提供高效规划，尤其在大型智能体场景中表现优异。"
conclusion: MD-PIBT是一个通用的MAPF规划框架，有效提升了大规模复杂环境下多智能体路径规划的效率和通用性。
---

## 摘要
现代多智能体路径规划（MAPF）算法必须在秒级时间内为拥挤环境中的数百到数千个智能体进行规划，这需要极高效率的算法。带有回溯的优先级继承（PIBT）是一种流行的算法，能够在这种情况下进行有效规划。然而，PIBT 受限于其基于规则的规划过程，且缺乏通用性，因为它将搜索限制在最多与一个其他智能体发生冲突的路径上。这一局限性同样适用于增强型 PIBT（EPIBT），即 PIBT 最近的一个扩展版本。在本文中，我们提出了一种通过在智能体依赖关系上进行规划来解决 MAPF 的新视角。受 PIBT 优先级继承逻辑的启发，我们定义了智能体依赖关系的概念，并提出了在智能体依赖关系上进行搜索的多重依赖 PIBT（MD-PIBT）。MD-PIBT 是一个通用框架，通过特定的参数化可以重现 PIBT 和 EPIBT。同时，其他配置可以产生 PIBT 或 EPIBT 无法表达的新型规划策略。我们的实验表明，MD-PIBT 在各种动力学约束下（包括卵石运动、旋转运动以及具有速度和加速度限制的差分驱动机器人），能够有效地为多达 10,000 个同质智能体进行规划。我们对 MAPF 的不同变体进行了全面评估，发现 MD-PIBT 在具有大型智能体的 MAPF 中特别有效。

## Abstract
Modern Multi-Agent Path Finding (MAPF) algorithms must plan for hundreds to thousands of agents in congested environments within a second, requiring highly efficient algorithms. Priority Inheritance with Backtracking (PIBT) is a popular algorithm capable of effectively planning in such situations. However, PIBT is constrained by its rule-based planning procedure and lacks generality because it restricts its search to paths that conflict with at most one other agent. This limitation also applies to Enhanced PIBT (EPIBT), a recent extension of PIBT. In this paper, we describe a new perspective on solving MAPF by planning over agent dependencies. Taking inspiration from PIBT's priority inheritance logic, we define the concept of agent dependencies and propose Multi-Dependency PIBT (MD-PIBT) that searches over agent dependencies. MD-PIBT is a general framework where specific parameterizations can reproduce PIBT and EPIBT. At the same time, alternative configurations yield novel planning strategies that are not expressible by PIBT or EPIBT. Our experiments demonstrate that MD-PIBT effectively plans for as many as 10,000 homogeneous agents under various kinodynamic constraints, including pebble motion, rotation motion, and differential drive robots with speed and acceleration limits. We perform thorough evaluations on different variants of MAPF and find that MD-PIBT is particularly effective in MAPF with large agents.

---

## 论文详细总结（自动生成）

### 论文结构化总结：通过多重依赖 PIBT 在 MAPF 智能体依赖关系上进行规划

#### 1. 核心问题与整体含义（研究动机和背景）
*   **核心问题**：在大规模多智能体路径规划（MAPF）中，现有的高效算法 **PIBT**（优先级继承回溯算法）及其变体（如 EPIBT）存在局限性。它们在规划时仅能处理**单一依赖关系**，即一个高优先级智能体的路径最多只能与一个低优先级智能体发生冲突。
*   **研究动机**：在现实场景（如自动化仓库、大型机器人协作）中，智能体可能体积较大或环境极度拥挤，一个动作往往会同时干扰多个智能体。现有的 PIBT 框架无法直接处理这种“多重依赖”，导致在复杂动力学约束或大型智能体场景下规划效率低下或容易陷入死锁。
*   **整体含义**：本文提出了 **MD-PIBT** 框架，通过将 MAPF 问题重新定义为在“智能体依赖图”（AgDG）上的搜索，打破了单一冲突的限制，实现了更具通用性和扩展性的多步规划。

#### 2. 方法论：核心思想与算法流程
*   **核心思想**：引入**智能体依赖（Agent Dependency）**概念，将规划过程视为构建和解析一个有向依赖图。
*   **关键技术细节**：
    *   **安全路径（Safe Path）**：每个智能体持有一个保证无冲突的备选路径（如原地等待）。
    *   **硬依赖（Hard Dependency）**：当智能体 $a_i$ 的拟选路径与未规划智能体 $a_j$ 的安全路径冲突时， $a_j$ 必须被规划以腾出空间。
    *   **软依赖（Soft Dependency）**：当拟选路径与已规划智能体的安全路径冲突时，记录潜在冲突，以便在回溯时转换。
    *   **依赖图（AgDG）**：通过维护一个有向图来管理这些依赖，允许一个智能体同时依赖多个下层智能体。
*   **算法流程**：
    1.  按优先级顺序为智能体选择 $w$ 步路径。
    2.  若路径与多个智能体的安全路径冲突，将这些智能体加入依赖图并放入待规划队列（实现优先级继承）。
    3.  **回溯机制**：若某个智能体无法找到可行路径，则触发“回退至安全路径”或“请求父节点重新规划”。
    4.  通过超参数 $C$ 控制允许的最大冲突数量，通过 $R$ 限制重试次数以保证运行时间。

#### 3. 实验设计
*   **实验场景**：
    *   **One-shot MAPF**：单次起终点规划，关注成功率和运行时间。
    *   **Lifelong MAPF**：持续任务分配，关注吞吐量（Throughput）。
*   **智能体模型**：
    *   **PM**（卵石运动）：标准网格移动。
    *   **PMLA**（大型智能体）：不同尺寸的智能体（如 $1\times1$ 和 $5\times5$ 混合）。
    *   **RM**（旋转运动）：考虑原地旋转。
    *   **DDR**（差分驱动机器人）：考虑速度、加速度等连续动力学约束。
*   **Benchmark 与对比方法**：
    *   使用 MAPF 标准测试集（random, warehouse, maze, room, Paris 等地图）。
    *   **对比基准**：PIBT、EPIBT（增强型 PIBT）。

#### 4. 资源与算力
*   **硬件设备**：
    *   **One-shot 实验**：本地机器，AMD Ryzen 3990X (64核), 192GB RAM。
    *   **Lifelong 实验**：多台机器，包括 AMD Ryzen 7980X (256GB RAM) 以及高性能计算集群（HPC）使用的 AMD EPYC 7742 节点。
*   **训练/运行时间**：算法为在线规划，非深度学习训练。One-shot 设置了 1800s-3600s 的时限；Lifelong 模拟 1000 个时间步，每步限时 1s。

#### 5. 实验数量与充分性
*   **实验规模**：
    *   针对不同地图和智能体数量（最高达 **10,000** 个智能体）进行了大量重复实验（每组 10-25 次实例）。
    *   进行了详尽的**超参数消融实验**，涵盖了窗口大小 $w$、执行步数 $h$、冲突限制 $C$、重试限制 $R$ 等。
*   **充分性评价**：实验设计非常充分且客观。作者不仅在标准 Benchmark 上验证了性能，还专门设计了 PMLA 场景来展示 MD-PIBT 处理多重依赖的独特优势。通过对比不同动力学模型，证明了框架的通用性。

#### 6. 主要结论与发现
*   **大型智能体场景的突破**：在 PMLA（大型智能体）任务中，MD-PIBT 显著优于 PIBT 和 EPIBT。因为大型智能体必然会同时与多个小智能体冲突，MD-PIBT 的多重依赖处理能力是解决此类死锁的关键。
*   **通用性验证**：MD-PIBT 是一个通用框架，通过调整参数可以完美复现 PIBT 和 EPIBT 的行为。
*   **标准场景表现**：在标准 PM 或 RM 模型下，MD-PIBT 的表现与最先进的 PIBT/EPIBT 持平，证明其在提升复杂场景处理能力的同时，并未牺牲基础场景的效率。
*   **可扩展性**：成功在 1 秒内为 10,000 个差分驱动机器人生成了可行路径。

#### 7. 优点
*   **理论完备性**：通过定义智能体依赖图，为优先级规划提供了一个更严谨、通用的数学视角。
*   **灵活性**：支持多种动力学约束和超参数配置，能够适应从简单网格到复杂物理机器人的多种需求。
*   **解决死锁能力强**：在拥挤环境和异构尺寸智能体场景下，比传统方法更稳健。

#### 8. 不足与局限
*   **计算开销**：由于需要维护和操作复杂的依赖图，MD-PIBT 的运行时间通常略高于极简的 PIBT（尽管仍在秒级以内）。
*   **超参数敏感性**：性能高度依赖于 $w, C, R$ 等参数的选择，不同地图的最佳配置差异较大，可能需要针对特定应用进行调优。
*   **路径质量**：作为一种基于规则和局部搜索的方法，其路径的社会最优性（Sum-of-costs）仍有提升空间，有时会略逊于全局搜索算法。

（完）
