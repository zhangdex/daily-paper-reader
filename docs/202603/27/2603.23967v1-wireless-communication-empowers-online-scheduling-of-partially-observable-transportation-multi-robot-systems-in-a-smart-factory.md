---
title: Wireless communication empowers online scheduling of partially-observable transportation multi-robot systems in a smart factory
title_zh: 无线通信赋能智能工厂中部分可观测运输多机器人系统的在线调度
authors: "Yaxin Liao, Qimei Cui, Kwang-Cheng Chen, Xiong Li, Jinlian Chen, Xiyu Zhao, Xiaofeng Tao, Ping Zhang"
date: 2026-03-25
pdf: "https://arxiv.org/pdf/2603.23967v1"
tags: ["query:robot-planning"]
score: 9.0
evidence: 在线多机器人任务分配与调度
tldr: "针对智能工厂中多机器人系统在部分可观测环境下的在线调度难题，本文提出一种结合无线M2M通信与路径规划的新型框架。通过让AGV交换意图信息（如计划路径）来克服观测限制，并采用免重传多链路传输、模拟退火MRTA及拥塞感知A*算法。实验证明该方案在提高调度效率的同时降低了计算开销，揭示了面向调度的无线通信设计在智能工厂中的独特价值。"
selection_source: fresh_fetch
motivation: 智能工厂中动态环境和部分可观测性使得多机器人系统的实时无碰撞调度面临巨大挑战。
method: "提出一种将无线M2M通信与路径调度耦合的框架，通过交换AGV意图信息并结合模拟退火与拥塞感知A*算法进行在线优化。"
result: 实验表明，该集成方案在有限信道资源和高负载条件下，显著提升了调度效率并降低了计算开销。
conclusion: 面向调度的无线通信设计与传统人际通信有本质区别，为无线联网智能工厂提供了新的技术机遇。
---

## 摘要
在智能工厂中实现敏捷且可重构的生产流依赖于在线多机器人任务分配（MRTA），这需要对运输多机器人系统（T-MRS，如协作式自动导引车 AGV）进行在线无碰撞且无拥塞的路径调度。由于实时运行需求以及 T-MRS 与生产 MRS 之间的动态交互，在动态工厂环境下部分可观测条件下的在线调度仍然是一个重大且尚未得到充分探索的挑战。本文提出了一种新型的通信赋能在线调度框架，该框架将无线机器对机器（M2M）组网与路径调度显式耦合，使 AGV 能够交换意图信息（如规划路径），从而克服部分观测限制并辅助在线调度的复杂计算。具体而言，我们将智能 AGV 的意图和传感器数据确定为新的 M2M 流量，并定制了无重传多链路传输网络，以满足实时运行需求。这种面向调度的组网随后与基于模拟退火的 MRTA 方案和基于拥塞感知的 A* 路径调度方法相结合。该通信与调度集成方案允许 AGV 以较低的计算开销动态调整无碰撞且无拥塞的路径。数值实验展示了无线通信对 T-MRS 性能的影响，并表明即使在高 AGV 负载条件和有限信道资源下，所提出的集成方案与其它基准方案相比也能显著提高调度效率。此外，结果表明，面向调度的无线 M2M 通信设计与人与人之间的通信有本质区别，这暗示了无线网络化智能工厂中的新技术机遇。

## Abstract
Achieving agile and reconfigurable production flows in smart factories depends on online multi-robot task assignment (MRTA), which requires online collision-free and congestion-free route scheduling of transportation multi-robot systems (T-MRS), e.g., collaborative automatic guided vehicles (AGVs). Due to the real-time operational requirements and dynamic interactions between T-MRS and production MRS, online scheduling under partial observability in dynamic factory environments remains a significant and under-explored challenge. This paper proposes a novel communication-enabled online scheduling framework that explicitly couples wireless machine-to-machine (M2M) networking with route scheduling, enabling AGVs to exchange intention information, e.g., planned routes, to overcome partial observations and assist complex computation of online scheduling. Specifically, we determine intelligent AGVs' intention and sensor data as new M2M traffic and tailor the retransmission-free multi-link transmission networking to meet real-time operation demands. This scheduling-oriented networking is then integrated with a simulated annealing-based MRTA scheme and a congestion-aware A*-based route scheduling method. The integrated communication and scheduling scheme allows AGVs to dynamically adjust collision-free and congestion-free routes with reduced computational overhead. Numerical experiments shows the impacts from wireless communication on the performance of T-MRS and suggest that the proposed integrated scheme significantly enhances scheduling efficiency compared to other baselines, even under high AGV load conditions and limited channel resources. Moreover, the results reveal that the scheduling-oriented wireless M2M communication design fundamentally differs from human-to-human communications, implying new technological opportunities in a wireless networked smart factory.

---

## 论文详细总结（自动生成）

### 论文总结：无线通信赋能智能工厂中部分可观测运输多机器人系统的在线调度

#### 1. 核心问题与整体含义（研究动机和背景）
在智能工厂中，实现敏捷生产依赖于自动导引车（AGV）等运输多机器人系统（T-MRS）的实时调度。然而，现有的调度面临三大挑战：
*   **部分可观测性**：AGV 仅能通过车载传感器感知局部环境，无法获知远端拥塞或他机意图。
*   **动态交互性**：生产任务与运输任务高度耦合，环境动态变化剧烈。
*   **通信与调度脱节**：传统研究要么假设通信完美，要么仅优化通信指标（如吞吐量），忽略了通信对下游调度任务（如避障、减缓拥塞）的直接贡献。
**研究目标**：提出一个将无线机器对机器（M2M）组网与路径调度显式耦合的框架，通过交换“意图信息”来克服观测限制。

#### 2. 方法论：核心思想与关键技术
论文提出了一个**通信赋能的在线调度框架**，主要包含以下核心组件：
*   **核心思想**：将 AGV 的规划路径（意图）和传感器数据定义为新型 M2M 流量，通过无线通信实现全局信息的融合，从而将分布式决策提升至接近全局最优的水平。
*   **任务分配（MRTA）**：采用基于**大邻域搜索（LNS）的模拟退火（SA）算法**。通过不断“破坏”和“修复”任务分配方案，在满足生产优先级约束的前提下最小化最大完工时间（Makespan）。
*   **路径调度（MRRSP）**：提出**拥塞感知 A* 算法**。利用边缘服务器生成的“时空拥塞图”，在 A* 搜索的代价函数中加入拥塞惩罚项，引导 AGV 主动绕开高密度区域。
*   **无线组网设计**：针对实时性要求，设计了**免重传的多链路传输方案**。AGV 在多个正交信道上同步发送数据包副本，利用频率分集抵御碰撞和干扰，确保低延迟下的信息交换。

#### 3. 实验设计
*   **场景设置**：10×10 的网格工厂环境，包含 60 条生产线，每条线 4 个任务。AGV 数量从 2 扩展到 100 台。
*   **对比基准（Benchmarks）**：
    1.  **无控调度（Uncontrolled）**：不考虑碰撞和拥塞。
    2.  **局部推理调度（Local reasoning-based）**：无通信，仅靠局部传感器避障。
    3.  **理想通信调度**：假设通信无损、无延迟。
    4.  **带误差的通信调度**：考虑不同链路错误率（$\sigma=0.1, 0.3$）。
*   **评估指标**：最大完工时间（Makespan）、通信吞吐量、传输成功率。

#### 4. 资源与算力
*   **硬件环境**：Intel® Xeon® Platinum 8369B CPU。
*   **软件环境**：Python 3.8。
*   **算力说明**：文中未提及 GPU 使用情况，因为该研究主要基于启发式搜索和算法优化，而非深度学习训练。计算复杂度分析显示，单次路径规划在毫秒级完成，满足在线实时性要求。

#### 5. 实验数量与充分性
*   **实验规模**：针对 AGV 数量（K）、通信间隔（D）、选择信道数（S）、可用信道总数（C）等多个维度进行了参数扫描实验。
*   **充分性**：每组实验重复 5 次并取平均值以消除随机性。实验覆盖了从低负载到极高负载（100 台 AGV）的场景，并对比了理想与非理想通信环境，实验设计较为客观、全面。

#### 6. 主要结论与发现
*   **效率提升**：在 30 台以上 AGV 的规模下，通信赋能方案比局部推理方案效率提升了 **54.42%**。
*   **通信与调度解耦的局限**：实验发现，使通信吞吐量最大化的配置（如通信间隔 D）并不一定是使调度效率最优的配置。
*   **多链路优势**：在 AGV 密度适中时，多链路传输能显著提升可靠性；但在极高密度下，过多的链路副本反而会加剧信道碰撞。
*   **可扩展性**：无线通信有效缓解了部分可观测性带来的“死锁”和“盲目等待”问题，显著增强了 MRS 的系统容量。

#### 7. 优点
*   **跨层设计**：打破了通信与控制的壁垒，将通信质量直接映射到工业生产指标（Makespan）上。
*   **实时性强**：采用免重传机制和高效的 A* 变体，适合动态变化的工厂环境。
*   **意图交换概念**：通过交换“未来路径”而非仅仅是“当前位置”，实现了前瞻性的拥塞规避。

#### 8. 不足与局限
*   **环境规模有限**：实验主要在 10×10 的小型网格中进行，更大规模（如 100×100）的复杂工厂环境下的计算开销和通信延迟表现有待验证。
*   **避障规则较简单**：冲突解决依赖于预设的优先级规则（如北向优先），在极端复杂的对峙场景下可能不是最优解。
*   **无线模型简化**：虽然考虑了碰撞和随机错误，但对多径衰落、阴影遮挡等复杂无线信道特性的建模相对简化。

（完）
