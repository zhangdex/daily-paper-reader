---
title: Learning-guided Prioritized Planning for Lifelong Multi-Agent Path Finding in Warehouse Automation
title_zh: 仓储自动化中终身多智能体路径规划的学习引导优先规划
authors: "Han Zheng, Yining Ma, Brandon Araki, Jingkai Chen, Cathy Wu"
date: 2026-03-25
pdf: "https://arxiv.org/pdf/2603.23838v1"
tags: ["query:robot-planning"]
score: 9.0
evidence: 强化学习引导的多智能体导航优先规划
tldr: 针对现代仓库自动化中的终身多智能体路径规划（Lifelong MAPF）挑战，本文提出了一种结合强化学习与搜索规划的框架 RL-RH-PP。该方法利用强化学习动态分配智能体优先级，并结合滚动窗口优先级规划（PP）进行路径搜索。通过注意力机制神经网络自动解码优先级顺序，RL-RH-PP 在复杂仓库环境下实现了更高的吞吐量和更强的泛化能力，有效缓解了交通拥堵。
selection_source: fresh_fetch
motivation: 传统的搜索算法在处理终身 MAPF 的长期动态性时成本较高，而纯机器学习方法在性能上尚无定论，因此需要结合两者的优势。
method: 提出 RL-RH-PP 框架，将动态优先级分配建模为 POMDP，并利用基于注意力机制的神经网络引导搜索算法进行顺序单智能体规划。
result: 在真实仓库模拟中，该方法在总吞吐量上超越了现有基准，并在不同智能体密度、规划时界和地图布局下表现出良好的泛化性。
conclusion: 学习引导的方法能够有效增强传统启发式算法，通过主动管理拥堵和优化流量分配，显著提升大规模仓库自动化的运行效率。
---

## 摘要
终身多智能体路径规划（Lifelong MAPF）对于现代仓储自动化至关重要，它要求多个机器人持续规划无冲突路径，以优化整体系统吞吐量。然而，仓库环境的复杂性和终身 MAPF 的长期动态特性通常需要对经典的基于搜索的求解器进行复杂的调整。虽然机器学习方法已得到探索，但其相对于基于搜索的方法的优越性仍无定论。在本文中，我们提出了强化学习（RL）引导的滚动时域优先规划（RL-RH-PP），这是首个将 RL 与基于搜索的规划相结合用于终身 MAPF 的框架。具体而言，我们利用经典的优先规划（PP）作为骨干，因其在与基于学习的优先级分配策略集成时具有简单性和灵活性。通过将动态优先级分配建模为部分可观测马尔可夫决策过程（POMDP），RL-RH-PP 利用了终身规划的序列决策特性，同时将智能体之间复杂的时空交互交给强化学习处理。一个基于注意力的神经网络实时自回归地解码优先级顺序，使 PP 规划器能够进行高效的序列单智能体规划。在真实仓库模拟中的评估表明，RL-RH-PP 在基准方法中实现了最高的总吞吐量，并能有效地泛化到不同的智能体密度、规划时域和仓库布局。我们的解释性分析表明，RL-RH-PP 主动为拥堵的智能体分配优先级，并战略性地引导智能体绕开拥堵区域，从而缓解交通流并提升吞吐量。这些发现突显了学习引导方法在增强现代仓储自动化中传统启发式算法方面的潜力。

## Abstract
Lifelong Multi-Agent Path Finding (MAPF) is critical for modern warehouse automation, which requires multiple robots to continuously navigate conflict-free paths to optimize the overall system throughput. However, the complexity of warehouse environments and the long-term dynamics of lifelong MAPF often demand costly adaptations to classical search-based solvers. While machine learning methods have been explored, their superiority over search-based methods remains inconclusive. In this paper, we introduce Reinforcement Learning (RL) guided Rolling Horizon Prioritized Planning (RL-RH-PP), the first framework integrating RL with search-based planning for lifelong MAPF. Specifically, we leverage classical Prioritized Planning (PP) as a backbone for its simplicity and flexibility in integrating with a learning-based priority assignment policy. By formulating dynamic priority assignment as a Partially Observable Markov Decision Process (POMDP), RL-RH-PP exploits the sequential decision-making nature of lifelong planning while delegating complex spatial-temporal interactions among agents to reinforcement learning. An attention-based neural network autoregressively decodes priority orders on-the-fly, enabling efficient sequential single-agent planning by the PP planner. Evaluations in realistic warehouse simulations show that RL-RH-PP achieves the highest total throughput among baselines and generalizes effectively across agent densities, planning horizons, and warehouse layouts. Our interpretive analyses reveal that RL-RH-PP proactively prioritizes congested agents and strategically redirects agents from congestion, easing traffic flow and boosting throughput. These findings highlight the potential of learning-guided approaches to augment traditional heuristics in modern warehouse automation.

---

## 论文详细总结（自动生成）

这篇论文介绍了一种名为 **RL-RH-PP** 的创新框架，旨在解决现代仓储自动化中的终身多智能体路径规划（Lifelong MAPF）难题。以下是对该论文的结构化总结：

### 1. 核心问题与整体含义（研究动机和背景）
*   **核心问题**：在自动化仓库中，成百上千个机器人需要持续不断地处理新任务（终身规划），同时避免碰撞并最大化系统吞吐量。
*   **研究背景**：传统的基于搜索的方法（如 CBS、PBS）在处理大规模、长时域动态环境时计算成本极高；而现有的纯机器学习方法在性能上往往难以稳定超越经过优化的搜索算法。
*   **研究动机**：利用强化学习（RL）处理复杂的时空交互和序列决策，同时保留优先规划（PP）算法的高效性，实现“学习引导搜索”的混合架构。

### 2. 方法论：核心思想与技术细节
*   **核心思想**：将终身 MAPF 建模为**部分可观测马尔可夫决策过程（POMDP）**，由 RL 策略动态生成智能体的全局优先级顺序，再由滚动时域优先规划器（RH-PP）根据该顺序计算无冲突路径。
*   **关键技术**：
    *   **Transformer 架构编码器**：采用交替的**时间注意力（Temporal Attention）**和**空间注意力（Spatial Attention）**机制。时间注意力捕捉单个智能体轨迹的长期依赖，空间注意力建模智能体间的动态交互。
    *   **自回归解码器**：基于编码后的智能体嵌入，自回归地生成总优先级顺序（Total Priority Order）。
    *   **Top-K 采样与修复机制**：RL 策略并行采样 $K$ 个候选优先级顺序，通过启发式函数筛选最优者，并结合冲突修复机制确保路径的绝对安全。
    *   **奖励函数设计**：综合考虑了目标距离、拥堵惩罚（是否被迫等待）和规划可行性惩罚。

### 3. 实验设计
*   **实验场景**：
    *   **Amazon 履行中心地图**：中等障碍物密度（15.3%）。
    *   **Symbotic 仓库地图**：高障碍物密度（56.6%），包含狭窄通道和瓶颈区域（本文首次引入该真实工业布局）。
*   **基准方法（Benchmarks）**：
    *   基于搜索的方法：RH-CBS、RH-PBS。
    *   规则/启发式方法：PIBT（分布式）、WPPL（2023 机器人竞赛冠军方案）、基础 RH-PP。
*   **对比指标**：每智能体平均吞吐量（TPA）、总吞吐量、求解时间。

### 4. 资源与算力
*   **硬件设备**：
    *   **GPU**：单张 NVIDIA RTX 6000 Ada。
    *   **CPU**：AMD Ryzen Threadripper PRO 5995WX（128 核，用于并行环境采样）。
*   **训练时长**：在智能体数量 $N=120$ 的最复杂设置下，训练大约需要 **4 天**。
*   **推理效率**：单次规划步骤的 GPU 推理约 32ms，CPU 评估约 46ms（$N=100$ 时）。

### 5. 实验数量与充分性
*   **实验规模**：
    *   涵盖了不同智能体密度（$N=40$ 到 $140$）。
    *   测试了不同规划时界（$w=5$ 到 $30$）。
    *   进行了 **5 种不同仓库布局**的零冲（Zero-shot）泛化实验。
    *   **消融实验**：针对奖励权重、编码器组件（时/空注意力）、优先级启发式策略等进行了详细对比。
*   **评价**：实验设计非常充分且客观。通过引入从未见过的地图变体和极端拥堵场景，验证了算法的鲁棒性和泛化能力。

### 6. 主要结论与发现
*   **性能领先**：RL-RH-PP 在所有测试场景中均实现了最高或接近最高的吞吐量，平均比随机优先级的 RH-PP 提升了 **25%**。
*   **拥堵管理**：解释性分析显示，RL 策略学会了给拥堵区域的智能体分配高优先级，并引导边缘智能体“主动后退”以腾出通道，这种行为在传统启发式中很难实现。
*   **强泛化性**：在固定地图尺寸下，模型无需重新训练即可直接应用于不同的机器人数量和规划步长。

### 7. 优点与亮点
*   **混合架构**：巧妙结合了深度学习的决策能力和搜索算法的安全性保障。
*   **工业相关性**：使用了 Symbotic 真实的仓库布局，研究结果对实际物流自动化具有高度参考价值。
*   **可解释性**：通过优先级热力图和智能体轨迹分析，直观展示了 RL 如何通过“回溯”和“让路”解决死锁。

### 8. 不足与局限
*   **地图尺寸限制**：由于编码器使用了绝对位置嵌入（Position Embedding），模型目前只能在训练时定义的地图尺寸内泛化，无法直接迁移到更大或更小的地图。
*   **计算瓶颈**：虽然推理很快，但在 Top-K 采样较大时，CPU 端的路径评估是串行的，限制了实时性（未来需多线程优化）。
*   **训练成本**：4 天的训练时间对于快速迭代部署仍有一定门槛。

（完）
