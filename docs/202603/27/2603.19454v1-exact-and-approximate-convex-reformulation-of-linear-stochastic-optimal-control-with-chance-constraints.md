---
title: Exact and Approximate Convex Reformulation of Linear Stochastic Optimal Control with Chance Constraints
title_zh: 具有机会约束的线性随机最优控制的精确与近似凸重构
authors: "Tanmay Dokania, Yashwanth Kumar Nakka"
date: 2026-03-19
pdf: "https://arxiv.org/pdf/2603.19454v1"
tags: ["query:ra-rl-pe"]
score: 9.0
evidence: 受限最优控制与轨迹生成
tldr: 本研究针对具有机会约束的离散时间随机线性系统，提出了一种等效的凸优化表述方法。通过提升状态向量以显式编码矩信息，该方法能够精确处理多步线性机会约束，消除了传统方法的保守性，显著提升了可行性与最优性。对于二次机会约束，研究推导出了更紧致的凸近似。实验证明，该框架在无人机轨迹生成任务中，即使在噪声水平超过现有方法一个数量级的情况下，依然能保持可行。
selection_source: fresh_fetch
motivation: 旨在解决随机线性控制系统中机会约束处理过于保守、导致可行域缩小和性能下降的问题。
method: 通过提升状态向量来显式编码矩信息，将线性机会约束转化为精确的凸优化形式，并为二次约束提供紧致的凸松弛。
result: 在四旋翼无人机轨迹生成实验中，该方法在噪声水平比现有技术高出一个数量级时仍能成功求解。
conclusion: 该研究通过精确的凸重构显著提升了随机最优控制的鲁棒性，为处理复杂随机约束提供了更优的数学框架。
---

## 摘要
本文针对受线性机会约束的离散时间随机线性系统，提出了一种等价的凸优化公式，并针对二次机会约束提出了一种紧致的凸松弛方法。通过提升状态向量以显式编码矩信息，该公式能够精确地捕捉多个时间步长内状态和控制的线性机会约束，且不具保守性，从而在可行性和最优性方面实现了显著提升。对于二次机会约束，我们推导出了在证明上比现有方法保守性更低的凸近似。我们在四旋翼无人机的最小捕捉（minimum-snap）轨迹生成任务上验证了该框架，结果表明，在噪声水平超出先前公式运行范围一个数量级的情况下，所提方法仍然可行。

## Abstract
In this paper, we present an equivalent convex optimization formulation for discrete-time stochastic linear systems subject to linear chance constraints, alongside a tight convex relaxation for quadratic chance constraints. By lifting the state vector to encode moment information explicitly, the formulation captures linear chance constraints on states and controls across multiple time steps exactly, without conservatism, yielding strict improvements in both feasibility and optimality. For quadratic chance constraints, we derive convex approximations that are provably less conservative than existing methods. We validate the framework on minimum-snap trajectory generation for a quadrotor, demonstrating that the proposed approach remains feasible at noise levels an order of magnitude beyond the operating range of prior formulations.

---

## 论文详细总结（自动生成）

### 论文总结：具有机会约束的线性随机最优控制的精确与近似凸重构

#### 1. 核心问题与研究动机
在随机运动规划中，系统面临传感器、动力学或障碍物带来的不确定性。传统的确定性规划器无法处理状态分布的“管道”特征，导致安全评估不准确。现有的随机最优控制方法在处理**机会约束（Chance Constraints, CC）**时，通常假设反馈律是给定的，或者采用高度保守的近似，这往往导致解的次优性，甚至在噪声较大时无法找到可行解。本文的核心目标是为离散时间线性随机系统寻找一种**等效且非保守的凸优化重构方法**，以同时处理线性、二次及多步混合机会约束。

#### 2. 方法论
论文提出了一种基于**提升表示（Lifted Representation）**的建模方法：
*   **核心思想**：不直接传播协方差矩阵，而是将状态 $x_k$ 和控制 $u_k$ 表示为一个**随机基（Stochastic Basis）** $e(k)$ 的线性组合。这个基由初始状态的不确定性和各时刻的独立高斯噪声组成。
*   **关键技术细节**：
    *   **提升表示**：定义 $x_k = \mu_k + \Psi_k e(k)$ 和 $u_k = \nu_k + V_k e(k)$，其中 $\mu, \nu$ 是均值，$\Psi, V$ 是编码随机性的矩阵。
    *   **精确重构**：证明了在该表示下，**线性机会约束（LCC）**可以精确地转化为**二阶锥约束（SOCP）**，且不带任何保守性。
    *   **多步约束**：该框架支持跨多个时间步长的状态与控制混合约束（如功率限制或摩擦圆约束）。
    *   **二次约束近似**：针对**二次机会约束（QCC）**，推导了两种凸近似方法：线性矩阵不等式（LMI）和二次约束（QC）。证明了这两种方法均比传统的马尔可夫界（Markov bound）更紧致。
*   **算法流程**：将原始随机问题转化为一个确定性的凸优化问题（Problem 2），包含线性动力学方程、SOCP 约束和 LMI/QC 约束，可使用标准凸优化求解器高效解决。

#### 3. 实验设计
研究通过四旋翼无人机的**最小捕捉（Minimum-snap）轨迹生成**任务进行验证：
*   **实验场景**：
    1.  **圆形竞技场（Circle Arena）**：在圆形区域内避障，通过 20 边形凸多边形近似边界。
    2.  **漏斗走廊（Funnel Corridor）**：穿越一个逐渐变窄的梯形走廊。
*   **对比基准（Benchmarks）**：
    *   **Lew et al. [7]**：基于连续凸化（Successive Convexification）的方法。
    *   **Rapakoulias et al. [11]**：基于迭代协方差转向（Covariance Steering）的方法。
*   **评估指标**：可行性（能否找到解）和最优性（目标函数代价值）。

#### 4. 资源与算力
论文**未明确说明**具体的硬件配置（如 GPU/CPU 型号）或具体的训练/求解时长。但由于该方法最终转化为 SOCP 或 SDP（半正定规划）问题，通常可以在标准的桌面级 CPU 上利用现有的凸优化求解器（如 MOSEK 或 GUROBI）实现实时或近实时求解。

#### 5. 实验数量与充分性
*   **实验规模**：研究进行了参数扫描（Parameter Sweep），改变了噪声标准差 $\sigma$ 和环境几何难度（如走廊宽度、路标点位置）。
*   **充分性评价**：实验设计较为充分。通过对比不同噪声水平下的表现，清晰地展示了所提方法在极端噪声环境下的鲁棒性。此外，针对二次约束的近似，论文对比了 LMI、QC 和马尔可夫界三种方案，验证了不同各向异性噪声下的适用性。实验结果客观地反映了所提方法在可行域覆盖上的优势。

#### 6. 主要结论与发现
*   **可行性大幅提升**：所提方法在噪声水平比现有方法**高出一个数量级**的情况下依然保持可行。在基准方法失效的 $\sigma \approx 0.06$ 之后，该方法在 $\sigma = 0.85$ 时仍能找到解。
*   **最优性更佳**：由于减少了约束的保守性，该方法在相同环境下比对比方法降低了 **22% 至 43%** 的成本。
*   **理论验证**：证明了提升表示下的线性机会约束重构是**精确且无损的**。

#### 7. 优点
*   **非保守性**：通过显式编码矩信息，消除了线性机会约束中的近似误差。
*   **灵活性**：能够处理复杂的跨时间步长约束和状态-控制耦合约束。
*   **数学严谨性**：为二次约束提供了理论上更紧致的界限，并给出了 LMI 和 QC 两种方案的适用场景分析。

#### 8. 不足与局限
*   **计算复杂度**：随着预测时界 $N$ 和状态维度增加，提升表示中的克罗内克积（Kronecker product）会导致决策变量数量显著增加，可能在大规模系统中面临计算压力。
*   **模型限制**：目前仅限于**线性系统**和**仿射策略**。虽然可以结合连续凸化处理非线性系统，但其在高度非线性环境下的表现尚未直接验证。
*   **分布假设**：核心推导依赖于高斯分布假设，对于非高斯噪声（如多峰分布）的适用性有限。

（完）
