---
title: Conflict-Based Search for Multi Agent Path Finding with Asynchronous Actions
title_zh: 针对异步动作多智能体路径规划的冲突搜索
authors: "Xuemian Wu, Shizhe Zhao, Zhongqiang Ren"
date: 2026-03-19
pdf: "https://arxiv.org/pdf/2603.18866v1"
tags: ["query:robot-planning"]
score: 9.0
evidence: 异步动作的多智能体路径规划
tldr: 针对多智能体路径规划（MAPF）中同步动作假设的局限性，本文研究了异步动作下的路径规划问题（MAPF-AA）。针对现有CCBS算法因连续等待时间导致的不完备性问题，提出了一种新型算法CBS-AA。该算法在保证完备性和最优性的基础上，引入了冲突解决技术以提升扩展性，实验证明其能显著减少搜索分支，为实际应用提供了更可靠的规划方案。
selection_source: fresh_fetch
motivation: 传统MAPF算法多依赖同步动作假设，而现有的连续时间冲突搜索算法（CCBS）因无法处理无限状态空间而存在不完备性缺陷。
method: 提出CBS-AA算法，通过规避连续等待时间带来的理论问题，实现了异步动作环境下具有完备性和最优性保证的路径规划。
result: "实验结果显示，CBS-AA及其冲突解决技术在提高算法扩展性的同时，能够将搜索过程中的分支数量减少多达90%。"
conclusion: CBS-AA成功解决了异步MAPF中的完备性挑战，为多智能体系统在复杂现实场景下的应用提供了高效且最优的搜索框架。
---

## 摘要
多智能体路径规划 (MAPF) 旨在为多个智能体寻找从各自起点到各自终点的无碰撞路径，同时最小化路径成本。大多数现有的 MAPF 算法依赖于同步动作的共同假设，即所有智能体的动作同时开始且始终耗时一个时间单位，这可能会限制 MAPF 规划器在实际中的应用。为了摆脱这一假设，连续时间冲突搜索 (CCBS) 是一种流行的方法，可以为异步动作下的 MAPF (MAPF-AA) 寻找最优解。然而，最近发现 CCBS 是不完备的，这是由于连续等待时间产生的不可数无限状态空间所致。本文提出了一种新方法，即异步动作冲突搜索 (CBS-AA)，它绕过了这一理论问题，并能在保证完备性和解的最优性的情况下解决 MAPF-AA。基于 CBS-AA，我们还开发了冲突解决技术，以进一步提高 CBS-AA 的可扩展性。我们的测试结果表明，该方法可以将分支数量减少高达 90%。

## Abstract
Multi-Agent Path Finding (MAPF) seeks collision-free paths for multiple agents from their respective start locations to their respective goal locations while minimizing path costs. Most existing MAPF algorithms rely on a common assumption of synchronized actions, where the actions of all agents start at the same time and always take a time unit, which may limit the use of MAPF planners in practice. To get rid of this assumption, Continuous-time Conflict-Based Search (CCBS) is a popular approach that can find optimal solutions for MAPF with asynchronous actions (MAPF-AA). However, CCBS has recently been identified to be incomplete due to an uncountably infinite state space created by continuous wait durations. This paper proposes a new method, Conflict-Based Search with Asynchronous Actions (CBS-AA), which bypasses this theoretical issue and can solve MAPF-AA with completeness and solution optimality guarantees. Based on CBS-AA, we also develop conflict resolution techniques to improve the scalability of CBS-AA further. Our test results show that our method can reduce the number of branches by up to 90%.

---

## 论文详细总结（自动生成）

### 针对异步动作多智能体路径规划的冲突搜索 (CBS-AA) 论文总结

#### 1. 核心问题与整体含义
本文研究的是**异步动作下的多智能体路径规划 (MAPF-AA)**。传统 MAPF 假设所有智能体动作同步且耗时统一（一个时间单位），这在现实中（如不同速度的机器人）并不成立。现有的连续时间冲突搜索 (CCBS) 虽然试图解决此问题，但近期研究发现其在处理“连续等待时间”时存在理论上的**不完备性**（即可能找不到解，即使解存在）。本文的核心动机是提出一种既能保证**完备性**又能保证**最优性**的新型算法 **CBS-AA**，并解决大规模智能体场景下的扩展性挑战。

#### 2. 方法论
论文提出了 **CBS-AA** 框架，包含以下关键技术：
*   **底层规划器 (SIPPS-WC)：** 改进了安全间隔路径规划 (SIPP)，使其适应连续时间。引入了“等待冲突 (Waiting Conflict)”机制，在打破平局时优先选择与其他智能体路径冲突更少的路径。
*   **冲突分类：** 将冲突细化为三种类型：进入-进入 (IN-IN)、退出-进入 (OUT-IN) 和等待-进入 (WAIT-IN)。
*   **单动作约束 (CSA)：** 针对 CCBS 的缺陷，CSA 不再对特定时长的等待动作加约束，而是对顶点在特定时间点或区间进行占用约束，从而规避了无限等待时长导致的搜索发散。
*   **多动作约束传播 (CMA)：** 这是提升效率的核心。利用“持续占用 (Duration Occupancy)”原理，当发生冲突时，不仅约束当前动作，还根据智能体的移动速度推算并禁止一系列会导致冲突的相关动作和时间区间，从而大幅剪枝高层搜索树。

#### 3. 实验设计
*   **实验场景：** 使用了四种不同类型的标准地图：`empty-32-32`（空地）、`random-32-32-20`（随机障碍物）、`den312d`（复杂游戏地图）和 `warehouse`（仓库地图）。
*   **智能体设置：** 智能体数量从 10 增加到 50，每个智能体赋予 1 到 20 之间的随机速度。
*   **对比基准 (Benchmarks)：**
    1.  **CCBS：** 现有的连续时间冲突搜索算法。
    2.  **LS-M*：** 一种基于 A* 的精确算法（用于验证最优性）。
    3.  **消融对比：** 对比了 CSA（基础修正版）、CMA（引入约束传播）和 CMAS（引入改进底层规划器）。

#### 4. 资源与算力
*   **硬件平台：** 实验在一台配备 **Intel Core i7-11800H CPU** 的计算机上运行。
*   **软件实现：** 使用 C++ 编写。
*   **算力细节：** 论文未涉及 GPU 训练（属于搜索类算法，非深度学习），每个实例的运行时间限制为 30 秒或 120 秒。

#### 5. 实验数量与充分性
*   **实验规模：** 针对四种地图、多种智能体规模（10-50）进行了大量重复实验，涵盖了成功率、高层节点扩展数、运行时间等多个维度。
*   **充分性评价：** 实验设计较为充分。通过与 LS-M* 的对比验证了算法的最优性；通过不同地图的测试验证了鲁棒性；通过 CSA/CMA/CMAS 的对比清晰地展示了各技术模块的贡献。实验过程遵循了 MAPF 领域的标准测试流程，具有较高的客观性。

#### 6. 主要结论与发现
*   **完备性与最优性：** 证明了 CBS-AA 在理论上是完备且最优的，解决了 CCBS 的失效问题。
*   **效率提升：** 引入 CMA 约束传播技术后，高层搜索树的节点扩展数减少了高达 **90%**。
*   **成功率：** 在复杂场景（如仓库地图）中，CMAS 在 50 个智能体规模下的成功率远高于 CCBS 和基础版 CSA。
*   **性能对比：** 相比于 LS-M*，CBS-AA 能在更短的时间内找到相同的最优解，且能处理更多数量的智能体。

#### 7. 优点
*   **理论严谨：** 成功修复了 CCBS 算法中被学术界新发现的完备性漏洞。
*   **约束传播创新：** CMA 技术通过物理逻辑（速度与占用时间）预判冲突，极大地提高了 CBS 框架在连续时间域下的搜索效率。
*   **实用性强：** 考虑了异构速度，更贴近工业机器人和自动驾驶的实际应用场景。

#### 8. 不足与局限
*   **假设限制：** CMA 的某些约束生成逻辑依赖于“假设 1”（智能体在各边上的移动速度恒定或边长统一），虽然可以通过在长边插入中间节点来缓解，但在极端非均匀图中可能会增加图的规模。
*   **实时性挑战：** 尽管比前人方法快，但在智能体数量极多（如数百个）或地图极大时，作为精确算法的 CBS-AA 仍可能面临指数级复杂度增长，难以满足极高实时性的需求。
*   **动态环境：** 目前主要针对静态地图中的多智能体协调，未深入讨论存在不可预测动态障碍物的情况。

（完）
