---
title: Grounding Vision and Language to 3D Masks for Long-Horizon Box Rearrangement
title_zh: 将视觉与语言对齐至 3D 掩码以实现长程箱体重排
authors: "Ashish Malik, Caleb Lowe, Aayam Shrestha, Stefan Lee, Fuxin Li, Alan Fern"
date: 2026-03-24
pdf: "https://arxiv.org/pdf/2603.23676v1"
tags: ["query:robot-planning"]
score: 9.0
evidence: 长时程规划与箱体重新排列任务
tldr: "针对长程3D物体重新排列任务，现有方法在处理复杂几何和多目标时存在局限。本文提出RAMP-3D，通过将视觉语言模型扩展到3D掩码预测，将规划任务转化为序列化的“抓取对象”和“放置区域”掩码预测。该方法在包含多达30个箱子的仓库环境中表现优异，成功率达79.5%，为长程规划提供了一种基于掩码的反应式策略新思路。"
selection_source: fresh_fetch
motivation: 传统的符号规划器和2D视觉语言模型在处理具有复杂3D几何约束和大量物体的长程重排任务时表现不佳。
method: 提出RAMP-3D模型，通过预测成对的3D掩码（抓取目标和放置区域）来反应式地生成多步取放动作。
result: "在11种仓库任务变体中实现了79.5%的成功率，显著优于基于2D视觉语言模型的基准方法。"
conclusion: 基于3D掩码的反应式策略是替代传统符号规划流水线、实现长程3D任务规划的一种极具潜力的方法。
---

## 摘要
我们研究了在 3D 环境中，仅利用视觉观测从不完全指定的自然语言目标出发进行长程规划，重点关注多步 3D 箱体重排任务。现有方法通常依赖于符号规划器（其状态与目标的关联对齐较为脆弱），或者依赖于从 2D 视觉语言模型 (VLM) 直接生成动作序列。这两种方法在处理大量物体、丰富的 3D 几何形状以及隐式语义约束的推理时都面临困难。3D VLM 的最新进展展示了自然语言指代物与 3D 分割掩码之间的强对齐能力，这表明了实现更通用规划能力的潜力。我们扩展了现有的 3D 对齐模型，并提出了反应式动作掩码规划器 (RAMP-3D)，该规划器将长程规划建模为成对 3D 掩码的序列反应式预测：一个“物体掩码”指示抓取对象，一个“目标区域掩码”指定放置位置。该系统处理 RGB-D 观测和自然语言任务规范，以反应式地生成用于 3D 箱体重排的多步取放动作。我们在包含 1-30 个箱体和多种自然语言约束的仓库式环境中，针对 11 种任务变体进行了实验。RAMP-3D 在长程重排任务上实现了 79.5% 的成功率，并显著优于基于 2D VLM 的基准模型，证明了基于掩码的反应式策略是长程规划中替代符号流水线的一种极具前景的选择。

## Abstract
We study long-horizon planning in 3D environments from under-specified natural-language goals using only visual observations, focusing on multi-step 3D box rearrangement tasks. Existing approaches typically rely on symbolic planners with brittle relational grounding of states and goals, or on direct action-sequence generation from 2D vision-language models (VLMs). Both approaches struggle with reasoning over many objects, rich 3D geometry, and implicit semantic constraints. Recent advances in 3D VLMs demonstrate strong grounding of natural-language referents to 3D segmentation masks, suggesting the potential for more general planning capabilities. We extend existing 3D grounding models and propose Reactive Action Mask Planner (RAMP-3D), which formulates long-horizon planning as sequential reactive prediction of paired 3D masks: a "which-object" mask indicating what to pick and a "which-target-region" mask specifying where to place it. The resulting system processes RGB-D observations and natural-language task specifications to reactively generate multi-step pick-and-place actions for 3D box rearrangement. We conduct experiments across 11 task variants in warehouse-style environments with 1-30 boxes and diverse natural-language constraints. RAMP-3D achieves 79.5% success rate on long-horizon rearrangement tasks and significantly outperforms 2D VLM-based baselines, establishing mask-based reactive policies as a promising alternative to symbolic pipelines for long-horizon planning.

---

## 论文详细总结（自动生成）

### 论文总结：将视觉与语言对齐至 3D 掩码以实现长程箱体重排 (RAMP-3D)

#### 1. 核心问题与整体含义
论文针对的是机器人领域中**长程（Long-Horizon）3D 物体重新排列**任务。核心挑战在于：如何让机器人仅凭原始视觉观测（RGB-D）和模糊的自然语言指令（如“按颜色分类堆叠，高度不超过2层”），在复杂的 3D 环境中进行多步规划。
*   **研究动机**：传统的符号规划器依赖于脆弱的人工定义谓词，难以处理复杂的 3D 几何约束；而现有的 2D 视觉语言模型（VLM）缺乏对 3D 空间、遮挡和体积关系的深度理解。
*   **核心目标**：跳过显式的符号逻辑层，直接将 3D 视觉语言对齐能力转化为“感知到动作”的规划能力。

#### 2. 方法论
论文提出了 **RAMP-3D (Reactive Action Mask Planner)**，其核心思想是将规划任务转化为**成对 3D 掩码的序列预测**。
*   **核心架构**：基于 UniVLG（一种 3D 视觉语言对齐模型）。输入多视角 RGB-D 图像和自然语言指令，通过 Swin Transformer 提取视觉特征，RoBERTa 提取文本特征，并在 3D 空间进行体素化融合。
*   **关键技术细节**：
    *   **成对对比特征（Pair-contrastive features）**：引入学习到的查询投影，计算“抓取对象”和“放置区域”之间的兼容性得分，确保预测的动作在逻辑上是一致的。
    *   **掩码空间动作表示**：模型直接输出两个 3D 分割掩码（$M_{pick}$ 和 $M_{target}$），分别代表下一步要移动的物体和目标位置。
    *   **终止预测头**：增加一个轻量级 MLP 预测任务是否完成。
*   **训练流程**：在 IsaacSim 中使用 Oracle 规划器生成专家轨迹，利用 LLM（gpt-oss-120b）对指令进行改写以增强语言泛化性，并加入辅助任务（如识别空闲位置、已放置物体等）来强化场景理解。

#### 3. 实验设计
*   **场景与数据集**：在 IsaacSim 模拟的仓库环境中进行，包含托盘、货架、3 种颜色的箱子（最多 30 个）和干扰物。数据集包含约 9.5k 个场景样本（2.2k 用于测试）。
*   **任务变体**：设计了 11 种任务，涵盖高度限制、颜色优先级、货架/托盘优先级、堆叠顺序、可达性约束等。
*   **基准方法 (Benchmark)**：
    *   **2D-pointer**：结合了 GPT-5.1（负责高层逻辑规划）和 Molmo-72B（负责在 2D 顶视图中进行坐标点选）。
*   **评估模式**：分为“对齐目标模式”（Snap-to-target，理想化放置）和“自由形式模式”（Free-form，完全由模型预测位置）。

#### 4. 资源与算力
*   论文提到了使用 **gpt-oss-120b** 模型进行自然语言数据的增强（改写）。
*   **关于 GPU 和训练时长**：文中**未明确说明**具体的 GPU 型号、数量以及训练所需的总时长。

#### 5. 实验数量与充分性
*   **实验规模**：测试了 2.2k 个独立场景，涵盖 1 到 30 个物体的不同复杂度。
*   **消融实验**：进行了语言泛化性测试（保留特定模板不参与训练）和辅助任务消融实验。
*   **充分性评价**：实验设计较为充分，通过对比 2D 顶尖模型证明了 3D 表征的必要性。任务变体丰富，涵盖了从简单放置到复杂逻辑约束的多种情况。实验过程考虑了单步准确率和多步长程成功率，评估较为客观。

#### 6. 主要结论与发现
*   **性能优异**：RAMP-3D 在长程重排任务中实现了 **79.5%** 的总成功率，远超基于 2D VLM 的基准（单步有效性仅 41.4%）。
*   **3D 优势**：直接在 3D 掩码空间操作能更好地处理几何可行性和空间占用，尤其是在处理货架等多层结构时。
*   **复杂度影响**：随着物体数量增加（20-30个），成功率有所下降，主要原因是长程操作中的误差累积和缺乏前瞻性搜索。
*   **语言泛化**：模型对未见过的指令表达方式具有较强的鲁棒性。

#### 7. 优点
*   **端到端掩码规划**：避免了繁琐的符号化建模，将复杂的 3D 几何约束隐式地学习在掩码预测中。
*   **成对一致性设计**：通过对比学习确保了抓取和放置动作的匹配，这是长程规划的关键。
*   **强大的 3D 感知**：相比 2D 方法，能更好地处理遮挡和多层堆叠逻辑。

#### 8. 不足与局限
*   **缺乏前瞻性（Lookahead）**：模型是纯反应式的（Reactive），在处理需要全局最优或具有复杂依赖关系（如“为了拿底部的箱子必须先移开顶部的”）的任务时，可能会陷入局部最优。
*   **视觉多样性有限**：实验主要集中在箱体（Box）重排，虽然几何关系复杂，但语义类别较少，尚未在更通用的家庭场景中验证。
*   **误差累积**：在“自由形式”执行模式下，微小的放置偏差会随步数增加而放大，导致最终任务失败。

（完）
