---
title: Optimal Solutions for the Moving Target Vehicle Routing Problem with Obstacles via Lazy Branch and Price
title_zh: 基于延迟分支定价算法的带障碍物动态目标车辆路径问题最优解
authors: "Anoop Bhat, Geordan Gutow, Surya Singh, Zhongqiang Ren, Sivakumar Rathinam, Howie Choset"
date: 2026-03-23
pdf: "https://arxiv.org/pdf/2603.21880v1"
tags: ["query:robot-planning"]
score: 9.0
evidence: 拦截运动目标的多智能体轨迹优化
tldr: 本文研究带障碍物的移动目标车辆路径规划问题（MT-VRP-O），旨在为多个智能体寻找拦截移动目标的最佳轨迹。针对避障运动规划计算量大的挑战，提出了一种名为“延迟松弛连续性分支定价”（Lazy BPRC）的算法。该方法在分支定价框架下，利用松弛连续性约束计算成本下界，并采用凸集图（GCS）搜索技术延迟评估真实路径成本，从而在保证最优性的同时显著提升了求解效率。
selection_source: fresh_fetch
motivation: 针对移动目标路径规划中避障运动规划计算极其耗时的问题，寻求一种高效的最优求解方案。
method: 提出Lazy BPRC算法，结合分支定价框架、成本下界延迟评估以及基于凸集图（GCS）的路径搜索。
result: 实验证明该算法在求解效率上比对比方法提升了高达一个数量级。
conclusion: 通过延迟计算和连续性松弛技术，Lazy BPRC为复杂动态环境下的多智能体路径规划提供了高效的最优解法。
---

## 摘要
带障碍物的动态目标车辆路径问题（MT-VRP-O）旨在为多个智能体寻找轨迹，以共同拦截一组移动目标。每个目标具有一个或多个必须访问的时间窗，且智能体必须避开静态障碍物并满足速度和容量限制。我们引入了具有松弛连续性的延迟分支定价算法（Lazy BPRC），用于寻找 MT-VRP-O 的最优解。Lazy BPRC 应用了 VRP 的分支定价框架，该框架在受限主问题（RMP）和定价问题之间交替进行。RMP 旨在从有限的路径子集中为每个智能体选择一系列目标-时间窗配对（称为路径），而定价问题则向该有限子集中添加路径。传统上，求解 RMP 需要计算智能体遵循有限子集中每条路径的成本。在 MT-VRP-O 中计算这些成本具有很高的计算强度，因为它需要在移动目标之间进行无碰撞运动规划。Lazy BPRC 通过使用每条路径成本的下界来求解 RMP，从而延迟成本计算；这些下界是通过具有松弛连续性约束的运动规划计算得出的。我们根据需要延迟评估路径的真实成本。我们通过在凸集图（GCS）上搜索最短路径来计算路径成本，并利用我们的连续性松弛方法加速这一搜索过程。实验表明，Lazy BPRC 的运行速度比两种消融实验快达一个数量级。

## Abstract
The Moving Target Vehicle Routing Problem with Obstacles (MT-VRP-O) seeks trajectories for several agents that collectively intercept a set of moving targets. Each target has one or more time windows where it must be visited, and the agents must avoid static obstacles and satisfy speed and capacity constraints. We introduce Lazy Branch-and-Price with Relaxed Continuity (Lazy BPRC), which finds optimal solutions for the MT-VRP-O. Lazy BPRC applies the branch-and-price framework for VRPs, which alternates between a restricted master problem (RMP) and a pricing problem. The RMP aims to select a sequence of target-time window pairings (called a tour) for each agent to follow, from a limited subset of tours. The pricing problem adds tours to the limited subset. Conventionally, solving the RMP requires computing the cost for an agent to follow each tour in the limited subset. Computing these costs in the MT-VRP-O is computationally intensive, since it requires collision-free motion planning between moving targets. Lazy BPRC defers cost computations by solving the RMP using lower bounds on the costs of each tour, computed via motion planning with relaxed continuity constraints. We lazily evaluate the true costs of tours as-needed. We compute a tour's cost by searching for a shortest path on a Graph of Convex Sets (GCS), and we accelerate this search using our continuity relaxation method. We demonstrate that Lazy BPRC runs up to an order of magnitude faster than two ablations.

---

## 论文详细总结（自动生成）

这篇论文介绍了一种名为 **Lazy BPRC**（具有松弛连续性的延迟分支定价算法）的新方法，旨在解决带障碍物的移动目标车辆路径问题（MT-VRP-O）。以下是对该论文的结构化总结：

### 1. 核心问题与整体含义（研究动机和背景）
*   **核心问题**：如何在存在静态障碍物、目标持续移动、具有特定时间窗限制以及智能体速度和容量约束的复杂环境下，为多个智能体规划出总行驶距离最短的最优拦截轨迹。
*   **研究背景**：该问题（MT-VRP-O）是旅行商问题（TSP）和车辆路径问题（VRP）的泛化，属于 NP-hard 问题。
*   **研究动机**：传统的 VRP 算法在处理移动目标和避障时，需要频繁调用高昂的无碰撞运动规划算法来计算路径成本。这种计算开销使得在大规模或复杂环境下的最优求解变得极其困难。

### 2. 方法论：核心思想与技术细节
*   **核心思想**：采用**分支定价（Branch-and-Price）**框架，通过“延迟评估”策略减少昂贵的运动规划调用。即先利用计算廉价的“成本下界”来筛选潜在的最优路径，仅在必要时才计算精确的避障路径成本。
*   **关键技术细节**：
    *   **松弛连续性（Relaxed Continuity）**：将目标的时间窗划分为多个段（Segments），忽略时间连续性约束，仅计算空间上的最短路径，从而快速获得路径成本的下界（Lower Bound）和上界（Upper Bound）。
    *   **延迟评估（Lazy Evaluation）**：在列生成（Column Generation）过程中，受限主问题（RMP）首先基于成本下界进行优化。只有当某个路径组合被选为潜在最优解时，才调用精确求解器。
    *   **SPP-GCS 路径搜索**：利用凸集图（GCS）上的最短路径算法来计算精确的避障成本。
    *   **仿射启发式函数（Affine Heuristic）**：提出了一种新的障碍物感知启发式函数 $h_n(t)$，通过对段图（Segment-graph）搜索结果进行最小二乘拟合，加速 GCS 内部的搜索效率。

### 3. 实验设计
*   **实验场景**：在 2D 网格地图中进行模拟，目标具有分段线性轨迹，智能体需从仓库出发拦截目标并返回。
*   **对比基准（Benchmarks）**：
    1.  **Non-Lazy BPRC**：不使用延迟评估，生成路径时立即计算精确成本。
    2.  **No-Affine-Heuristic**：使用延迟评估，但采用现有的（FMC*）障碍物无关启发式函数。
*   **变量控制**：分别改变目标数量（3-15个）、地图分辨率（10-30）和智能体容量（3-7）来测试算法性能。

### 4. 资源与算力
*   **硬件环境**：Intel i9-9820X 3.3GHz CPU（10核），禁用超线程，128 GB RAM。
*   **并行化**：在定价问题的后继生成、路径成本评估、段间距离计算等方面采用了并行计算。
*   **时长限制**：每个实例的求解时限设定为 10 分钟。

### 5. 实验数量与充分性
*   **实验规模**：针对每个变量配置（如不同的目标数、分辨率等）运行了多组实例。
*   **充分性评价**：实验设计较为充分，涵盖了影响 VRP 复杂度的核心维度（规模、环境复杂度、约束强度）。通过消融实验（Ablation Studies）清晰地展示了“延迟评估”和“新型启发式函数”各自对性能提升的贡献，实验结果具有客观性。

### 6. 主要结论与发现
*   **效率提升**：Lazy BPRC 的运行速度比非延迟版本快达 **44 倍**，比使用旧启发式函数的版本快达 **13 倍**。
*   **延迟评估的有效性**：证明了在 RMP 中使用下界进行初步筛选可以极大地减少昂贵的运动规划调用次数。
*   **障碍物感知的重要性**：实验表明，考虑障碍物信息的启发式函数在复杂地图（高分辨率）下具有更强的搜索引导能力。

### 7. 优点
*   **首创性**：这是首个能够为带障碍物的移动目标 VRP 提供**最优解**（而非近似解）的算法。
*   **策略巧妙**：将运筹学中的分支定价与机器人学中的运动规划（GCS）深度结合，利用延迟计算缓解了计算瓶颈。
*   **理论完备**：论文提供了算法能够收敛至全局最优解的理论证明。

### 8. 不足与局限
*   **可扩展性限制**：尽管比对比方法快，但作为最优求解器，在目标数量超过 15 个或容量约束较弱时，仍会触及 10 分钟的时间限制，难以处理超大规模任务。
*   **环境假设**：目前主要针对静态障碍物和分段线性目标轨迹，对于动态障碍物或更复杂的目标运动模型（如非线性）的适用性有待验证。
*   **维度限制**：实验主要在 2D 空间进行，虽然理论上可扩展至 3D，但计算复杂度会进一步激增。

（完）
