---
title: "SG-VLA: Learning Spatially-Grounded Vision-Language-Action Models for Mobile Manipulation"
title_zh: SG-VLA：学习用于移动操作的空间定位视觉-语言-动作模型
authors: "Ruisen Tu, Arth Shukla, Sohyun Yoo, Xuanlin Li, Junxi Li, Jianwen Xie, Hao Su, Zhuowen Tu"
date: 2026-03-24
pdf: "https://arxiv.org/pdf/2603.22760v1"
tags: ["query:robot-planning"]
score: 9.0
evidence: 用于移动操作的具身智能与规划
tldr: 针对移动操作机器人在复杂家居环境中的挑战，本文提出SG-VLA框架。该框架通过引入多视图RGB、深度信息和时间序列，并结合全局位置、抓取启发和分割掩码等辅助任务协同训练，显著增强了模型的空间感知与表征能力。在处理13维高维动作空间时，该方法在家庭重排任务中表现出色，显著优于传统的直接模仿学习方法。
selection_source: fresh_fetch
motivation: 现有的VLA模型在处理涉及全局布局和精细几何的复杂移动操作任务时，由于缺乏空间感知能力，导致控制性能不佳。
method: 通过多模态输入增强并协同训练多个辅助解码器，从共享特征中重建机器人位姿、抓取启发和分割掩码等空间信息，以强化空间接地表征。
result: 在抓取、放置、开启和关闭等一系列家庭重排任务中，该方法均取得了显著的性能提升，大幅超越了基准模仿学习模型。
conclusion: 通过辅助任务和多模态学习实现空间接地，是提升通用家政机器人VLA模型规模化应用能力的有效方向。
---

## 摘要
视觉-语言-动作（VLA）模型在机器人控制方面展现出巨大潜力，但在复杂的家庭环境中的表现仍不理想。移动操作需要对全局场景布局、细粒度几何结构以及高维连续动作进行推理，这使得标准的模仿学习难以胜任。我们提出了一种学习空间定位 VLA 模型的框架，通过辅助任务协同训练和多模态输入增强来强化感知与表示。我们的方法解决了控制 13 维动作空间的挑战，该空间涉及底盘运动、机械臂关节和夹持器执行器的协调。为了丰富空间理解，该模型整合了多视角 RGB 观测、深度线索和短期时间历史，提供了全局场景结构和局部操作上下文的视角。为了提高表示质量，我们协同训练了辅助解码器，从共享的视觉-语言特征中重建可解释的中间信号——包括机器人全局位置、关节配置、抓取适宜性（affordances）、目标物体相对位姿以及分割掩码。这些目标提供了密集监督，促使骨干网络开发出具有空间定位和操作感知能力的潜表示。通过对家庭重排任务的广泛评估，我们的方法在抓取、放置、打开和关闭操作中均取得了持续的改进，显著优于直接模仿学习。我们的研究结果表明，通过辅助学习和多模态学习实现空间定位，为将 VLA 模型扩展到通用家用机器人提供了一个强有力的方向。

## Abstract
Vision-Language-Action (VLA) models show promise for robotic control, yet performance in complex household environments remains sub-optimal. Mobile manipulation requires reasoning about global scene layout, fine-grained geometry, and high-dimensional continuous actions, making standard imitation learning insufficient. We introduce a framework for learning spatially-grounded VLA models that strengthens perception and representation through auxiliary task co-training and multi-modal input enhancement. Our method addresses the challenge of controlling a 13-dimensional action space involving coordinated base motion, arm articulation, and gripper actuation. To enrich spatial understanding, the model incorporates multi-view RGB observations, depth cues, and short temporal history, providing perspectives of both global scene structure and local manipulation context. To improve representation quality, we co-train auxiliary decoders that reconstruct interpretable intermediate signals - including global robot position, joint configurations, grasp affordances, target-object relative pose, and segmentation masks - from shared visual-language features. These objectives provide dense supervision that encourages the backbone to develop spatially grounded, manipulation-aware latent representations. Through extensive evaluation on home rearrangement tasks, our approach achieves consistent improvements across picking, placing, opening, and closing operations, substantially outperforming direct imitation learning. Our findings suggest that spatial grounding through auxiliary and multi-modal learning provides a strong direction for scaling VLA models toward general-purpose domestic robots.

---

## 论文详细总结（自动生成）

以下是对论文《SG-VLA: Learning Spatially-Grounded Vision-Language-Action Models for Mobile Manipulation》的结构化总结：

### 1. 核心问题与研究动机
*   **核心问题**：现有的视觉-语言-动作（VLA）模型虽然在桌面操作中表现出色，但在复杂的**移动操作（Mobile Manipulation）**任务（如家庭环境中的重排）中表现不佳。
*   **研究背景**：移动操作要求机器人同时处理全局场景布局、细粒度物体几何以及高维连续动作（涉及底盘移动与机械臂协同）。
*   **动机**：作者认为标准的直接模仿学习（Direct Imitation Learning）提供的监督信号不足，导致模型缺乏对空间的深刻理解。因此，需要通过增强感知输入和引入辅助任务来强化模型的“空间接地（Spatial Grounding）”能力。

### 2. 方法论
SG-VLA 框架的核心思想是通过多模态输入和辅助任务协同训练，迫使模型学习具有空间感知能力的潜表征。
*   **核心架构**：
    *   **骨干网络**：采用 Prismatic VLM 架构，视觉编码器融合了 DINOv2（空间特征）和 SigLIP（语义特征），语言模型使用轻量级的 Qwen2.5-0.5B。
    *   **动作空间**：控制一个 **13 维**的动作向量，包括底盘位姿（3D）、躯干高度（1D）、机械臂关节角度（7D）和夹持器状态（2D）。
    *   **动作生成**：支持直接预测离散 Token 或通过一个可选的 **Flow Matching 动作专家**（100M 参数）生成高精度连续动作。
*   **关键技术细节**：
    *   **多模态增强**：引入头部和手部双视角 RGB、归一化深度图以及短期历史观测。
    *   **辅助解码器（Auxiliary Decoders）**：在训练期间，利用共享的 VLM 特征重建 5 类信号：机器人全局位置、抓取成功状态、目标物体位姿、关节配置（qpos）和目标物体分割掩码。
    *   **三阶段渐进式训练**：
        1.  **解码器适配阶段**：冻结 VLM，仅让随机初始化的辅助解码器学习解释 VLM 特征，避免噪声梯度破坏预训练权重。
        2.  **联合微调阶段**：开启全量梯度回传，通过辅助任务损失和动作预测损失共同优化。
        3.  **动作头独立训练阶段**：冻结 VLM，专门训练 Flow Matching 动作专家。

### 3. 实验设计
*   **数据集与场景**：使用 **ManiSkill-HAB** 模拟基准测试，包含 TidyHouse、PrepareGroceries 和 SetTable 三个长程家庭重排任务。
*   **数据规模**：共 4.4 万个情节（Episodes），包含 140 万次状态转换（Transitions）。
*   **任务类型**：涵盖抓取（Pick）、放置（Place）、打开（Open）和关闭（Close）四种基本操作。
*   **对比方法**：
    *   **OpenVLA (7B)**：作为大规模 VLA 基准。
    *   **Base VLM (Qwen2.5-0.5B)**：仅使用直接模仿学习的基准模型。
    *   **消融实验**：对比了不同输入模态（单视角 vs 多视角、有无深度图）以及不同辅助任务组合的效果。

### 4. 资源与算力
*   **硬件**：使用了 **8 台 NVIDIA A100 GPU**。
*   **训练配置**：使用 Adam 优化器，学习率恒定为 2e-5，全局 Batch Size 为 512。
*   **时长**：分割掩码相关模型训练 5 个 Epoch，其他模型训练 10 个 Epoch。

### 5. 实验数量与充分性
*   **实验规模**：论文进行了系统的消融实验，涵盖了输入模态、辅助任务类型、训练策略（是否渐进式）以及动作生成方式（离散 vs 连续）。
*   **评估标准**：每个任务运行 30 个情节并计算平均成功率。
*   **充分性评价**：实验设计较为全面，通过对比 OpenVLA 证明了移动操作任务的难度，并通过逐项添加辅助任务验证了各组件的贡献。实验结果显示 SG-VLA 在 SetTable 任务上的平均成功率从 60% 提升至 73%，提升显著且客观。

### 6. 主要结论与发现
*   **空间信息至关重要**：多视角 RGB 结合深度信息比单视角 RGB 显著提升了性能（成功率从 0.04 提升至 0.32 以上）。
*   **辅助任务的增益**：**关节位置（qpos）预测**和**分割掩码重建**对性能提升贡献最大，分别增强了模型对自身状态和物体定位的理解。
*   **训练策略的影响**：直接协同训练会导致性能下降，而**渐进式训练**是确保辅助任务起到正面作用的关键。
*   **动作头权衡**：Flow Matching 动作专家在精细的抓取/放置任务中表现更好，但在涉及底盘移动的开启/关闭任务中，离散 Token 预测反而更具优势。

### 7. 优点
*   **高效的监督信号**：通过辅助任务将“空间常识”显式引入 VLA 模型，解决了模仿学习信号稀疏的问题。
*   **轻量化与高性能**：使用 1.3B 参数的模型在特定任务上超越了 7B 的 OpenVLA，证明了架构设计的有效性。
*   **训练稳定性**：提出的三阶段训练方案有效解决了多任务学习中的梯度干扰问题。

### 8. 不足与局限
*   **时间序列失效**：实验发现加入历史观测（过去 4 帧）反而导致性能下降，作者推测模型未能有效整合时间信息，或任务本身更偏向反应式控制。
*   **动作专家局限性**：Flow Matching 动作头在移动导航类任务中表现不佳，模型尚不能完美统一精细操作与大范围移动。
*   **环境局限**：实验主要在 ManiSkill-HAB 模拟器中完成，缺乏在真实物理世界中的泛化性验证。

（完）
