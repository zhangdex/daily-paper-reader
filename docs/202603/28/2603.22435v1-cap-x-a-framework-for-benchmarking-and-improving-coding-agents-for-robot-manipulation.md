---
title: "CaP-X: A Framework for Benchmarking and Improving Coding Agents for Robot Manipulation"
title_zh: CaP-X：一个用于基准测试和改进机器人操纵编码智能体的框架
authors: "Max Fu, Justin Yu, Karim El-Refai, Ethan Kou, Haoru Xue, Huang Huang, Wenli Xiao, Guanzhi Wang, Fei-Fei Li, Guanya Shi, Jiajun Wu, Shankar Sastry, Yuke Zhu, Ken Goldberg, Linxi \"Jim\" Fan"
date: 2026-03-23
pdf: "https://arxiv.org/pdf/2603.22435v1"
tags: ["query:robot-planning"]
score: 9.0
evidence: 具身操纵与机器人控制原语的代码即策略框架
tldr: 本研究针对“代码即策略”（CaP）在机器人操作中的有效性展开系统研究，提出了CaP-X开源框架。该框架包含交互式环境CaP-Gym和评估基准CaP-Bench，对12种前沿模型进行了测试。研究发现，虽然模型性能依赖人工抽象，但通过多轮交互、执行反馈和自动技能合成等测试时计算手段，可显著提升鲁棒性。基于此，作者开发了CaP-Agent0和CaP-RL，在仿真和真实机器人上均实现了高可靠性。
selection_source: fresh_fetch
motivation: 旨在解决“代码即策略”方法在机器人操作中缺乏系统评估，且过度依赖人工预设抽象层的问题。
method: 提出了包含CaP-Gym环境、CaP-Bench基准、免训练代理CaP-Agent0及强化学习CaP-RL的综合框架。
result: 实验表明，通过增加测试时计算（如多轮交互和反馈）能有效弥补模型在低级原语操作下的性能差距。
conclusion: CaP-X为提升机器人编码代理的自主性与可靠性提供了重要的开源平台和技术路径。
---

## 摘要
“代码即策略”（Code-as-Policy）探讨了可执行代码如何补充数据密集型的视觉-语言-动作（VLA）方法，但其作为具身操纵自主控制器的有效性仍未得到充分探索。我们提出了 CaP-X，这是一个用于系统研究机器人操纵中“代码即策略”智能体的开源框架。其核心是 CaP-Gym，这是一个交互式环境，智能体通过合成和执行组合了感知与控制原语的程序来控制机器人。在此基础上，CaP-Bench 在不同的抽象级别、交互程度和感知落地方面评估了前沿的语言和视觉-语言模型。通过对 12 个模型的评估，CaP-Bench 揭示了一个一致的趋势：性能随人工设计的抽象而提高，但随这些先验知识的移除而下降，暴露了对设计者脚手架（scaffolding）的依赖。同时，我们观察到这种差距可以通过扩展智能体测试时计算（agentic test-time computation）来弥补——通过多轮交互、结构化执行反馈、视觉差异化、自动技能合成和集成推理——即使智能体在底层原语上运行，也能显著提高鲁棒性。这些发现使我们推导出 CaP-Agent0，这是一个无需训练的框架，在模拟和真实实体上的多项操纵任务中实现了人类水平的可靠性。我们进一步引入了 CaP-RL，表明具有可验证奖励的强化学习可以提高成功率，并以极小的差距实现从模拟到现实（sim2real）的迁移。总之，CaP-X 为推进具身编码智能体提供了一个原则性的、开源的平台。

## Abstract
"Code-as-Policy" considers how executable code can complement data-intensive Vision-Language-Action (VLA) methods, yet their effectiveness as autonomous controllers for embodied manipulation remains underexplored. We present CaP-X, an open-access framework for systematically studying Code-as-Policy agents in robot manipulation. At its core is CaP-Gym, an interactive environment in which agents control robots by synthesizing and executing programs that compose perception and control primitives. Building on this foundation, CaP-Bench evaluates frontier language and vision-language models across varying levels of abstraction, interaction, and perceptual grounding. Across 12 models, CaP-Bench reveals a consistent trend: performance improves with human-crafted abstractions but degrades as these priors are removed, exposing a dependence on designer scaffolding. At the same time, we observe that this gap can be mitigated through scaling agentic test-time computation--through multi-turn interaction, structured execution feedback, visual differencing, automatic skill synthesis, and ensembled reasoning--substantially improves robustness even when agents operate over low-level primitives. These findings allow us to derive CaP-Agent0, a training-free framework that recovers human-level reliability on several manipulation tasks in simulation and on real embodiments. We further introduce CaP-RL, showing reinforcement learning with verifiable rewards improves success rates and transfers from sim2real with minimal gap. Together, CaP-X provides a principled, open-access platform for advancing embodied coding agents.

---

## 论文详细总结（自动生成）

### CaP-X：机器人操纵编码智能体基准测试与改进框架总结

#### 1. 论文的核心问题与整体含义
论文探讨了“代码即策略”（Code-as-Policy, CaP）在机器人操纵中的有效性。虽然 LLM 能够生成控制代码，但现有方法高度依赖人类预设的高级 API（如 `stack_objects()`），这掩盖了模型在底层逻辑和感知落地上的真实弱点。研究动机在于：系统性地评估编码智能体在不同抽象层级下的表现，并探索如何通过增加“测试时计算”（Test-time Computation）而非依赖海量数据训练，来提升机器人的自主操纵能力。

#### 2. 方法论：核心思想与关键技术
论文提出了 **CaP-X** 框架，包含以下四个核心组件：
*   **CaP-Gym**：一个基于 Gymnasium 的交互式环境，集成了 RoboSuite、LIBERO-PRO 和 BEHAVIOR。它采用 REPL（读取-求值-打印循环）模式，允许智能体通过编写 Python 代码调用感知（如 SAM3, Molmo）和控制（如 PyRoki 逆运动学）原语。
*   **CaP-Bench**：一个多维度的评估基准，从**抽象级别**（高级宏指令 vs. 底层原子原语）、**时间交互**（单轮 vs. 多轮调试）和**感知落地**（纯文本反馈 vs. 视觉差异反馈）三个维度测试模型。
*   **CaP-Agent0**：一个免训练的智能体架构。关键技术包括：
    *   **视觉差异模块 (VDM)**：将视觉观察转化为结构化自然语言，描述前后两帧的变化。
    *   **自动合成技能库**：从成功的底层代码执行记录中自动提取、推广通用的工具函数。
    *   **并行推理与集成**：同时采样多个候选代码，由中心智能体进行合成。
*   **CaP-RL**：利用可验证奖励进行强化学习（RLVR）。使用 **GRPO（群体相对策略优化）** 算法对开源模型（如 Qwen2.5-Coder-7B）进行后训练，优化其生成控制代码的逻辑。

#### 3. 实验设计
*   **模型范围**：评估了 12 个前沿模型，包括闭源（GPT-5.2, GPT-o1, Gemini-3-Pro, Claude 4.5 等）和开源（Qwen3-235B, DeepSeek-V3.1, Kimi K2 等）。
*   **任务场景**：
    *   **核心任务**：7 个基础操纵任务（抬起、堆叠、擦拭、插拔等）。
    *   **扩展任务**：LIBERO-PRO（30 个长程任务）和 BEHAVIOR（移动操纵任务）。
    *   **真实世界**：在 Franka Panda 和 AgiBot G1 机器人上进行了零样本（Zero-shot）测试。
*   **对比基准**：以人类专家编写的代码作为性能上限，并对比了 OpenVLA、$\pi_0$ 等主流视觉-语言-动作（VLA）模型。

#### 4. 资源与算力
*   论文提到 CaP-Gym 支持高吞吐量的并行评估。
*   在 **CaP-RL** 部分，使用了 GRPO 算法对 7B 参数模型进行后训练。
*   **具体算力细节**：文中未明确给出具体的 GPU 型号、数量或总训练时长，但强调了其感知和控制原语被实现为无状态服务，以支持大规模并行化。

#### 5. 实验数量与充分性
*   **实验规模**：每个任务/层级组合进行了 **100 次试验**，总计覆盖了 12 个模型在 8 个不同评估层级（S1-S4, M1-M4）的表现。
*   **消融实验**：对 CaP-Agent0 的各个组件（VDM、技能库、并行查询）进行了详细的消融分析。
*   **充分性评价**：实验设计非常全面，涵盖了从仿真到现实、从单臂到双臂、从固定基座到移动底座的多种场景。对比了当前最先进的闭源和开源模型，实验结果具有较高的客观性和说服力。

#### 6. 主要结论与发现
*   **抽象依赖性**：模型性能随 API 抽象程度降低而显著下降，说明 LLM 仍依赖人类设计的“脚手架”。
*   **测试时计算的价值**：多轮交互（纠错）和视觉差异反馈（VDM）能显著弥补底层原语操作下的性能差距。
*   **感知落地的瓶颈**：直接输入原始 RGB 图像（M2 层级）反而可能降低性能，而将视觉转化为结构化文本（M3/M4）效果更好。
*   **RL 的有效性**：通过 CaP-RL 训练的小模型在代码可靠性和几何推理上能大幅超越其基座模型，且 Sim-to-Real 差距极小。

#### 7. 优点与亮点
*   **系统性强**：首次系统性地解构了 API 抽象对 CaP 方法的影响。
*   **免训练高性能**：CaP-Agent0 证明了通过合理的智能体工作流，无需微调即可在复杂任务上达到人类专家水平。
*   **开源贡献**：提供了统一的 CaP-Gym 环境，填补了编码智能体在具身智能领域缺乏标准化测试平台的空白。
*   **Sim-to-Real 路径清晰**：通过抽象 API 避开了视觉域差异，实现了极高的迁移成功率。

#### 8. 不足与局限
*   **高频接触任务挑战**：对于需要极高频率视觉伺服或复杂接触动力学的任务（如精细插入、倾倒液体），纯代码生成方式仍显脆弱。
*   **推理延迟**：多轮交互和并行推理虽然提高了成功率，但也显著增加了完成任务的时间成本。
*   **对 VLM 的依赖**：VDM 模块本身依赖于一个强大的视觉语言模型，若底层 VLM 描述错误，会导致编码智能体产生连锁反应。
*   **应用限制**：目前主要集中在 Python 可描述的逻辑任务，对于非结构化、纯感官驱动的动作（如揉面团）覆盖不足。

（完）
