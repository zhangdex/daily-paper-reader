---
title: "HUGE-Bench: A Benchmark for High-Level UAV Vision-Language-Action Tasks"
title_zh: HUGE-Bench：高层级无人机视觉-语言-动作任务基准
authors: "Jingyu Guo, Ziye Chen, Ziwen Li, Zhengqing Gao, Jiaxin Huang, Hanlue Zhang, Fengming Huang, Yu Yao, Tongliang Liu, Mingming Gong"
date: 2026-03-20
pdf: "https://arxiv.org/pdf/2603.19822v1"
tags: ["query:robot-planning"]
score: 9.0
evidence: 无人机视觉-语言-动作任务与安全过程导向轨迹
tldr: 针对现有无人机视觉语言导航基准主要关注长指令且缺乏安全评估的问题，本文提出了HUGE-Bench，一个面向高层视觉-语言-动作（HL-VLA）任务的新基准。该基准包含4个真实数字孪生场景和8类高层任务，利用3DGS-Mesh混合表示实现逼真渲染与碰撞检测。通过引入过程导向和碰撞感知的评估指标，揭示了现有VLA模型在处理简洁指令和安全执行方面的显著差距，为无人机自主性研究提供了诊断性测试平台。
selection_source: fresh_fetch
motivation: 现有的无人机导航基准依赖冗长的步骤描述，难以评估模型在处理简短高层指令及确保飞行安全方面的能力。
method: 构建了基于3DGS-Mesh表示的数字孪生环境，包含大规模轨迹数据，并设计了过程忠实度与碰撞感知的多维度评估指标。
result: 实验表明，现有的先进VLA模型在高层语义理解和安全执行任务上存在巨大挑战，无法满足复杂环境下的作业需求。
conclusion: HUGE-Bench填补了高层无人机任务评估的空白，为开发具备安全意识和复杂任务执行能力的无人机自主系统提供了重要工具。
---

## 摘要
现有的无人机（UAV）视觉-语言导航（VLN）基准已经实现了语言引导的飞行，但它们主要关注长篇、逐步的路线描述，并采用以目标为中心的评估方式，这使得它们在实际操作中的诊断价值有限，因为实际操作要求将简短的高层级指令转化为安全的多阶段行为。我们提出了 HUGE-Bench，这是一个针对高层级无人机视觉-语言-动作（HL-VLA）任务的基准，旨在测试智能体能否理解简洁的语言并执行具有安全意识的复杂、面向过程的轨迹。HUGE-Bench 包含 4 个真实世界的数字孪生场景、8 个高层级任务和 256 万米的轨迹，并构建在对齐的 3D 高斯泼溅（3DGS）-网格（Mesh）表示之上，该表示结合了照片级逼真的渲染与具备碰撞检测能力的几何结构，以实现可扩展的生成和碰撞感知评估。我们引入了面向过程和碰撞感知的指标，以评估过程保真度、终端准确性和安全性。对具有代表性的最先进 VLA 模型的实验表明，在高层级语义完成和安全执行方面存在显著差距，突显了 HUGE-Bench 作为高层级无人机自主性诊断测试平台的价值。

## Abstract
Existing UAV vision-language navigation (VLN) benchmarks have enabled language-guided flight, but they largely focus on long, step-wise route descriptions with goal-centric evaluation, making them less diagnostic for real operations where brief, high-level commands must be grounded into safe multi-stage behaviors. We present HUGE-Bench, a benchmark for High-Level UAV Vision-Language-Action (HL-VLA) tasks that tests whether an agent can interpret concise language and execute complex, process-oriented trajectories with safety awareness. HUGE-Bench comprises 4 real-world digital twin scenes, 8 high-level tasks, and 2.56M meters of trajectories, and is built on an aligned 3D Gaussian Splatting (3DGS)-Mesh representation that combines photorealistic rendering with collision-capable geometry for scalable generation and collision-aware evaluation. We introduce process-oriented and collision-aware metrics to assess process fidelity, terminal accuracy, and safety. Experiments on representative state-of-the-art VLA models reveal significant gaps in high-level semantic completion and safe execution, highlighting HUGE-Bench as a diagnostic testbed for high-level UAV autonomy.

---

## 论文详细总结（自动生成）

以下是对论文《HUGE-Bench: A Benchmark for High-Level UAV Vision-Language-Action Tasks》的结构化深度总结：

### 1. 核心问题与整体含义（研究动机和背景）
*   **核心问题**：现有的无人机（UAV）视觉-语言导航（VLN）基准主要依赖冗长、逐步的路径描述，且评估指标过于关注终点（如成功率 SR）。但在实际操作中，用户通常给出简短的高层指令（如“检查左侧建筑”），这要求无人机具备语义理解、子任务分解、多阶段执行及安全避障的能力。
*   **研究背景**：现有的模拟器在“视觉逼真度”和“物理碰撞检测”之间存在权衡。网格（Mesh）模型支持碰撞检测但视觉真实感差；神经渲染（如 3DGS）视觉效果好但缺乏物理几何属性。
*   **整体含义**：HUGE-Bench 填补了高层级视觉-语言-动作（HL-VLA）任务的空白，通过构建真实场景的数字孪生，评估无人机在处理模糊指令时的过程忠实度和安全性。

### 2. 论文提出的方法论
*   **核心思想**：构建一个对齐的 **3DGS-Mesh 混合表示**。利用 3D 高斯泼溅（3DGS）提供照片级逼真渲染，利用三角网格（Mesh）提供物理碰撞查询和深度信息。
*   **关键技术细节**：
    *   **HL-VLA 任务建模**：将简短指令分解为隐含的子任务序列（如：识别目标 -> 接近 -> 下降 -> 绕飞 -> 返回）。
    *   **数据生成管线**：从真实世界采集数据（DJI M400 + L2 载荷），重建 3DGS 和 Mesh，通过 LLM 辅助标注地标和生成空间描述指令。
    *   **仿真平台**：基于 NVIDIA Isaac Sim 开发，支持 GPU 加速渲染和 PhysX 物理引擎，实现可扩展的轨迹采集。
*   **评估指标**：
    *   **轨迹覆盖率 (TCR)**：衡量预测轨迹与标准过程轨迹的重合程度，用于评估过程导向任务。
    *   **碰撞率 (CR)**：统计发生碰撞的频率。
    *   **碰撞感知成功加权路径长度 (CSPL)**：综合衡量任务成功、路径效率和无碰撞执行。

### 3. 实验设计
*   **数据集与场景**：包含 4 个真实数字孪生场景（办公楼、城市街区、沼泽农田、建筑道路），覆盖 6.45 平方公里。
*   **任务类型**：定义了 8 类任务，包括目标降落、道路巡检、建筑巡检、区域测绘、不同高度/半径的绕飞、螺旋下降和避障穿越。
*   **Benchmark 规模**：256 万米轨迹数据，包含 5,330 条训练轨迹，以及 Seen（已见地标）和 Unseen（未见地标/指令）测试集。
*   **对比方法**：评估了四种最先进的 VLA 模型：**OpenVLA**、**$\pi_0$**、**$\pi_{0.5}$** 和 **FastVLM**。

### 4. 资源与算力
*   **训练配置**：
    *   **$\pi_0$ / $\pi_{0.5}$**：微调 5 个 epoch，每张 GPU 的 batch size 为 64。
    *   **FastVLM**：微调 5 个 epoch，每张 GPU 的 batch size 为 32，学习率 $2.5 \times 10^{-5}$。
    *   **OpenVLA**：使用 LoRA 微调（rank 32），每张 GPU 的 batch size 为 18。
*   **算力说明**：文中提到了“per-GPU batch size”，暗示使用了多 GPU 并行训练，但未明确给出具体的 GPU 型号（如 A100 或 H100）及总训练时长。

### 5. 实验数量与充分性
*   **实验组数**：
    *   对比了 4 种模型在 8 类任务上的表现。
    *   进行了 **Real-to-Sim** 分析（对比真实轨迹训练与合成轨迹训练的效果）。
    *   进行了 **高层 vs. 低层指令** 对比实验。
    *   进行了 **从头训练 vs. 微调** 的消融实验。
*   **充分性与公平性**：实验涵盖了 Seen 和 Unseen 场景，测试了模型的泛化能力。所有基准模型均在相同的输入格式（首帧 + 当前帧 + 指令）下评估，确保了对比的公平性。

### 6. 论文的主要结论与发现
*   **性能差距显著**：即使是目前最强的 VLA 模型（如 $\pi_0$），在处理高层指令和复杂多阶段任务时仍面临巨大挑战，尤其是在过程完成度（TCR）上。
*   **模型表现排序**：基于 $\pi$ 架构的模型（经过大规模机器人数据预训练）表现优于 OpenVLA 和 FastVLM，显示了跨具身泛化能力的价值。
*   **任务难度差异**：目标降落最简单，而避障穿越和螺旋下降最难。
*   **安全意识缺失**：现有模型在避障任务中的碰撞率（CR）较高，说明仅靠视觉理解不足以保证飞行安全，需要更强的 3D 空间推理。

### 7. 优点
*   **混合表示创新**：巧妙结合 3DGS 和 Mesh，解决了无人机仿真中“看得真”与“碰得到”的矛盾。
*   **评估维度全面**：不再只看终点，通过 TCR 指标强制模型关注执行过程，更符合工业巡检等实际需求。
*   **真实感强**：基于真实世界采集的数字孪生，比纯合成环境更具诊断价值。

### 8. 不足与局限
*   **环境静态性**：目前基准主要针对静态环境，未考虑移动障碍物、动态光照或天气变化（如风力影响）。
*   **Sim-to-Real 鸿沟**：虽然使用了数字孪生，但模拟器中的物理特性与真实无人机动力学之间仍存在差异，模型在实机上的表现尚待验证。
*   **算力细节模糊**：未详细记录训练所需的具体硬件资源消耗，不便于其他研究者评估复现成本。

（完）
