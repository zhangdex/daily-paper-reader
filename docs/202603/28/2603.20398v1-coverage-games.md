---
title: Coverage Games
title_zh: 覆盖博弈
authors: "Orna Kupferman, Noam Shenwald"
date: 2026-03-20
pdf: "https://arxiv.org/pdf/2603.20398v1"
tags: ["query:ra-rl-pe"]
score: 9.0
evidence: 针对对抗性干扰者的多智能体规划
tldr: 本研究提出并探讨了“覆盖博弈”（Coverage Games），这是一种针对多智能体规划的新型框架。在该博弈中，覆盖者控制多个智能体与对抗性的干扰者博弈，目标是确保所有预设目标至少被一个智能体满足。该框架扩展了传统双人多目标博弈，允许在智能体间动态分解目标，适用于多机器人监控和资源耗尽预防等场景。研究深入分析了其理论特性、判定复杂性及特殊情况下的表现，为多智能体协作与对抗提供了新的理论视角。
selection_source: fresh_fetch
motivation: 旨在解决系统在无法完全控制智能体或面临对抗环境时，如何通过多智能体协作确保所有目标被覆盖的规划难题。
method: 定义了覆盖者与干扰者之间的博弈模型，研究了目标的动态分解机制、博弈确定性以及不同约束下的判定算法。
result: 给出了覆盖博弈胜负判定问题的完整复杂性分析，并针对单玩家、固定智能体或目标数量等特殊场景进行了深入探讨。
conclusion: 覆盖博弈为多智能体系统中的目标分配与对抗规划提供了严谨的理论框架，在监控和资源管理等领域具有重要应用价值。
---

## 摘要
我们引入并研究了覆盖博弈（coverage games）——这是一种用于多智能体规划的新型框架，适用于系统运行多个智能体但对其没有完全控制权，或者与由多个智能体组成的环境进行交互的场景。该博弈在拥有一组目标的覆盖者（coverer）与干扰者（disruptor）之间进行。覆盖者操作多个智能体，这些智能体与对抗性的干扰者进行交互。如果每个目标都由至少一个智能体满足，则覆盖者获胜；否则，干扰者获胜。因此，覆盖博弈通过允许在不同智能体之间进行（可能是动态的）目标分解，扩展了具有多目标的传统双人博弈。它们具有广泛的应用，既包括系统作为覆盖者的场景（例如，多机器人监控、多线程系统中的覆盖），也包括系统作为干扰者的场景（例如，防止资源耗尽、确保非拥塞）。我们首先研究了覆盖博弈的理论性质，包括确定性（determinacy）以及预先在智能体之间分解目标的能力。随后，我们研究了判定覆盖者还是干扰者获胜的问题。除了对问题紧确复杂度（tight complexity）的全面分析外，我们还考虑了一些有趣的特殊情况，例如单人博弈情况以及具有固定数量智能体或目标的设置。

## Abstract
We introduce and study coverage games - a novel framework for multi-agent planning in settings in which a system operates several agents but does not have full control on them, or interacts with an environment that consists of several agents. The game is played between a coverer, who has a set of objectives, and a disruptor. The coverer operates several agents that interact with the adversarial disruptor. The coverer wins if every objective is satisfied by at least one agent. Otherwise, the disruptor wins.   Coverage games thus extend traditional two-player games with multiple objectives by allowing a (possibly dynamic) decomposition of the objectives among the different agents. They have many applications, both in settings where the system is the coverer (e.g., multi-robot surveillance, coverage in multi-threaded systems) and settings where it is the disruptor (e.g., prevention of resource exhaustion, ensuring non-congestion).   We first study the theoretical properties of coverage games, including determinacy, and the ability to a priori decompose the objectives among the agents. We then study the problems of deciding whether the coverer or the disruptor wins. Besides a comprehensive analysis of the tight complexity of the problems, we consider interesting special cases, such as the one-player cases and settings with a fixed number of agents or objectives.

---

## 论文详细总结（自动生成）

这是一份关于论文《Coverage Games》（覆盖博弈）的结构化深入分析报告：

### 1. 核心问题与整体含义
*   **研究动机**：在多智能体规划（Multi-agent Planning）中，系统往往需要操作多个智能体（如无人机群、多线程进程）来完成一组任务。传统的双人博弈模型通常假设系统只有一个智能体，或者所有智能体共享同一个宏观目标。
*   **核心问题**：论文引入了“覆盖博弈”框架，解决的是：**当系统无法完全控制智能体（存在对抗性环境），且需要确保一组目标（Objectives）中的每一个都至少被其中一个智能体完成时，如何制定策略？**
*   **整体含义**：该研究扩展了传统的双人多目标博弈，重点在于目标的**动态分解**——即在博弈过程中，如何根据环境的反馈，灵活地将剩余目标分配给不同的智能体。

### 2. 方法论
*   **核心思想**：将问题建模为覆盖者（Coverer，拥有 $k$ 个智能体）与干扰者（Disruptor，拥有单一对抗策略）之间的博弈。
*   **关键技术细节**：
    *   **覆盖准则**：覆盖者获胜的条件是：对于目标集 $\beta$ 中的每一个目标 $\alpha_i$，都存在至少一个智能体 $j$，其产生的轨迹满足该目标。
    *   **目标分解（Decomposability）**：研究了目标是否可以预先分配。结论是通常无法预先分解，必须依赖“分叉（Fork）”顶点。
    *   **分叉（Fork）概念**：定义了博弈图中的特殊顶点，在这些点上，覆盖者可以策略性地让不同的智能体走向不同的后续路径，从而实现目标的物理拆分。
*   **算法流程**：
    *   论文提出了一个基于递归的 PSPACE 算法来判定覆盖者的胜负。
    *   算法首先寻找“分叉”顶点，若当前状态无法分叉，则尝试作为单智能体博弈解决；若存在分叉，则递归地在子博弈中验证目标覆盖的可行性。

### 3. 实验设计
*   **研究性质**：本论文属于**理论计算机科学与博弈论**领域，主要通过数学证明和复杂度分析来验证结论，而非传统的实验数据测试。
*   **分析场景**：
    *   **目标类型**：深入分析了 Büchi 目标（无限次访问目标集）和 co-Büchi 目标（最终停留在目标集外）。
    *   **对比基准（Benchmark）**：以传统的双人多目标博弈（$k=1$ 的情况）和单人覆盖路径规划问题为对比基准。
    *   **参数化分析**：对比了固定智能体数量 $k$、固定目标数量 $|\beta|$ 以及一般情况下的复杂度差异。

### 4. 资源与算力
*   **算力说明**：文中**未提及**任何关于 GPU、服务器集群或具体训练时长的内容。
*   **资源消耗**：该研究的贡献在于给出了算法的理论复杂度界限（如 PSPACE、NP、PTIME），而非实际运行的资源消耗。

### 5. 实验数量与充分性
*   **充分性评价**：作为理论论文，其“实验”的充分性体现在对所有逻辑分支的完备证明。
*   **覆盖范围**：
    *   证明了覆盖博弈的**非确定性**（即可能存在双方都无必胜策略的情况）。
    *   对 Büchi 和 co-Büchi 两种核心目标类型进行了详尽的复杂度分类。
    *   分析了从单人到多人、从固定参数到变量参数的所有组合。
*   **客观性**：通过严格的归约（Reduction，如从 3SAT 或 QBF 归约）证明了复杂度的下界，逻辑严密且客观。

### 6. 主要结论与发现
*   **复杂度图谱**：
    *   **覆盖问题**：一般情况下是 **PSPACE-完全**的。如果智能体数量 $k$ 固定，降为 **NP-完全**；如果目标数量固定，则为 **PTIME**。
    *   **干扰问题**：一般情况下是 **$\Sigma_2^P$-完全**的。
*   **确定性缺失**：与传统的双人零和博弈不同，覆盖博弈不满足确定性，这源于智能体之间信息的不完全共享以及干扰者策略的单一性。
*   **分解特性**：证明了目标分解与博弈图的结构（分叉点）紧密相关，而非仅仅取决于目标集合本身。

### 7. 优点
*   **模型创新**：首次形式化了“覆盖”这一多智能体协作对抗的核心需求，比传统的“所有智能体达成同一目标”更符合实际规划场景。
*   **理论深度**：不仅给出了复杂度，还深入探讨了策略的记忆性需求（Memory Requirements）和符号化表示方法。
*   **参数化洞察**：清晰地揭示了“目标数量”比“智能体数量”更能显著增加问题的计算难度。

### 8. 不足与局限
*   **应用限制**：模型假设干扰者对所有智能体使用**相同**的策略。在某些现实场景中，环境可能会针对不同智能体采取不同的对抗手段，该模型尚未覆盖此类情况。
*   **策略空间**：覆盖者的获胜策略可能极其复杂（非记忆性策略），在实际嵌入式设备或实时系统中部署可能存在存储和计算瓶颈。
*   **缺乏实证验证**：论文完全基于理论推导，缺乏在具体多机器人仿真平台（如 ROS/Gazebo）上的性能表现数据。

（完）
